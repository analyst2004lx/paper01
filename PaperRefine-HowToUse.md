# 怎么用 PaperRefinePrompt.md（v3 · 一步步复制版）

这份文件是给**你**看的操作说明，不用给模型。要给模型的是 `PaperRefinePrompt.md`（运行时协议）。两份都是通用协议，**不绑定仓库里的任何一篇论文**；换稿时只改轮 0 的四项配置（见第 2.1 节）。

第 3 节起，每个灰框都可以**原样复制**给模型。**只有轮 0 那个框需要你填四项**，其余框一个字都不用改——块的位置、原文、行号、卡片内容，全部由模型自己去读。

> 文中形如 `§1`、`§5.1`、`R3`、`S0`、`C2` 的编号，一律指 `PaperRefinePrompt.md` 里的条目。
> `{{PAPER}}`、`{{CARD}}`、`{{OUTPUT}}` 这类是**配置变量**，在轮 0 一次性解析完，之后原样发给模型即可，不要替换成具体路径。
> `F-SETUP`、`F-RESULT` 这类是**功能 ID**，不是章号——你的论文第几章对应哪个功能，由模型在轮 1 自己判定并列表给你确认。

---

## 1. 先说不要怎么用

**不要说**：`请按照 PaperRefinePrompt.md 的要求修改我的论文，输出中文版。`

模型会跳过底账与卡片，直接从摘要开始整篇重写，交给你一大段新文本；期间大概率把数字宏展开成写死的数字、把 `\cite` 键"顺手统一"、把 `%` 注释里的复现命令删掉。你既无法逐条判断，也无法回滚。协议第 10 节把这条列为一号失败模式。

**正确的用法**：一轮一块，先诊断后改写。每轮你只做三件事——**看四个数、跑两条命令、决定采纳哪几条。**

---

## 2. 准备（一次性，5 分钟）

1. 不需要配置版本控制。**安全性由两件事保证**：① 所有修改只写施工稿，你的原始 tex 一个字都不会被改；② 每应用一条修改，模型都会在台账的「改动记录」表追加一行（原文串 → 改后串），那张表就是撤销日志。想回到任何一步，说一句话就行（见第 6 节）。
2. 四个值想清楚（就是协议 §0 配置区里唯一需要人填的部分）：

   | 变量 | 你要填的值 |
   |---|---|
   | `{{PAPER}}` | 主 tex 的仓库相对路径 |
   | `{{JOURNAL}}` | 目标刊名 |
   | `{{PAGE_LIMIT}}` | 篇幅上限（页数或字数） |
   | `{{TARGET_LANG}}` | **定稿正文**要中文还是英文：`中文` / `英文` / `中英双版` / `同 {{LANG}}`（不换语言） |

   其余十来个变量不用管，模型在轮 0 自己探测或推导，回填给你确认。现稿语言 `{{LANG}}` 是探测出来的，不用你填；语言选项的完整说明见第 9 节。

3. 不用改体检脚本（`check_tex.py` 之类），也不用复制它——模型会在主 tex 同目录下自己找，找不到会在 §7 生成一个替代脚本。

4. **不用准备任何其他文件。** 卡片、台账、评分、施工稿都是流程自己建的，会陆续出现在主 tex 的同一个目录里：

   | 文件 | 什么时候出现 | 里面是什么 |
   |---|---|---|
   | `<主名>-score.md` | 轮 2 | 改前评分卡 + 投稿判定 + 问题清单；收尾时追加改后一节 |
   | `<主名>-card.md` | 轮 3 | 论文卡片（模型从你的论文里抽出来的，**不是你要写的**） |
   | `<主名>-ledger.md` | 轮 4 | 块清单 + 改动记录（撤销日志） |
   | `<主名>_AI-Modify-<日期>.tex` | 第一次采纳修改时 | 施工稿，所有改动都在这里 |
   | `reference-suggested.bib` | 跑 F-REF 时 | 候选新文献 |

   如果这些文件因为上次跑到一半已经存在，模型不会默认沿用也不会直接覆盖，它会问你 `续跑` 还是 `重建`（重建会先把旧文件改名加 `.bak` 后缀）。

**你的原始 tex 不会被改。** 模型第一次落盘时会自动复制出一份施工稿，命名规则是 `{{STEM}}_AI-Modify-{{DATE}}.tex`（`{{STEM}}` 是主 tex 的文件主名，`{{DATE}}` 是首次施工那天）。此后所有修改只写施工稿，原文件是你的回滚基准。

### 2.1 换一篇论文时

本流程跟哪篇稿走，只取决于轮 0 的 `{{PAPER}}`。卡片、台账、评分、施工稿都从该路径推导，出现在**那篇 tex 的同目录**。不要把上一篇的产出文件复制过来凑数，也不要把上一篇的体检脚本拷进新稿目录——让模型在新目录下自己找或按协议生成。

**适合**

内容已齐、表达未定稿的 LaTeX 稿；有主张需要被证据收窄。中英文、换语言、缺某一功能都可以。轮 1 映射不到的功能标 `MISSING_FUNCTION`（例如没有独立证明章就不需要 `F-PROOF`），对应施工框跳过即可，不要硬填。

**会打折**

- 没有独立实验设置的纯理论稿：`F-SETUP` / `F-RESULT` 的卡片仍按实验口径提问，许多条会变成不适用，你要在卡片里裁决，不要让模型编一套“实验”。
- Word / Markdown 稿：协议假定一份可编译的 tex；体检脚本找不到会生成替代，但编译命令仍要你能跑通。
- 还在补实验或补证明的半成品：模型不能补做实验、不能判定科学正确性。缺的证据只会标 `EVIDENCE_NEEDED`，主张强度仍要你拍板。

**必须重开一轮**

不要拿上一篇的 `*-card.md`、`*-ledger.md`、`*-score.md` 或施工稿去改另一篇。新开对话，重发轮 0，让模型对新稿重新探测、抽卡片、出台账。同一对话里只换 `{{PAPER}}` 而不重建产出文件，卡片会串台。上一篇跑到一半的文件若碰巧已在新稿目录里，模型会按协议问你 `续跑` 还是 `重建`——换稿一律选 `重建`。

---

## 3. 装载与四轮准备

### 轮 0 · 装载（填几行，只填这一次）

```text
读 @PaperRefinePrompt.md 全文。这是运行时协议，你必须按 §1 RUNBOOK 逐状态推进，不得跳步。

配置区（§0）取值：
{{PAPER}} = <填：主 tex 的仓库相对路径>
{{JOURNAL}} = <填：目标刊名>
{{PAGE_LIMIT}} = <填：篇幅上限>
{{TARGET_LANG}} = <填：中文 / 英文 / 中英双版 / 同 {{LANG}}>
{{REPLY_LANG}} = 跟随提问
{{TRANSLATE_WHEN}} = 最后
其余全部按 §0 自己探测或推导。

现在只做一件事：读 {{PAPER}}，把 §0 里"探测"与"推导"两类的值列成表并注明依据；
对推导出的产出文件，逐个说明它现在是否已存在（已存在的按 §0 末尾规则问我续跑还是重建）；
再按 §0.1 报出语言方案结论一句。不要开始 S0，不要给任何修改建议，不要创建任何文件。列完等我确认。
```

**检查点**：`{{OUTPUT}}` 的名字对不对；`{{CHECK}}` 是否真的找到了体检脚本；`{{BUILD}}` 给的命令你能直接跑通；`{{LANG}}` 判对了没有；**语言方案那句结论是否符合你的预期**（施工阶段正文写什么语言、§9.A 做不做翻译）。

### 轮 1 · 建底账（S0）

```text
配置已确认。执行 S0 INTAKE：只读，不得修改任何字符，不得给出任何修改建议（一条也不给）。
按 §1 S0 的七项逐项输出，报告 ≤100 行，然后在 GATE-A 停下。
第 7 项功能映射表要特别仔细：按功能判定，不按章号和标题字面；一节承担两个功能、一个功能散在多节、
功能缺失、出现手册未定义的功能，都要照 §1 的映射规则如实标出。
```

**检查点**：① 它是否真的一条建议都没给（给了就回 `违约：S0 不得给修改建议，重做本轮`）；② **功能映射表逐行核对**——这是整套流程唯一需要你确认结构的地方，映射错了后面全错；③ 数字宏条数对不对（自己 `rg -c "newcommand"` 比一下）；④ 骨架各节行数加总是否接近全文行数（差太多说明它漏读了）；⑤ 占位符清单是否包含你知道的那几处。

若某个功能它判错了（例如把"实验设置"的内容全算进了结果），直接回一句：`功能映射表第 N 行改为：<功能ID> → <小节标题>，重出该表。`

### 轮 2 · 基线评分（S0B）——改之前先量一次

```text
继续。执行 S0B 基线评分，按 §8，对象是 {{PAPER}}（原始稿，不是施工稿）。
输出：12 维评分卡（每维必须带位置作依据）+ 投稿判定（Accept / Minor Revision / Major Revision / Reject）
+ 一句话理由（点名压低判定的那一维）+ 问题清单 P-list + 三条最可能的拒稿理由。
新建 {{SCORE}} 文件，写进它的"基线"一节。
只诊断不给改法：不得输出任何改写后的句子。P 编号一经分配即冻结，不复用不重排。
```

**检查点**：每一维是否都带了可核对的位置（只有分数和形容词的那一维，回 `第 N 维给分不给位置，重出该维`）；`阻断` 级 P 条目你是否认同；判定与分数是否自洽（有 0 分维却判 Minor 就是错的）。

看完确认一句 `基线已确认，不得再改`。基线是后面一切对比的锚，而它评的是原始稿——原始稿永不被改，所以随时可以回头核对它给的每一条依据。

> 这个分是模型模拟审稿人给的，**不是真实评审结果**。它的价值在逐维依据和 P 条目，不在总分。别拿它去预测录用。

### 轮 3 · 抽卡片（S1）

```text
继续。执行 S1：按 §3 的模板从论文里抽取卡片，新建 {{CARD}} 文件并把卡片全文写进去。
只读论文、不改论文，但卡片必须落盘成文件——只在对话里打印不算完成。
抽不出来的写 MISSING，不要编、不要替我补；原文自相矛盾的写 CONFLICT 并集中列在文件开头。
落盘后只在对话里给摘要（文件路径、各节是否齐备、MISSING/CONFLICT/TODO 各几条），不要把整份卡片再贴一遍。
写完在 GATE-B 停下。
```

**检查点（这一轮价值最高，值得花 1–2 小时）**：卡片是后面每一轮的金标准，卡片错了，之后每块都在错的基准上施工。逐条自己改：核心主张是否写成了**可证伪的命题**而不是题目式短语；贡献是否 ≤4 条且每条都能指到"方法哪一块 + 哪张表/图/检验"；"不声称"是否 ≥5 条；数字账本的口径列（同挂钟还是同代数、样本量、检验）是否准确；统计口径里"多重比较校正"是不是 `无，需补`；局限预算填了几处；开头的 CONFLICT 是否都已裁决。

**这一轮的产出是一个文件，不是一段聊天内容。** 你直接在编辑器里打开 `<主名>-card.md` 改，改完存盘就行——此后不用再贴卡片，模型每轮自己读。

### 轮 4 · 出台账与顺序（S2）

```text
卡片已确认（{{CARD}} 现内容即金标准）。
执行 S2：按 §4 的依赖顺序，结合轮 1 的功能映射表与轮 2 的 P-list 归属列出台账，新建 {{LEDGER}} 文件并写入。
每块必须有定位锚（优先 \label，无则小节标题原文，都无则首句前 12 字）；行号只作参考值。
每块要挂上它关联的 P 条目编号。在 GATE-C 停下。
```

**检查点**：第一块必须是承担 `F-SETUP` 的那一块，**不是摘要**（若排了摘要在前，回 `违约：§4 要求自下而上，重排`）；每块 ≤120 行；摘要、引言、结论是否被保持为整块未切开；每块的定位锚是否唯一（重复的锚会让模型定位到错的地方）；P 条目是否都被挂到了某一块（挂不上的应该是 `S4` 级的全文问题）。

---

## 4. 逐块施工：照顺序复制下面 13 个框

每个框都是完整提示词，**不用填任何东西**（框里的 `{{CARD}}`、`{{OUTPUT}}` 等变量模型在轮 0 已经解析过，原样发给它就行）。模型按 §2.5 自己读卡片、读台账、用锚定位原文。

每块跑完，先做第 5 节的采纳与落盘，再复制下一个框。跳过某一块也没关系（例如论文没有独立的证明章，就跳过第 6 个框），但**不要打乱顺序**。

**1. 实验设置与口径**

```text
施工 F-SETUP，按 §5.1。自己按台账的定位锚找到本块并读取，不要向我索要原文。
按 §1 S3 的七步走：诊断表 → 逐条修改 → 越界发现 → 需作者决定 → 验收表 → 自检块。
只用 §5.1 这一张卡片。不要动文件，等我说"应用第 N 条"。
```

**2. 结果、消融与负对照**

```text
施工 F-RESULT，按 §5.2。自己按台账的定位锚找到本块并读取。
按 §1 S3 的七步走，只用 §5.2 这一张卡片。不要动文件，等我说"应用第 N 条"。
```

**3. 图表与图注**

```text
施工 F-FIG，按 §5.3。自己找到全部 figure/table 环境及其正文引用处。
先做单读测试（6 问逐图逐表），再给图注改写、正文接口问题与制图返工清单。
末尾附"必须重新生成的图表清单"并标明改哪个脚本。不要改绘图脚本，不要动文件。
```

**4. 方法与机制**

```text
施工 F-METHOD，按 §5.4。自己按台账的定位锚找到本块并读取。
按 §1 S3 的七步走，只用 §5.4 这一张卡片。特别执行那条：未获净收益的机制所占篇幅是否与它在结论中的地位相称。
不要动文件，等我说"应用第 N 条"。
```

**5. 问题、符号与假设**

```text
施工 F-MODEL，按 §5.5。自己按台账的定位锚找到本块并读取。
按 §1 S3 的七步走，只用 §5.5 这一张卡片。不要动文件，等我说"应用第 N 条"。
```

**6. 形式化与证明**（论文把证明单独成章时才跑；否则跳过）

```text
施工 F-PROOF，按 §5.6。自己按台账的定位锚找到本块并读取。
C5 全套执行：文中每处"可证/等价/保证/最优/可采纳/收敛"逐一判定 OK / PROOF_NEEDED / OVERCLAIM / RHETORIC。
PROOF_NEEDED 与 OVERCLAIM 一律进 NEEDS_AUTHOR_DECISION，不许你自己降级措辞后当作已解决。
不要动文件，等我说"应用第 N 条"。
```

**7. 引言与贡献列表**

```text
施工 F-INTRO，按 §5.7。自己按台账的定位锚找到本块并读取（引言必须整块，不许切）。
按 §1 S3 的七步走。贡献列表必须与 {{CARD}} 的贡献表逐条同构：可压缩措辞，不可改含义或条数。
不要动文件，等我说"应用第 N 条"。
```

**8. 相关工作**

```text
施工 F-RELATED，按 §5.8。自己按台账的定位锚找到本块并读取。
按 §1 S3 的七步走，不查 C4。凡是转述他人局限或结论而无原文依据的地方，插 [ref_check] 标记而不要下判断。
不要动文件，等我说"应用第 N 条"。
```

**9. 局限**

```text
施工 F-LIMIT，按 §5.9，C2 全套执行（A 识别 → B 决策树 → C 三种合法形态 → D 预算 → E 负结果写法 → F 不要误删有方法学价值的自我批评）。
先给辩护清账表（每处：位置 / 原句数 / 保护对象 / 判定 删·压·搬·留 / 改后句数 / 去向），再给逐条改写，
最后给预算核对与"应搬进给审稿人回复信"的草稿片段。不要动文件，等我说"应用第 N 条"。
```

**10. 结论、洞见与未来工作**

```text
施工 F-CONCL，按 §5.10。自己按台账的定位锚找到本块并读取（结论必须整块）。
按 §1 S3 的七步走。重点查：是否出现第 5 条贡献、是否出现正文没有的数字、洞见的普遍化程度是否超出证据。
不要动文件，等我说"应用第 N 条"。
```

**11. 摘要**

```text
施工 F-ABS，按 §5.11。摘要必须整块处理。
按四段信息重构（设定与失效假设 / 方法最小可复述形态 / 主结果带口径 / 范围句），每个数字必须带"相对谁、什么预算、什么检验"。
若发现某条局限会反驳摘要里的无条件主张，收窄摘要，不要去加长局限。不要动文件，等我说"应用第 N 条"。
```

**12. 题目与关键词**

```text
施工 F-TITLE，按 §5.12。给 3 个候选标题（机制型，不要金句型），每个标注长度与名词堆叠层数，
并核对机制名与摘要、引言贡献、方法小标题、结论是否逐字一致（含 \title[] 短标题）。
关键词给一版，逐个说明它在摘要或引言的哪一处出现过。不要动文件。
```

**13. 参考文献**（需要你先动手插标记）

模型不许在没有标记的地方加引用。你先通读全文，在需要外部依据的句子末尾按 §5.13 的语法插 `[ref_insert]` / `[ref_check]`，再复制这个框：

```text
执行 F-REF，按 §5.13。只处理带 [ref_insert] 与 [ref_check] 标记的位置，未标记处一律不得增删引用。
任何书目信息不确定就标 UNVERIFIED 并留空，不要填。新条目只追加到 {{BIB_NEW}}，不要改 {{BIB}}。
末尾附参考文献全局体检（年份分布、重复条目、未被引用的条目、期刊缩写、DOI、格式是否匹配目标刊）。
```

**检查点**：每一篇候选文献你都必须自己打开原文核对。标 `UNVERIFIED` 的，不核实就不许进稿。**这一步没有替代方案。**

---

## 5. 每块的采纳与落盘

诊断和改写看完后，回一句（这是你唯一需要自己组织语言的地方）：

```text
应用修改 1、3、4。
修改 2 不采纳，理由：那句口径句是审稿人一定会问的，保留。
修改 5 改用你给的 A 方案。
按 §2.6 写盘：只写 {{OUTPUT}}，精确字符串替换，一条一次，不得合并、不得顺手改邻近文字。
改完跑 {{CHECK}}，报出与上一轮的告警差异。
```

落盘后自己跑一遍第 6 节的两条命令，然后冻结并进入下一块：

```text
F-SETUP 冻结，更新台账状态，并报本块关联的 P 条目现在是什么状态。
```

---

## 6. 每轮通用检查点

**看四个数**（都在模型的自检块里，缺任何一项就退回重做）：改写率 ≤30%、改后字数比 ≤105%、触及数字宏 = 0、触及 `\cite`/`\label` 键 = 0。（新增指向既有 label 的 `\ref` 是允许的，但必须被逐一列出。）

**再看三件事**：验收表是否每条都带了位置（没有位置的判断等于没判断）；"越界发现"里的问题它有没有顺手动手改（改了就是违 R5）；`NEEDS_AUTHOR_DECISION` 是不是被它自己替你决定了。

**跑两条命令**（把变量换成轮 0 探测出的实际值）：

```powershell
py {{CHECK}}                 # 与上一轮比告警差异，新增即回滚
{{BUILD}}                    # 编译必须过
```

`{{CHECK}}` 报出"定义但未引用"的宏，几乎总是意味着某个数字宏被展开成了写死的数字——立即回滚那一条。

**再逐字看一遍改动**：模型给的每条修改都带原文与改后文，对着看就行；要复核落盘结果，让它回一句 `列出本块的改动记录`（台账里那张表）。

**回滚三档**，都是一句话，不需要任何工具：

| 情况 | 说的话 |
|---|---|
| 某一条改坏了 | `回滚修改 4`（4 是台账改动记录号） |
| 整块都不满意 | `回滚 F-SETUP 全部修改` |
| 想全部推倒重来 | `恢复原样`——删掉施工稿、从原始稿重新复制，原始稿始终完好 |

回滚永远比在坏文本上继续修补便宜。（如果你本来就在用 git，每块一个提交当然更方便看差异，但这套流程不依赖它。）

**中途抽检（可选，每 3–4 块一次）**：

```text
抽检。只重评受最近这几块影响的维度与它们关联的 P 条目，不要重出完整评分卡、不要重编 P-list。
写入 {{SCORE}} 的"抽检"一节，注明覆盖了哪几维。
```

抽检的用处是早发现"改出了退步"，而不是看分数涨没涨。

---

## 7. 收尾四轮

```text
全部块已冻结。执行 S4 全文校验，按 §7。只报冲突与最小修改集，不要做任何润色。
术语统一在本轮一次性执行。另按 §7 末尾要求生成那个独立的一致性检查脚本（只用标准库，不要改 {{CHECK}}）。
```

```text
执行 S5 终审并对比基线，按 §8，对象是 {{OUTPUT}}。
输出：12 维评分卡 + 投稿判定 + 逐维对比表（基线分 / 现分 / 变化 / 变化的直接原因，必须指到台账的改动记录号）
+ P-list 复审表（每条 P 的现状态与证据位置）+ 新增问题（NEW-01 起编）+ 三条最可能的拒稿理由 + 分级意见。
严守 §8.5：基线不可改；涨分必须有归因，给不出归因的维度保持基线值；有退步必须如实降分；
只要还有阻断级 P 未解决，判定不得优于 Major Revision。
最后回答一句：判定 <基线> → <现在>；若未改善，卡在哪一维、为什么这次没能动它。
```

```text
全文校验已通过。执行 §9.A 语言定稿：先按 §0.1 说明本轮属于"不换语言"还是"换语言"，再执行对应部分。
不换语言就只做术语锁核对与母语规范清单，修改写入 {{OUTPUT}}，不要产生新文件。
换语言则先只出术语锁对照表，等我确认后再逐段翻译，最后给回译对照，译稿另存，不要动原语言施工稿。
```

```text
执行 §9.B 投稿前行政关卡：占位符清零、与前作的关系、稿件规范、投稿包（期刊适配核对表、cover letter、
highlights、建议审稿人）。逐项给 通过/失败 + 位置。
```

---

## 8. 分数怎么看

**要看的**：`阻断` 级 P 条目是否清零（这是唯一硬指标）；有没有维度**退步**（退步说明某轮改坏了，比涨分重要）；判定有没有跨档（Major → Minor 才是实质进展，35 → 35.5 没有意义）；终审的"变化原因"列是否都指到了具体修改（指不到的涨分是幻觉）。

**不要看的**：总分绝对值、单维 ±1 的波动、以及"改后比改前好"这句话本身——模型天然会这么说。协议 §8.5 就是为了压住这个倾向；如果对比表里**一次降分都没有**，那份对比大概率不可信，回一句 `按 §8.5 第 4 条重审，逐块检查是否有退步`。

**判定档位**：`Reject`（任一维 0 分，或总分 <24）→ `Major Revision`（24–35）→ `Minor Revision`（36–43）→ `Accept`（≥44，罕见）。一篇内容已齐、表达未定稿的稿子，典型路径是基线 Major、收尾后 Minor。

---

## 9. 语言：三个开关

在轮 0 设一次，之后每轮不用再说。三个变量各管一件事，**不要混在一起想**：

| 变量 | 管什么 | 取值 | 默认 |
|---|---|---|---|
| `{{LANG}}` | 现稿正文是什么语言 | 中文 / 英文 | 探测得出，不用你填 |
| `{{TARGET_LANG}}` | **定稿正文**要什么语言 | `中文` / `英文` / `中英双版` / `同 {{LANG}}` | `同 {{LANG}}`（不换语言） |
| `{{REPLY_LANG}}` | 诊断表、说明这些**给你看的文字**用什么语言 | `中文` / `英文` / `跟随提问` | `跟随提问` |
| `{{TRANSLATE_WHEN}}` | 什么时候换语言 | `最后` / `边改边译` | `最后` |

**四种常见组合怎么填**：

- 中文稿，就出中文 → `{{TARGET_LANG}} = 中文`（或 `同 {{LANG}}`）。§9.A 只做中文语言定稿，不翻译。
- 中文稿，最终要投英文刊 → `{{TARGET_LANG}} = 英文`。**施工阶段正文仍然是中文**，全文校验通过后在 §9.A 一次性译成英文，另存 `_en` 文件，中文稿保留为工作稿。
- 英文稿，就出英文 → `{{TARGET_LANG}} = 同 {{LANG}}`。§9.A 降级为语言定稿轮：不翻译，只跑中式英语清单与术语锁核对。
- 要中英两版都发 → `{{TARGET_LANG}} = 中英双版`。模型会在 §9.A **停下**要你裁决重复发表的问题（同一内容中英双投会被认定为 duplicate publication），这是内容决策，不是翻译问题。

**三条别踩的规则**：

1. **`{{REPLY_LANG}}` 不等于正文语言。** 你用英文提问、现稿是中文，诊断表可以是英文，但改写出来的正文必须仍是中文。协议 §10 把混淆这两者列为失败模式。
2. **默认不要边改边译。** `{{TRANSLATE_WHEN}} = 最后` 是有理由的：换语言会改变句长、连接词与段落切分，而这些正是风格检查要判断的对象。边改边译等于每处修改做两遍，还会让改写率失去意义。
3. 真要边改边译，把 `{{TRANSLATE_WHEN}}` 显式设成 `边改边译`，模型会先把四项代价复述一遍等你确认（每块交付双语两份、改写率按现稿语言算、术语锁必须提前到轮 3 冻结、全文校验要跑两遍）。

---

## 10. 常问错的话 → 正确的话

| 你可能会说 | 会发生什么 | 应该说 |
|---|---|---|
| 按手册改一下我的论文 | 跳过底账与卡片，整篇重写，宏被展开 | 轮 0 → 轮 1 的框 |
| 帮我润色得更学术一些 | 同义替换，字数变多，信息不变 | `施工 F-INTRO，按 §5.7` |
| 把所有章节都改一遍 | 超出 R2 预算，输出被截断，后半段崩 | 一轮一块 |
| 顺便把参考文献也补上 | 违 R5 跨界，且会编文献 | 先插标记，再单独跑 F-REF |
| 这段你觉得怎么改好就改 | 它会替你决定主张强度 | 给 A/B 选项，由你选 |
| 这是第 5 章，按实验那张卡改 | 章号可能对不上功能 | 只说功能 ID，位置让它查台账 |
| 我把原文贴给你 | 浪费上下文，且贴错行会改到别处 | 什么都不贴，让它按锚自己读 |
| 顺便把这块译成英文 | 结构还没定就换语言，等于白译一遍 | 轮 0 设 `{{TARGET_LANG}} = 英文`，翻译等 §9.A |
| 现在打个分看看改好没 | 无基线可比，只能给个孤零零的分 | 改之前先跑轮 2 存基线 |
| 帮我把分提上去 | 它会去优化分数而不是稿子 | 只问 `阻断级 P 还剩哪几条` |

---

## 11. 节奏预估与边界

一篇 2500 行的稿子约 16–20 轮，每轮 20–40 分钟（大头是你自己逐条审改写）。卡片那一轮单独花 1–2 小时最值，基线评分那一轮约 20 分钟。

**每 3–4 块新开一个对话**，开头只需重发轮 0 的框（三行配置）+ 一句 `卡片、台账、评分见 {{CARD}} / {{LEDGER}} / {{SCORE}}，继续施工 <功能ID>`。长对话里模型会逐渐忘记禁改清单，表现是突然开始展开数字宏或改引用键。

**边界要清楚**：模型永远做不了四件事——判定科学正确性、补做实验、确认文献真实存在、决定主张强度。这四类它只能输出 `EVIDENCE_NEEDED` / `NEEDS_AUTHOR_DECISION`，由你拍板。评分也在这条边界内：它能指出"这里缺一个校正声明"，不能替你判断"这个结论是否正确"。换稿时的适合 / 会打折 / 必须重开一轮，见第 2.1 节。

---

## 12. 速查

| 场景 | 说的话 |
|---|---|
| 开始 | 轮 0 的框（填 `{{PAPER}}` / `{{JOURNAL}}` / `{{PAGE_LIMIT}}` / `{{TARGET_LANG}}`） |
| 换一篇论文 | 新开对话，重发轮 0；产出文件选 `重建`，不要沿用上一篇的卡片/台账/施工稿（第 2.1 节） |
| 建底账 | `执行 S0 INTAKE，在 GATE-A 停下` |
| 改前打分 | `执行 S0B 基线评分，按 §8，对象是 {{PAPER}}` |
| 卡片改完 | `卡片已确认（{{CARD}} 现内容即金标准），执行 S2 出台账` |
| 开工一块 | `施工 F-SETUP，按 §5.1` |
| 采纳部分 | `应用修改 2、5、6；修改 3 我不同意，理由：……` |
| 出问题 | `违约 R3，重做本轮` / `回滚修改 4` |
| 一块做完 | `F-SETUP 冻结，更新台账状态` |
| 中途体检 | `抽检` |
| 全部做完 | `执行 S4` → `执行 S5 终审并对比基线` → `§9.A 语言定稿` → `§9.B 行政关卡` |
| 只改一段 | `急救` + 贴那一段（走 §9.D，不进 RUNBOOK） |

**十一条铁律**：改前先存基线分 → 卡片先冻结 → 自下而上（实验在前，摘要在后）→ 一轮一块 → 先诊断后改写 → 逐条取舍 → 只写施工稿不动原文件 → 每次应用后跑体检脚本 → 冻结的块不再碰 → 文献必须自己核 → 中译英放最后。
