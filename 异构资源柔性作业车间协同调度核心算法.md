# 异构资源柔性作业车间协同调度核心算法
——论文《Integrated Scheduling of Flexible Job Shops with Heterogeneous Robot Arms and Conflict-Free AGV Routing: A Closed-Loop Bilevel Framework》的算法参考文档

> **文档用途**:本文档是后续**代码实现的唯一规格说明**。所有数据结构、算法步骤、默认参数、验证用例均以此为准;与《建模问题梳理_异构资源柔性作业车间协同调度.md》(下称"建模文档")的假设条款(A1–F3)保持一致,冲突时以建模文档为准。

---

## 一、论文创新点

1. **运输时间是决策的函数,而非常数矩阵**。车间布局显式建模为有向图 G=(V,E),AGV 的端到端运输耗时 = 所选路径弧时间之和 + 途中让行等待,由无冲突路由决策产生;这区别于标准 FJSP_PT 中"位置间行驶时间为已知常数矩阵"的设定。(建模文档 D1–D3)
2. **RA 异构(部分柔性 + 能力集内效率异构)使"路由感知的改派"真正有意义**。每道工序有可选 RA 集合 Ω_ji,且同一工序在不同 RA 上加工时间不同(unrelated 结构);当通往某 RA 的路段拥堵时,把工序改派到路网更通畅、加工稍慢的 RA 可能整体更优——这一权衡只有在"异构 + 显式路网"同时存在时才出现。(建模文档 B1、B2、0 节创新锚点)
3. **调度层—路由层闭环双层框架(Closed-Loop Bilevel)**。上层决策指派/排序/任务链,下层返回各运输任务的**实际**无冲突起止时间,上层据此评价并迭代修改(含拥堵反馈驱动的改派);区别于既有"先排产、后路由、路由不反馈"的单向两阶段方法。(建模文档 D5)
4. **集成收益的量化实验**。以"路网拥堵度 × RA 异构度"双因子实验量化闭环集成相对两阶段方法的改进幅度,并以柔性度作为收益的使能因子。(建模文档 F2)

**非创新点(须避免误写)**:"AGV 送达即走、多车接力"是 FJSP_PT 自 Bilge & Ulusoy (1995) 以来的标准设定,只作为假设写出(建模文档 C4)。

---

## 二、关键建模假设(代码实现必须遵守)

以下 8 条是建模文档 A–E 节的压缩版,括号内为对应条款:

1. **静态完备信息**:t=0 全部工件与全部 AGV 位于装卸站 LU(节点 v0);无释放期/交货期;所有参数已知。(A4, C6)
2. **工件**:工件 j 有固定工艺链 O_j1→…→O_j,n(j);加工与运输均不可抢占;工件不可拆分;工件间无外生顺序。(A2, A3, A5)
3. **RA**:部分柔性,工序 O_ji 只能派给 m ∈ Ω_ji(多数工序 |Ω_ji|≥2,全柔性为特例);加工时间 t^P(j,i,m) 依赖机器;一机一时刻至多一道工序;无故障;装夹并入加工时间。(B1, B2, B3, B6, B7)
4. **缓冲与交接**:每台 RA 的输入/输出缓冲与 LU 暂存容量充足(无 blocking);装卸时间为 0 且不占用 RA。→ 代码含义:**工序开始时间 = max(工件到达时刻, RA 空闲时刻)**,两个自变量可独立递推;完工件即刻移入输出缓冲,RA 空闲时刻与 AGV 无关。(B4, B5)
5. **AGV**:NA 辆同质、单载、匀速(空载载货同速);不计加减速与电量;运输任务为单次搬运(LU→首道 RA;换机的相邻工序间;末道 RA→LU 回运,由开关 δ_return 控制,基本模型 δ_return=1),任意车可承接、可跨车接力;卸货后按任务链空载就位,不强制回 LU,出发时刻可被路由层推迟。(C1–C6)
6. **路网与冲突**:G 强连通,弧通行时间 τ(e) 为常数;**通行资源独占**(定义见第五节),占用时间窗为半开区间 [t_start, t_end);各节点停靠位容量充足,停靠不占用通行资源,允许多车同站停靠、允许停靠等待。(D1–D3)
7. **死锁避免**:路由层采用时间窗预约式规划,新路径只能使用空闲时段,计划期内无冲突无死锁;任意"指派+排序+任务链"编码解码后必得有限完工时间的可行方案。(D4, B4)
8. **目标**:最小化 makespan C_max;δ_return=1 时为最后一件成品返抵 v0 的时刻,δ_return=0 时为末道工序最大完工时刻。(E1)

---

## 三、问题的输入、输出与解的构成

### 3.1 算例输入(建议 JSON 格式)

```json
{
  "name": "example_3x3x2",
  "delta_return": 1,
  "jobs": [
    {"id": 1, "num_ops": 3},
    {"id": 2, "num_ops": 2},
    {"id": 3, "num_ops": 2}
  ],
  "machines": [
    {"id": 1, "node": "r1"},
    {"id": 2, "node": "r2"},
    {"id": 3, "node": "r3"}
  ],
  "proc_time": {
    "(1,1)": {"1": 4, "2": 6, "3": 5},
    "(1,2)": {"1": 7, "2": 5, "3": 8},
    "(1,3)": {"1": 6, "2": 9, "3": 5},
    "(2,1)": {"1": 5, "2": 4, "3": 7},
    "(2,2)": {"1": 8, "2": 6, "3": 6},
    "(3,1)": {"1": 3, "2": 5, "3": 4},
    "(3,2)": {"1": 6, "2": 4, "3": 7}
  },
  "num_agvs": 2,
  "network": {
    "lu_node": "v0",
    "nodes": ["v0", "c1", "c2", "r1", "r2", "r3"],
    "corridors": [
      {"u": "v0", "v": "c1", "time": 2},
      {"u": "c1", "v": "r1", "time": 2},
      {"u": "c1", "v": "c2", "time": 3},
      {"u": "c2", "v": "r2", "time": 2},
      {"u": "c2", "v": "r3", "time": 2},
      {"u": "r1", "v": "c2", "time": 4}
    ]
  }
}
```

约定:
- `proc_time` 中键 `(j,i)` 表示工序 O_ji;值字典的键为 RA 编号,**缺失的 RA 即不在 Ω_ji 内**(部分柔性由稀疏性表达;上例为全柔性特例,故每行三列俱全);
- `corridors` 为**双向物理走廊**:每条走廊生成两条方向相反的有向弧,但**共用同一个独占预约资源**(见 5.1),从而自然禁止相向冲突与同弧同向追赶;
- 所有时间取非负整数(分钟),便于精确复现与断言测试。

### 3.2 解(一个完整方案)的构成

| 决策层次 | 内容 | 由谁产生 |
| --- | --- | --- |
| 1. 指派 | 每道工序 O_ji → 某台 m ∈ Ω_ji | 上层编码(MA 段) |
| 2. 机器排序 | 每台 RA 上工序的加工先后 | 上层编码(OS 段隐含) |
| 3. 运输任务派生与车辆指派 | 任务清单由指派派生;每个任务派给某辆 AGV 并在车上排序 | 解码时按派工规则确定(见 6.3) |
| 4. 路由 | 每个任务空载段+载货段的路径、出发时刻、途中等待 | 下层路由层 |

### 3.3 输出

- 全局时刻表:每道工序 (开始, 完成, 机器);每个运输任务 (车辆, 空载段路径与时窗, 载货段路径与时窗);每辆 AGV 的完整轨迹(含等待);
- C_max 与统计量:各车空载时间、总让行等待时间、各走廊占用率(供拥堵反馈与 E2 扩展目标使用);
- 可行性自检报告(见第九节校验器)。

---

## 四、核心算法总体架构

```text
┌─────────────────────── 上层:调度层(GA / 模因算法) ───────────────────────┐
│  种群(染色体 = MA 段 + OS 段)                                              │
│    │  选择/交叉/变异                                                        │
│    ▼                                                                        │
│  解码器(事件驱动仿真)──每遇运输任务──▶ 调用下层路由 ──▶ 实际起止时间      │
│    │                                                                        │
│    ▼                                                                        │
│  适应度 = 路由后的真实 C_max      ◀── 拥堵统计(各走廊等待时间)────────────┤
│    │                                                                        │
│    ▼                                                                        │
│  拥堵反馈局部搜索:对精英个体,依据拥堵统计把关键工序改派到                  │
│  "路网更通畅、加工稍慢"的 RA,重解码,择优接受                              │
└──────────────────────────────────────────────────────────────────────────┘
                          ▲                    │
            实际时间反馈  │                    │  计划任务(起讫点、最早出发时刻)
                          │                    ▼
┌─────────────────────── 下层:路由层(时间窗预约) ──────────────────────────┐
│  预约表(每条走廊的半开占用时窗列表)+ 时间窗 Dijkstra                       │
│  输入:起点、终点、最早出发时刻;输出:最早到达的无冲突路径(含等待)并落表  │
└──────────────────────────────────────────────────────────────────────────┘
```

**闭环的两条反馈通路**(这是与两阶段方法的本质区别,代码必须都实现):
1. **评价级反馈**:每个个体的适应度是**路由后**的真实 C_max(两阶段方法用常数矩阵估计排产、路由只做事后修复);
2. **决策级反馈**:路由层输出的拥堵统计直接驱动上层的改派算子(局部搜索),显式实现"拥堵 → 改派到稍慢 RA"的创新机制。

---

## 五、下层:无冲突路由层

### 5.1 冲突模型(严格定义,校验器同此)

- **通行资源** = 物理走廊(corridor)。走廊 {u,v} 的两条方向弧 u→v、v→u 共用一个预约资源;同一时刻至多一辆 AGV 在该走廊上行驶(不论方向)——同时排除相向冲突与同向追赶;
- **占用时窗**:车辆于 t 时刻从 u 进入走廊,占用窗为 **[t, t+τ)**(半开);t+τ 时刻资源已释放,另一车可在同一时刻进入(对应建模文档快照⑥ t=39 在 c1 的衔接);
- **节点**:穿越节点不停留、占用为零测度,不设节点预约;等待一律发生在节点**停靠位**(容量充足,不占通行资源)。因此**预约表只有走廊一种资源**——实现简单且与建模文档 D2"停靠与通行分离"完全一致;
- t=0 多车同在 v0 停靠位、终态多车同在 v0,均合法。

### 5.2 预约表(ReservationTable)

```text
数据结构: dict[corridor_id] -> 按起始时刻排序的占用区间列表 [(t_start, t_end, agv_id, task_id), ...]
接口:
  earliest_entry(corridor, t) -> t' ≥ t   # 从 t 起最早可进入该走廊的时刻(区间空隙查找)
  reserve(corridor, t_start, t_end, agv, task)   # 落表(断言无重叠)
  release_all(task)                              # 撤销某任务的全部预约(局部搜索重解码时用)
```

### 5.3 时间窗 Dijkstra(单任务单段路由)

```text
函数 route(start_node, goal_node, t_depart_earliest) -> (path, timeline, t_arrive)

标签: (node, t) = 于时刻 t 位于 node 的停靠位,代价 = t
初始化: push (start_node, t_depart_earliest)
循环: 弹出 t 最小的标签 (u, t)
  若 u == goal_node: 回溯得到 path 与各段 (走廊, 进入时刻, 离开时刻), 返回
  对 u 的每条出弧 (u→v, 走廊 c, 时长 τ):
    t_enter = ReservationTable.earliest_entry(c, t)   # 允许在 u 停靠等待到 t_enter
    松弛标签 (v, t_enter + τ)
```

要点:
- **等待被搜索天然涵盖**:earliest_entry 自动产生"在起点/中途节点停靠等待"的方案,建模文档 C5 的"默认尽早出发、可被路由层推迟"无须单独处理;
- 弧时间为常数且预约区间固定时,该搜索对"最早到达"是最优的(FIFO 性质);
- 找到路径后调用 reserve 逐段落表——**先规划者先占用**(prioritized planning),规划顺序由解码器的事件顺序决定(见 6.2),确定性由此保证;
- 复杂度:每次 route 为 O(|E|·W·log) 级(W 为单走廊平均预约区间数),被上层高频调用,须用堆 + 邻接表实现。

### 5.4 派生量:理想最短路矩阵 t*

对全体取放点/LU 两两跑一遍**无预约**的 Dijkstra,得 t*(d1,d2):
- 用途 1:解码器中车辆派工规则的快速估计(6.3);
- 用途 2:实例特征参数 T̄t/T̄p 的计算;
- 用途 3:两阶段 baseline 的排产阶段用它当常数运输时间矩阵(第八节)。

---

## 六、上层:调度层

### 6.1 编码(两段式染色体)

设全部工序总数 N(含回运伪工序前,见下),则:

- **MA 段**(machine assignment):长度 N 的向量,第 k 个位置对应固定枚举顺序下的工序 O_ji,取值 ∈ Ω_ji;
- **OS 段**(operation sequence):长度 N 的**工件号重复序列**(job j 出现 n(j) 次),第 x 次出现表示 O_jx;OS 是全局加工优先序,同机工序的相对顺序即由 OS 中先后决定。

**回运的统一处理(δ_return=1)**:为每个工件追加一道**伪工序** O_j,n(j)+1,固定"机器"= LU(节点 v0)、加工时间 = 0、不占任何 RA。伪工序不进 MA 段(机器固定),但**进 OS 段**(job j 在 OS 中出现 n(j)+1 次),其"运输"即成品回运任务。这样回运任务与普通任务在同一解码流程中竞争车辆,排序由 OS 统一决定,无需特殊分支。

任务/车辆指派(子问题 3)**不编码**,由解码时的派工规则确定(6.3);这是 FJSP_PT 文献的通行做法,保证任意染色体可解码。(扩展:第三段 TA 编码,留作后续对比,不在基本实现内。)

### 6.2 解码器(事件驱动仿真;适应度评价的核心)

```text
函数 decode(MA, OS, instance) -> (timetable, C_max, congestion_stats)

状态: 每台 RA 的空闲时刻 free[m]=0;每个工件的当前位置 pos[j]=v0、就绪时刻 ready[j]=0;
      每辆 AGV 的当前位置 loc[k]=v0、空闲时刻 avail[k]=0;预约表清空。

按 OS 从左到右取工序 O_ji(含回运伪工序):
  m ← MA 中该工序的机器(伪工序 m=LU);dest ← loc(m)
  1) 若 pos[j] == dest:            # 同机连续工序,无运输任务(C4)
       arrive ← ready[j]
  2) 否则产生运输任务 T=(j, i, pickup=pos[j], dest, ready=ready[j]):
       k* ← 派工规则选车(见 6.3)
       # 空载段:从 loc[k*] 出发,最早出发时刻 avail[k*]
       (…, t_at_pickup) ← route(loc[k*], pickup, avail[k*])        # loc[k*]==pickup 时该段为空
       t_load ← max(t_at_pickup, ready[j])                          # 车等件或件等车(B4)
       # 载货段:
       (…, arrive) ← route(pickup, dest, t_load)
       更新 loc[k*] ← dest;avail[k*] ← arrive                      # 卸货即走/即空闲(B5、C5)
  3) 工序开工与完工:
       start ← max(arrive, free[m])                                 # B4 核心公式
       finish ← start + t^P(j,i,m)                                  # 伪工序 t^P=0
       free[m] ← finish;pos[j] ← dest;ready[j] ← finish
C_max ← max over 全部工件的最后一道(伪)工序 finish
```

要点:
- OS 天然是工艺先后的拓扑序(第 x 次出现即第 x 道工序),解码中 ready[j] 恒已知;
- 路由的**预约顺序 = OS 扫描中任务产生的顺序**,解码完全确定(同一染色体必得同一时刻表);
- 半主动解码 + B4/D3/D4 保证任意染色体解码可行、C_max 有限(建模文档 B4 三重保证);
- congestion_stats:解码过程中记录每条走廊上所有车辆的"进入前等待时长"累计,即 wait[c] = Σ(t_enter − t_ready_at_entry),供 6.5 使用。

### 6.3 车辆派工规则(子问题 3 的启发式)

对任务 T,逐辆 AGV k 估算**送达时刻**:

```text
est(k) = max( avail[k] + t*(loc[k], pickup), ready[j] ) + t*(pickup, dest)
```

取 est 最小者(并列取车号小者)为 k*。默认用理想矩阵 t* 估算(快、不动预约表);配置项 `dispatch_exact=True` 时改用两次试探性 route 精确评估(NA 小时可用,须回滚试探预约)。

### 6.4 遗传算子(基本 GA 配置)

| 环节 | 方法 |
| --- | --- |
| 初始化 | MA 段:按 Ω_ji 均匀随机;OS 段:工件重复序列随机洗牌。另注入两个启发式个体:MA 取行内最小 t^P;MA 取"全局负载均衡"贪心 |
| 选择 | 锦标赛(size=2)+ 精英保留 elite=5 |
| 交叉 | OS 段:POX(按工件子集保序交换);MA 段:均匀交叉。交叉率 pc=0.8 |
| 变异 | OS 段:随机交换两位;MA 段:随机选一道工序改派到 Ω_ji 内另一 RA。变异率 pm=0.2 |
| 适应度 | decode 返回的真实 C_max(评价级闭环) |
| 终止 | max_gen=200 或连续 stall_gen=30 代无改进 |
| 种群 | pop=100 |

### 6.5 拥堵反馈局部搜索(决策级闭环;创新机制的算法化)

每代对最优的 top_ls=10% 个体执行:

```text
1) 从该个体的 timetable 提取关键路径(从决定 C_max 的最后事件反向追溯:
   工序完工 ← 开工 ← max{工件到达, 机器空闲} 中取值较大的一支 ← 上游运输/上游工序,直至 t=0);
2) 对关键路径上的每道实工序 O_ji(按对 C_max 的时距贡献降序,至多试 L_ls=5 道):
   对每个候选 m' ∈ Ω_ji \ {当前 m},计算改派评分:
       score(m') = t*(pos_prev, loc(m')) + t^P(j,i,m') + λ · congestion(loc(m'))
     其中 congestion(loc(m')) = 通往 loc(m') 的入走廊等待统计 wait[c] 之和,λ 默认 0.5;
   取 score 最小的 m' 生成邻居(仅改 MA 一位),完整重解码;
3) 若邻居 C_max 更小则接受替换,并可继续从新个体迭代(首改进策略,最多 3 轮)。
```

该算子直接实现创新点 2 的机制:**拥堵统计(来自下层)改变指派决策(上层)**——"通往快机的路堵,则改派到路顺的慢机"。消融实验(去掉此算子 vs. 保留)即创新点的直接证据之一。

---

## 七、参数默认值汇总

| 参数 | 默认值 | 说明 |
| --- | --- | --- |
| pop / max_gen / stall_gen | 100 / 200 / 30 | 种群与终止 |
| pc / pm / elite | 0.8 / 0.2 / 5 | 遗传算子 |
| top_ls / L_ls / λ | 10% / 5 / 0.5 | 拥堵反馈局部搜索 |
| dispatch_exact | False | 派工规则用 t* 估算 |
| delta_return | 1 | 基本模型计回运 |
| random_seed | 必须显式传入 | 全流程可复现(F1) |

---

## 八、对比基线(实验必备)

1. **两阶段 open-loop(核心对照)**:排产阶段用与本文相同的 GA,但运输时间取常数 t* 矩阵(即退化为标准 FJSP_PT 求解);得到最优排产后,**冻结**指派/排序/任务链,再交路由层按同一预约机制逐任务路由,冲突以"原地等待、顺延"修复,报告修复后的真实 C_max。与闭环法的差值即"集成收益",按 F2 的拥堵×异构双因子扫描;
2. **闭环无反馈算子**(消融):完整框架去掉 6.5 局部搜索,量化决策级反馈的单独贡献;
3. **小规模精确解**:对示例级算例用 MILP/CP(可后续单独建模)提供下界参照(F3);
4. **调度规则基线**(可选):SPT + 最近车派工,给出快速下界参考。

---

## 九、验证用算例与测试断言(实现后必须全部通过)

**基准算例**:第 3.1 节 JSON 即建模文档第六、七节的示例(3 工件、3 RA、2 AGV、6 节点 6 走廊、全柔性特例、δ_return=1)。

| # | 测试点 | 断言 |
| --- | --- | --- |
| T1 | 最短路矩阵 | t*(v0,r1)=4, t*(r1,r2)=6, t*(r1,r3)=6, t*(r2,r3)=4 |
| T2 | 时刻表校验器 | 将建模文档第七节给出的完整可行方案(makespan=51)手工录入为时刻表,校验器判定:无走廊时窗重叠(半开区间;t=39 在 c1 的衔接合法)、工序时序/机器独占/车辆单载全部满足、C_max=51 |
| T3 | 解码可行性 | 随机生成 1000 条染色体逐一解码,校验器全部通过、C_max 全部有限 |
| T4 | 解码确定性 | 同一染色体解码两次,时刻表逐字段相等 |
| T5 | 同机免运输 | 构造相邻工序同机的染色体,确认不产生运输任务 |
| T6 | 回运开关 | 同一染色体在 δ_return=0/1 下解码,后者 C_max ≥ 前者,且前者等于末道实工序最大完工时刻 |
| T7 | 冲突消解 | 构造双车同时争用 v0–c1 的场景(如两首道任务同时出发),确认后规划者被推迟且总时刻表无重叠(对应快照① AGV2 让行) |
| T8 | 优化有效性 | 完整 GA(seed 固定)在基准算例上求得 C_max ≤ 51 |

**校验器(validator)独立于解码器实现**:输入时刻表 + 算例,逐条检查(a) 工艺先后,(b) 机器独占,(c) Ω 合法性与 t^P 一致,(d) 车辆单载与位置连续性,(e) 走廊时窗独占(半开),(f) C_max 口径。校验器是防止解码器与路由层隐性 bug 的仲裁者。

---

## 十、建议代码结构与命名映射

```text
project/
  instance.py        # 算例载入/生成(JSON schema 见 3.1);特征参数计算(T̄t/T̄p、异构度、柔性度、NA/NM)
  network.py         # 图结构、理想最短路 t*、ReservationTable、route()(5.2–5.4)
  decoder.py         # decode()(6.2)、派工规则(6.3)、congestion_stats
  ga.py              # 染色体、算子(6.4)、拥堵反馈局部搜索(6.5)、主循环
  baseline.py        # 两阶段 open-loop(八.1)、规则基线
  validator.py       # 独立校验器(九)
  experiments/       # F2 因子扫描脚本;结果表与甘特图输出
  tests/             # T1–T8
```

| 文档符号 | 代码命名建议 |
| --- | --- |
| O_ji / Ω_ji / t^P(j,i,m) | `op(j,i)` / `eligible[j][i]` / `proc_time[j][i][m]` |
| G=(V,E) / τ(e) / t* | `Network.nodes/corridors` / `corridor.time` / `Network.ideal_dist` |
| NA / NM / n / δ_return | `num_agvs` / `num_machines` / `num_jobs` / `delta_return` |
| C_max / 关键路径 | `makespan` / `critical_path` |
| 预约时窗 [t1,t2) | `Reservation(t_start, t_end, agv, task)` |

---

## 十一、与建模文档的追溯关系(自检)

| 本文档章节 | 依据的建模文档条款 |
| --- | --- |
| 二.1–8 假设 | A1–A5, B1–B7, C1–C6, D1–D4, E1(I.4 精简版) |
| 三.1 输入 | H 节一~四(参数表示);H 六(示例数据) |
| 五 路由层 | D1–D4(冲突定义、半开时窗、停靠分离、预约式死锁避免) |
| 六.2 解码 | B4(max 公式与三重保证)、C4/C5(任务触发与空载去向)、3.2 触发规则 |
| 六.5 反馈搜索 | 0 节创新锚点、D5 闭环、F2 预期结论 |
| 八 基线 | D5(两阶段对照)、F3(最优性对标、口径统一) |
| 九 验证 | H 七(makespan=51 完整方案与六个快照) |
| 十二 数据集 | F1(算例生成规则)、F2(实验因子)、F3(对标口径)、C6/E1(回运口径) |

---

## 十二、对比测试数据集与实验分层方案

**结论**:没有任何现成公开数据集同时包含"部分柔性 + 能力集内时间异构 + 显式路网无冲突路由"(这正是本文创新);但 **FJSPT 公开基准集**可作为底座,采用"**第一层退化对标 + 第二层受控扩展**"的实验设计。全部基准托管于:

> https://fastmanufacturingproject.wordpress.com/2019/04/11/fjspt-instances/ (2025 年仍在线,社区活跃使用)

### 12.1 候选公开数据集评估

| 数据集 | 规模 | 柔性 | 时间异构 | 显式路网 | 用途 |
| --- | --- | --- | --- | --- | --- |
| Bilge & Ulusoy (1995) | 82 实例(10 工件集 × 4 布局,2 AGV) | 无(\|Ω\|=1) | 无 | 无(常数矩阵) | 退化特例对标(文献结果最多);**4 个布局为单向导轨布局图,可还原为有向图 G** |
| Deroussi (2014) | 10 实例 | 有(每工序 2 台备选) | 无(备选机时间相同) | 无 | "柔性但零异构"对照组 |
| Kumar (2011) | 56 实例 | 有 | 无 | 无 | T̄t/T̄p 因子现成分组(≥0.25 / <0.25) |
| Homayouni set 1(SFJST/MFJST01-10) | 20 中小实例(Fattahi FJSP + 运输) | **有** | **有** | 无 | **第一层核心对标集** |
| Homayouni set 2(Brandimarte MK + Chambers-Barnes) | 31 大实例(55–240 工序) | **有**(MK 自带部分柔性) | **有** | 无 | 大规模测试;Ω 结构直接沿用 |

**不采用**:Pal 等 2023(多智能体综述)所用数据(Kacem 修改实例 + MK 加随机 setup/运输时间)——属"FJSP with transportation time"类,只计运输时间、**不调度车辆**(无有限 AGV 车队与任务指派),与本问题结构不符,且数据非完全公开(available on request)。

### 12.2 第一层:退化对标(证明上层算法竞争力)

- **配置**:关闭路由层冲突约束,运输时间取理想最短路矩阵 t*(代码上即 `route()` 退化为查 `Network.ideal_dist`,预约表禁用);δ_return=0(与基准口径一致);
- **数据**:Homayouni set 1/2 + Bilge 82 实例(+可选 Deroussi/Kumar);
- **对比对象(文献已发表结果,无需重跑)**:Homayouni & Fontes 2021(LAHC/局部搜索)、Homayouni 等 2023(BRKGA,多数实例最优)、Ham 2020(**CP 精确最优值**,即 F3 的下界参照)、Chaudhry 等 2022(GA);
- **报告口径**:与 best-known 的 gap(%);注意 Homayouni & Fontes 2021 已证明 Nouri 等 2016 部分结果无效,引用时排除。

### 12.3 第二层:受控扩展(展示闭环集成收益)

在第一层基准上按 F1 规则派生扩展实例,三个扩展维度:

- **(a) 路网 G**:优先由 Bilge 的 4 个导轨布局还原有向图(节点、弧、弧时间——最可辩护的来源);对无布局的 Homayouni 实例,按建模文档 D1 构造网格/环形路网,以路径冗余度、瓶颈弧占比控制拥堵倾向;
- **(b) 异构度 H**:对能力集内各机加工时间乘独立均匀扰动系数,扫描行内变异系数(如 H ∈ {0, 0.15, 0.3, 0.5});H=0 时退化为原基准(回归检验);
- **(c) 柔性度 F**:MK 自带部分柔性直接沿用;Fattahi/Bilge 类按目标平均 \|Ω\|/NM 随机生成(保证多数 \|Ω\|≥2,见 B1)。

**扩展实例命名规则**:`<基础实例>-L<布局>-H<异构度>-F<柔性度>[-A<AGV数>]`,如 `BU04-L2-H0.3-F0.6-A3`;生成脚本与随机种子随论文公开(F1 可复现要求)。

**核心实验矩阵**(对应 F2):拥堵度(布局/车流) × 异构度 H 双因子为主试验;柔性度 F、NA/NM、T̄t/T̄p 为辅助扫描。每个格子上跑:闭环完整版、闭环去反馈算子(消融)、两阶段 open-loop(八.1)、小实例 MILP/CP 下界。**预期结论**:拥堵与异构越高,闭环相对两阶段的改进越大;F→\|Ω\|=1 时改派机制失效、收益衰减。

### 12.4 数据准备清单(代码实现时执行)

1. 从上述网站下载 Bilge(82)、Deroussi(10)、Kumar(56)、Homayouni set 1(20)、set 2(31)原始文件;
2. 编写 `instance.py` 的格式转换器:各基准原始格式 → 3.1 节 JSON schema(退化模式下 `network` 用完全图 + t* 弧时间表示,或直接提供 `ideal_dist` 矩阵);
3. 从 Bilge 论文的 4 个布局图数字化导轨拓扑,存为 4 个可复用的 `network` 模板;
4. 实现扩展实例生成器(a/b/c 三维度 + 命名规则 + 种子记录),并将每个扩展实例的五个特征参数(H 五节)写入实例文件头;
5. 建立 best-known 结果表(从 Homayouni 2021/2023、Ham 2020、Chaudhry 2022 论文辑录),供实验脚本自动计算 gap。

## 十三、新论文章节结构参考(与旧论文的对应关系)

**结论**:新论文(*Integrated Scheduling of Flexible Job Shops with Heterogeneous Robot Arms and Conflict-Free AGV Routing: A Closed-Loop Bilevel Framework*)的章节结构以**旧论文 `paper01/paper - 20260326.tex` 为主要参考**。对比过 Pal 等 2023(ESWA,多智能体 FJSP)后确认:两者顶层骨架相同(引言→文献→问题→方法→实验→结论),但旧论文在方法章的"框架总览 → 两层求解器 → 层间协调与理论性质"组织、以及实验章的 Baselines/Ablation/Scalability/Sensitivity 分层上深度更高,且与本文档的内容天然一一对应。

### 13.1 方法章对应表(旧论文第 4 章 ↔ 新论文)

| 旧论文第 4 章(Hybrid DP Framework) | 新论文对应内容 | 本文档出处 |
| --- | --- | --- |
| Framework Overview | 闭环双层框架总览(两条反馈回路) | 第四节 |
| CSS-DP(连续层求解器) | 下层:时间窗 Dijkstra 无冲突路由 | 第五节 |
| DES-DP(离散层求解器) | 上层:GA/Memetic 调度层 | 第六节 |
| Coordination Mechanism | 评价级闭环(真实 C_max 做适应度)+ 决策级闭环(拥堵反馈改派) | 四、6.5 |
| Convergence / Complexity | 解码三重保证、可行性论证、复杂度分析 | 6.2、九 |

### 13.2 实验章对应表(旧论文第 5 章 ↔ 新论文)

| 旧论文第 5 章小节 | 新论文对应实验 | 本文档出处 |
| --- | --- | --- |
| Baseline Methods | 两阶段 open-loop、无反馈消融、规则基线、小实例 MILP/CP | 第八节 |
| Main Results | 公开基准退化对标 + 扩展算例主试验 | 12.2、12.3 |
| Ablation Study | 闭环去反馈算子 vs 完整版 | 8.2 |
| Scalability / Sensitivity | 拥堵度 × 异构度双因子;柔性度、NA/NM、T̄t/T̄p 辅助扫描 | 12.3 |
| Qualitative Analysis | 示例算例甘特图与关键路径叙事(closed vs twostage vs rule) | 九(T2/T8) |

### 13.3 需要调整与借鉴之处

- **问题建模章(第 3 章)重组**:旧论文按 CSS/DES 动力学分小节,新论文改为"车间调度模型(RA、工序、Ω)/ 路网与冲突模型(走廊、时间窗)/ 耦合约束与目标函数"三块,其余骨架照搬;
- **借鉴 ESWA 论文两点**:① 实验结果按"对手类别"分小节组织(与两阶段开环比、与规则/元启发式比),贴合"闭环 vs 开环"叙事;② Introduction 末尾放显式贡献 bullet + 一句话 gap 定位(如 "to the best of our knowledge, no work considers conflict-free routing coupled with heterogeneous robot arms"),作为审稿人检索创新点的锚点;
- **结论章沿用旧论文四段式**:Contributions → Insights → Limitations → Future Directions。
