# 异构资源柔性作业车间协同调度核心算法

——论文《**Conflict-Guided Feedback for Integrated Flexible Job Shop Scheduling and Conflict-Free AGV Routing with Heterogeneous Robot Arms/ Contention-Guided Feedback for Integrated Scheduling of Heterogeneous Robot Arms and Conflict-Free AGV Routing in Flexible Job Shops**》的算法参考文档

> **文档用途**:本文档是后续**代码实现的唯一规格说明**,同时也是**相关工作核查结论**与**实证检验结果**的唯一记录(原《创新方向设计方案_闭环双层框架深化.md》已合并入本文件 1.2、5.5、8.2、第十三节并删除)。所有数据结构、算法步骤、默认参数、验证用例均以此为准;与《建模问题梳理_异构资源柔性作业车间协同调度.md》(下称"建模文档")的假设条款(A1–F3)保持一致,**冲突时以建模文档为准**。
>
> **阅读提示**:第一~十二节是规格与设计;**第十三节是实施后的实证检验结果,其中推翻了若干设计预期**。若某处设计描述与第十三节的结论冲突,以第十三节为准。

---

## 一、论文创新点与创新性核查

### 1.1 创新点(已按实证结果修订)

1. **运输时间是决策的函数,而非常数矩阵**。车间布局显式建模为图 G=(V,E),AGV 的端到端运输耗时 = 所选路径弧时间之和 + 途中让行等待,由无冲突路由决策产生;这区别于标准 FJSP_PT 中"位置间行驶时间为已知常数矩阵"的设定。(建模文档 D1–D3)

2. **RA 异构(部分柔性 + 能力集内效率异构)使"路由感知的改派"真正有意义**。每道工序有可选 RA 集合 Ω_ji,且同一工序在不同 RA 上加工时间不同(unrelated 结构);当通往某 RA 的路段拥堵时,把工序改派到路网更通畅、加工稍慢的 RA 可能整体更优——这一权衡只有在"异构 + 显式路网"同时存在时才出现。**这是本文最不可替代的差异化资产**:最接近的竞品用的是每工序单机、加工时间固定的纯 JSP 基准,该机制在其问题设定下根本不存在(见 1.2)。(建模文档 B1、B2)

3. **冲突凭证制导的闭环反馈(Conflict-Guided Feedback,本文核心机制)**。层间接口向上传递的不是"时间",也不是"资源价格",而是**结构化的冲突凭证**——某次让行发生在哪条走廊、哪个时段、被谁挡住。它由三个部件构成:

   - **(a) 带类型标签的关键链归因**:把关键路径从"工序链"扩展为"工序—运输交替链",并把瓶颈精确归入五类之一(`operation` / `machine` / `upstream` / `vehicle` / `corridor`,后者带具体走廊与时段)。若不做这一步,开工被"工件到达"卡住时无法区分是上游慢、车不够还是路上堵,反馈算子必然无的放矢(详见 6.5 第 1 步);
   - **(b) 两族定向邻域算子**:拥堵有两种缓解方式——**换地方**(改派到路网更通畅的 RA)与**换时间**(错峰:把被堵工序前移或把对手工序后移)。后者的操作对象由冲突凭证直接**指认**,而非随机抽取,这是与通用 GA 变异算子的本质区别(详见 6.5 第 2 步);
   - **(c) 派车决策的闭环化**:派车不再用理想最短路矩阵估算送达时刻,而查预约表做真实两段试探。这补齐了框架中最后一处开环残余(详见 6.3)。

   与既有工作的区别:既有方法或是"先排产、后路由、路由不反馈"的单向两阶段,或是"生成后一次性重定时"的伪闭环(反馈只改时间轴、不改任何指派决策、且不迭代)。本文的反馈**每代每个体都发生**,且**改变决策而非仅改时间**。(建模文档 D5)

4. **集成收益的量化实验与严格的实验协议**。以"路网拥堵度 × RA 异构度"双因子实验量化闭环集成相对两阶段方法的改进幅度,以柔性度作为收益的使能因子。**并强制两条协议:同算力预算比较、≥10 个种子且报告离散度**——诊断实验证实,不遵守这两条会把"多花了算力"误读为"机制更好"(见 8.2、13.2)。(建模文档 F2)

**附:一个可复现的负面结果及其机制解释(方法论贡献)**。本文实现并检验了"把层间接口价格化"的设想——由下层导出走廊-时段的影子价格,上层与路由都按价格计价。结果是该机制**系统性有害**,且换用定义式有限差分价格同样无效。给出的机制解释("重复计价")进一步推出一条对同类架构有普适意义的设计推论(见 5.5、13.3)。如实报告负面结果 + 机制解释,在方法论上比再增加一个改进型启发式更难被质疑。

### 1.2 相关工作核查:创新性归属

**核查的三条总结论**:

1. **总体创新性成立,且比预想更稳固**。2026 年 1 月上线的直接竞品在其局限性一节**自认**其框架是"两阶段顺序优化而非真正的闭环",这句话是本文闭环主张的最佳引用锚点;
2. **单项技术大多已有出处**,创新性必须落在**耦合机制**上,不能落在"我们用了 PBS / 我们考虑了拥堵"这类表述上;
3. **RA 时间异构是护城河**,所有创新点的论述都应围绕它组织。

#### 直接竞品:Sensors 2026, 26(2):543

*A New Framework for Job Shop Integrated Scheduling and Vehicle Path Planning Problem*(2025-11 投稿,2026-01-13 上线)。三层框架:调度层(改进 DQN / D3QPN)→ 任务分配层(甘特图预处理自动生成运输任务)→ 路径执行层(PBS + Space-Time A\*);建立 MIDN 模型含点—边冲突约束;基准 FT06、LA、SWV、YN,车队 4–5 辆。

**四条实质差异**(逐条可辩护):

| # | 维度 | 竞品 | 本文 |
| --- | --- | --- | --- |
| 1 | 问题类别 | JSP-T,原文明确"每道工序有固定加工时间",基准均为**每工序单机**的纯 JSP;**无机器指派决策、无加工时间异构** | FJSP,部分柔性 + 能力集内时间异构;创新点 2 的改派机制在竞品设定下**不可能存在** |
| 2 | **耦合方式(最关键)** | 原文自述:"operates primarily as a **two-stage sequential optimization strategy rather than a fully dynamic, real-time closed-loop system**… relies on generating an initial high-quality schedule which is then refined via a **one-time feedback adjustment**… this 'Generate-then-Refine' approach… The global optimality of the final solution is partially dependent on the quality of the initial schedule." | 评价级闭环(路由后真实 C_max 作适应度,每代每个体)+ 决策级闭环(冲突凭证驱动改派与错峰) |
| 3 | 反馈内容 | 到达时刻,用于修正时间轴 | 结构化冲突凭证,用于修正决策 |
| 4 | 上层方法 | DQN / D3QPN | GA / 模因算法。**此条为中性差异,不构成优势,不必强调** |

引用第 2 行原文即可完成 gap 定位。

#### 次近竞品:Sensors 2023, 23(9):4526

FJSP + 无冲突路由,两阶段:第一阶段混合 GA 排产,第二阶段基于 AGV 编码的 GA + 时间窗 Dijkstra 解冲突。**是 FJSP(有柔性),但两阶段单向、无反馈**。这正是 `baseline.py` 中 `two_stage_baseline` 所对标的方法族。

#### 分技术要素的出处归属

| 技术要素 | 已有出处 | 本文可主张的增量 |
| --- | --- | --- |
| PBS / Space-Time A\* 做车间无冲突路由 | Sensors 2026(路径执行层) | 嵌入适应度内循环 + 时序耦合下的收敛性(**尚未实现**,见 13.5) |
| CBS 及 makespan 目标变体(EBC、CBSm) | AAAI/IFAAMAS 2025 | 仅作小规模**下层最优性下界**参照,不主张改进 CBS |
| MAPF-LNS / LNS2 / ALNS(destroy-repair、RandomWalk、bandit 选邻域) | IJCAI 2021、AAAI 2022、SoCS 2024–2025 | 迁移到"调度适应度的增量评价",而非 MAPF 自身求解(**尚未实现**) |
| 拥堵感知派车规则(congestion function / penalty) | Electronics 2026, 15(1):139 | 其拥堵度 = 邻近 AGV 数量(空间密度、DES 仿真、纯车队调度);本文为**预约表导出的真实试探**,且驱动**机器改派**而非仅派车 |
| AGV 派车作为染色体第三段(OS+MA+AD 三层编码) | Appl. Sci. 2025(Q-HMOABC, FJSP-MTA) | 已有,**不作为创新点**;若用只作对比配置 |
| 派车用规则、机器/排序用学习或搜索 | ESWA 2025(CADRL)等 | 与本文一致,属通行做法 |

### 1.3 非创新点与须避免的表述

- **"AGV 送达即走、多车接力"**:FJSP_PT 自 Bilge & Ulusoy (1995) 以来的标准设定,只作为假设写出(建模文档 C4);
- **"我们用 PBS / 时间窗 Dijkstra 做无冲突路由"**:已被上述竞品抢占,不可作为贡献表述;
- **"我们考虑了拥堵感知派车"**:Electronics 2026 已有;本文的增量在"真实试探"与"驱动改派",须精确措辞;
- **"把 AGV 派车编入染色体第三段"**:Appl. Sci. 2025 已有;
- **伪创新四类(建议完全回避)**:给 GA 加自适应交叉概率;混一个 VNS/模拟退火进去叫"混合算法";把 Dijkstra 换成 A\* 并宣称加速;加一个多目标(能耗/负载均衡)充实贡献。这四类在 FJSP-AGV 文献里密度极高,加了反而暴露主贡献单薄。

---

## 二、关键建模假设(代码实现必须遵守)

以下 8 条是建模文档 A–E 节的压缩版,括号内为对应条款:

1. **静态完备信息**:t=0 全部工件与全部 AGV 位于装卸站 LU(节点 v0);无释放期/交货期;所有参数已知。(A4, C6)
2. **工件**:工件 j 有固定工艺链 O_j1→…→O_j,n(j);加工与运输均不可抢占;工件不可拆分;工件间无外生顺序。(A2, A3, A5)
3. **RA**:部分柔性,工序 O_ji 只能派给 m ∈ Ω_ji(多数工序 |Ω_ji|≥2,全柔性为特例);加工时间 t^P(j,i,m) 依赖机器;一机一时刻至多一道工序;无故障;装夹并入加工时间。(B1, B2, B3, B6, B7)
4. **缓冲与交接**:每台 RA 的输入/输出缓冲与 LU 暂存容量充足(无 blocking);装卸时间为 0 且不占用 RA。→ 代码含义:**工序开始时间 = max(工件到达时刻, RA 空闲时刻)**,两个自变量可独立递推;完工件即刻移入输出缓冲,RA 空闲时刻与 AGV 无关。(B4, B5)
5. **AGV**:NA 辆同质、单载、匀速(空载载货同速);不计加减速与电量;运输任务为单次搬运(LU→首道 RA;换机的相邻工序间;末道 RA→LU 回运,由开关 δ_return 控制,基本模型 δ_return=1),任意车可承接、可跨车接力;卸货后按任务链空载就位,不强制回 LU,出发时刻可被路由层推迟。(C1–C6)
6. **路网与冲突**:G 强连通,弧通行时间 τ(e) 为常数;**通行资源独占**(定义见 5.1),占用时间窗为半开区间 [t_start, t_end);各节点停靠位容量充足,停靠不占用通行资源,允许多车同站停靠、允许停靠等待。(D1–D3)
7. **死锁避免**:路由层采用时间窗预约式规划,新路径只能使用空闲时段,计划期内无冲突无死锁;任意"指派+排序+任务链"编码解码后必得有限完工时间的可行方案。(D4, B4)
8. **目标**:最小化 makespan C_max;δ_return=1 时为最后一件成品返抵 v0 的时刻,δ_return=0 时为末道工序最大完工时刻。**口径唯一定义为"全部工件末道(伪)工序完成时刻的最大值,不含任何空载段"**(见 13.4 关于当前实现口径分散的待办)。(E1)

**假设与创新点的支撑关系**(创新点变更后的追溯):

| 创新点 | 依赖的假设 | 说明 |
| --- | --- | --- |
| 1 运输时间为决策函数 | 假设 6、7 | 独占走廊 + 半开时窗 + 预约式死锁避免,使"让行等待"成为可计算的量 |
| 2 异构使改派有意义 | 假设 3 | Ω_ji 非单元素且 t^P 依机器而异,是改派存在的前提;|Ω_ji|=1 时机制自动失效(F2 的使能因子检验) |
| 3(a) 关键链五类归因 | 假设 4 | `start = max(arrive, free[m])` 的两个自变量可独立递推,才能判定 `bind` 落在哪一支,归因才有定义 |
| 3(b) 错峰算子 | 假设 6 中"停靠不占通行资源" | 允许无限等待,故 OS 移位后必仍可解码,不会因错峰产生死锁 |
| 3(c) 派车试探 | 假设 7 | 任意染色体可解码,故试探失败也有兜底;回滚不改变可行性 |
| 4 集成收益实验 | 假设 8 | C_max 口径唯一,跨档比较才成立 |

---

## 三、问题的输入、输出与解的构成

### 3.1 算例输入(JSON 格式)

```json
{
  "name": "example_3x3x2",
  "delta_return": 1,
  "jobs": [
    {"id": 1, "num_ops": 3},
    {"id": 2, "num_ops": 2},
    {"id": 3, "num_ops": 2}
  ],
  "machines": [
    {"id": 1, "node": "r1"},
    {"id": 2, "node": "r2"},
    {"id": 3, "node": "r3"}
  ],
  "proc_time": {
    "(1,1)": {"1": 4, "2": 6, "3": 5},
    "(1,2)": {"1": 7, "2": 5, "3": 8},
    "(1,3)": {"1": 6, "2": 9, "3": 5},
    "(2,1)": {"1": 5, "2": 4, "3": 7},
    "(2,2)": {"1": 8, "2": 6, "3": 6},
    "(3,1)": {"1": 3, "2": 5, "3": 4},
    "(3,2)": {"1": 6, "2": 4, "3": 7}
  },
  "num_agvs": 2,
  "network": {
    "lu_node": "v0",
    "nodes": ["v0", "c1", "c2", "r1", "r2", "r3"],
    "corridors": [
      {"u": "v0", "v": "c1", "time": 2},
      {"u": "c1", "v": "r1", "time": 2},
      {"u": "c1", "v": "c2", "time": 3},
      {"u": "c2", "v": "r2", "time": 2},
      {"u": "c2", "v": "r3", "time": 2},
      {"u": "r1", "v": "c2", "time": 4}
    ]
  }
}
```

约定:
- `proc_time` 中键 `(j,i)` 表示工序 O_ji;值字典的键为 RA 编号,**缺失的 RA 即不在 Ω_ji 内**(部分柔性由稀疏性表达;上例为全柔性特例,故每行三列俱全);
- `corridors` 为**双向物理走廊**:每条走廊生成两条方向相反的有向弧,但**共用同一个独占预约资源**(见 5.1),从而自然禁止相向冲突与同弧同向追赶;
- 所有时间取非负整数(分钟),便于精确复现与断言测试;
- 可选键 `_comment` 用于记录算例的设计意图(如拥堵型算例的构造理由),载入时忽略。

**已内置的两个算例**:

| 算例 | 规模 | 特征 | 用途 |
| --- | --- | --- | --- |
| `example_3x3x2` | 3 工件 / 3 RA / 2 AGV / 6 节点 6 走廊 | 全柔性特例,δ_return=1,已知可行解 C_max=51 | T1–T8 验证基准 |
| `congested_8x4x4` | 8 工件×3 工序 / 4 RA / 4 AGV / 7 节点 8 走廊 | 哑铃拓扑,中间 c1–c2(τ=6)为设计意图上的瓶颈;近端 RA1/RA2 慢、远端 RA3/RA4 快;Tt/Tp=1.16,异构度 0.30,柔性度 0.96,NA/NM=1.0 | 机制诊断:构造"是否值得为快机穿越拥堵"的张力 |

**关于 `congested_8x4x4` 的实测修正(重要,影响算例设计)**:占用率画像显示该算例**并非单瓶颈**,而是**两级串联咽喉**(closed 档,C_max=82,桶宽 6.75,共 13 桶):

| 走廊 | 平均占用 | 峰值 | 忙桶数 |
| --- | --- | --- | --- |
| `c1–c2`(设计瓶颈) | 0.68 | 1.00 | 12 / 13 |
| **`v0–c1`(LU 出口)** | **0.55** | **1.00** | 11 / 13 |
| `c1–r2` | 0.38 | 1.00 | 10 / 13 |
| 其余 5 条 | ≤ 0.25 | ≤ 0.59 | ≤ 9 |

LU 出口 `v0–c1` 的负载与设计瓶颈接近,且同样出现满占用的时段。这一点有实质影响:**`v0–c1` 的拥堵与机器指派无关**——每个工件的首道送达与成品回运都必须穿过它,不论工序派给哪台 RA;而 `c1–c2` 的拥堵才是**指派相关**的(只有去远端快机的工件才用它)。因此该算例的总拥堵中有相当一部分**不含决策杠杆**,它只是抬高所有方案的基线延误,却不为"改派/错峰"提供可利用的差异。

**这可能是 13.2 中各机制未显示增益的一个结构性原因**(尚未证实,需对照实验):机制能利用的信号被决策无关的串联排队稀释了。**对 12.3 扩展算例生成的直接要求**:应显式控制"指派相关拥堵占总拥堵的比例",例如给 v0 设多个出口或加宽 LU 出口走廊,使串联漏斗不再是紧约束,把瓶颈集中到指派相关的中段。建议把该比例作为算例特征参数与拥堵度并列记录。

### 3.2 解(一个完整方案)的构成

| 决策层次 | 内容 | 由谁产生 |
| --- | --- | --- |
| 1. 指派 | 每道工序 O_ji → 某台 m ∈ Ω_ji | 上层编码(MA 段) |
| 2. 机器排序 | 每台 RA 上工序的加工先后 | 上层编码(OS 段隐含) |
| 3. 运输任务派生与车辆指派 | 任务清单由指派派生;每个任务派给某辆 AGV 并在车上排序 | 解码时按派车规则确定(见 6.3) |
| 4. 路由 | 每个任务空载段+载货段的路径、出发时刻、途中等待 | 下层路由层 |

### 3.3 输出

- 全局时刻表:每道工序 (开始, 完成, 机器);每个运输任务 (车辆, 空载段路径与时窗, 载货段路径与时窗);每辆 AGV 的完整轨迹(含等待);
- C_max 与统计量:各车空载时间、总让行等待时间、各走廊累计让行等待(`congestion`);
- **走廊占用率画像**(`summary.json` 的 `occupancy` 字段):每条走廊的平均/峰值占用与忙桶数、瓶颈走廊、最忙的若干走廊-时段。由 `report.corridor_occupancy` **从时刻表独立重算**,仅对每个模式的最终最优解算一次,不参与搜索(前瞻性信号的使用边界见 5.2);
- 实例特征参数与复合下界:除 T̄t/T̄p、异构度、柔性度、NA/NM 外,还含结构指标 `funnel_share` / `lu_min_cut` / `far_group_cut` 与三分量下界(定义见 12.3.5);
- 可行性自检报告(见第九节校验器)。

---

## 四、核心算法总体架构

```text
┌─────────────────────── 上层:调度层(GA / 模因算法) ───────────────────────┐
│  种群(染色体 = MA 段 + OS 段)                                              │
│    │  选择 / POX 交叉 / 均匀交叉 / 变异                                     │
│    ▼                                                                        │
│  解码器(事件驱动仿真)──每遇运输任务──▶ 派车 + 调用下层路由 ──▶ 实际时间   │
│    │                                                                        │
│    ▼                                                                        │
│  适应度 = 路由后的真实 C_max                        ①评价级反馈             │
│    │                                                                        │
│    ▼                                                                        │
│  冲突制导局部搜索(对精英个体):                     ②决策级反馈             │
│    第 1 步 关键链五类归因(operation/machine/upstream/vehicle/corridor)     │
│    第 2 步 按归因派算子:改派(换地方) | 错峰(换时间)                      │
│    第 3 步 首改进接受,最多 ls_rounds 轮                                     │
└──────────────────────────────────────────────────────────────────────────┘
              ▲                                        │
   ①真实起止时间                                       │ 计划任务
   ②**冲突凭证**(走廊 c、时段 [t1,t2)、对手任务)      │ (起讫点、最早出发时刻)
              │                                        ▼
┌─────────────────────── 下层:无冲突路由层(时间窗预约) ────────────────────┐
│  容量化预约表(每条走廊的半开占用时窗列表,支持撤销与检查点回滚)            │
│  + 时间窗 Dijkstra(最早到达)                                              │
│  [可选并默认关闭:价格加权多标签 Pareto 搜索,见 5.5 —— 经检验有害]        │
└──────────────────────────────────────────────────────────────────────────┘
```

**闭环的两条反馈通路**(与两阶段方法的本质区别,代码必须都实现):

1. **评价级反馈**:每个个体的适应度是**路由后**的真实 C_max(两阶段方法用常数矩阵估计排产、路由只做事后修复);
2. **决策级反馈**:路由层输出的**冲突凭证**(而非仅时间、也非价格)驱动上层的定向邻域算子,显式实现"拥堵 → 改派到稍慢 RA"与"拥堵 → 错峰避让"两条机制。

**为何是"冲突凭证"而不是"价格"**:见 5.5 的负面结论与 13.3 的机制解释。简言之——走廊争用的延误已完整内化在车辆自身的到达时刻中并被适应度计入,再对占用计价属重复计价;而冲突凭证不改变下层的目标函数,只改变上层邻域的**构造方式**,故不引入这种重复。

---

## 五、下层:无冲突路由层

### 5.1 冲突模型(严格定义,校验器同此)

- **通行资源** = 物理走廊(corridor)。走廊 {u,v} 的两条方向弧 u→v、v→u 共用一个预约资源;同一时刻至多一辆 AGV 在该走廊上行驶(不论方向)——同时排除相向冲突与同向追赶;
- **占用时窗**:车辆于 t 时刻从 u 进入走廊,占用窗为 **[t, t+τ)**(半开);t+τ 时刻资源已释放,另一车可在同一时刻进入(对应建模文档快照⑥ t=39 在 c1 的衔接);
- **节点**:穿越节点不停留、占用为零测度,不设节点预约;等待一律发生在节点**停靠位**(容量充足,不占通行资源)。因此**预约表只有走廊一种资源**——实现简单且与建模文档 D2"停靠与通行分离"完全一致;
- t=0 多车同在 v0 停靠位、终态多车同在 v0,均合法。

### 5.2 容量化预约表(ReservationTable)

```text
数据结构: dict[corridor_id] -> 按起始时刻排序的占用区间列表 [(t_start, t_end, agv_id, task_id), ...]
接口:
  earliest_entry(corridor, t, τ) -> t' ≥ t   # 从 t 起最早可占用 [t', t'+τ) 的时刻
  feasible_entries(corridor, t, τ, limit)    # 若干个可行进入时刻(首个即最早);多标签路由用
  reserve(corridor, t_start, t_end, agv, task)   # 落表(断言不超容量)
  release_all(task) -> int                       # 撤销某任务的全部预约
  checkpoint() -> token / rollback(token)        # 检查点与回滚
  occupancy(bucket_width) -> {(corridor, bucket): util}   # 走廊-时段占用率 ∈ [0,1]
容量:
  capacity(corridor, t_start, t_end) -> int      # 默认 1(独占语义);跨桶取最小值
```

**容量参数化的用途**:默认容量恒为 1,与独占语义完全等价(重叠计数须为 0)。容量提升**仅**用于影子价格的有限差分探测(5.5)——把某个走廊-时段的容量临时置为 2、重解一次,C_max 的改善量即该时空槽位的边际价值。

**checkpoint / rollback 的用途**:支撑 6.3 的派车试探。评估某辆车时,空载段必须**真实落表**载货段才能看见它占用的时窗(否则两段可能被规划到同一走廊的同一时段,得到不可实现的乐观估计),评估完毕后整体回滚,不留痕迹。

**两类拥堵信号不可混用**(概念区分,写入论文的方法学要点):

| 信号 | 来源 | 性质 | 正确用途 |
| --- | --- | --- | --- |
| `occupancy`(占用率) | 预约表 | **前瞻性**:我若此时前往,可能受阻 | 改派/派车的事前评分 |
| `congestion`(累计让行) | 解码统计 | **回顾性**:这里已经真的堵住了 | 定位关键瓶颈、构造冲突凭证 |

早期实现把回顾性信号当前瞻信号使用,是一处概念混用;现已按上表分离。

### 5.3 时间窗 Dijkstra(单任务单段路由,默认路径)

```text
函数 route(start, goal, t0, agv, task, commit=True) -> RoutePlan
RoutePlan 字段: start, goal, t0, arrive, segments[], wait_by_corridor{}, price_cost
                并提供 wait_events() -> [(走廊, 等待起, 等待止, 时长)]

标签: (node, t) = 于时刻 t 位于 node 的停靠位,代价 = t
初始化: push (start, t0)
循环: 弹出 t 最小的标签 (u, t)
  若 u == goal: 回溯得到 segments,返回
  对 u 的每条出弧 (u→v, 走廊 c, 时长 τ):
    t_enter = ReservationTable.earliest_entry(c, t, τ)   # 允许在 u 停靠等待到 t_enter
    松弛标签 (v, t_enter + τ)
若 commit: 逐段 reserve 落表
```

要点:
- **等待被搜索天然涵盖**:earliest_entry 自动产生"在起点/中途节点停靠等待"的方案,建模文档 C5 的"默认尽早出发、可被路由层推迟"无须单独处理;
- **单标签的正确性**:因为节点可无限等待且停靠不占通行资源,"早到弱优于晚到"的支配关系成立(早到者总能等待以模拟晚到者的任何后续),故每节点只保留最早到达时刻即为最优——不需要 SIPP 式的多区间标签。这一性质在引入第二个准则(如价格)后立即失效,那时**必须**改为多标签 Pareto 搜索(见 5.5);
- 找到路径后逐段落表——**先规划者先占用**(prioritized planning),规划顺序由解码器的事件顺序决定(见 6.2),确定性由此保证;
- `wait_events()` 保留了让行的**时刻**而非仅总量,这是关键链归因与冲突凭证得以定位到"哪个时段"的前提;
- 复杂度:每次 route 为 O(|E|·W·log) 级(W 为单走廊平均预约区间数),被上层高频调用,须用堆 + 邻接表实现;
- `conflict_free=False` 时整段退化为查理想最短路矩阵、不动预约表(规格 12.2 第一层对标与两阶段基线第一阶段用)。

### 5.4 派生量:理想最短路矩阵 t*

对全体取放点/LU 两两跑一遍**无预约**的 Dijkstra,得 t*(d1,d2):
- 用途 1:派车规则 `rule` 档的快速估计(6.3);
- 用途 2:实例特征参数 T̄t/T̄p 的计算;
- 用途 3:两阶段 baseline 的排产阶段用它当常数运输时间矩阵(第八节);
- 用途 4:改派评分中"接近程度"一项(6.5)。

### 5.5 价格加权多标签路由与影子价格(已实现,**默认关闭**)

> **实证结论先行**:本小节机制经检验在本问题上**系统性有害**,`theta` 默认为 0(关闭)。保留实现与本节说明,是为了在论文中如实报告负面结果并给出机制解释。数据见 13.2,解释见 13.3。

**层间接口的价格化设想**:下层不只回答"最早何时到达",而在(到达时刻,占用他人资源的代价)上取 Pareto 前沿,按 `arrive + θ · price_cost` 择优。

```text
PriceTable: pi(c, b)   # 走廊 c 在时间桶 b 内每单位占用时长的边际 C_max 代价
  桶宽 Δ 默认取该算例平均加工时间,使时段分辨率与工序时间尺度对齐
  price_cost(路径) = Σ_段 Σ_桶 pi(c,b) × 该段在桶 b 内的占用时长
量纲: pi = (松弛一单位容量换来的 C_max 改善) / Δ,为无量纲比率;
      故 price_cost 具时间量纲,可与 arrive 直接相加,θ 为无量纲协调强度。
```

**多标签搜索的正确性**:标签为 (到达时刻 t, 累计价格 g);支配关系为 t₁≤t₂ 且 g₁≤g₂(成立的前提同 5.3——节点可无限等待,故早到且更便宜者能模拟任何后续);每条弧使 t 与 g 均非减,故 key = t + θ·g 沿路径单调非减,按 key 出队时首次弹出 goal 即该标量化下的最优。θ=0 或价格表为空时退化为 5.3 的单标签搜索,**与价格化前逐字节等价**(这保证了 θ=0 档是干净的对照)。标签预算耗尽时回退到单标签搜索,保证解码永不失败(建模文档 B4)。

**两种价格估计器**:

| 估计器 | 做法 | 定位 |
| --- | --- | --- |
| `finite_difference_prices` | 定义式。冻结全部上层决策(含派车序列)**与路由策略**,逐槽位把容量 1→2 重解一次,`pi = 改善量 / Δ` | 报告用的**参照版本**;不需 LP 求解器,不依赖凸性假设;代价为每槽位一次重解 |
| `surrogate_prices` | 代理。用关键链加权的让行等待作一阶近似,单次解码即得 | 每代刷新用 |

`price_agreement` 报告两者在共同槽位上的 Spearman 秩相关,用于检验代理价能否替代定义式价格。**有限差分的两个"只变一个东西"要求**:上层决策全部冻结;探测解码与基准解码使用同一价格表与 θ(否则差分会把"容量放宽"与"路由准则改变"两种效应混在一起)。

**为何失效**:见 13.3。**推论(对同类架构有普适意义)**:在"下层用等待精确消解冲突 + 适应度用完整解码的真实 C_max"这种架构下,给下层加任何形式的拥堵惩罚都很可能有害;层间接口值得升级的方向是传**结构信息**(冲突凭证),而非传价格。

---

## 六、上层:调度层

### 6.1 编码(两段式染色体)

设全部工序总数 N,则:

- **MA 段**(machine assignment):长度 N 的向量,第 k 个位置对应固定枚举顺序下的工序 O_ji,取值 ∈ Ω_ji;
- **OS 段**(operation sequence):长度 N 的**工件号重复序列**(job j 出现 n(j) 次),第 x 次出现表示 O_jx;OS 是全局加工优先序,同机工序的相对顺序即由 OS 中先后决定。

**回运的统一处理(δ_return=1)**:为每个工件追加一道**伪工序** O_j,n(j)+1,固定"机器"= LU(节点 v0)、加工时间 = 0、不占任何 RA。伪工序不进 MA 段(机器固定),但**进 OS 段**(job j 在 OS 中出现 n(j)+1 次),其"运输"即成品回运任务。这样回运任务与普通任务在同一解码流程中竞争车辆,排序由 OS 统一决定,无需特殊分支。

任务/车辆指派(子问题 3)**不编码**,由解码时的派车规则确定(6.3);这是 FJSP_PT 文献的通行做法,保证任意染色体可解码。

**关于第三段 TA 编码**:文献已有(Appl. Sci. 2025 的 OS+MA+AD 三层编码),**不作为本文创新点**,且会破坏"任意染色体可解码"的保证(需额外修复算子)、显著膨胀搜索空间。本文改用更轻的**稀疏禁派集**方案(`decode(forbid=...)` 参数已就位,但 GA 侧尚未填充,见 13.4);若需与文献对标三层编码,只作为消融配置出现。

### 6.2 解码器(事件驱动仿真;适应度评价的核心)

```text
函数 decode(inst, net, ma, os_seq, conflict_free=True, forced_dispatch=None,
            prices=None, theta=0.0, bucket_width=0.0, capacity_override=None,
            max_entry_options=3, collect_occupancy=False, dispatch="rule",
            forbid=None) -> DecodeResult

DecodeResult 字段: makespan, ops{}, transports[], dispatch_order[], congestion{},
                   conflict_free, price_cost_total, occupancy{}

状态: 每台 RA 的空闲时刻 free[m]=0;每个工件的当前位置 pos[j]=v0、就绪时刻 ready[j]=0;
      每辆 AGV 的当前位置 loc[k]=v0、空闲时刻 avail[k]=0;预约表清空。

按 OS 从左到右取工序 O_ji(含回运伪工序):
  m ← MA 中该工序的机器(伪工序 m=LU);dest ← loc(m)
  1) 若 pos[j] == dest:            # 同机连续工序,无运输任务(C4)
       arrive ← ready[j]
  2) 否则产生运输任务 T=(j, i, pickup=pos[j], dest, ready=ready[j]):
       k* ← 派车(见 6.3;forbid 可排除若干车,至少保留一辆)
       (…, t_at_pickup) ← route(loc[k*], pickup, avail[k*])          # 空载段
       t_load ← max(t_at_pickup, ready[j])                            # 车等件或件等车(B4)
       (…, arrive) ← route(pickup, dest, t_load)                      # 载货段
       更新 loc[k*] ← dest;avail[k*] ← arrive                        # 卸货即走/即空闲(B5、C5)
       累加 congestion[c] 与 price_cost_total
  3) 工序开工与完工:
       bind ← "machine" 若 free[m] > arrive 否则 "arrive"             # 归因所需
       start ← max(arrive, free[m])                                   # B4 核心公式
       finish ← start + t^P(j,i,m)                                    # 伪工序 t^P=0
       free[m] ← finish;pos[j] ← dest;ready[j] ← finish
C_max ← max over 全部(伪)工序 finish
```

要点:
- OS 天然是工艺先后的拓扑序(第 x 次出现即第 x 道工序),解码中 ready[j] 恒已知;
- 路由的**预约顺序 = OS 扫描中任务产生的顺序**,解码完全确定(同一染色体 + 同一价格表必得同一时刻表);
- 半主动解码 + B4/D3/D4 保证任意染色体解码可行、C_max 有限(建模文档 B4 三重保证);
- `bind` 字段记录 `start` 由哪一支决定,是 6.5 第 1 步归因的入口;
- **状态单向传递**:解码严格按 OS 从左到右推进,状态只向前传,不存在回向依赖。因此若邻居只改动 OS 位置 x 之后的部分,则位置 x 之前的全部事件与预约逐字段不变、可复用——这是增量评价的理论依据(**尚未实现**,见 13.5)。

### 6.3 车辆派车(子问题 3 的启发式)

对任务 T,配置项 `dispatch` 提供两种实现:

**`rule` 档(开环,快)**——逐辆 AGV k 用理想矩阵估算送达时刻:

```text
est(k) = max( avail[k] + t*(loc[k], pickup), ready[j] ) + t*(pickup, dest)
```

取 est 最小者(并列取车号小者)。此式**完全不查预约表**,可能选中一辆"看起来近、实际路上全被占住"的车——这是框架中最后一处开环残余。

**`exact` 档(闭环,默认)**——对每辆候选车做真实两段路由,取实际送达最早者:

```text
for k in 候选车:
    token ← table.checkpoint()
    empty  ← route(loc[k], pickup, avail[k], commit=True)    # 必须真实落表
    t_load ← max(empty.arrive, ready[j])
    loaded ← route(pickup, dest, t_load, commit=False)
    est(k) ← loaded.arrive
    table.rollback(token)
```

| 取值 | 做法 | 代价 |
| --- | --- | --- |
| `rule` | 查理想矩阵 t*(开环) | 每任务 0 次额外路由 |
| `exact` | 预约表真实试探(闭环) | 每任务 2·N_A 次路由,实测约慢 5 倍 |

`rule` 档在价格表非空且 θ>0 时会在 est 中附加节点价格项;由于 θ 默认为 0,该分支默认不生效。

> **实证提示**:同代数比较下 `exact` 优于 `rule`(拥堵算例约 4.7%),但 `exact` 每次评估贵约 5 倍;放宽 `rule` 档早停使两者运行时间相当后,优势消失。**任何涉及 `dispatch` 的对比必须在同算力预算下进行**,见 13.2。不过在 12.3 生成的 `high` 档上,同挂钟预算下 `exact` 仍优于 `rule` 约 5.6%(n=2,见 13.2(iv))——差别来自算例的拥堵结构而非协议。

### 6.4 遗传算子(基本配置)

| 环节 | 方法 |
| --- | --- |
| 初始化 | MA 段:按 Ω_ji 均匀随机;OS 段:工件重复序列随机洗牌。另注入两个启发式个体:MA 取行内最小 t^P;MA 取"全局负载均衡"贪心 |
| 选择 | 锦标赛(size=2)+ 精英保留 elite=5 |
| 交叉 | OS 段:POX(按工件子集保序交换);MA 段:均匀交叉。交叉率 pc=0.8 |
| 变异 | OS 段:随机交换两位;MA 段:随机选一道工序改派到 Ω_ji 内另一 RA。变异率 pm=0.2 |
| 适应度 | decode 返回的真实 C_max(评价级闭环) |
| 终止 | max_gen=200 或连续 stall_gen=30 代无改进 |
| 种群 | pop=100 |

> 这些是通用算子,**不构成创新点**。本文的算子层面贡献在 6.5 的两族定向邻域。

### 6.5 冲突制导局部搜索(决策级闭环;创新点 3 的算法化)

每代对最优的 top_ls=10% 个体执行。

#### 第 1 步:带类型标签的关键链归因

从决定 C_max 的最后事件反向追溯。**早期实现只回溯工序链**(`bind=='machine'` → 同机前一工序,否则 → 本工件前一道工序),运输段完全不在链上;于是开工被"工件到达"卡住时,无法区分是上游慢、车不够还是路上堵,反馈算子因此无的放矢。现扩展为五类标签:

| 标签 | 含义 | 判定 | 应派算子 |
| --- | --- | --- | --- |
| `operation` | 该工序自身的加工占用 | 非伪工序即记 | — |
| `machine` | 被同机前一工序占用所卡 | `bind=='machine'` 且有同机前序 | OS 重排(通用变异) |
| `upstream` | 车已先到、件未好 | `ready ≥ 空载到达` | 沿链回溯到 (j, i−1) |
| `vehicle` | 件已好、车未到且非路网所致 | 空载让行占比 ≤ `CORRIDOR_SHARE` | 换车(禁派集,**GA 侧未实现**,见 13.4) |
| `corridor` | 运输途中在某走廊某时段的让行,**带具体走廊与时段** | 由 `wait_events()` 逐次记账 | 改派(换地方)或错峰(换时间) |

`CORRIDOR_SHARE = 0.5`:空载段中让行等待占"让行 + 行驶"之比超过该阈值时归因于走廊,否则归因于车辆可用性。载货段的让行**无论瓶颈在哪一侧都单独记一笔** `corridor` 项,因为它实际拖长了到达时刻。

派生查询:
- `critical_real_ops(result)` —— 关键链上的实工序(去重、保持追溯顺序),供改派算子挑候选;
- `critical_corridor_slots(result, Δ)` —— 关键链上出现的走廊-时段槽位,供影子价格加权与错峰定位;
- `blocking_opponents(result, c, t1, t2)` —— **冲突凭证的具体内容**:在 [t1,t2) 占用走廊 c、从而造成让行的对手任务。

#### 第 2 步:两族定向邻域算子

拥堵有两种缓解方式——**换地方**与**换时间**,早期实现只有前者。

**(a) 改派算子(换地方)**。对关键链上至多 L_ls=5 道实工序,对每个候选 m' ∈ Ω_ji \ {当前 m}:

```text
score(m') = t*(pos_prev, loc(m')) + t^P(j,i,m')
            [+ θ · pi(loc(m'), t_start) · t*(pos_prev, loc(m'))]
```

前两项分别是"接近程度"与"加工时长",同为时间量纲。方括号内为价格项,**默认 θ=0 即不启用**(见 5.5)。取 score 最小者(并列取小机号)生成邻居,仅改 MA 一位。

> **已弃用的早期评分**:`score = t* + t^P + λ · congestion(loc(m'))`,λ=0.5。`congestion` 是**全计划期、全车辆**的累计让行等待,随算例规模单调增长,而前两项是单道工序尺度的量。在 240 工序算例上累计等待可达数百,`λ·congestion` 将完全支配评分,退化为"只挑最不堵的机器"、丧失对加工时间的权衡——**恰好破坏本文创新点 2 要主张的核心权衡**。λ 因此不可跨算例比较,不具备做敏感性分析的资格。

**(b) 错峰算子(换时间)**。取关键链上让行最久的一次 `corridor` 项,用 `blocking_opponents` 从预约表**反查是谁占着该走廊的该时段**,然后给出两个定向邻居:

- 把**被堵工序**在 OS 中**前移**(提前发起运输,抢占更空闲的时段);
- 把**对手工序**在 OS 中**后移**(错峰避让)。一次只动一个对手,保持邻域小而定向。

**OS 移位的可行性保证**:移位只与**异工件**基因交换,故工件内工序先后序天然保持,移位后仍是合法排列,6.1 的可解码性不受影响。

该算子与通用 GA 变异的本质区别在于:操作对象是由下层的冲突信息**指认**出来的一对具体工序,而非随机抽取。这是"决策级闭环"应有的形态,也是层间接口传递**结构信息**(而非仅传时间或价格)的具体体现——即论文标题中的 Conflict-Guided Feedback。

#### 第 3 步:接受判据

若邻居 C_max 更小则接受替换,并从新个体重新提取关键链继续迭代(首改进策略,最多 ls_rounds=3 轮);一轮内所有邻居均无改进则终止。

> **实证提示**:错峰算子在同代数下带来约 2.9% 改进(拥堵算例,3 种子),但同算力预算复核后优势不显著;当前 n=3 的实验无法支持机制性结论。见 13.2、13.6。在 12.3 生成的 `high` 档上同算力增益为 3.5%、`funnel` 档上为 1.9%(n=2,见 13.2(iv)),方向符合"决策相关拥堵才给得出信号"的预期,仍待 ≥10 种子确认。

---

## 七、参数默认值汇总

| 参数 | 默认值 | 说明 |
| --- | --- | --- |
| pop / max_gen / stall_gen | 100 / 200 / 30 | 种群与终止 |
| time_budget_sec | None | 同算力预算(8.2 协议 1);给定则每代末查挂钟超时即停,并在结果中回报 `stopped_by ∈ {stall, max_gen, budget}`。批跑时由 `run_matrix` 设定,并同时把早停阈值放宽到实质关闭,否则各档不会真正用完预算 |
| pc / pm / elite / tournament | 0.8 / 0.2 / 5 / 2 | 通用遗传算子(6.4) |
| top_ls / L_ls / ls_rounds | 10% / 5 / 3 | 冲突制导局部搜索(6.5) |
| use_conflict_ops | True | 启用错峰算子(6.5b) |
| dispatch | `exact` | 派车查预约表试探(闭环);`rule` 为开环对照 |
| **theta** | **0.0** | 价格协调强度;**默认关闭**,>0 经检验有害(5.5、13.3) |
| price_top_k / price_refresh | 24 / 5 代 | 价格表规模与刷新频率(θ>0 时生效) |
| max_entry_options | 3 | 多标签路由每弧考察的进入时刻数;1 = 只考虑最早 |
| fd_calibrate / fd_slots | False / 8 | 有限差分定义式价格校准(报告用) |
| bucket_width Δ | 平均加工时间 | 时段分辨率,与工序时间尺度对齐 |
| CORRIDOR_SHARE | 0.5 | 走廊/车辆归因分界(6.5 第 1 步) |
| OFF_CRITICAL_WEIGHT | 0.25 | 非关键链让行在代理价中的折扣 |
| delta_return | 1 | 基本模型计回运 |
| seed | 42(实验中必须显式传入并记录) | 全流程可复现(F1) |

---

## 八、对比基线与实验协议

### 8.1 递进式消融链

`main.py --mode ablation`,每一档只比上一档多闭合一个环节:

| # | 档位 | 配置 | 用途 |
| --- | --- | --- | --- |
| 1 | `rule` | SPT + 最近车派工,闭环解码一次 | 快速参考 |
| 2 | `twostage` | 排产用常数 t* 矩阵(退化为标准 FJSP_PT),冻结指派/排序/派车序列后交路由层修复,报告修复后真实 C_max | **核心对照**:与闭环法的差即"集成收益",按 F2 双因子扫描 |
| 3 | `nofeedback` | 完整框架去掉 6.5 局部搜索 | 决策级反馈的单独贡献 |
| 4 | `opendispatch` | `dispatch=rule`(派车回到开环) | 派车闭环(创新点 3c)的单独贡献 |
| 5 | `nostagger` | `use_conflict_ops=False` | 错峰算子(创新点 3b 换时间)的单独贡献 |
| 6 | `closed` | 完整版(θ=0) | **本文方法** |
| 7 | `priced` | 完整版 + θ>0 | **负面对照**,如实报告价格机制失效(5.5、13.3) |

补充参照:**下界**。零成本的复合下界(工件链 / 机器负载 / LU 漏斗三分量取最大,见 12.3.5)已实现,每个算例的 `_features` 头即带该值,可直接算 gap 上限;它较松(实测 gap 上限 35%–61%),故**小规模精确解**仍是待办,优先级见 13.6。

### 8.2 三条必须遵守的实验协议

前两条已被诊断实验证实为必要,第三条是实现批跑脚本时发现的,同样不遵守则结论不可信。**协议已由 `tools/run_matrix.py` 强制执行**(见 8.4)。

1. **同算力预算**。第 4、5 档比完整版快数倍,若只比同代数,会把"多花了算力"误读为"机制更好"。必须对快档放宽早停 / 加大代数至**运行时间相当**后再比较。诊断显示:同代数下派车闭环约 4.7% 增益,同算力下增益消失(13.2)。实现方式为 `GAConfig.time_budget_sec`(挂钟预算,每代末检查),批跑时按"完整方法的自然用时"逐算例标定,并把早停放宽到实质关闭。
2. **足够的种子数与离散度报告**。拥堵算例上单档三个种子的极差可达 11(如 [71, 69, 80]),而各档均值之差仅 1–3。**至少 10 个种子**,报告均值与标准差并做配对检验(Wilcoxon signed-rank,同种子配对;实现见 `algorithm/stats.py`,|d| 无并列时用精确零分布)。**必须报告"非平局对数"**:makespan 取整后两档常在多数种子上完全打平,`n_eff` 远小于 n 本身就是"分辨不出差异"的直接证据。
3. **两种算力口径都要报告(等时间 / 等评估数)**。`twostage` 的第一阶段在**理想运输模型**下搜索(路由退化为查 t\* 表),单次评价比闭环便宜**一到两个数量级**(实测 0.43 ms/次 vs 闭环 17–20 ms/次、`priced` 72–82 ms/次)。于是:

   - **等挂钟时间**等于给两阶段基线数十倍的搜索次数 → 偏向廉价代理模型的开环法;
   - **等评估数**又等于让闭环法多花数十倍 CPU → 偏向闭环法。

   **两种口径都不中立**,只报一种就是在选择对自己有利的那种。故 `run_matrix` 同时支持 `--budget auto`(等时间)与 `--budget gen`(等代数),论文须并列给出。同时必须报告**停机原因**:若某档被预算掐停(`budget`)而对手已自然收敛(`stall`),该比较测的是收敛速度而非机制优劣,应加大预算重跑。

### 8.3 诊断工具

`tools/sweep_price.py`:一键复现 13.2 的全部数字。扫描维度为 θ × `max_entry_options` × 价格估计器 × `dispatch` × `use_conflict_ops`,并内置同算力复核档(对开环派车档放宽 `max_gen`/`stall_gen`)。每档跑多个种子,输出均值/最好/最差/秒每次/评估数,并对每个解跑一次独立校验。

### 8.4 矩阵批跑器(协议的执行者)

`tools/run_matrix.py` 是 12.3.6 主试验的运行入口,把 8.2 的三条协议做成默认行为而非人工纪律:

| 机制 | 实现 |
| --- | --- |
| 预算标定 | `--budget auto`:每算例先用完整方法在默认停机规则下跑一次,其用时即该算例**全部档位共享**的挂钟预算。以完整方法(而非最快档)为基准,否则等于把所有档一起限制在欠收敛区间 |
| 可中断续跑 | 每完成一个 (算例, 档位, 种子) 立即向 JSONL 账本追加一行并 `fsync`;重跑同名 `--run` 自动跳过已完成项。任务顺序为"算例 → 种子 → 全部档位",使中断后留下的是**完整配对块**而非半个种子 |
| 跑与报告分离 | 报告全部由账本重算,`--report-only` 可在跑到一半时出当前结论 |
| 正确性守护 | 每个解过一遍独立校验器,并与算例自带复合下界比对;校验失败或低于下界的项在报告顶部单列,不允许被均值掩盖 |
| 预算体检 | 自动报出各档"毫秒/评价"与停机原因,并在"一档被预算掐停、另一档已收敛"时给出欠收敛警告(协议 3) |
| 预期判定 | 自动检验 12.3.6 三条预期。预测 3 的配对键是 **(H, 种子)**——`high` 与 `funnel` 在同 H 同种子下只差 LU 出口容量(T12 已固化),故这是唯一不含其他差异的配对方式 |

```powershell
py -m tools.run_matrix --preset smoke              # 流程自检(2 算例 x 7 档 x 2 种子)
py -m tools.run_matrix --preset p3 --dry-run       # 先看任务数与预计耗时
py -m tools.run_matrix --preset p3                 # high/funnel 受控对比(预测 3)
py -m tools.run_matrix --preset full --budget gen  # 完整矩阵的等代数口径
py -m tools.run_matrix --preset full --report-only  # 随时出当前报告
```

---

## 九、验证用算例与测试断言(实现后必须全部通过)

**基准算例**:3.1 节 JSON 即建模文档第六、七节的示例(3 工件、3 RA、2 AGV、6 节点 6 走廊、全柔性特例、δ_return=1)。

| # | 测试点 | 断言 |
| --- | --- | --- |
| T1 | 最短路矩阵 | t*(v0,r1)=4, t*(v0,r2)=7, t*(v0,r3)=7, t*(r1,r2)=6, t*(r1,r3)=6, t*(r2,r3)=4,且对称 |
| T2 | 时刻表校验器 | 将建模文档第七节的完整可行方案(C_max=51)手工录入为时刻表,校验器判定:无走廊时窗重叠(半开区间;t=39 在 c1 的衔接合法)、工序时序/机器独占/车辆单载全部满足、C_max=51 |
| T3 | 解码可行性 | 随机生成 1000 条染色体逐一解码,校验器全部通过、C_max 全部有限 |
| T4 | 解码确定性 | 同一染色体解码两次,时刻表逐字段相等 |
| T5 | 同机免运输 | 构造相邻工序同机的染色体,确认只产生"首道送达 + 成品回运"两个运输任务 |
| T6 | 回运开关 | 同一染色体在 δ_return=0/1 下解码,后者 C_max ≥ 前者,且前者等于末道实工序最大完工时刻 |
| T7 | 冲突消解 | 构造双车同时争用 v0–c1 的场景,确认后规划者被推迟(AGV2 等到 t=2 进入、t=6 抵达 r1)且总时刻表无重叠 |
| T8 | 优化有效性 | 完整算法(seed 固定)在基准算例上求得 C_max ≤ 51 |
| T9 | 禁派集 | 禁掉基线所选车辆后该任务必须换车且方案仍可行;**全禁时回退到完整车队**(可解码性兜底),C_max 有限 |
| T10 | 占用率一致性 | `dispatch=exact` 下,预约表侧 `occupancy()` 与时刻表侧 `corridor_occupancy()` 两套独立计算逐槽位相等,且取值 ∈ [0,1]。**该断言同时验证派车试探的落表-回滚无残留**——若回滚泄漏,预约表侧会多出时刻表里不存在的占用 |
| T11 | 生成算例可行性与下界合法性 | 四档拥堵度 × H∈{0,0.3} 生成的算例:强连通、随机染色体可解码可校验;H=0 时实测异构度**恰为 0**,H=0.3 时偏差 < 0.06;**任意染色体的 C_max 不低于复合下界**(该断言同时守护下界推导与解码约束) |
| T12 | 拥堵度旋钮受控性 | `high` 与 `funnel` 同种子下加工时间/机器位置/T̄t/T̄p 逐字段相同、仅 LU 割为 2 vs 1;`mid` 与 `high` 仅远端割为 2 vs 1 而 LU 割相同;同种子生成结果逐字节可复现(F1) |
| T13 | 统计工具正确性 | 精确 Wilcoxon 与已知值逐位一致(全同向 5 对 → p=0.0625;教科书 9 对算例 → statistic=5、p=0.0391,与 `scipy.stats.wilcoxon` 相同);全平局时报 `n_eff=0`、`p=1` 而非伪显著;`describe([71,69,80])` 复现 13.2 的极差 11;秩相关在无变异时返回 `None` 而非 0 |
| T14 | 批跑基础设施 | ① 放开早停与代数上限后,`time_budget_sec` 必须真的掐停(`stopped_by="budget"` 且用时不超预算数倍),且不给预算时行为不变;② 账本追加后重新打开能识别已完成项、`budget` 记录不混入结果集(续跑的唯一依据);③ **配对不错位**:`high` 侧多一个 `funnel` 没有的种子时,配对数必须缩为 2 且均值不含该种子——按列表顺序拼接会静默给出一个看似正常的 p 值 |

**校验器(validator)独立于解码器实现**,输入时刻表 + 算例,逐条检查八项:

| 编号 | 检查内容 |
| --- | --- |
| (a) | 工序编号完整性、工艺先后、加工时长与 t^P 一致 |
| (b) | 机器独占(同机工序不重叠) |
| (c) | Ω 合法性(工序未被指派到不可行 RA) |
| (d) | AGV 走廊合法性与通行时间、分段不重叠、路径连续性(单载与位置连续) |
| (e) | 走廊时间窗独占(半开区间) |
| (f) | 送达先于开工、起运不早于前道完工(耦合约束) |
| (g) | C_max 口径一致性:等于全部工件末道(伪)工序完成时刻的最大值,**不含空载段** |
| (h) | 载货段不得晚于 C_max 结束(空载归位段允许晚于,不属工件完工) |

**(g)/(h) 分离的理由**:早期实现把"全部 AGV 分段"并进 (g) 的期望值,导致 C_max 有两套口径(见 13.4 第 1 项)。现 (g) 严格等于假设 8 的定义,而原先那点越界防御由 (h) 独立承担——载货段晚于 C_max 意味着某工件在"报告完工"后还在被搬运,是真错误;空载归位晚于 C_max 则合法。

校验器是防止解码器与路由层隐性 bug 的仲裁者;**它必须独立实现,不得复用解码器的中间量**。同理,报告层的 `corridor_occupancy` 也从时刻表独立重算而非读预约表,T10 才有作为证据的资格。

---

## 十、代码结构与命名映射

```text
clbs/
  main.py            # 入口:载入 -> 求解(九种模式)-> 独立校验 -> 落盘
  algorithm/
    instance.py      # 算例载入(JSON schema 见 3.1);特征参数(T̄t/T̄p、异构度、柔性度、
                     #   NA/NM + 结构指标)、复合下界 simple_lower_bound(12.3.5)
    generator.py     # 受控扩展算例生成:布局模板、容量旋钮、H/F 控制、T̄t/T̄p 标定(12.3)
    network.py       # 图结构、t*、结构指标(funnel_share / lu_cut / far_group_cut,12.3.5)、
                     #   容量化 ReservationTable(5.2)、PriceTable(5.5)、Router(5.3/5.5)
    pricing.py       # 影子价格:有限差分定义式 + 代理式 + 秩相关一致性(5.5)
    decoder.py       # decode()(6.2)、dispatch_rule/dispatch_exact(6.3)、
                     #   关键链五类归因与冲突凭证(6.5 第 1 步)
    ga.py            # 染色体、通用算子(6.4)、冲突制导局部搜索(6.5)、主循环、
                     #   挂钟预算闸门 time_budget_sec 与 stopped_by(8.2 协议 1)
    baseline.py      # ARMS 七档定义与 solve_arm 统一入口(8.1);两阶段 open-loop
    stats.py         # 描述统计、精确/近似 Wilcoxon 配对检验、Spearman(8.2 协议 2)
    validator.py     # 独立校验器,八项检查 (a)–(h)(九)
    report.py        # 甘特图、走廊占用率画像(3.3)、摘要行
  tools/
    sweep_price.py   # 机制诊断扫描,含同算力复核(8.3)
    gen_instances.py # 扩展算例矩阵生成 CLI(12.3.4)
    run_matrix.py    # 矩阵批跑器:预算标定、账本续跑、配对检验、预期判定(8.4)
  input/             # example_3x3x2.json(基准)、congested_8x4x4.json(拥堵型)
    ext/             #   生成的受控扩展算例(子目录,默认 glob 不扫)
  output/            # 各算例的 summary.json / timetable_*.json / gantt_*.txt
    matrix/<run>/    #   批跑账本 records.jsonl + summary.json + report.md(8.4)
  experiments/       # 自造算例的汇总 CSV(进论文的表,由导出脚本生成)
  database/          # 公开数据集分支的唯一入口(12.1/12.4):raw/(只读原始件)、
                     #   json/(转换后算例)、network/(还原路网模板)、refvalues/
                     #   (文献参考值)、MANIFEST.csv(来源 + SHA256)
  output_database/   # 公开算例的运行结果(形状同 output/)
  experiments_database/ # 公开算例的汇总 CSV(形状同 experiments/)
  tests/             # T1–T14
  README.md, requirements.txt   # 纯标准库,无第三方依赖
```

| 文档符号 | 代码命名 |
| --- | --- |
| O_ji / Ω_ji / t^P(j,i,m) | `(j,i)` / `inst.eligible(j,i)` / `inst.proc_time[(j,i)][m]` |
| G=(V,E) / τ(e) / t* | `Network.nodes/adj` / `Network.corridor_time[cid]` / `Network.ideal_dist` |
| NA / NM / n / δ_return | `num_agvs` / `num_machines` / `num_jobs` / `delta_return` |
| C_max / 关键链 | `makespan` / `critical_chain()` → `List[CriticalItem]` |
| 预约时窗 [t1,t2) | `(t_start, t_end, agv, task)` |
| 影子价格 pi(c,b) / 桶宽 Δ | `PriceTable.get(cid, b)` / `PriceTable.bucket_width` |
| 冲突凭证 | `blocking_opponents(result, cid, t1, t2)` |
| 占用率 util(c,b) | `ReservationTable.occupancy(Δ)`(搜索内)/ `report.corridor_occupancy(timetable, Δ)`(报告,独立重算) |

---

## 十一、与建模文档的追溯关系(自检)

| 本文档章节 | 依据的建模文档条款 |
| --- | --- |
| 二.1–8 假设 | A1–A5, B1–B7, C1–C6, D1–D4, E1(I.4 精简版) |
| 三.1 输入 | H 节一~四(参数表示);H 六(示例数据) |
| 五 路由层 | D1–D4(冲突定义、半开时窗、停靠分离、预约式死锁避免) |
| 六.2 解码 | B4(max 公式与三重保证)、C4/C5(任务触发与空载去向)、3.2 触发规则 |
| 六.3 派车 | C1(同质车队)、C5(空载就位与出发时刻为决策量) |
| 六.5 反馈搜索 | 0 节创新锚点、D5 闭环、F2 预期结论 |
| 八 基线与协议 | D5(两阶段对照)、F1(可复现)、F3(最优性对标、口径统一) |
| 九 验证 | H 七(C_max=51 完整方案与六个快照) |
| 十二 数据集 | F1(算例生成规则)、F2(实验因子)、F3(对标口径)、C6/E1(回运口径) |

---

## 十二、对比测试数据集与实验分层方案

**结论(2026-08-17 依据实测改写,原断言过强)**:没有任何现成公开**基准集**同时包含"部分柔性 + 能力集内时间异构 + 显式路网无冲突路由";但**存在个别已发表算例**同时具备这三项——**Lyu 等 (2019)** 的 Illustrative Example 2 与其第二组 14 个测试问题即是(详见 12.1 末的核查记录),故不能再说"没有任何现成公开数据集"。这一支仍不能直接拿来跑,原因不是缺三项特征,而是**冲突语义多一条约束**(它的节点容量为 1,本文的节点是零测度通过点),以及那 14 个问题的**逐段行驶时间未发表**。因此实验设计不变,仍采用"**第一层退化对标 + 第二层受控扩展**",但理由要换成上面这两条,不能再靠"找不到数据"。FJSPT 公开基准集托管于:

> https://fastmanufacturingproject.wordpress.com/2019/04/11/fjspt-instances/ (2025 年仍在线,社区活跃使用)

### 12.1 候选公开数据集评估

| 数据集 | 规模 | 柔性 | 时间异构 | 显式路网 | 用途 |
| --- | --- | --- | --- | --- | --- |
| Bilge & Ulusoy (1995) | 82 实例(10 工件集 × 4 布局,2 AGV) | 无(\|Ω\|=1) | 无 | 无(常数矩阵);4 张矩阵**全部不对称**但**全部满足三角不等式**,故可还原为**有向**图、**不可**还原为无向走廊(实测 4/4 与 0/4,见 12.4 第 3 项) | 退化特例对标(文献结果最多);~~4 个布局可还原为图 G~~ **不能用作受控算例的布局来源** |
| Deroussi (2014) | 10 实例 | 有(每工序 2 台备选) | 无(备选机时间相同) | 无 | "柔性但零异构"对照组 |
| Kumar (2011) | 56 实例 | 有 | 无 | 无 | T̄t/T̄p 因子现成分组(≥0.25 / <0.25) |
| Homayouni set 1(SFJST/MFJST01-10) | 20 中小实例(Fattahi FJSP + 运输) | **有** | **有** | 无;7 个布局矩阵**全部不对称**,其中 2 个违反三角不等式,故**不可还原为图 G**(与 Bilge 相反) | **第一层核心对标集**;已转换并跑通(12.4) |
| Homayouni set 2(Brandimarte MK + Chambers-Barnes) | 31 大实例(55–240 工序) | **有**(MK 自带部分柔性) | **有** | 无 | 大规模测试;Ω 结构直接沿用 |
| **Lyu 等 (2019)** IEEE Access | Example 2:4 工件 / 6 机 / 20 工序 / 20 节点;第二组:14 个测试问题(3–10 工件、3–8 机、6 张栅格布局 9–25 节点) | **有**(斜杠记法给备选机) | **有**(备选机工时不同,如 `M2(12)/M3(15)`) | **有**——Example 2 给 20×20 邻接矩阵(逐段时长),第二组给 6 张带编号节点的栅格图 | **三项全有,是原断言的反例**;但节点容量为 1(多一条约束)、且第二组逐段时长未发表,故不能直接跑 |

**不采用**:Pal 等 2023(多智能体综述)所用数据(Kacem 修改实例 + MK 加随机 setup/运输时间)——属"FJSP with transportation time"类,只计运输时间、**不调度车辆**(无有限 AGV 车队与任务指派),与本问题结构不符,且数据非完全公开(available on request)。

> **已结案(2026-08-17,原始件已取得并逐页核过)**:原始 PDF 已下载到
> `clbs/database/raw/lyu_2019/lyu2019_ieee_access_8723142.pdf`(IEEE Access 开放获取,
> 16 页,内嵌 Type 1 字体因而可机读;来源与 SHA256 见 `database/MANIFEST.csv`)。
> **待定项的答案是:那句断言确实错了**,Lyu 三项全有,已按上表与本节开头改写。逐条核过的证据:
>
> 1. **冲突语义**——原文 III 节:"the layout is represented by a bidirectional network, but only
>    one vehicle at a time can transit a **segment**"。段(segment)= 边,故是**边排他**,与本文
>    走廊排他同构,亦即与假设 A6 一致。这一条先前的记录是准确的。
> 2. **部分柔性 + 时间异构**——同段接着说 "Each operation can be processed on a few different
>    machines but with **different** [processing times]"。数据侧两处印证:Example 2 的 Table 3 是
>    "工序 × 机器"工时矩阵,以 "—" 表示该机不能加工;第二组的 Table 7 用斜杠给备选,
>    如 `M2(12)/M3(15)`(备选机工时**不等**),也有三选一的工序。故两项都成立。
>    注:这两张表是从 PDF 渲染图上读的,**逐格数值尚未逐一校对**;此处只用来判定"有无备选、
>    备选工时是否相等"这两个定性问题,真要转换算例时必须重新逐格核对。
> 3. **显式路网**——Example 2 的 Figure 1 配 Table 4,是一张 **20×20 邻接矩阵**,给的是**相邻**
>    点之间的逐段时长,这正是本项目 `corridors` 所需的形式,而**不是**全对最短路矩阵。
>    原文措辞 "the transit times between any two points" 听着像全对,但该矩阵**非对角线上大量
>    为 0**,而连通图的全对最短路矩阵不可能有非对角零元,故只能是"0 = 不相邻";判据是这个零元
>    模式而非原文措辞。第二组的 Figures 10–15 是 6 张带编号节点的栅格图(9/16/16/25/25/25 节点)。
>    故这一支不存在 HF 那种"矩阵不可还原成走廊图"的问题。
> 4. **参照值**——Table 5 给 Example 2 在 1–6 车下的最小/平均完工时间(129/92.5/88/84.5/82/82),
>    Table 6 给第二组 14 个问题在 3–5 档车队规模下的最小/平均完工时间与最优车数。有参照值可对。
>
> **但仍不能直接跑,理由已换成两条实质性的,且必须如实写明,不能再说"找不到数据"**:
>
> - **(a) 冲突语义多一条约束**:其约束 (11) "ensures that a **node** can be occupied by only one
>   vehicle at a given time",即节点容量为 1,并显式区分 point / opposite-direction /
>   road-junction 三类冲突;而本文的节点是零测度通过点,只对走廊排他。故 Lyu 的模型比本文**更紧**。
>   把本文方法直接跑上去等于放松了它的节点容量,可能得出在其模型下不可行的解,完工时间因而可能
>   **低于**其发表值——那不是赢,是口径不同,数字不可比。这与 van Os & Basten (2026) 不能直接跑
>   是同一类原因(节点容量假设),不是同一条约束。
> - **(b) 第二组逐段行驶时间未发表**:Figures 10–15 只画拓扑与机器位置,不标边权;全文只说
>   "AGVs move at a constant speed",未给栅格的段长或段时。栅格规整,取单位边权是**合理猜测**
>   但仍是我们自己引入的假设而非读到的数据,一旦猜错,Table 6 的参照值就不可比。相反,
>   **Example 2 是完整可转换的**(Table 3 + Table 4 + Table 5 齐全),只是只有一个算例,
>   且仍受 (a) 制约。
>
> 结论:这一支的价值是**佐证本文的建模选择有文献先例**(双向网络 + 边排他),而不是可直接对标的
> 基准。若将来要用它,必须先在本文模型里加上节点容量 1,那是另一件工作。
>
> **`paper01/paper.tex` §5.1.1 那一处已改写,改法如下**,以后此处若要改动须与之对齐。不再说
> "公开基准不含通道网络、争用无法定义"这种一般性断言(Lyu 就是反例),而改成一条只依赖实测的
> 论证,分两级、**不要混成一句**:
>
> 1. **7/7 张矩阵不对称** → 不可能是\*\*本项目\*\*任何一张路网的 t\*。理由是本项目的走廊无向、
>    每条只有一个 `time`,由它算出的 t\* 必然对称。这一级判定的是"能否跑争用档";
> 2. **其中 2 张(3-M、7-M)违反三角不等式** → 根本不是**任何**图的最短路闭包,连有向图也还原
>    不出来。这一级更强,但只覆盖 2 张。
>
> 结论只说"强行反推会得到**另一个**算例(那些位置对上严格更快),参考值不再可比",**不说**
> "更容易"——退化档下更快确实不会更差,但争用档下更快的通道可能制造更多重叠,单调性没有保证。
> 这个说法不依赖"公开基准都没有网络",Lyu 出现也不会推翻它。取数与体检见
> `clbs/database/README.md` 与 `clbs/experiments_database/fidelity.csv`;该表有**两个**可还原列,
> `digraph_reconstructible`(只查三角不等式,5/7 为真)与 `corridor_reconstructible`(还要求对称,
> **0/7** 为真),只看前一列会误以为那 5 个布局能拿去跑争用档。
>
> **论文局限 (3) 已就 Lyu 单独补一段(2026-08-17)**,引用键 `lyu2019approach`(bib 条目已加,
> 页码 74909--74924,已编译核对无未定义引用)。补的理由是:局限 (3) 原先给出的不可比理由是
> **van Os & Basten 的**冲突语义(单向图、预约节点),而 Lyu 原始模型是**双向 + 边排他**,与本文
> 在这一维上同构,故那条理由排除不掉它。补上的两条即上面的 (a)(b):节点容量 1 使本文模型成为
> 它的**松弛**(可能给出在其模型下不可行的解、完工时间反而更低,故不可比),以及大的那一组
> **未发表逐段边权**。同时点明其正面意义:"双向走廊 + 边排他"在文献中已有先例,不是为本文方便
> 而设。**注意 §5.1.1 未动**——那一处只讲 HF 实测,Lyu 不构成反例,无须改;若将来想把 Bilge 的
> 4/4 不对称也并进去以扩大实测基数(7/7 → 11/11、跨两个独立数据集族),那是一次\*\*可选的加强\*\*,
> 已评估过但本轮**有意未做**。

### 12.2 第一层:退化对标(证明上层算法竞争力)

- **配置**:关闭路由层冲突约束(代码上即 `conflict_free=False`,`route()` 退化为查 `Network.ideal_dist`、预约表禁用);运输时间**原样取所发布的矩阵**,不做最短路合成(理由见 12.4 第 2 项:合成会改变算例);δ_return=0(与基准口径一致);车队规模统一为 2 辆(Lim & Moon 2023 §4 明确 HF/Kumar/Deroussi 各族均为两车);算例带 `-ideal` 后缀,与争用版本 `-excl` **分表报告**;
- **数据**:Homayouni set 1/2 + Bilge 82 实例(+可选 Deroussi/Kumar);**已落地:set 1 全 20 个**;
- **对比对象(文献已发表结果,无需重跑)**:Homayouni & Fontes 2021(LAHC/局部搜索,并含 16 个 MILP 可证最优值)、Lim & Moon 2023(两阶段启发式,MFJST07–10 的 best-known 取此)、Homayouni 等 2023(BRKGA,多数实例最优)、Ham 2020(**CP 精确最优值**,即 F3 的下界参照)、Chaudhry 等 2022(GA);
- **报告口径**:与参照值的 gap(%),`proven_optimal` 与 `best_known` **两种强度分列不合并**——前者 gap 有绝对含义,后者只是当前最好上界。注意 Homayouni & Fontes 2021 已证明 Nouri 等 2016 部分结果无效,引用时排除;
- **硬性判定**:解低于 `proven_optimal` 即视为口径或转换出错(同一个数学问题,可证最优是硬地板),这是这一档**唯一的结果层保真证据**——退化档没有 AGV 分段,校验器的 (d)(e)(f)(h) 天然为空,"校验通过"不能充当转换正确的凭据;
- **实测结果**:见 12.4 末"第一层退化对标结果",并已写入论文 §5.9(`sec:external`)与局限 (3)(4)(5)。

### 12.3 第二层:受控扩展(展示闭环集成收益)

在第一层基准上按 F1 规则派生扩展实例,三个扩展维度:

**已实现**:`algorithm/generator.py` + `tools/gen_instances.py`(用法见 12.3.4)。

#### 12.3.1 三个扩展维度

- **(a) 路网 G**:模板化布局(哑铃 `dumbbell` / 网格 `grid`),拥堵由两个**容量旋钮**控制:`lu_exits`(LU 出口条数)与 `mid_lanes`(中段并行通道数)。两个旋钮**只改容量、不改距离**——无论取值多少,v0 到近端枢纽恒为 2、近端到远端恒为 `mid_time`,故同布局下不同容量的算例其 t* 矩阵完全相同,拥堵是唯一变量。

  > **并行通道的实现约束**:走廊的预约资源按端点对归并(`network.corridor_id`),**同端点对的重复边会塌缩成同一个独占资源**,达不到扩容效果。故每条并行通道必须经由互不相同的中间节点,并令各跳时间之和保持恒定。
  >
  > ~~后续可选:由 Bilge 的 4 个导轨布局还原图作为最可辩护的真实布局来源(12.4 待办第 3 项)。~~
  > **此路已否决(2026-08-17)**:Bilge 的四张矩阵全部不对称,不可能是任何无向走廊网络的 t\*,
  > 详见 12.4 第 3 项。**自造布局不是退而求其次,而是唯一可行的做法**——公开基准里凡是给了
  > 行驶时间的,给的都是单向导轨下的不对称矩阵(hf 7/7、bu 4/4 均如此)。真要找带无向路网的
  > 现成算例,目前只有 Lyu 等 (2019),但它多一条节点容量约束(见 12.1 末)。

- **(b) 异构度 H**:先抽独立扰动,再**标准化为零均值单位方差后乘 H**,使每行(工序)的总体变异系数(取整前)精确等于 H,**与 |Ω| 无关**。直接用均匀扰动的话 CV 会随 |Ω| 漂移、小 |Ω| 上尤其不稳,H 就失去作为实验因子的资格。实测命中情况(目标→实测):0→0、0.15→0.139、0.3→0.288、0.5→0.500。**H=0 时同一工序在各机耗时相同**,即改派不再有加工时间上的\*权衡\*,原设计据此把它当作 F2 使能关系的回归档。**该预期已被 12.3.6 预测 2 的实测否定**:H=0 那一组的集成增益反而是四档最高(6.12%),因为工时相同意味着改派成了代价为零的纯拥堵决策,\*机会\*不减反增。这一档仍然有用,但它检验的是"收益是否依赖异构",答案是不依赖,而不是"机制是否失效"。

> 另外一个用途:`0.15→0.139` 这一档恰好落在公开基准的实测中位附近(`clbs/database/json/hf/` 的 20 个算例,同口径实测 H 中位 0.135、最大 0.258),故它是论文 §5.1.1 用来说明"H 取 0.3 这处外推没有抬高主效应"的那个锚点。

- **(c) 柔性度 F**:按目标平均 |Ω| = F·NM 随机化取整,恒保证 |Ω| ≥ 2(B1);F·NM < 2 时直接报错并给出可用下限。实测 F=0.6 → 0.583(24 工序上的取整误差),目标值与实测值**都写入算例头**。

#### 12.3.2 T̄t/T̄p 标定(必须做,否则拥堵度因子被污染)

生成后按目标值缩放加工时间,使 T̄t/T̄p 命中 `tt_tp_target`(默认 1.0)。

**只缩放加工时间、不动路网**:T̄t 由拓扑决定、是各拥堵档位的**结构**特征,若改路网来调该比值就把"运输强度"和"网络结构"混在了一起;而缩放加工时间对 H(变异系数,尺度无关)与 F(|Ω| 大小)均无影响,是干净的标定杠杆。取整带来漂移,故迭代若干轮。

标定前四档的 T̄t/T̄p 为 0.40–0.46(运输相对加工太便宜,路由几乎不重要);标定后统一为 ≈1.00,**各档之间只剩网络结构的差异**。

#### 12.3.3 拥堵度四档与受控对比

| 档位 | 布局 | LU 出口 | 中段通道 | LU 割 | 远端割 | 定位 |
| --- | --- | --- | --- | --- | --- | --- |
| `low` | 网格 3×3 | — | — | 2 | 2 | 高路径冗余,低拥堵对照 |
| `mid` | 哑铃 | 2 | 2 | 2 | 2 | 中等 |
| `high` | 哑铃 | 2 | **1** | 2 | **1** | 中段争用,**拥堵与指派相关** |
| `funnel` | 哑铃 | **1** | 1 | **1** | 1 | 额外收成单点漏斗,**多出的拥堵与指派无关** |

**`high` 与 `funnel` 是一组受控对比,这是本轮新增的关键实验设计**:同种子下两者的加工时间、机器位置、T̄t/T̄p 逐字段相同,唯一差别是 LU 出口容量(2 条 vs 1 条,已由 T12 固化为断言)。`funnel` 多出来的那部分拥堵**与机器指派无关**——每个工件的首道送达与成品回运都必经,不论工序派给哪台 RA。

因此该对比可直接检验 3.1 实测修正提出的诊断:**若各反馈机制只在 `high` 上显示增益、在 `funnel` 上消失,即证明"决策无关拥堵稀释机制信号"**。初步单次运行(H=0.3, seed=42, pop=40)已显示 `funnel` 比 `high` 差 12.7%(80 vs 71),即halving LU 容量的纯代价;机制层面的对比尚待按 8.2 协议补齐。

`mid` 与 `high` 则构成第二组受控对比(只差中段容量,LU 割相同),用于分离"决策相关拥堵"的效应。

#### 12.3.4 命名、复现与用法

命名规则:`S<工件>x<RA>x<AGV>-L<布局><LU出口><中段通道>-H<异构度>-F<柔性度>-A<AGV数>-s<种子>`,如 `S8x4x4-LD21-H0.3-F0.6-A4-s42`(D=dumbbell, G=grid)。公开基准派生的实例沿用 12.1 的原名作前缀。

每个算例文件自带两个非算法字段(`parse_instance` 忽略以 `_` 开头的键):
- `_spec`:完整生成参数含随机种子 → 逐字节可复现(F1,已由 T12 断言);
- `_features`:实测特征 + 目标值 + 复合下界(见 12.3.5)。

```powershell
py -m tools.gen_instances --list        # 只打印特征表,不落盘
py -m tools.gen_instances              # 默认 4 档 × 4 个 H = 16 个算例 → input/ext/
py -m tools.gen_instances --seeds 42 7 2024 --jobs 10 --machines 5
```

`input/ext/` 为子目录,故 `main.py` 的默认 glob(`input/*.json`)不会扫到它,默认运行仍只跑两个手写算例。

#### 12.3.5 新增的算例特征与零成本下界

`feature_params(inst, ideal_dist, net)` 现附加三个**结构指标**(拥堵度因子必须与之并列报告,否则无法判断拥堵是否含决策杠杆):

| 指标 | 含义 |
| --- | --- |
| `funnel_share` | 典型 LU→RA 行程中落在"所有 LU→RA 最短路共用走廊"上的时间比例 ∈ [0,1];越低则机制可利用的信号越强 |
| `lu_min_cut` / `lu_cut_corridors` | 使 LU 与全部 RA 失联的最小走廊集合(单位容量最小割)。**= 漏斗宽度**,给出 LU 同时能发车数的上限;值为 1 即存在单点漏斗 |
| `far_group_cut` | 隔离"到 LU 距离高于中位数的 RA 组"所需的最小走廊数 = 深层瓶颈容量。单台 RA 的连通度恒被自己的支线卡成 1、无区分力,故必须按组求割 |

`simple_lower_bound(inst, net)` 给出零成本的 makespan 复合下界,三个分量各自都是合法松弛、取最大值:

| 分量 | 论证 |
| --- | --- |
| `job_chain` | 逐工件:首道送达最短行程 + 各工序在 Ω 内的最小加工时间之和 + 回运最短行程;松弛掉换机运输(≥0)与全部排队 |
| `machine_load` | Σ_op min_m t^P / NM;松弛掉工艺先后与运输 |
| `lu_cut` | δ_return=1 时每工件必须穿越漏斗两次,漏斗由 k 条独占走廊组成,一次穿越占用 τ(c)。把 X 次穿越分配到割边并最小化 max_c x_c·τ(c),得 **C_max ≥ X / Σ_{c∈cut}(1/τ_c)** |

三者互不支配(1 抓最长工件、2 抓总负载、3 抓路网咽喉)。**这不是紧下界**,只用于量级判断与回归防错(T11 断言任意染色体的 C_max 不低于该界)。实测:

| 算例 | job_chain | machine_load | lu_cut | LB | 已知最好 | gap 上限 |
| --- | --- | --- | --- | --- | --- | --- |
| `example_3x3x2` | **22.0** | 10.3 | 12.0 | 22.0 | 34.0 | ≤ 35.3% |
| `congested_8x4x4` | 22.0 | 25.8 | **32.0** | 32.0 | 82.0 | ≤ 61.0% |

**拥堵算例上的紧约束分量是漏斗界(32),高于机器负载(25.8)与工件链(22)**——即该算例至少 39% 的 makespan 由一条零决策杠杆的走廊强制,这以独立于占用率的方式再次确认了 3.1 的实测修正。

#### 12.3.6 核心实验矩阵与预期结论

**主试验**(对应 F2):拥堵度四档 × 异构度 H 四值,每格 ≥10 种子;柔性度 F、NA/NM、T̄t/T̄p 为辅助扫描。每个格子上跑 8.1 的七档 + 复合下界(+ 小实例精确解),并**严格执行 8.2 的两条协议**。

**预期结论**(须由实验检验):
1. 拥堵与异构越高,闭环相对两阶段的改进越大 —— **拥堵这一半成立,异构这一半被否定**。合并 `p3` 与 `lowmid` 两个批次得 16 格(四争用档 × 四 H 档)× 10 种子,按 H 分组的集成增益为 H=0 → 6.12%、H=0.15 → 5.62%、H=0.3 → 5.95%、H=0.5 → 4.38%,16 格上 Spearman(增益, H) = **−0.21**,即**非单调且弱负**,收益不随 H 上升。口径提醒:每组只有 4 格,而逐格增益的散布是 0.28%~9.28%,远大于组间差距,故这批数据只支撑"不随 H 上升"这条否定式,**不足以**断言 H=0.5 真的更低;
2. H=0(|Ω| 内无差异)时改派机制失效、收益衰减 → 验证异构度是收益的使能因子 —— **被否定**。H=0 那一组的集成增益 6.12% 是四组之\*最高\*,H>0 十二格均值 5.32%。原因是这条预期把"权衡"与"机会"混为一谈:H=0 时 Ω 内各臂工时相同,改派**不再需要用加工时间去换通行时间**,于是成为一个代价为零的纯拥堵决策,机会不减反增。故异构度是使"选哪台臂"**成为一个权衡**的前提,但**不是收益的放大器**;
3. **`high` 上的机制增益应大于 `funnel` 上的**(本轮新增的可判定预测)—— **初步支持(n=2,三个消融档方向一致,见 13.2(iv));待 ≥10 种子确认**。

> 预测 1、2 的判定于 2026-08-18 从既有账本(`output/matrix/p3`、`output/matrix/lowmid` 的 `summary.json`)读出,**未重跑**——这批数据本就横跨四个 H 档,此前只报了 16 格聚合值(论文 \GainIntegMatrix = 5.52%),逐 H 分解一直躺在 `predictions.P1` 里没有进正文。论文已据此改写:§5.1.1 增补"取值落在公开算例的什么位置"一段,§5.5 增补"异质度不是收益的放大器"一段,§3 算例特征处的"H=0 改派权衡随之关闭"补上了权衡/机会之分。

判定由 `tools/run_matrix.py` 自动完成(8.4)。预测 3 的配对键是 **(H, 种子)**:`high` 与 `funnel` 在同 H 同种子下只差 LU 出口容量,故这是唯一不含其他差异的配对方式;按列表顺序拼接会在某格缺失时静默错位,T14 对此有断言。

### 12.4 数据准备清单

目录已就位:公开数据集这一支落在 `clbs/database/`(原始件 + 转换后算例 + 路网模板 +
参考值),结果落 `clbs/output_database/`,汇总落 `clbs/experiments_database/`,与自造算例的
`input/` / `output/` / `experiments/` 严格分家。流程、两个 regime 的报告口径与三道验收标准
见 `clbs/database/README.md`。

| # | 事项 | 落地位置 | 状态 |
| --- | --- | --- | --- |
| 1 | 下载 Bilge(82)、Deroussi(10)、Kumar(56)、Homayouni set 1(20)、set 2(31)、Lyu(2019)原始文件 | `database/raw/<key>/` | **已完成(2026-08-17 补齐最后两份)**:六份原始 PDF 全部在库,逐文件来源与 SHA256 见 `database/MANIFEST.csv`。补齐的两份:**Bilge** 取自同一托管站的另一个页面(`/2019/04/11/jspt-instances/`,不是 fjspt 那页,故先前按 fjspt 页找必然找不到);**Lyu** 取自 IEEE Access 开放获取(DOI 解析到 arnumber 8723142)。注意 **Bilge 那份是纯栅格**(无内嵌字体,`pdftotext` 全文仅 5 字符),不可机读,故其提取物是**人工转录**,见第 3 项 |
| 2 | 格式转换器:各基准原始格式 → 3.1 节 JSON schema(退化模式下**直接提供 `ideal_dist`**,不要造完全图) | `tools/convert_public.py`,产物落 `database/json/` | **hf 已完成**(20 算例);mk / cb / dn / ku 待做 |
| 3 | ~~从 Bilge 论文的 4 个布局图数字化导轨拓扑,存为可复用的 `network` 模板~~ | `database/network/` | **已否决(2026-08-17,原始件到手后实测)**:原定验收"还原图的全对最短路与原文矩阵逐项相等"**在无向走廊下不可能达成**。Table 13 的四张矩阵**全部不对称**(如布局 1 的 LU→M1=6 而 M1→LU=12,布局 4 更悬殊:LU→M1=4 而 M1→LU=18),而无向走廊算出的 t\* 必然对称,故逐项相等无解。四张矩阵**都满足三角不等式**(与 HF 的 2/7 违反不同),即可还原为**有向**图——但本项目的走廊是无向的,用不上。复现:`py -m tools.check_fidelity bu --csv`,结果 `experiments_database/fidelity_bu.csv`(有向图 4/4、无向走廊 **0/4**),转录物 `database/extracted/bu/layouts_4machines.txt`(**人工转录,待第二人复核**)。**故受控算例的布局仍只能自造**(12.3.1),这一条不再是"待办"而是"此路不通" |
| 4 | 扩展实例生成器(a/b/c 三维度 + T̄t/T̄p 标定 + 命名规则 + 种子记录 + 特征头) | `algorithm/generator.py` | **已完成**(12.3) |
| 5 | best-known 结果表(Homayouni 2021/2023、Ham 2020、Chaudhry 2022 辑录),供自动算 gap | `database/refvalues/<key>.csv` | **hf 已完成**:16 个 MILP 可证最优 + 4 个 best-known,共 52 行,来源为 Homayouni & Fontes 2021 与 Lim & Moon 2023 |
| 6 | 退化对标批跑器:跑 `ideal` 档、自动算 gap、三条硬性判定 | `tools/run_database.py` | **已完成**:20×10 格全部通过判定,结果见下 |

**第 2 项的写法已定,原文那句"用完全图 + t\* 弧时间"要划掉。** 实测 hf 的 7 个布局矩阵
**全部不对称**(往返时长不等,单向导轨的必然结果),其中 3-M 与 7-M 还**违反三角不等式**。
本项目 `corridors` 是无向的、一条走廊只有一个 `time`,承载不了不对称;而任何图的最短路闭包
必然满足三角不等式,故那两个矩阵**不是任何图的闭包**,造完全图跑最短路会把非度量项抄成近路
(3-M 最多快 4,7-M 最多快 6),换来的是**另一个**算例、参考值不再可比。因此 `Network` 增开了
`ideal_dist` 旁路(`Instance.ideal_dist` / `Network(..., ideal_dist=...)`),矩阵原样承载;
矩阵型算例没有走廊图,`Router` 在 `conflict_free=True` 时显式拒绝。回归断言见 T15。

**第一层退化对标结果**(默认预算 p100g200s30,20 算例 × 10 种子,`experiments_database/`):

- 三条硬性判定 200 次运行全部通过:无低于可证最优、无低于自算下界、无校验失败。**第一条是
  转换保真的唯一结果层证据**——退化档与文献是同一个数学问题,可证最优是硬地板;
- 12/20 个算例最优解与参照值逐位相同,10 个小型算例(SFJST01–10)全部命中可证最优;
- 平均 gap 2.68%(中位 0.00%),但随规模单调恶化:四个最大算例(32–48 工序)均值 9.96%,
  最差 12.93%;
- **该 gap 是搜索能力上界,不是预算不足**:预算提到 p100g1000s100(挂钟 2.5 倍)后,10 个
  mfjs 算例的平均 gap 只从 5.37% 降到 4.53%,10 个中有 5 个一点没降,最大那个从 12.93% 只降到
  12.85%。200 次运行全部由 stall 早停(自然收敛),故不是"没跑够";
- **副产品:复合下界的松弛量被分离出来**。口径**只取那 16 个有可证最优值的算例**:另外 4 个的
  参照是 best-known,而真实最优只可能更小,拿它当分母会把"下界松"这一份**高估**(混算 20 个得
  13.3% / 14.9%,即 89%,比取严的结果更好看),故不用。在这 16 个上,同一个复合下界距可证最优
  的中位数为 **10.3%**,距本方法最好解的中位数为 **10.8%**;逐算例作差,"搜索欠缺"这一份的中位
  数为 **0.0** 个百分点(多数算例本就命中最优)、最大 **4.1** 个百分点,即间隙几乎全部来自下界
  自身松弛。这回答了 13.6 一直悬着的问题(论文局限 (4) 原话"无法区分搜索失败与下界松弛")。
  注意不能直接搬到自造拥堵算例上——那里有争用而该下界完全忽略争用,只会更松,但方向一致。

---

## 十三、实证检验结果与待办路线图

> 本节是**实施后**的检验记录,推翻了若干设计预期。凡与前十二节的设计描述冲突处,以本节为准。

### 13.1 已实现的改动清单

| 模块 | 改动 |
| --- | --- |
| `network.py` | 预约表容量化(按走廊-时段设容量)、撤销日志与 `release_all` / `checkpoint` / `rollback` / `occupancy`;`RoutePlan.wait_events()` 保留让行时刻;新增 `PriceTable`;`Router` 增加价格加权多标签 Pareto 搜索,θ=0 时逐字节退化为原单标签最早到达搜索 |
| `pricing.py`(新) | `finite_difference_prices`(定义式)、`surrogate_prices`(代理)、`candidate_slots`、`price_agreement`(Spearman 秩相关)、`default_bucket_width` |
| `decoder.py` | 新增 `dispatch_exact`(基于回滚的派车试探);关键链归因扩展为五类标签的 `critical_chain`,派生 `critical_real_ops` / `critical_corridor_slots` / `blocking_opponents`;`decode` 增加价格、容量、派车模式、禁派集等参数 |
| `ga.py` | 改派评分弃用量纲失衡的 `λ·累计等待`;新增冲突凭证制导的错峰算子(`os_shift` 定向移位);价格表按代刷新与可选有限差分校准 |
| `baseline.py` / `main.py` | 七档递进消融链 + CLI(`--mode ablation`、`--theta`、`--dispatch`、`--entry-options`、`--fd-calibrate`) |
| `tools/sweep_price.py`(新) | 机制诊断扫描,含同算力复核档 |
| `input/congested_8x4x4.json`(新) | 拥堵型算例(见 3.1 表) |

回归测试 T1–T8 全部通过。

**后续轮次的补充改动**(与本节数据无关,但影响其复现方式):

| 模块 | 改动 |
| --- | --- |
| `algorithm/generator.py` + `tools/gen_instances.py`(新) | 受控扩展算例生成(12.3) |
| `algorithm/ga.py` | 新增挂钟预算闸门 `time_budget_sec` 与 `stopped_by`,使 8.2 协议 1 由代码保证而非人工放宽早停 |
| `algorithm/baseline.py` | 七档收敛为单一定义 `ARMS` + `solve_arm`,`main.py` 与批跑器共用(消除"同名档位、不同配置"这类最难发现的实验错误);两阶段透传第一阶段停机原因 |
| `algorithm/stats.py`(新) | 描述统计、精确/近似 Wilcoxon 配对检验、Spearman 秩相关(8.2 协议 2);`pricing.price_agreement` 改为复用其秩函数 |
| `tools/run_matrix.py`(新) | 矩阵批跑器,协议的执行者(8.4) |

回归测试扩展为 T1–T14 并全部通过。

### 13.2 关键结果:同算力预算下,新机制均未显示优势

#### (i) 基准算例 `example_3x3x2`(pop=40, gen=60, seed=42,单种子)

| 档位 | C_max | 秒 | 评估数 |
| --- | --- | --- | --- |
| 1 `rule` | 48.0 | 0.0 | 1 |
| 2 `twostage` | **34.0** | 0.1 | — |
| 3 `nofeedback` | 34.0 | 1.08 | 1240 |
| 4 `opendispatch` | 35.0 | 0.39 | 840 |
| 5 `nostagger` | 35.0 | 0.97 | 840 |
| 6 `closed` | **34.0** | 1.39 | 1120 |
| 7 `priced` | 34.0 | 6.09 | 1120 |

**两个必须如实报告的读数**:

1. **闭环相对两阶段的集成收益在本算例上为 0**(均为 34.0)。原因是该算例只有 6 条走廊、2 辆车、7 道实工序,冲突稀疏,两阶段的排产结果几乎不需要修复。**这说明基准算例不足以支撑创新点 4 的主张,主试验必须在 12.3 的扩展算例上进行**;
2. 派车闭环与错峰算子各自带来 35.0 → 34.0(约 2.9%),但档 3 `nofeedback` 同样得到 34.0——即**局部搜索整体关掉也能到 34.0**,说明这里的 1 个单位差异更可能来自评估次数(1240 vs 840)而非机制,与下面 (ii) 的结论一致。

#### (ii) 拥堵算例 `congested_8x4x4`,3 个种子(42 / 7 / 2024),pop=60

| 配置 | C_max 均值 | 最好 | 最差 | 秒/次 | 评估数 |
| --- | --- | --- | --- | --- | --- |
| A 规则派车 + 无错峰(原框架) | 78.0 | 76 | 80 | 5.6 | 3480 |
| B A + 错峰算子 | 75.7 | 75 | 76 | 7.1 | 3820 |
| **A′ A 放宽早停(同算力)** | **73.3** | **69** | 80 | 33.0 | 20420 |
| B′ B 放宽早停(同算力) | 74.7 | 73 | 76 | 28.9 | 16880 |
| C A + 派车试探 | 74.3 | 72 | 76 | 27.8 | 4040 |
| D C + 错峰算子(完整版) | 76.3 | 73 | 81 | 21.9 | 2860 |
| E D + 价格 θ=0.15 | 73.7 | 73 | 75 | 52.5 | 3640 |

θ 单独扫描(`dispatch=exact`,3 种子):

| θ | 0 | 0.05 | 0.15 | 0.6 | 0.15(有限差分价) | 0.6(有限差分价) |
| --- | --- | --- | --- | --- | --- | --- |
| 均值 | **75.7** | 78.0 | 78.0 | 80.7 | 78.7 | 81.0 |

#### (iii) 单种子的危险性:一次反例

一次减算力单种子复核(`pop=40, gen=40, stall=15, seed=42`,两算例七档)在拥堵算例上给出:

```text
rule 184 | twostage 102 | nofeedback 81 | opendispatch 97 | nostagger 82 | closed 82 | priced 76
```

同一次运行里有两个互相矛盾的读数:**闭环相对两阶段改进 19.6%**(82 vs 102,这是集成收益首次在拥堵算例上显现,方向支持创新点 4);但**被判定有害的 `priced` 档反而最好(76),且 `nofeedback`(81)优于完整版 `closed`(82)**。

这不构成对 13.2 任何结论的推翻——n=1、且算力低于 (ii) 表——但它精确说明了为什么 8.2 协议 2 是必须的:**在这个方差水平上,单种子足以把任意档位排成任意顺序**。任何人只跑一个种子就能"证明"价格机制有效,也能"证明"局部搜索有害。引用本节数字时必须连同种子数与算力预算一起给出。

#### (iv) 受控扩展算例上的初步读数(**n=2,仅供定向,不构成结论**)

批跑器就绪后在 12.3 生成的 `high`/`funnel` 受控对比上跑了一次流程自检:**2 个种子**、`--budget auto`(标定预算 39.2 s / 26.4 s)、pop=40、七档全部通过校验。数字方向上首次支持创新点 3 与 4:

| 算例 | rule | twostage | nofeedback | opendispatch | nostagger | **closed** | priced |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `high`(LU 割 2) | 133.0 | 70.0 | 77.0 | 71.5 | 70.0 | **67.5** | 77.5 |
| `funnel`(LU 割 1) | 134.0 | 76.0 | 77.0 | 77.0 | 78.5 | **77.0** | 83.0 |

三点值得注意,但都必须连同"n=2、p≈0.5"一起引用:

1. **预测 3 在三个消融档上一致成立**:机制增益在 `high` 上为 12.3% / 5.6% / 3.5%(对 `nofeedback` / `opendispatch` / `nostagger`),在 `funnel` 上全部塌到 0% 附近(0.0% / −0.1% / 1.9%)。集成收益同样如此(`high` +3.6%,`funnel` −1.5%)。这正是 3.1 实测修正提出的诊断——**决策无关拥堵稀释机制信号**——所预言的模式;
2. **与 13.2 结论 2 的表面矛盾及其解释**:此处 `closed`(2520 次评估)在同挂钟预算下优于 `nofeedback`(3860 次评估)12.3%,即机制增益不再能用"多花算力"解释。差别不在算法而在**算例**:13.2 用的手写 `congested_8x4x4` 其拥堵被决策无关的 LU 漏斗支配(漏斗界 32 是紧约束分量),而生成的 `high` 档漏斗占比仅 0.29、紧约束分量是机器负载。**这是 13.6 第 1 条"算例的拥堵结构不对"这一诊断的正面验证**,也说明 13.2 的负面结论有很大一部分是算例造成的,而非机制本身;
3. **`priced` 在两个算例上都最差**(77.5 / 83.0),与 13.3 的机制解释一致,是价格失效的又一处独立证据。

`--budget auto` 下各档的实际算力对比也在此首次量出:两阶段档 0.42 ms/评价 vs 闭环 15.7 ms/评价 vs `priced` 82.6 ms/评价(196x),即 8.2 协议 3 的来源。

**必须做的下一步**:同样的对比在 ≥10 种子下重跑(`--preset p3`),否则以上任何一条都不能写进论文——13.2(iii) 已经演示过单/双种子足以把档位排成任意顺序。

#### (v) 三条结论

1. **价格加权路由系统性有害**,且随 θ 单调恶化;换用定义式有限差分价格同样无效 → 失效**不是价格估计不准**,而是机制本身错(解释见 13.3);
2. **派车试探与错峰算子的增益是"多花算力"的假象**。同代数下 C(74.3)优于 A(78.0)似有 4.7% 增益,但把 A 的早停放宽到同等运行时间后 A′ 达 73.3、单次跑出 69——优于任何新机制。C 每次评估贵 5 倍,只跑到 4040 次评估,而 A′ 跑到 20420 次;
3. **样本量不足以分辨**。A′ 三个种子为 [71, 69, 80],极差 11,而各档均值之差仅 1–3。当前 n=3 的实验**无法支持任何机制性结论**——包括负面结论中除第 1 条(θ 单调变差且跨两种估计器一致)之外的部分。

### 13.3 价格机制为何失效(机制解释,建议写入论文设计论证)

**走廊争用引起的延误已经完整内化在该车自身的到达时刻里。** 下层路由遇到占用就等待,等待时长直接进入该次运输的到达时刻,再经"到达 → 开工 → 完工"传导进 C_max,并被适应度如实计入。此时再按占用向车辆收一笔价格,等于**对同一份拥堵重复计价**:车辆为躲避一个"已经付过费"的成本而绕行或推迟出发,**自身延误是一阶且确定的,而所避免的外部性是二阶且不确定的**,故净效应为负,且随 θ 单调恶化。

真正的外部性只存在于**尚未被路由的后继任务**上(解码按 OS 顺序推进,先落表者只可能拖累后落表者)。但这份外部性同样已被"整体解码后的 C_max"计入适应度,GA 在选择层面已经看得到;把它再搬到路由的局部目标里并不增加信息,只增加扭曲。

**推论**:在"下层用等待精确消解冲突 + 适应度用完整解码的真实 C_max"这种架构里,给下层加**任何**形式的拥堵惩罚都很可能有害。层间接口值得升级的方向不是"传价格",而是"传**结构信息**"——即冲突凭证,它改变的是上层邻域的**构造方式**,不改变下层的目标函数。这正是本文标题采用 Conflict-Guided Feedback 而非"价格协调"的原因。

### 13.4 文档—代码一致性:已修与待修

**已修(本轮)**:

| # | 问题 | 处理 |
| --- | --- | --- |
| 1 | **C_max 口径分散在两处**(正确性风险) | `validator` 原在 δ_return=1 时把**全部 AGV 分段**(含空载)并进 (g) 的期望值,与 `decode` 的 `max(工序 finish)` 构成两套口径;当前算例中每车最后一段恒为载货段,两者恰好相等而掩盖了问题。**触发条件已写在建模文档 C5**("卸货后按任务链空载就位"):一旦照 C5 实现主动空载就位,校验器会对每个合法解报错,且因它是 T2/T3 的仲裁者,该失败会伪装成解码器 bug。现 (g) 严格收敛为假设 8 的定义,越界防御由新增的 (h) 独立承担 |
| 2 | **占用率从未采集** | 改为在**报告层独立重算**:新增 `report.corridor_occupancy` / `occupancy_profile`,从时刻表反推(不读预约表内部状态),仅对每模式最终最优解算一次,写入 `summary.json` 的 `occupancy` 字段并在控制台打印瓶颈走廊。搜索路径零影响;对 `twostage` / `rule` 这类不经 GA 的档同样可用。**首个产出即修正了 3.1 对拥堵算例"唯一瓶颈"的断言** |
| 3 | **`forbid` 与 `collect_occupancy` 是从未执行过的死分支** | 新增 T9(禁派集生效 + 全禁兜底)与 T10(两套占用率计算一致性)。T10 顺带首次验证了 `dispatch=exact` 的落表-回滚**无残留** |

**待修**:

| # | 问题 | 现状 | 影响 |
| --- | --- | --- | --- |
| 4 | **禁派集未接入 GA(机制缺口,有意暂缓)** | `decode(forbid=...)` 已可用且有 T9 覆盖,但 GA 侧从不填充 | 6.5 第 1 步中 `vehicle` 标签的"应派算子"仍落空,该类瓶颈只能靠通用变异碰运气。**按 13.6 的判断有意不做**:在实验协议尚不能把机制与噪声分开之前,增加第五个机制只会多一个无法标定的旋钮 |
| 5 | **增量评价未实现** | `release_all` / `checkpoint` / `rollback` 已就位,但局部搜索仍对每个邻居**全量重解码** | 6.2 末尾论证的"前缀复用"未落地;这是 13.5 各方向的算力地基 |
| 6 | `seed` 有默认值 42 | 规格要求"必须显式传入" | 实验脚本须显式传参并记录,勿依赖默认 |

### 13.5 备选深化方向(设计留痕,均未实现)

以下方向在设计阶段评估过,因 13.2 的结果暂缓。保留设计要点以备后续。

**(甲) 下层从贪心评估器升级为优化器**。现下层等价于 MAPF 中的**优先规划(PP)**,且优先序被硬绑定为解码器的 OS 扫描顺序,先到先占、不回退。升级路径:

- **不动点迭代(推荐主方案)**:外层固定指派与排序,内层交替"重排路由优先序 → 用新到达时刻前推更新时刻表",直至 C_max 不再改善。**接受判据**:仅当 `C_max(S_r) < C_max(S_{r−1})` 时接受新优先序,否则保留旧解并终止 → 得到单调不增序列,下界为该染色体在无冲突约束下的理想完工时间,故必然有限步终止。这个论证可填补第十四节指出的 "Convergence / Complexity" 空缺;
- **本问题与标准 MAPF 的关键差异(创新性所在)**:标准 MAPF 中各智能体的起点、终点、最早出发时刻均给定;这里任务就绪时刻来自上游工序完工、车辆可用时刻来自该车上一任务,**改变任一条路径会改变后续任务的起点与最早出发时刻,甚至改变任务归属**。这是带优先约束与时序耦合的多智能体路径问题;竞品的 PBS 用在"甘特图已定、任务清单已冻结"之后,规避了这一耦合;
- **小规模 CBS 下界(性价比最高,建议先做)**:固定上层决策,求该决策下的最优无冲突路由。差距小则可论证"贪心路由足够好、算力应投在上层"(支持现架构);差距大则证明本方向必要。**两种结果都能写**。

**(乙) 增量评价与局部修复**。基于 6.2 末尾的状态单向传递性质:改动 OS 位置 x 之后的部分时,位置 x 之前的事件与预约逐字段不变,可用检查点 + 重放实现前缀复用;另可加乐观下界剪枝(改派前用 `t* + t^P` 估下界,不优于当前最优则跳过解码)。这是纯工程加速,无独立创新性,但它是甲、丙两方向能否落地的前提。

**(丙) 反馈作用面继续扩大**。禁派集(见 13.4 第 2 项)是最轻的换车方案:染色体增加稀疏字段 `forbid[(j,i)] ⊂ 车辆集合`,仅对被归因为 `vehicle` 的少数任务生效,不引入完整 AD 编码段,可解码性不受影响,且可解释性强。

**(丁) 时空拥堵画像与占用率进上层评分**。时间分桶占用率与双信号分离已落地(5.2 表),报告层已可输出画像(13.4 已修第 2 项)。

**这里要修正一处过度推广**:13.3 的结论是"不要给**下层**加拥堵惩罚",它**不覆盖**上层改派评分。两者的结构不同——下层是重复计价(让行等待已内化进到达时刻并被适应度计入,再收一次即双重惩罚);而上层改派评分只用来**给候选邻居排序**,是否接受仍由真实解码的 C_max 裁定,因此那里的前瞻性占用率项是"对将要发生什么的预测替代量",不构成对同一份成本的二次收费。**所以"占用率进改派评分"仍是 13.3 未否决的活选项**,可作为 6.5(a) 中已弃用的 `λ·congestion` 的正确替代:

```text
score(m') = t*(pos_prev, loc(m')) + t^P(j,i,m') + λ · T̄p · util(loc(m'), bucket(t_start))
```

其中 `util ∈ [0,1]` 无量纲、`T̄p` 为平均加工时间使三项同量纲,λ 由此成为可跨算例比较、有资格做敏感性分析的耦合强度——这恰是原 `λ·congestion` 失格的地方(见 6.5a 的弃用说明)。

但**自适应 λ/θ**(ALNS 权重更新或多臂老虎机选参)**仍不建议投入**:自适应机制需要可靠的增益信号来更新权重,而 13.2 表明当前连固定参数下的机制差异都分辨不出。先解决 13.6 的算例与协议问题,再谈自适应。

### 13.6 行动优先级

1. **先造算例、再定协议,而非继续加机制**(**算例生成器、批跑器与 16 格矩阵实跑均已完成**;判定见 12.3.6)。当前无法判定任何机制,根因有三条:一是**算例太弱**——基准算例上闭环与两阶段打平(13.2(i)),连创新点 4 的主张都撑不起来(拥堵算例上单种子已见 19.6% 集成收益,方向正确,但需多种子确认,见 13.2(iii));二是**算例的拥堵结构不对**——拥堵算例中相当一部分拥堵集中在 LU 出口这条**决策无关**的串联漏斗上(3.1 实测修正、12.3.5 的漏斗界独立印证;**13.2(iv) 已对这条诊断给出正面验证**:换到漏斗占比低的生成算例后,同算力下机制增益重新出现,而在 `funnel` 档上再次消失);三是**协议不足**——种子少、未控算力。
   - **已做(算例)**:12.3 的生成器,含容量旋钮、T̄t/T̄p 标定、`high`/`funnel` 受控对比、结构指标与复合下界,已生成 4 档 × 4 个 H 的默认矩阵;
   - **已做(协议与工具)**:8.4 的批跑器,把 8.2 三条协议做成默认行为(挂钟预算标定、≥10 种子、配对 Wilcoxon、预算体检、12.3.6 三条预期的自动判定),并支持可中断续跑与跑/报告分离。协议 3(等时间与等评估数两种口径都不中立)即在实现过程中发现;
   - **已做(矩阵实跑)**:`p3` 与 `lowmid` 两个批次合计 16 格(四争用档 × 四 H 档)× 10 种子 = 1280 次运行,invalid 为空。预测 1 的异构那一半与预测 2 **均被否定**(见 12.3.6 的判定与其中的口径提醒),预测 3 待补足种子。**注意这批次跑于降本措施引入之前**,故其派遣消融读数(−0.74%,0/16 显著)不可与降本后的阶梯批次混用,论文 §5.6 已就此专门交代;
2. **界/下界**(**廉价版已完成,精确解待办**)。A′ 单次跑出 69 而完整版停在 76,说明**瓶颈在搜索能力而非层间耦合**;不知道最优值就无法判断还有多少空间。现已有零成本复合下界(12.3.5),但它较松(gap 上限 35%–61%),只够做量级判断,**小规模精确解仍是待办**。注意:本机 pip 被代理阻断,`pulp`/`ortools` 均无法安装,需自包含实现(建议按 13.5 甲的小规模 CBS 或自写分支定界)或更换环境;
3. **13.4 的一致性项**:已修 3 项(C_max 口径、占用率采集、死分支测试覆盖),余 3 项(禁派集接入 GA、增量评价、seed 显式化)中前两项为有意暂缓的机制/工程项;
4. **论文定位**:既然机制层面的改动在当前算例上无法证明增益,靠"再加一个机制"摆脱"方法组合"质疑是走不通的。可行的替代是把贡献放到**问题与理论**层面——异构 RA 下的耦合结构刻画、精确模型与下界、以及 13.3 这类"什么不管用及其原因"的结构性结论。后者在方法论上比又一个改进型启发式更难被质疑。
   - 12.3.3 的 `high`/`funnel` 受控对比与 12.3.5 的漏斗界为这条定位提供了新素材:**"哪一部分拥堵值得反馈"本身就是一个可形式化、可实验判定的结构性问题**,且它是本文异构 + 显式路网设定下才成立的命题。

---

## 十四、新论文章节结构参考(与旧论文的对应关系)

**结论**:新论文的章节结构以**旧论文 `paper01/paper - 20260326.tex` 为主要参考**。对比过 Pal 等 2023(ESWA,多智能体 FJSP)后确认:两者顶层骨架相同(引言→文献→问题→方法→实验→结论),但旧论文在方法章的"框架总览 → 两层求解器 → 层间协调与理论性质"组织、以及实验章的 Baselines/Ablation/Scalability/Sensitivity 分层上深度更高,且与本文档内容天然一一对应。

### 14.1 方法章对应表(旧论文第 4 章 ↔ 新论文)

| 旧论文第 4 章(Hybrid DP Framework) | 新论文对应内容 | 本文档出处 |
| --- | --- | --- |
| Framework Overview | 闭环双层框架总览(两条反馈回路) | 第四节 |
| CSS-DP(连续层求解器) | 下层:时间窗 Dijkstra 无冲突路由 | 第五节 |
| DES-DP(离散层求解器) | 上层:GA / Memetic 调度层 | 第六节 |
| Coordination Mechanism | 评价级闭环(真实 C_max 作适应度)+ **决策级闭环(冲突凭证制导的改派与错峰)** | 四、6.5 |
| Convergence / Complexity | 解码三重保证、单标签路由最优性论证、状态单向传递性质 | 5.3、6.2、九 |

> **空缺提示**:旧论文该章有独立的收敛性/复杂度小节,本文目前只有可行性与最优性论证,**缺少收敛性定理**。13.5(甲)的不动点迭代接受判据可填补此处;若不做该方向,应在写作时明确说明本文为启发式、不主张收敛性,避免章节结构照搬后出现空章。

### 14.2 实验章对应表(旧论文第 5 章 ↔ 新论文)

| 旧论文第 5 章小节 | 新论文对应实验 | 本文档出处 |
| --- | --- | --- |
| Baseline Methods | 七档递进消融链 + 小实例精确解/下界 | 8.1 |
| Main Results | 公开基准退化对标 + 扩展算例主试验 | 12.2、12.3 |
| Ablation Study | 逐环节闭合的递进消融(开环 → 评价级闭环 → +决策级反馈 → +派车闭环 → +错峰算子),**并含负面对照 `priced`** | 8.1、13.2 |
| Scalability / Sensitivity | 拥堵度 × 异构度双因子;柔性度、NA/NM、T̄t/T̄p 辅助扫描 | 12.3 |
| Qualitative Analysis | 示例算例甘特图与**关键链五类归因**叙事(closed vs twostage vs rule) | 6.5、九(T2/T8) |
| — (新增) | **实验协议说明**:同算力预算与种子数要求,并解释为何必要 | 8.2、13.2 |

### 14.3 需要调整与借鉴之处

- **问题建模章(第 3 章)重组**:旧论文按 CSS/DES 动力学分小节,新论文改为"车间调度模型(RA、工序、Ω)/ 路网与冲突模型(走廊、时间窗)/ 耦合约束与目标函数"三块,其余骨架照搬;
- **方法章需新增一节"层间接口设计"**:并列讨论三种接口(只传时间 / 传价格 / 传冲突凭证),用 13.3 的机制解释论证为何选第三种。这一节是新标题 Conflict-Guided Feedback 的落脚点,也是把负面结果转化为正面论证的地方;
- **借鉴 ESWA 论文两点**:① 实验结果按"对手类别"分小节组织(与两阶段开环比、与规则/元启发式比),贴合"闭环 vs 开环"叙事;② Introduction 末尾放显式贡献 bullet + 一句话 gap 定位(如 "to the best of our knowledge, no work couples conflict-free routing with **heterogeneous** robot arms through decision-level feedback"),作为审稿人检索创新点的锚点;
- **文献章需正面处理直接竞品**:引用 1.2 表中第 2 行的原文自述("two-stage sequential … one-time feedback adjustment … Generate-then-Refine"),把 gap 定位在"闭环深度"而非"是否考虑冲突";
- **结论章沿用旧论文四段式**:Contributions → Insights → Limitations → Future Directions。其中 Limitations 应如实包含 13.2 的结论(机制增益在同算力下未获证实)与 13.4 的待修项,Future Directions 对应 13.5、13.6。



### 14.4 接下来的论文章节改动建议：
| 现在 | 建议 | 原因 |
|---|---|---|
| 标题 *Evaluate, Don't Coordinate: What Feedback Is Worth Its Cost* | 命名机制而非发现 | 这是一个**极好的发现型标题、致命的创新型标题** |
| Related Work 六个小节独占一章 | 压到三节 | MDPI 风格把相关工作大量并入引言 |
| 4.4 *What should cross the interface?* | 4.4 预约表感知派遣 | 用问句当小标题就是发现型的自我暴露 |
| 4.6 *Cost, and what is not claimed* | 4.6 复杂度与代价分析 | "不主张什么"移到结论的局限里 |
| 5.5–5.8 四节全是负面(*which congestion is worth feeding back* / *pricing: a negative result* / *why the mechanisms do not pay* / *threats to validity*) | 收成 5.5 消融 + 5.6 权衡 | **这四个小标题是论文被读成发现型的主因** |
| *Threats to validity* 独立成节 | 并入结论的 Limitations | 那是实证软件工程的惯例,OR 刊少见 |
| 无编号假设清单、无符号表 | 补上(3.2 / 3.3) | 2023、2026 都有,审稿人预期 |
| 无适用区间、无真实配置验证 | 新增 5.4 / 5.7 | 装 A/B/C 与"接近实际场景"的论证 |

一句话概括这次改动:**负面结果一个都不删,但全部从"章节标题"降级为"消融证据"。** 同样的实验、同样的诚实,叙事从"我们发现协调机制都不管用"变成"我们在三个候选接口设计中选定了唯一经得起同挂钟检验的那个"。这是标准做法,不是包装。

标题备选：
Reservation-Aware Vehicle Dispatching for Integrated Flexible Job Shop Scheduling and Conflict-Free AGV Routing with Heterogeneous Robot Arms
Reservation-Aware Dispatching in a Closed-Loop Bilevel Framework for Integrated Flexible Job Shop Scheduling and Conflict-Free AGV Routing

创新点(五条,前三条承重)
一、问题层:一个在最接近的两篇文献里表述不出来的权衡。 机器柔性、加工效率异构、走廊拥堵三者同时在场,且拥堵进入目标函数。"最短走廊拥堵时,去一台更慢但连通性好的臂可能更优"——Sensors 2023 有柔性但上层在常数运输时间矩阵上排产,看不见拥堵;Sensors 2026 有真正的 MAPF 路由但用经典 JSP 基准,机器由算例固定,没有柔性。两边各缺一半,这句话在它们的模型里连写都写不出来。

二、框架层:闭环的价值被首次量化。 把无冲突路由放进适应度函数,相对 2026 那篇的忠实结构 −12.80%(p=0.0058),在试探派车下 −16.39%(p=0.0001,19 胜 0 负)。这里要注意措辞:"把评价器放进循环"这个想法并不深刻,深刻的是它很贵、因而从没人量过它值多少。2026 那篇作者自己把 "fully iterative loop" 列为未来工作。所以贡献是定量 + 让它付得起,不是这个念头本身。

三、机制层:预约表探测式派车,以及让它付得起的降本。 闭环内 −5.81%(p=0.0129);它替换的正是 2026 在用的 Idle Priority 规则(argmin Cost(path_k, L_start)),打的是现行发表实践。两处可证等价的优化——可采纳下界剪枝 + 胜者路径复用——带来 1.82 倍加速、路由调用减少 49.4%,成本倍数从 4.6 压到 2.5。机制解释:AGV 同构,车与车唯一的差别是站位与路通不通,所以选车是纯粹的拥堵决策,信息能全额兑现;选臂掺加工时长,后者压过走廊差异。稳健性:九配置(五布局 × 三车臂比 × 三柔性,争用 6.6%~33.5%)平均 −4.80%,p=0.0038,且不依赖任何参数落在特定区间(秩相关 +0.000)。

四、互补性:全篇最有意思的一条。 同一个派车机制,开环下只值 −2.79% 且不显著,闭环内值 −5.81% 且显著;端到端 −18.50% 超过两者独立时该有的 −15.2%。拥堵感知的派车装在一张对拥堵视而不见的排程上,几乎不值钱。 这条把前两个贡献焊成一体,别人拆不走其中一个。

五、方法层:一套让"何时值得管拥堵"可被回答的实验装置。 争用强度可控的算例生成(标定到 6.6%~33.5%)、统一执行器(任何来源的计划都过同一个仿真器和校验器)、同挂钟而非同代数、四级基线阶梯且每一级对应一篇真实发表的结构。附三个受控负结果(走廊定价 +0.47% p=0.70;凭证制导打分只捕获随机到神谕之间 26–29%;穷举真解码同挂钟输 2.49%)与一条代价律(试探档代数 × 车队规模 ≈ 常数,16 辆车时剪枝失效)。

需要说明的是第五条在投稿时通常不单列为 contribution,而是化进方法与实验章节;但三个负结果建议保留为显式的 ablation,它们是"我们为什么选这个设计"的正当理由。