# 异构机械臂柔性作业车间调度与 AGV 无冲突路径规划集成问题

## ——应用场景介绍

---

## 一、一句话定位

研究的是这样一个问题:**在一个由能力各不相同的机械臂和数量有限的 AGV 共同构成的柔性作业车间里,把"生产调度"与"AGV 无冲突路径规划"放在一起统一优化**,目标是最小化最大完工时间(makespan),即让最后一件成品尽可能早地完成并运回装卸站。

这个问题的英文表述为 *Integrated Scheduling of Flexible Job Shops with Heterogeneous Robot Arms and Conflict-Free AGV Routing*。

---

## 二、车间是什么样的

### 2.1 车间里有三类要素

**第一类:加工资源——机械臂(Robot Arm,下称 RA)**

车间里有若干台机械臂,每台固定安装在一个工位上。它们的能力**不完全相同**:受末端执行器型号、负载能力、定位精度等物理条件限制,每台机械臂只能加工全部工序中的一个子集。而且,即使两台机械臂都能做同一道工序,它们所需的时间也可以不同。

**第二类:运输资源——AGV(自动导引小车)**

车间里有固定数量的 AGV,它们彼此完全相同:速度一样、一次只能运一个工件、没有个体差异。它们的任务是在装卸站与各机械臂工位之间搬运工件。**车队规模是有限的**,不假设"车足够多"。

**第三类:装卸站(Load/Unload Station,下称 LU)**

车间的唯一出入口。所有工件的毛坯在计划开始时都堆放在这里,所有成品最终也要运回这里。计划开始时,全部 AGV 也停在这里待命。

### 2.2 车间布局是一张路网图,不是一张距离表

这是本课题与多数同类研究最直观的一个差别。多数文献把车间布局简化为"任意两个工位之间的行驶时间矩阵",而本课题把布局**显式建模为一张图**:

- **节点**:装卸站、每台机械臂的取放点、以及路口/汇合点;
- **走廊(边)**:连接节点的可通行路段,每条走廊有已知的通行时间;
- **通行规则**:**走廊很窄,同一时刻只允许一辆 AGV 在其中行驶,不论方向**。两车相向而行会撞车,同向追赶也不允许。

除通行走廊外,每个节点旁另设**停靠位**,容量充足。空闲的车、等待让行的车、等待工件完工的车都停在停靠位上,**停靠不占用走廊,不挡别人的路**。这一区分很重要——正因为有它,计划开始时所有 AGV 才能同时停在装卸站而不违反"一条走廊一辆车"的规则。

下面是本课题一直使用的示例车间的路网(括号内为该节点对应的资源,连线上的数字为单向通行时间,单位:分钟):
c1、c2：crossing：路口/汇合节点,本身不加工也不装卸,只是几条走廊交汇的通行点
r1、r2、r3：robot arm：机械臂的取放点,分别对应 RA1、RA2、RA3。AGV 在这里把工件卸给机械臂、或取走加工完的工件
```text
                    [r1]  ← RA1 的取放点
                   /     \
                  2       4
                 /         \
[v0]===2===[c1]-----3-----[c2]---2---[r2]  ← RA2 的取放点
 ↑          ↑              |
 装卸站     路口            2
 (LU)                      |
                         [r3]  ← RA3 的取放点
```

| 走廊 | 通行时间(分钟) |
| --- | --- |
| v0 – c1 | 2 |
| c1 – r1 | 2 |
| c1 – c2 | 3 |
| r1 – c2 | 4 |
| c2 – r2 | 2 |
| c2 – r3 | 2 |

从这张图可以直接读出一件重要的事:**走廊 c1–c2 和节点 c2 是全车间的咽喉**。从装卸站去 RA2、RA3 都必须经过它们,机械臂之间的转运也大多要经过。这里就是冲突的高发地段。

### 2.3 一个工件的完整旅程

```text
   毛坯在 LU 等待
        │
        │  ① AGV 来接,运到第 1 道工序被指派的机械臂
        ▼
   在该机械臂旁的缓冲区排队 → 上机加工 → 完工后移入输出缓冲区
        │
        │  ② 若下一道工序在另一台机械臂上,AGV 来接,运过去
        │     (若下一道工序恰好在同一台机械臂上,则不需要搬运)
        ▼
      ……重复,直到最后一道工序完成……
        │
        │  ③ AGV 来接成品,运回 LU
        ▼
   成品返抵 LU,该工件完成
```

**makespan 就是最后一件成品返抵 LU 的时刻。** 注意这里包含了成品回运——后面会看到,这一段会显著改变问题的性质。

---

## 三、这个场景的三个关键特征

前面描述的车间,乍看和文献里常见的"柔性作业车间 + AGV"没什么两样。真正让这个问题不同的,是下面三个特征。**它们同时存在,是本课题的核心权衡。**

### 特征一:机械臂的异构是"双层"的

**第一层是能力异构。** 每道工序只有一部分机械臂能做,能做的那些构成该工序的"可选机器集合"。这不是人为设定,而是物理约束的直接后果:换不了对应夹具的机械臂做不了这道工序,负载不够的举不动这个工件,精度不够的达不到公差要求。

**第二层是效率异构。** 在可选机器集合内部,同一道工序在不同机械臂上的加工时间**可以不同**。快的机械臂可能是新设备、行程更短、程序优化得更好。

举个具体的例子,示例算例中工件 J1 的第一道工序在三台机械臂上的加工时间分别是 **4、6、5 分钟**——最快的 RA1 比最慢的 RA2 快了 50%。

这两层叠加起来,意味着"把这道工序交给谁"是一个**有真实代价差异**的决策,而不是一个无关紧要的选择。

### 特征二:运输时间不是常数,而是决策的函数

这是本课题最核心的一点。

在传统研究中,"从 A 点运到 B 点要多久"是一个查表就能得到的常数。而在本课题中:

```text
   一次搬运的实际耗时  =  所选路径上各段走廊的通行时间之和
                        +  途中为了给别的车让行而停靠等待的时间
```

两项都由决策产生,而不是给定的参数:

- **第一项取决于选哪条路**。同样从 r1 到 r2,可以走 r1→c2→r2(4+2=6 分钟),遇到堵塞也可以绕行 r1→c1→c2→r2(2+3+2=7 分钟);
- **第二项取决于路网当时的占用状况**,而占用状况又取决于**别的车此刻在干什么**——也就是取决于其他所有搬运任务的安排。

所以,**这一趟要花多久,不能在排产之前算出来**;而排产又必须知道要花多久才能定得下来。这是整个问题最本质的循环依赖。

### 特征三:AGV 数量有限,瓶颈可能落在运输侧

车队规模有限,意味着工件可能"完工了却没车来接",在输出缓冲区干等。示例算例中 AGV 数与机械臂数之比是 2:3,属于运输偏紧的配置。当然，这个配置可以更改并用在小论文中做实验。

这个比值决定了瓶颈落在哪一侧:

- **车相对少**:瓶颈在运输侧,空车调度和路径规划的价值大;
- **车相对多**:任务分配变得无所谓,但路网上的车流密度上升、冲突加剧;
- **车多且不冲突**:问题退化成"只考虑固定运输时间的柔性作业车间调度"，但不太符合现实情况。

### 三个特征叠加,产生本课题的核心权衡

```text
   通往加工最快的那台机械臂的路段正在拥堵
              │
              ├── 方案 A:仍然派给它,加工快,但工件要在路上多等
              └── 方案 B:改派给另一台机械臂,加工慢一点,
                          但路网通畅,工件能更早开工
              │
        哪个整体更优?  ← 本课题要回答的问题
```

**这个权衡的成立需要三个条件同时满足:**

| 缺了哪个条件 | 会退化成什么 |
| --- | --- |
| 缺"效率异构" | 机械臂之间没有快慢差别,改派没有代价也没有收益,但不太符合现实情况 |
| 缺"显式路网" | 运输时间是常数,不存在"哪条路堵了"这回事,退化为经典问题,但不太符合现实情况 |
| 缺"车队有限" | 车足够多则互不干扰,不产生冲突与等待,但不太符合现实情况 |

换句话说,**这三个特征不是三条并列的假设,而是一个整体**。这也是本课题问题定义的核心所在。

---

## 四、与传统 FJSP 的差别在哪里

这一节把本课题放在整个问题谱系中定位。下表按"考虑得越来越多"的顺序排列:

| 维度 | 经典 JSP<br>(作业车间调度) | 经典 FJSP<br>(柔性作业车间调度) | 含运输的 FJSP<br>(FJSP with Transportation) | **本课题** |
| --- | --- | --- | --- | --- |
| 每道工序能在哪台机器上做 | 唯一指定 | 有可选集合,**需要决策** | 有可选集合 | 有可选集合,且集合由物理能力决定 |
| 加工时间 | 固定 | **依赖所选机器** | 依赖所选机器 | 依赖所选机器(效率异构) |
| 工序间的搬运 | **不考虑**,视为瞬时 | 不考虑 | 考虑,时间为**已知常数矩阵** | 考虑,**时间是决策的函数** |
| 运输车辆 | 无 | 无 | 有限台数,**需要派车与排序** | 有限台数,需要派车与排序 |
| 车间布局 | 不建模 | 不建模 | 只体现为距离/时间矩阵 | **显式建模为路网图** |
| 车与车之间 | —— | —— | **互不干扰**(一辆车的行驶时间与其他车无关) | **相互冲突**:需让行、等待、绕行 |
| "何时出发"是不是决策 | —— | —— | 否(只要不迟到,早走晚走等价) | **是**(占用路网的时段不同,会影响别的车) |
| 需要做的决策 | 1 项:排序 | 2 项:指派 + 排序 | 3 项:+ 派车与任务链 | **4 项:+ 无冲突路由** |
| 典型求解范式 | 单层优化 | 单层优化 | 一体化编码,或"先排产、后路由"两阶段 | **调度层—路由层闭环协同** |

**用一句话概括这个递进关系:**

> 经典 FJSP 假设"搬运不花时间";
> 含运输的 FJSP 假设"搬运花固定的时间";
> **本课题认为"搬运要花多少时间,取决于你怎么调度"。**

这句话就是本课题相对传统 FJSP 的全部增量所在。它带来的直接后果是:传统方法里"先算好运输时间矩阵、再排产"的做法,在本问题中**在逻辑上就是不成立的**——因为矩阵本身依赖于排产结果。

---

## 五、需要决策什么:四个相互耦合的子问题

```text
  ┌─ 子问题 1 ─ 工序指派:每道工序交给哪台机械臂做
  │                    │
  │                    ▼  指派确定后,才知道工件要从哪运到哪
  ├─ 子问题 2 ─ 工序排序:每台机械臂上先做哪道、后做哪道
  │                    │
  │                    ▼  排序确定后,才知道各次搬运的时间要求
  ├─ 子问题 3 ─ 运输任务分配:每次搬运派哪辆车,每辆车的任务先后
  │                    │
  │                    ▼  任务链确定后,才知道各车要走哪些起讫点
  └─ 子问题 4 ─ 无冲突路由:每段行程走哪条路、何时出发、在哪等待
                       │
                       └──────────────┐
                                      ▼
                    实际运输耗时  →  反过来改变前三项的最优选择
```

前三个子问题合起来就是文献中标准的"含运输的柔性作业车间调度"。**第四个子问题,以及它与前三者之间的双向耦合(图中那条向上的反馈箭头),是本课题区别于既有工作的核心。**

### 一个容易被忽略的边界:什么是输入,什么是决策的产物

这一点值得单独强调,因为它常被误解:

| 是算例给定的输入参数 | 不是输入,是决策产生的结果 |
| --- | --- |
| 工件数、工序结构 | **有哪些搬运任务**(取决于指派:相邻两道工序同机则不产生搬运) |
| 每道工序的可选机器集合与加工时间 | **每次搬运从哪运到哪**(取决于指派) |
| 机械臂数量与所在节点 | **每次搬运的端到端耗时**(取决于选路与让行) |
| AGV 数量 | 每次搬运由哪辆车执行、何时出发 |
| 路网拓扑与各走廊通行时间 | 各车的空驶路线与停靠位置 |

这也是本问题与车辆路径问题(VRP)一类问题的分界:**VRP 的任务清单是事先给定的,本问题的任务清单本身就是决策的产物。**

---

## 六、一个具体的算例:把场景讲活

抽象描述容易空泛,下面用一个 3 工件 / 3 台机械臂 / 2 辆 AGV 的小算例完整走一遍。

### 6.1 算例设定

**路网**:即第 2.2 节那张图。

**加工时间矩阵**(单位:分钟;本例取"每道工序三台机械臂都能做"的简化情形,以便集中展示效率异构):

| 工序 | RA1 | RA2 | RA3 |
| --- | --- | --- | --- |
| 工件 J1 第 1 道 | **4** | 6 | 5 |
| 工件 J1 第 2 道 | 7 | **5** | 8 |
| 工件 J1 第 3 道 | 6 | 9 | **5** |
| 工件 J2 第 1 道 | 5 | **4** | 7 |
| 工件 J2 第 2 道 | 8 | **6** | **6** |
| 工件 J3 第 1 道 | **3** | 5 | 4 |
| 工件 J3 第 2 道 | 6 | **4** | 7 |

(加粗为该行最快的机械臂。可以看到,行内最快与最慢相差 20% 到 80% 不等——这就是效率异构。)

工件 J1 有 3 道工序,J2、J3 各 2 道,共 7 道工序。2 辆 AGV 与全部毛坯在时刻 0 都位于装卸站 v0。

**这个算例的两个特征值得注意**:平均加工时间约 5.7 分钟,而取放点之间的平均最短路时间约 5.3 分钟——**运输与加工处于同一量级**,这正是集成调度价值最大的区间。

### 6.2 一个可行方案

下面给出一个**已经消解了全部路网冲突**的可行方案(它未必最优,只用于说明机制):

**指派**:J1 三道工序依次给 RA1、RA2、RA3;J2 两道给 RA2、RA3;J3 两道给 RA1、RA2。

**执行时刻表**:

| 资源 | 时间线 |
| --- | --- |
| RA1 | 4–8 加工 J1 第 1 道;12–15 加工 J3 第 1 道 |
| RA2 | 9–13 加工 J2 第 1 道;18–23 加工 J1 第 2 道;30–34 加工 J3 第 2 道 |
| RA3 | 17–23 加工 J2 第 2 道;27–32 加工 J1 第 3 道 |
| AGV1 | 0–4 载 J1(v0→c1→r1);4–8 空载返回 v0;8–12 载 J3(v0→c1→r1);12–18 载 J1(r1→c2→r2);18–24 空载(r2→c2→r1);24–30 载 J3(r1→c2→r2);30–34 停靠 r2 等 J3 完工;34–41 回运 J3 成品(r2→c2→c1→v0);之后停靠 v0 |
| AGV2 | 0–2 在 v0 让行等待;2–9 载 J2(v0→c1→c2→r2);9–13 停靠 r2;13–17 载 J2(r2→c2→r3);17–19 在 r3 择时待发;19–23 空载(r3→c2→r2);23–27 载 J1(r2→c2→r3);27–34 回运 J2 成品(r3→c2→c1→v0);34–44 空载赴 r3(其中 36–39 在 c1 停靠让行);44–51 回运 J1 成品(r3→c2→c1→v0);之后停靠 v0 |

**结果**:三个工件的加工分别在 23、32、34 完成;三件成品返抵装卸站的时刻分别是 34、41、51。

**makespan = 51 分钟。**

### 6.3 三个关键时刻

下面挑三个时刻,分别说明一件事。

---

**时刻 t = 1:冲突是在计划阶段消解的,不是执行时的意外**

```text
                    [r1]                     RA1 空闲
                   /     \
                  2       4
                 /         \
[v0]===2===[c1]-----3-----[c2]---2---[r2]    RA2 空闲
 ▲                          |
                            2
                            |
                          [r3]               RA3 空闲

  ===   AGV1(载 J1)正在 v0–c1 走廊上行驶,占用时段 [0, 2)
  ▲     AGV2(已装载 J2)在 v0 的停靠位上等待
```

两辆车都要从 v0 出发,但 v0–c1 这条走廊同一时刻只容一辆。于是 AGV2 只能在停靠位上等到 t=2 才出发。

**要点**:这 2 分钟的等待是**计划的一部分**,在排程时就已经算进去了,不是执行过程中临时刹车避让。这正是"无冲突路径规划"这个词的含义——冲突在纸面上就已经解决了。

---

**时刻 t = 19:"何时出发"本身就是一个决策变量**

```text
                    [r1]
                   /     \
                  2       4
                 /         \
[v0]---2---[c1]-----3-----[c2]===2===[r2]    RA2 正在加工 J1 第 2 道
                            |                (18–23)
                            2
                            |
                          [r3]  ▲            RA3 正在加工 J2 第 2 道
                                              (17–23)

  ===   AGV1 空载从 r2 返回 r1,正占用 c2–r2 走廊,占用时段 [18, 20)
  ▲     AGV2 停靠在 r3,17 时刻就已卸完货,却直到 19 时刻才出发
```

AGV2 的下一个任务是去 r2 接 J1(J1 将在 23 时刻完工)。**它为什么不 17 点就走?**

因为如果 17 点出发,它会在 19–21 这段时间沿 c2→r2 行驶,而 AGV1 正在 18–20 反方向占用同一条走廊——**迎头相撞**。

路由层的处理是让它**推迟到 19 时刻出发**:19–21 走 r3→c2,21–23 走 c2→r2,**23 时刻整点到达 r2,恰好赶上 J1 完工**,实现零等待交接。

**要点**:在运输时间为常数的传统模型里,"在原地等一会儿再走"和"先开过去等"对结果完全等价,所以文献根本不把出发时刻当作变量。但在有路网冲突的场景中,两种做法**占用路网的时段不同**,会影响其他车辆,于是出发时刻成为一个真正的决策变量。**这是"必须集成建模才能捕捉、两阶段方法捕捉不到"的直接证据。**

---

**时刻 t = 51:回运段同样受冲突约束,makespan 被拖长**

```text
                    [r1]
                   /     \
                  2       4
                 /         \
[v0]---2---[c1]-----3-----[c2]---2---[r2]    三台机械臂自 34 时刻起
 ▲▲                         |                 全部空闲
                            2
                            |
                          [r3]

  ▲▲   AGV1 与 AGV2 均已停靠 v0,三件成品全部返抵装卸站
```

值得注意的是最后这段时间发生了什么:

- 全部加工在 **34 时刻**就已结束,三台机械臂此后一直空闲;
- 但 J1 的成品自 32 时刻起就滞留在 r3 的输出缓冲区——**两辆车都在忙**,没人来接;
- AGV2 从 v0 空车去接它时,又在 c1–c2 走廊上与 AGV1 的回运任务迎头相遇,**被迫在 c1 停靠让行 3 分钟**(36–39);
- 最终 AGV2 在 44 时刻才取到 J1 成品,51 时刻运回装卸站。

**要点**:加工在 34 结束,makespan 却是 51——**多出来的 17 分钟完全由运输侧造成**。这说明把成品回运计入目标之后,运输侧的瓶颈会进一步显性化;也说明如果只优化加工段,会严重低估真实的完工时间。

### 6.4 全程甘特图

(每个字符代表 1 分钟;A/B/C 分别代表工件 J1/J2/J3 的加工或载运,e = 空车行驶,w = 等待或择时待发,· = 空闲或停靠)

```text
 时刻      0         10        20        30        40        50
           012345678901234567890123456789012345678901234567890
 RA1       ····AAAA····CCC····································
 RA2       ·········BBBB·····AAAAA·······CCCC·················
 RA3       ·················BBBBBB····AAAAA···················
 AGV1      AAAAeeeeCCCCAAAAAAeeeeeeCCCCCC····CCCCCCC··········
 AGV2      wwBBBBBBB····BBBBwweeeeAAAABBBBBBBeewwweeeeeAAAAAAA
```

这张图一眼可以看出三件事:

1. **两辆 AGV 近乎满载**,而三台机械臂大量时间空闲——瓶颈明确在运输侧;
2. **34 分钟之后三台机械臂全部空闲**,但 AGV 仍在工作,makespan 被这条"回运尾巴"拖到 51;
3. AGV2 行中出现了三处 `w`(等待):开头的让行、中段的择时待发、后段的让行——**等待在这个问题里是常态,而且是有意安排的**。

### 6.5 从这个算例能读出的三个结论

1. **同一份指派方案,换一种路由安排,makespan 就不一样。** 因此不能先固定排产再去规划路径。
2. **反过来,路网的拥堵状况应当回头影响指派决策。** 比如 J1 的第 2 道工序,派给 RA2 加工要 5 分钟、派给 RA1 要 7 分钟;但如果通往 RA2 的路段正在拥堵,而 J1 此刻就在 RA1 上,那么"就地留在 RA1"省下的整整一趟搬运,很可能超过 2 分钟的加工损失。**这正是本课题要捕捉的机会。**
3. **加工、运输、路网冲突三者的耦合是双向的**,任何单向的两阶段处理都会漏掉一部分优化空间。

---

## 七、模型边界:做了哪些假设、不考虑什么

### 7.1 三条关键假设及其作用

**假设一:各机械臂的输入/输出缓冲区容量充足**

每台机械臂旁都有足够大的暂存区:待加工的工件可以在旁边排队,加工完的工件可以先移到输出缓冲区等 AGV 来取,不必占着机械臂。

这条假设看似平常,实际上起三个作用:

- **让两个时间量可以分开计算**。工序的开始时间等于"工件到达时刻"和"机械臂空闲时刻"两者中的较晚者;有了缓冲区,前者只取决于车、后者只取决于机器,互不牵扯,整张时刻表才能按时间顺序一路推算出来;
- **排除死锁**。设想 AGV 载着工件到达,而目标机械臂上一件成品还占着机位没人取走——车上的工件放不下,车又走不了,就会形成"车等机器、机器等车"的循环等待。有缓冲区就不会发生;
- **保证任何方案都算得出结果**。这是后续算法能高频评价候选方案的前提。

> **如果去掉这条假设会怎样?** 问题会变成"阻塞型作业车间 + AGV"一类问题,性质发生实质变化:工件完工后仍占着机械臂直到被取走,于是机械臂能不能开下一件,取决于 AGV 什么时候来——机器排序与车辆调度之间从"影响目标值"升级为"影响可行性",还可能死锁。这是一个独立的研究方向,不在本课题第一阶段范围内。

**假设二:AGV 在机械臂处的装卸时间忽略不计,且不占用机械臂**

工件进出缓冲区不需要机械臂停下来配合。这条在本课题中必须明确声明,因为加工资源本身就是机械臂,容易让人误以为要由它来完成上下料。

**假设三:静态问题,信息完备**

第一阶段研究静态问题:全部工件在计划开始时就已知、都在装卸站待运;不设投放时间与交货期;加工数据、车间布局、车辆状态在做决策时全部已知。工业互联网提供的实时感知能力,在这一阶段体现为"状态可知"。

### 7.2 明确不考虑的内容(留作后续扩展)

| 不考虑的内容 | 理由 |
| --- | --- |
| AGV 电量与充电行为 | 属次要因素,可由车队规模的冗余吸收;若建模会引入充电节点与续航约束,冲淡主线 |
| 机械臂与 AGV 的故障 | 属动态扰动,归入第二阶段的滚动重调度 |
| 有限缓冲/阻塞 | 见 7.1 的辨析,是另一类问题 |
| 工件动态到达、紧急插单 | 第二阶段动态扩展的内容 |
| 装夹换型时间 | 视为已并入加工时间;若需要,可按"同机同工件连续加工时为 0"的规则单独建模 |

这些内容都不会改变前面描述的基本模型,是在其之上的叠加。

---

## 八、创新点在哪里

### 8.1 四个创新点

**创新点一:运输时间是决策的函数,而不是常数矩阵**

车间布局显式建模为图,端到端搬运耗时由"选路 + 让行等待"共同决定。这与标准做法中"位置间行驶时间为已知常数矩阵"的设定有本质区别。

**创新点二:机械臂的时间异构,使"路由感知的改派"真正有意义**

这是本课题**最不可替代的资产**。当通往某台机械臂的路段拥堵时,把工序改派到路网更通畅、加工稍慢的另一台机械臂,整体可能更优。**这个权衡只有在"效率异构"和"显式路网"同时存在时才会出现**——只要缺一个,它就不存在。后面会看到,最接近的同类工作恰恰就缺了前者。

**创新点三:冲突凭证制导的闭环反馈**

这是求解框架层面的创新。上下两层之间传递的不是一个笼统的"实际耗时",而是**结构化的冲突凭证**——某次让行发生在哪条走廊、哪个时段、被谁挡住的。有了这个信息,上层才能做出**有针对性**的调整:

- 知道是哪条走廊堵了,才能判断"改派到哪台机械臂"能绕开它(**换地方**);
- 知道是哪个时段与谁冲突,才能判断"把哪道工序前移或后移"能错开峰值(**换时间**)。

如果只反馈一个总时间,上层就只能盲目地随机扰动。

**创新点四:集成收益的量化实验**

以"路网拥堵程度 × 机械臂异构度"作为双因子,量化闭环集成方法相对传统两阶段方法的改进幅度,并以柔性度(平均每道工序有几台机械臂可选)作为收益的使能因子。**预期结论**:拥堵越重、异构度越高,集成的收益越大;而当每道工序只有一台机器可选时,改派机制自动失效,收益随之消失。

这一组实验正面回答"集成建模增加的复杂度是否值得"这个必然会被问到的问题。

### 8.2 与最接近的两篇同类工作的对比

**对比对象一(最接近的竞品)**

> Ruiqi Li, Jianlin Mao, Xing Wu, Wenna Zhou, Chengze Qian, Haoshuang Du.
> **"A New Framework for Job Shop Integrated Scheduling and Vehicle Path Planning Problem."**
> *Sensors*, 2026, 26(2): 543. DOI: 10.3390/s26020543.
> (昆明理工大学;2025 年 11 月投稿,2026 年 1 月 13 日上线)

该文提出三层框架:调度层用改进的深度 Q 网络(DQN)生成加工方案 → 任务分配层把搬运任务分给机器人 → 路径执行层用基于优先级的搜索(PBS)求解无冲突路径。测试基准为 FT、LA、SWV、YN 等经典作业车间算例。

**四条实质差异:**

| # | 维度 | 该文 | 本课题 |
| --- | --- | --- | --- |
| 1 | 问题类别 | 作业车间 + 运输。原文明确"每道工序有固定的加工时间",所用基准全部是**每道工序只有一台机器**的经典作业车间算例——**没有机器指派决策,也没有加工时间异构** | 柔性作业车间。有可选机器集合 + 效率异构。**创新点二的改派机制在该文的问题设定下根本不可能存在** |
| 2 | **耦合方式(最关键)** | 原文在局限性一节中**自述**:该框架"主要是一种**两阶段顺序优化策略,而非真正的动态实时闭环系统**",其机制"依赖于先生成一个高质量的初始排程,然后通过**一次性的反馈调整**加以修正",因此"最终解的全局最优性部分依赖于初始排程的质量" | 两条反馈通路:一是每个候选方案的评价都基于**路由之后**的真实完工时间;二是路由层输出的冲突凭证**驱动上层修改指派与顺序**。反馈**每一代、每个候选方案都发生** |
| 3 | 反馈的内容 | 到达时刻,用于修正时间轴 | 结构化的冲突凭证,用于**修正决策本身** |
| 4 | 上层方法 | 深度强化学习 | 遗传算法 / 模因算法。**此条只是路线不同,不构成优势** |

第 2 行那段原文自述,是本课题"闭环"主张最好的引用锚点——**竞品自己承认了它不是闭环**。

**对比对象二(次接近)**

> **"An Approach to Integrated Scheduling of Flexible Job-Shop Considering Conflict-Free Routing Problems."**
> *Sensors*, 2023, 23(9): 4526. DOI: 10.3390/s23094526.

该文确实是柔性作业车间(有指派决策),也确实考虑了无冲突路由,但采用严格的**两阶段**做法:第一阶段用混合遗传算法排产,第二阶段在排产结果既定的前提下,用基于 AGV 编码的遗传算法加时间窗 Dijkstra 消解冲突。**路由结果不回头影响排产**。

这正是本课题实验中要对标的那一类方法。

### 8.3 需要避免的表述

有几点虽然本课题也做了,但**不宜当作创新点**,因为已有明确出处:

- "AGV 送完就走、不同区段可由不同车接力"——这是该领域自 1995 年以来的标准设定,只能作为假设写出;
- "用时间窗 Dijkstra / 优先级搜索做无冲突路由"——已被上述竞品占据;
- "考虑了拥堵感知的派车规则"——已有文献做过,本课题的增量必须精确表述为"基于真实预约表的试探,且用于驱动**机器改派**而非仅仅派车"。

---

## 九、打算怎么解,以及目前的进展

### 9.1 求解框架:调度层—路由层闭环

```text
┌────────────── 上层:调度层(遗传算法 / 模因算法) ──────────────┐
│                                                                │
│   候选方案种群(每个方案 = 一份指派 + 一份工序顺序)            │
│              │                                                 │
│              ▼  逐个事件推演,每遇到一次搬运就调用下层          │
│   解码为完整时刻表                                             │
│              │                                                 │
│              ▼                                                 │
│   方案好坏 = 路由之后的真实完工时间      ← 反馈通路①(评价级)  │
│              │                                                 │
│              ▼                                                 │
│   针对拥堵的定向改进:                     ← 反馈通路②(决策级)  │
│     ① 找出决定完工时间的关键链,判定瓶颈成因                    │
│     ② 若因走廊拥堵 → 改派到更通畅的机械臂(换地方)             │
│                     或 把冲突双方错开时段(换时间)             │
└────────────────────────────────────────────────────────────────┘
        ▲                                        │
        │ ① 各次搬运的真实起止时间                │ 搬运任务
        │ ② 冲突凭证:哪条走廊、哪个时段、被谁挡   │ (起讫点、最早出发时刻)
        │                                        ▼
┌────────────── 下层:无冲突路由层(时间窗预约) ──────────────┐
│   为每条走廊维护一张占用时段表;新路径只能使用空闲时段,        │
│   从机制上排除冲突与死锁,而不是"先走再避"                     │
└────────────────────────────────────────────────────────────────┘
```

与传统两阶段方法的区别就在左侧那两条**向上**的箭头:传统方法只有向下的一条。

### 9.2 目前进展

- **算法代码已完成并跑通**,包含完整的独立校验器(不复用求解逻辑,单独检查时刻表是否真的满足全部约束),示例算例的全部测试用例通过;
- **公开基准数据集已收集**(含 20 个中小规模、31 个大规模的国际通用算例),格式转换工作在进行中;
- **初步实验结果需要谨慎解读**:在本文这个 6 条走廊、2 辆车的小算例上,闭环相对两阶段的收益为 0——原因是冲突太稀疏,两阶段的结果几乎不需要修复。**这恰恰说明主实验必须在拥堵程度可控的扩展算例上做**。在初步生成的拥堵算例上,闭环相对两阶段有约 3.6% 的改进,方向支持预期,但目前只跑了 2 个随机种子,**样本量偏少**;
- **下一步**:按"同等算力预算、不少于 10 个随机种子并报告离散度"的协议重跑完整对比实验。

---

## 附录:其它问题和回答

**问:缓冲区无限这个假设是不是太理想化了?**
答:见 7.1 的辨析。去掉它问题会变成阻塞型作业车间,机器排序与车辆调度之间从"影响目标值"升级为"影响可行性",还需要专门的死锁检测机制,现有基准算例与解码规则都不再适用。这是一个独立的研究方向,业界也确实有相应的文献分支。本课题第一阶段先不涉及。而且，现实应用中，离散制造行业的特征之一就是工站之间有缓冲区。

**问:多辆车怎么能同时停在装卸站?这不违反"一条走廊一辆车"吗?**
答:模型中区分了"通行占用"和"停靠占用"。走廊和正在穿越的节点是独占的,但每个节点旁另设停靠位,容量充足。停靠不占用通行资源,所以多车同站停靠是合法的。

**问:为什么要把成品回运也算进去?文献里很多都不算。**
答:因为回运段同样受路网冲突约束,不算进去会低估运输侧的瓶颈——本文的示例算例里,加工在 34 分钟就结束了,但 makespan 是 51,多出的 17 分钟全部来自回运段。模型代码中设了一个开关,可以切换到"不计回运"的口径,以便与不计回运的文献基准做数值对比。

**问:AGV 之间为什么假设完全相同?现实中车队也可能异构。**
答:是为了让"异构"这个变量只出现在加工资源一侧,使实验结论能干净地归因。车辆异构可以作为扩展,但它带来的是同一类型的权衡,不会改变问题结构。

**问:静态问题的实际意义有多大?真实车间总是在变的。**
答:静态是第一阶段。动态场景下的做法是周期性或事件触发地重新求解,静态求解器是其内核。而且本课题的核心机制(拥堵反馈改派)在动态场景中价值更大,因为路网状态变化更频繁。

**问:这个问题的规模能做到多大?**
答:该问题是 NP 难的,精确方法只能处理很小的规模。计划用精确方法在小规模算例上求最优解作为参照下界,中大规模用元启发式方法,并与既有的两阶段方法对比。已收集的公开基准中,最大的算例有 240 道工序（现实应用中极少在一个车间里有这么多道工序）。
