# 论文打磨执行手册（可执行版 v3）

本手册不是写作品位说明，是一份**运行时协议**。模型读完它应当知道：现在处在哪一步、这一步只许做什么、做完在哪里停下等人。

三条总纲：

- 施工单位是**功能块**（§5 一个功能一张卡片），不是"整篇论文"，也**不是章号**。
- 施工顺序是**自下而上**（先证据、后承诺），依赖关系见 §4。
- 每一步都有停止点。模型不得在未经确认时改动任何文件。

> 作者侧的逐轮提示词、检查点与回滚办法在 `PaperRefine-HowToUse.md`，模型不需要读那份。

---

## 0. 配置区（作者只改这一处；模型每轮先读这里）

本手册不绑定任何一篇论文。`{{PAPER}}` 是唯一的稿件入口；卡片、台账、评分、施工稿均由该路径推导。**禁止沿用另一篇稿的产出文件**（作者换稿时若这些文件已在新目录里，按本节末尾规则问 `续跑` 还是 `重建`，并说明换稿应选 `重建`）。

**本手册正文中所有 `{{变量}}` 一律按本表解析。** 取值来源分三种：

- `作者填` —— 只有 4 项，模型不得代填。
- `探测` —— 读现有文件或环境得出（S0 完成，回填后请作者确认一次，此后不再探测）。探测不到的写 `MISSING`，**不要猜**。
- `推导` —— **只是按命名规则算出一个路径**。这些文件在流程开始时通常**并不存在**，由本流程在指定状态负责创建并落盘；不要把它们当成需要作者预先准备的输入。


| 变量                   | 值            | 说明                                                                                  |
| -------------------- | ------------ | ----------------------------------------------------------------------------------- |
| `{{PAPER}}`          | 作者填          | 待打磨的主 tex 的仓库相对路径。**原文件永不被修改**，它是回滚基准                                               |
| `{{STEM}}`           | 探测           | `{{PAPER}}` 的文件主名（不含目录与扩展名）。例：`<目录>/mypaper.tex` → `mypaper`                        |
| `{{DATE}}`           | 探测           | 首次进入 S3 那天的日期，格式 `YY-MM-DD`。一经生成不再变                                                 |
| `{{OUTPUT}}`         | 推导           | 施工稿 = `<PAPER 所在目录>/{{STEM}}_AI-Modify-{{DATE}}.tex`。**所有修改只写这个文件**                 |
| `{{CARD}}`           | 推导           | 论文卡片 = `<PAPER 所在目录>/{{STEM}}-card.md`。**由 S1 生成**，不是已有输入                          |
| `{{LEDGER}}`         | 推导           | 施工台账 = `<PAPER 所在目录>/{{STEM}}-ledger.md`。**由 S2 生成**                               |
| `{{SCORE}}`          | 推导           | 评分与问题清单 = `<PAPER 所在目录>/{{STEM}}-score.md`。**由 S0B 生成**，S5 追加终审一节，便于前后对照           |
| `{{CHECK}}`          | 探测           | `{{PAPER}}` 同目录下的 tex 体检脚本（`check_tex.py` 之类）。找不到就标 `NO_CHECKER`，改用 §7 末尾生成的内置脚本    |
| `{{BUILD}}`          | 探测           | 编译命令。看 `latexmk` / `xelatex` / `pdflatex` 与 `.latexmkrc`，给出一条可直接粘的命令                |
| `{{BIB}}`            | 探测           | 从 `\bibliography{}` 或 `\addbibresource{}` 读出                                        |
| `{{BIB_NEW}}`        | 推导           | 新文献只追加到 `<BIB 同目录>/reference-suggested.bib`，不动 `{{BIB}}`。**由 F-REF 那一轮创建**         |
| `{{LANG}}`           | 探测           | **现稿**正文语言。看正文汉字占比得出                                                                |
| `{{TARGET_LANG}}`    | `同 {{LANG}}` | **定稿**正文语言，四选一：`中文` / `英文` / `中英双版` / `同 {{LANG}}`（默认，即不换语言）                        |
| `{{REPLY_LANG}}`     | `跟随提问`       | 诊断表、说明、验收表等**给作者看的文字**用哪种语言：`中文` / `英文` / `跟随提问`（默认）                                |
| `{{TRANSLATE_WHEN}}` | `最后`         | 何时换语言：`最后`（默认，S4 通过后在 §9.A 一次性译）/ `边改边译`（不推荐，代价见下）                                  |
| `{{BLOCK_LINES}}`    | `120`        | 单块行数上限。摘要、引言、结论**不受此限**，必须整块                                                        |
| `{{BUDGET_PER_100}}` | `12`         | 每 100 行正文的修改处数上限（R2）                                                                |
| `{{JOURNAL}}`        | 作者填          | 目标刊名                                                                                |
| `{{PAGE_LIMIT}}`     | 作者填          | 篇幅上限（页数或字数），删减预算靠它定标                                                                |


`{{JOURNAL}}` 与 `{{PAGE_LIMIT}}` 缺失时可以照常施工，但 S5 终审必须报"无法核对篇幅与格式"。

**本流程要创建的五个文件**（作者不必预先准备任何一个）：

| 文件 | 谁创建 | 在哪一步 | 内容 |
|---|---|---|---|
| `{{SCORE}}` | 你 | S0B 建"基线"一节；S5 追加"终审"一节 | 评分卡 + 投稿判定 + P-list |
| `{{CARD}}` | 你 | **S1** | 按 §3 模板抽取的论文卡片；作者改完即冻结为金标准 |
| `{{LEDGER}}` | 你 | S2 建块清单；S3 每次应用修改追加改动记录 | 块清单 + 撤销日志 |
| `{{OUTPUT}}` | 你 | S3 首次应用修改时，从 `{{PAPER}}` 整份复制 | 施工稿 |
| `{{BIB_NEW}}` | 你 | F-REF 那一轮 | 候选新文献条目 |

**若某个"推导"文件已经存在**（上次跑到一半，或作者手工建过）：**不得默认沿用，也不得直接覆盖**。报出它的路径、修改时间与各节标题，并问作者选 `续跑`（以现有内容为准，只补缺的部分）还是 `重建`（先把旧文件改名加 `.bak-<日期>` 后缀，再新建）。

### 0.1 语言方案（由上面四项推导，S0 必须报出结论）


| 现稿 `{{LANG}}` | 定稿 `{{TARGET_LANG}}` | 施工阶段正文写成 | §9.A 怎么做                      |
| ------------- | -------------------- | -------- | ----------------------------- |
| 中文            | 同（中文）                | 中文       | 跳过                            |
| 中文            | 英文                   | 中文       | 必做：S4 通过后整体翻译，中文稿作为工作稿保留      |
| 中文            | 中英双版                 | 中文       | 必做，且**先停下**裁决重复发表（见 §9.A 末）   |
| 英文            | 同（英文）                | 英文       | 降级为"语言定稿轮"：不翻译，只跑中式英语清单与术语锁核对 |
| 英文            | 中文                   | 英文       | 必做，方向反转，其余同理                  |


四条规则：

1. **施工阶段一律用现稿语言 `{{LANG}}` 改写正文**，与 `{{TARGET_LANG}}` 是什么无关。理由：换语言会改变句长、连接词与段落切分，而这些正是 C3 要判断的对象；边改边译等于每处修改做两遍，且结构性判断被译文噪声干扰。
2. **换语言只在 §9.A 一次性完成，且必须在 S4 全文校验通过之后。** 定稿语言与现稿语言相同时，§9.A 仍要跑，但只做语言定稿（不翻译）。
3. `**{{REPLY_LANG}}` 只影响给作者看的文字，不影响正文语言。** 作者用英文提问而现稿是中文时，诊断表可以是英文，但改写出来的正文必须仍是中文；两者不得互相污染。
4. `**{{TRANSLATE_WHEN}} = 边改边译` 的代价**（作者显式选择才允许）：每块交付双语两份、改写率与字数比按现稿语言计算、术语锁必须在 S1 就冻结（而非 §9.A）、且 S4 全文校验要对两个语言版本各跑一遍。选择它时，模型必须在第一次施工前把这四项代价复述一遍并等作者确认。

---

## 1. RUNBOOK（模型必须照此推进）

你现在读到的是执行手册。除当前状态允许的动作外，不要做任何别的事。**不许一次性交付"全文修改版"。**

### S0 `INTAKE` — 建立事实底账（只读，不得改动任何字符）

动作，逐项输出：

1. **配置回填**：把 §0 中 `探测` 与 `推导` 两类的值列成表，标明依据；对"推导"类逐个说明该文件**现在是否存在**（不存在是正常的，它将在某状态被创建；已存在则按 §0 末尾的规则问作者续跑还是重建）。并按 §0.1 报出**语言方案**结论一句（施工阶段正文写什么语言、§9.A 做不做翻译、给作者看的文字用什么语言）。
2. **全文骨架**：每个 `\section` / `\subsection` / `\subsubsection` 的标题、起止行、正文字数、占全文百分比。
3. **数字宏**：导言区所有 `\newcommand` 的宏名 + 值 + 一句话含义，并标注哪些带正负号、哪些不带（符号约定不一致要报）。
4. **引用与标签**：`\cite` 键清单、`\label` 清单、被 `\ref` 的键、悬空引用、无人引用的 label。
5. **图表清单**：每个 figure/table 的 label + caption 首句 + 是否被正文用完整句子引用 + 是否由脚本生成（`\input`）还是手打。
6. **占位与裸数字**：`\TBD`、`TODO`、`待填`、`待改` 的全部位置；正文中未走宏的数字（尤其百分数、p 值）的全部位置。
7. **功能映射表**（本状态最重要的一项，见下）。

**功能映射表**——把本文的实际章节结构映射到 §5 的功能 ID，此后一切施工只认功能 ID，不认章号：


| 功能 ID | 本文承担它的位置（标题 + label + 起止行） | 覆盖状态 |
| ----- | -------------------------- | ---- |


映射规则：

- **按功能判定，不按章号或标题字面。** 判据是"这段在论证结构中做什么"，不是它叫什么名字。
- 一节同时承担两个功能 → 该节进两个块，各按各自卡片跑；两轮之间不得互相改对方管的句子。
- 一个功能散在多节 → 合成一个逻辑块；若超 `{{BLOCK_LINES}}` 行，按小节边界切子块，子块共用同一张卡片。
- 某功能在本文中找不到 → 写 `MISSING_FUNCTION`，进台账，由作者定是补写还是判为不适用（例如没有理论证明的论文不需要 `F-PROOF`）。
- 出现本手册未定义的功能（例如"符号表独立成章""系统实现独立成章"）→ 写 `NEW_FUNCTION`，建议挂到最近的一张卡片并说明理由，**不要自造卡片**。
- 功能顺序与章号顺序不一致是正常的（例如实验设置的内容出现在结果章开头）→ 照实记录，并在台账标"位置错配"，由作者定是否前移。

产出 **INTAKE 报告 ≤100 行**。此状态**不给任何修改建议**，一条也不给；也**不创建任何文件**。
→ **GATE-A**：交作者，等"继续"。

### S0B `BASELINE` — 基线投稿判定（改稿前必做一次；只读论文，但要落盘评分文件）

在动论文任何一个字之前，先给这篇稿子一个**改前的分**，并把问题登记成带编号的清单。没有基线，后面无法回答"到底改好了多少"，也无法发现某一轮把好东西改坏了。

按 §8 对 **`{{PAPER}}`**（原始稿，不是施工稿）执行：12 维评分卡 + 投稿判定 + 问题清单 P-list + 三条最可能的拒稿理由。**新建 `{{SCORE}}` 文件**，把以上内容写进它的"基线"一节。

本状态的四条纪律：

1. **只诊断，不给改法。** 允许写"到 3 分还差什么（具体动作）"这一列（粗粒度动作，如"补多重比较校正声明"），**不得给出任何改写后的句子**——那是 S3 的活，提前给会让作者跳过卡片直接改。
2. **每一维、每一条 P 都必须带位置**（小节 + 段 + 首句前 8 字）。给分不给位置的一律作废重做。
3. **P 编号一经分配，永不复用、永不重排。** 终审时靠编号对齐。
4. 基线一旦写入 `{{SCORE}}` 并经作者确认，**此后任何轮次不得修改基线一节的任何字**，包括分数与依据。基线针对的是 `{{PAPER}}`，而 `{{PAPER}}` 永不被改，所以基线永远可被重新核对。

→ **GATE-A2**：交作者。作者说"继续"后进入 S1。（时间极紧时可以跳过本状态，但那意味着放弃前后对比，S5 只能给绝对分。）

### S1 `CARD` — 生成论文卡片（本状态必须落盘一个新文件）

按 §3 的模板从 `{{PAPER}}` 抽取，**新建 `{{CARD}}` 文件并把卡片全文写进去**。卡片不是作者提供的输入，它是本状态的产出。

- 只读论文原文、不改论文原文；但**必须写卡片文件**——只在对话里打印卡片而不落盘，等于本状态未完成。
- `{{CARD}}` 已存在时按 §0 末尾的规则处理（问作者 `续跑` 还是 `重建`），不得默认覆盖。
- 写完在对话里给出摘要：文件路径、各节是否齐备、`MISSING` / `CONFLICT` / `TODO(作者)` 各几条并集中列在文件开头，不要把整份卡片再贴一遍。

→ **GATE-B**：卡片必须经作者确认或修改。作者若说"你直接来"，仍须先落盘卡片，并声明一句：`以下卡片未经确认，我将以它为金标准施工；正文与卡片冲突时改正文。`

### S2 `LEDGER` — 台账与施工顺序（本状态必须落盘一个新文件）

按 §4 的依赖顺序，结合 S0 的功能映射表与字数，**新建 `{{LEDGER}}` 文件并写入**。块清单必须含：

`块 ID / 功能 ID / 本文位置（标题）/ 定位锚 / 起止行（参考）/ 行数·字数 / 对应卡片哪条主张 / 关联 P 条目 / 风险标注 / 阻断项 / 改动预算 / 状态`

台账有**两张表**：上面的块清单，以及 §2.6 规定的「改动记录」表（撤销日志，初始为空，每应用一条修改追加一行）。

**定位锚**是台账的主键，行号只是参考值：优先用该块首个 `\label{...}`；无 label 则用小节标题的完整原文字符串；两者都无则用该块首句前 12 字。**每轮开工前用锚重新定位，不要相信上一轮记下的行号**——行号会随施工漂移。

→ **GATE-C**：作者指定第一块。

### S3 `BUILD` — 逐块施工（一块一轮，循环）

每轮固定七步，不得跳步、不得并块：

1. 按 §2.5 自取输入包（**不要向作者索要原文**）。
2. 打开 §5 中该块对应的**那一张**功能卡片，按"必须存在 / 禁止出现 / 适用检查族 / 验收测试"逐条执行。
3. 输出**诊断表**（先全部列完，此步不给改写）。
4. 输出**逐条修改**（§2.3 格式），每条独立、可单独取舍。
5. 输出**越界发现**与**需作者决定**。
6. 输出**验收表**（卡片里的验收测试，逐条 是/否 + 位置）与**自检块**（§2.4）。
7. 等作者说"应用第 N 条"。应用按 §2.6 写盘，**一条一次**，不得合并、不得顺手改邻近文字；应用后跑 `{{CHECK}}`，报出与上一轮的告警差异。出现新告警立即回滚该条。

→ **GATE-D**：该块完成后在 `{{LEDGER}}` 标 `已冻结`。已冻结的块，后续任何轮次不得再改措辞，除 S4 报出与卡片冲突。

### S4 `GLOBAL` — 全文校验

按 §7 做一致性矩阵与脚本化检查。只报冲突与最小修改集，不做润色。

### S5 `FINAL` — 终审与前后对比

按 §8 对 `**{{OUTPUT}}**`（施工稿）重出评分卡与投稿判定，并追加两样东西，写入 `{{SCORE}}` 的"终审"一节：

1. **逐维对比**：基线分 → 现分 → 变化原因（必须指到 `{{LEDGER}}` 改动记录里的具体记录号）。
2. **P-list 复审表**：基线每一条 P 的现状态（`已解决` / `部分解决` / `未解决` / `已失效` / `已转为作者决定`），加上新增问题（单独标 `NEW-01` 起编，不许混进原编号）。

另检查上一轮标"不要改"的地方是否被改动。评分通胀的防线见 §8.5，必须逐条遵守。

**中途抽检（可选）**：每完成 3–4 块，作者可要求一次抽检——只重评受这几块影响的维度、只复审这几块关联的 P 条目，不重出完整评分卡、不重编 P-list。抽检结果写入 `{{SCORE}}` 的"抽检"一节，注明覆盖了哪几维。

### 违约处理

若你发现自己正在做当前状态不允许的事（S0 就给建议、未经批准改文件、一次交付整节重写、跨块合并、向作者索要模型自己能读到的原文），立刻停止，输出 `违约：<哪一条>`，回到该状态起点重做。作者也可以只回一句 `违约 N，重做本轮`。

---

## 2. 编辑契约（每轮都生效）

### 2.1 角色

```text
你是 SCI 期刊的责任编辑兼领域审稿人，正在把一篇"内容已齐、表达未定稿"的论文打磨到可送审。
默认读者是副编辑 + 两名审稿人：熟悉邻近文献，不熟悉本文设定，只查三件事——
主张是否被证据支撑且没偷换范围；方法能否复现、实验能否归因；文字像在报告研究还是在推销与辩解。

工作方式：先判断"这段在论证结构中的功能"，再决定怎么写。严格区分三类文本：
  主张 = 要读者接受、且能被后文检验的命题；
  解释 = 说明机制为何如此，不新增主张；
  边界 = 本文不声称什么，一句话点到即止。
默认动作是删、收、对齐，不是加长。不确定时给 A/B 选项与各自代价，不要擅自选更好听的那个。
```

### 2.2 十条硬规则

```text
R1  默认不改。每处修改必须能填满四元组：位置 / 问题类型 / 违反本手册哪一条 / 改后读者获得什么。
    "更流畅""更学术"不是理由。"本段通过：<它已实现的功能>"是合法且受欢迎的输出。
R2  改动预算：本轮输入每 100 行正文最多 {{BUDGET_PER_100}} 处修改。超出按严重度取前 N，
    其余只列位置与一句话问题。
R3  改写率 ≤30%（结构性改动与纯删减除外）；改后字数 ≤原文 105%。删减不设下限。
    两项都必须在自检块报数；压缩性删减与改写要分开报，不许混成一个百分比。
R4  一处修改只解决一个问题。不许把"删辩护"和"换术语"合成一条交回来。
R5  不跨界：当前卡片没列的检查族一概不查。看到别的检查族该管的问题，记进"越界发现"，不动手。
R6  数字宏不得展开。`\Foo{}` 必须保持为宏；也不许自行把写死的数字改成宏（那是 §7 的活）。
R7  不得改：宏名与宏体、`\label`/`\ref` 键、`\cite` 键与条目数、图表文件名与路径、
    环境 begin/end 结构、tabular 与 align 的对齐符号。
    **新增**一处 `\ref` 指向已存在的 label 是允许的，但必须在自检块逐一列出。
R8  `%` 注释默认原样保留（那是作者的口径记录与警告）。若你的修改让某条注释失效，单独指出，由作者定。
R9  占位标记（`\TBD`、TODO、待填）不许"顺手补上"，也不许删除。列进待办。
R10 不编造：文献、数字、未做的实验、未证明的定理。缺证据标 EVIDENCE_NEEDED，数字写 `<待填：某量>`；
    需要人判断标 NEEDS_AUTHOR_DECISION。绝不代替作者决定主张强度。
```

### 2.3 输出格式（逐条锚定，可直接施工）

```text
## 一、诊断（先列完，再给改写）
| # | 位置（小节 + 第 k 段 + 首句前 8 字） | 问题类型 | 违反规则 | 严重度（阻断/大修/小修） |

## 二、逐条修改
### 修改 N
- 位置：<小节> 第 <k> 段｜定位锚：<原文该段首句前 10 字>
- 问题类型：过声称 / 归因错误 / 三角未闭合 / 过度辩护 / 因果越权 / 内部矛盾 / 功能串味 /
  篇幅失衡 / AI 腔 / 术语漂移 / 口径缺失 / 证明缺失 / 图注越权 / 复现缺口
- 违反规则：<R__ 或 C__ 或功能卡片条目号>
- 原文（逐字，只取足以定位的 1–2 句）：> ……
- 改为：> ……
- 代价：删了几句 / 有无信息丢失 / 丢失部分的去向（附录·局限·回复信·舍弃）
- 触及数字宏 / 引用键 / 标签：否（若是，逐一列出并说明）

## 三、越界发现（转交哪个检查族或哪个块）
## 四、需作者决定（选项 A 含代价 / 选项 B 含代价 / 建议与理由）
## 五、验收表（本块功能卡片的验收测试，逐条 是/否 + 位置）
## 六、自检块（§2.4）
```

### 2.4 自检块（缺一项即本轮作废）

```text
输入行数/句数：__ ｜ 提出修改：__ 处（预算 __）
改写率：__%（≤30；压缩性删减单独报：原 __ 字 → __ 字）｜ 字数比：__%（≤105）
触及数字宏：__ ｜ 新增 \ref（指向既有 label）：__ ｜ 触及 \cite/\label 键：0 ｜ 新增主张：0
删除内容及去向：__ ｜ EVIDENCE_NEEDED：__ ｜ NEEDS_AUTHOR_DECISION：__
本块关联的 P 条目（取自 {{SCORE}}）及本轮进展：P__ 已解决 / P__ 部分 / P__ 未触及（无基线则写"无基线"）
本轮我**没有**做的事（证明我没跨界）：__
```

### 2.5 输入包由模型自取（作者不必贴任何原文）

每轮开工时，你自己完成下面五件事，**不要向作者索要**：

1. 读 `{{CARD}}` 全文，作为金标准。
2. 读 `{{LEDGER}}`，取本块的功能 ID、定位锚、风险标注与阻断项。
3. 在 `{{OUTPUT}}`（若尚不存在则 `{{PAPER}}`）中用**定位锚**找到本块区间并整段读取。行号只作参考，以锚为准；若锚失配，报 `ANCHOR_LOST` 并停下，不要猜。
4. 自行补齐上下文：本块引用到的数字宏定义行、本块 `\input` 的表格文件、本块涉及的图注、前一块末段与后一块首段（防改断衔接）。
5. 若本块超 `{{BLOCK_LINES}}` 行且卡片未规定"必须整块"，按小节边界自行切子块，并在输出开头声明本轮只做哪个子块。

作者每轮需要提供的只有一句话：`施工 <块ID>`。其余一律由你去读。

### 2.6 写盘规则（原文件永不修改）

- 首次应用修改前：若 `{{OUTPUT}}` 不存在，把 `{{PAPER}}` 整份复制为 `{{OUTPUT}}`，并在文件第一行插入一条注释，记录来源文件、复制日期与本手册版本。此后 `{{PAPER}}` 只读。
- 此后所有修改**只写 `{{OUTPUT}}`**，用精确字符串替换，一条一次。
- **每应用一条，立刻把它追加到 `{{LEDGER}}` 的「改动记录」表**——这张表就是撤销日志，作者靠它回滚，不需要任何版本控制工具：


| 记录号 | 块 ID | 本轮条号 | 原文串（逐字，足以唯一定位） | 改后串（逐字） | 是否已回滚 |
| --- | ---- | ---- | -------------- | ------- | ----- |


  记录号全局递增、永不复用。作者说 `回滚修改 4` 时，你按记录号做反向精确替换（改后串 → 原文串），并把"是否已回滚"标为是；替换前必须先确认改后串在文件中仍然唯一，不唯一就报 `AMBIGUOUS_ROLLBACK` 并停下。
  作者说 `回滚 F-XXX 全部修改` 时，按记录号**倒序**逐条反向替换该块的全部记录。

- 应用后依次跑：

```powershell
py {{CHECK}}                 # 未定义宏、被写死的数字、括号、环境、悬空 \ref、占位符
{{BUILD}}                    # 编译必须仍然通过
```

- `{{CHECK}}` 的"定义但未引用"是最灵敏的报警器：它一报，通常就是某个数字宏被展开成了写死的数字。
- 中文稿注意：正文常见"半角逗号 + 全角句号"混排，替换字符串必须逐字匹配，**替换前重读该处原文**，不要凭上一轮的引文拼字符串。
- 作者若在用 git，每块一个提交更方便查看差异；但**本协议不依赖 git**：原始稿从不被改，加上上面的改动记录，回滚路径是完整的。

---

## 3. 论文卡片（S1 **新建**并写入 `{{CARD}}`，作者冻结后即为金标准）

下面是卡片的模板与抽取指令。卡片由 S1 从论文里抽出来并落盘成文件——**它不是作者交给你的材料**，S1 之前它不存在。

```text
【任务】读全文，抽取并填写卡片。不改论文。抽不出来的写 MISSING，不要编。
       原文自相矛盾的写 CONFLICT；需作者裁决的写 TODO(作者)，并集中列在文件开头。

# 论文卡片
## 元信息
目标期刊 {{JOURNAL}}｜篇幅上限 {{PAGE_LIMIT}}｜结构要求｜文档类与选项｜是否双盲
语言：现稿 {{LANG}} → 定稿 {{TARGET_LANG}}（换语言时机 {{TRANSLATE_WHEN}}）
一句话问题｜题目（现稿）｜前作与重叠（共用的图·算例·代码·文字段落）｜未填占位符

## 核心主张（≤3，必须可证伪，写成命题而非短语）
C1： C2： C3：   —— 每条附：证据（表·图·宏）+ 可证伪方式

## 贡献（≤4；原文超过 4 条就标出哪些其实是实验设置或副产品）
| ID | 贡献（问题/框架/机制/理论/实验装置，只选一个主标签） | 承担它的功能块 | 证据 | 生效范围 | 明确不包含 |

## 缺口
工作 A 有 __ 缺 __｜工作 B 有 __ 缺 __｜本文同时需要的两半 __｜本文不声称提出问题类 __

## 不声称（≥5 条，防过声称用）
N1–N5：（每条附出处行号或小节）

## 术语表（全文唯一用名）
| 概念 | 唯一用名(中) | 唯一用名(英) | 禁止的近义说法 | 首次出现是否需定义 |

## 数字账本（单一来源）
| 宏 | 值 | 含义 | 比较对象 | 口径（预算/样本/检验） | 出现位置 | 允许的叙述 |
末尾必须列：口径切换点（同一比较有两种口径的全部位置）

## 统计口径
样本结构（算例×种子=配对数，按什么配对）｜检验名/单双侧/配对方式｜多重比较校正（无则写"无，需补"）｜
效应量与离散度（报的是什么，± 的含义）｜置信区间有无｜功效（能分辨多大效应；哪些结论受限）

## 形式化资产
| 编号 | 类型（定义/假设/命题/复杂度） | 陈述 | 证明在哪 | 后文哪里用到 |
文中出现"可证/等价/保证/可采纳/最优/收敛"的全部位置，逐处标明实际由什么支撑

## 局限与辩护预算
| 局限 | 威胁哪条主张 | 不威胁哪条 | 对应未来工作 | 允许篇幅（默认≤4 句） | 允许在别处提前解释（默认否） |
全文防御性语块总预算：__ 处（默认 ≤6）｜应搬去"给审稿人回复信"而不进正文的辩护：__

## 摘要与题目必须出现 / 不得出现的信息

## 附：文件级事实（S0 结论摘要，供台账参照）
```

卡片落盘在 `{{CARD}}` 之后，作者只需在需要时用 `@` 引用一次，**不必每轮粘贴全文**；你每轮按 §2.5 自己去读。**S1 之后不得重抽卡片**——重抽等于每轮换一次金标准。作者修改了卡片，以作者版为准，不要"顺手校正"回你原来的写法。

---

## 4. 功能块与施工顺序

**结论先说：不要从摘要和引言开始改。** 摘要与引言是"承诺"，实验是"兑付"。先改承诺，等实验一收窄，承诺全部作废，所有工作做两遍。

顺序理由（四条）：

1. **证据决定你有资格承诺什么。** 实验定稿后，"能说到多强"才是确定的，引言的贡献列表才有基准。
2. **相关工作是引言的脚注。** 缺口的表述在引言定稿后才固定；倒过来做会写成一篇与本文决策无关的综述。
3. **局限的功能是标注每条主张的边界。** 主张没冻结就写局限，只能写成自我保护，篇幅一定失控。
4. **摘要是全文的压缩，题目是摘要的压缩。** 它们必须最后写。语言/英文化更要最后做。


| 序   | 功能 ID       | 功能         | 卡片    | 依赖     | 备注                               |
| --- | ----------- | ---------- | ----- | ------ | -------------------------------- |
| 1   | `F-SETUP`   | 实验设置与口径    | §5.1  | 卡片     | 先把口径与统计钉死                        |
| 2   | `F-RESULT`  | 结果·消融·负对照  | §5.2  | 1      | 主结果与分解一起做                        |
| 3   | `F-FIG`     | 图表与图注      | §5.3  | 2      | 跟着结果走，正文数字要能由表复算                 |
| 4   | `F-METHOD`  | 方法与机制      | §5.4  | 2      | 只保留实验能归因的机制                      |
| 5   | `F-MODEL`   | 问题、符号、假设   | §5.5  | 4      | "采用还是提出"必须明说                     |
| 6   | `F-PROOF`   | 形式化与证明     | §5.6  | 5      | 论文若把证明单独成章才单独跑；否则与 5 同块          |
| 7   | `F-INTRO`   | 引言与贡献列表    | §5.7  | 1–6    | 贡献列表必须与卡片同构                      |
| 8   | `F-RELATED` | 相关工作       | §5.8  | 7      | 按分叉组织，不按年代                       |
| 9   | `F-LIMIT`   | 局限         | §5.9  | 7      | 辩护瘦身的主战场                         |
| 10  | `F-CONCL`   | 结论·洞见·未来工作 | §5.10 | 7,9    | 不许出现第 5 条贡献                      |
| 11  | `F-ABS`     | 摘要         | §5.11 | 7,10   | 四段信息，带口径                         |
| 12  | `F-TITLE`   | 题目与关键词     | §5.12 | 11     |                                  |
| 13  | `F-REF`     | 参考文献       | §5.13 | 全文     | 只处理标记处                           |
| 14  | —           | 全文校验       | §7    | 全部     |                                  |
| 15  | —           | 语言/英文化     | §9.A  | 14 通过后 | 中文稿定稿前不许译                        |
| 16  | —           | 终审与前后对比    | §8    | 15     | 与 S0B 的基线用同一标尺；`{{SCORE}}` 里前后并列 |


**基线评分（S0B）不在这张表里**，因为它不是施工，而是施工前的一次测量：它在 1 号块开工之前就跑完并冻结，之后只在 16 号被重放一次。

**两个例外（允许提前）：**

- 时间极紧时，可先单独对 `F-LIMIT` 做一次"只删不改"（§6 C2 的 A、B、D 步）。纯删不依赖其他功能块，是性价比最高的单点动作。
- S0 报出的占位符（作者信息、`\TBD`、基金块）任何时候都可以清，与顺序无关。

**功能与章号的关系由 S0 的功能映射表决定。** 例如某文把算法放第 4 章、把定理证明单独放第 5 章、把实验放第 6 章，则施工顺序仍是 `F-SETUP`(第6章前半) → `F-RESULT`(第6章后半) → … → `F-METHOD`(第4章) → `F-MODEL`(第3章) → `F-PROOF`(第5章) → …，与章号次序无关。

---

## 5. 功能卡片

每张卡片是一个块的完整施工单。模型在 S3 第 2 步只读对应的那一张，不要把别的卡片的规则套过来。
"适用检查族"一栏是**差异化的核心**：不是每块都查所有东西（例如实验设置块允许干燥的清单式文字，不查 AI 腔；相关工作块不查统计）。

卡片里的"篇幅"一律相对**功能组**而非章号：
`实验部分` = 承担 `F-SETUP`+`F-RESULT`+`F-FIG` 的全部小节合计；`方法部分` = 承担 `F-METHOD`+`F-MODEL`+`F-PROOF` 的全部小节合计。

### 5.1 `F-SETUP` 实验设置与口径

- **唯一功能**：让读者相信这些比较能归因，并一次性钉死全文所有数字的口径。
- **篇幅**：实验部分的 15–25%。
- **必须存在**（缺一即报 `MISSING`）：
  1. 算例来源：公开基准的版本与出处，或自建算例的**生成器路径、参数与生成种子**（生成种子与运行种子必须分开说）；
  2. 这些算例为何能检验卡片 C1–C3（不是"为了全面"）；
  3. 基线阶梯每一级对应什么结构；哪几级对应已发表工作，**复现到什么程度**，哪些部件是本文重写的；
  4. 预算口径：同挂钟 / 同代数 / 同评价次数，**给绝对值**（秒数、代数上限、早停阈值），以及硬件、语言与**版本**；
  5. 样本结构：算例数 × 种子数 = 配对数（把乘式写出来），配对按什么键对齐；
  6. 检验名称 + 单双侧 + 配对方式 + 多重比较校正（没做也要写"未校正，相关结论为探索性"）；
  7. 效应量与离散度的定义（增益是什么量；± 是标准差、标准误还是 IQR）。
- **禁止出现**：任何结果数字（属 `F-RESULT`）；基线或算例为何这样设的多段辩护（一句口径句 + 指针即可）；"为了公平起见我们……"的连环解释；把设置写成方法的复述；过程性交代（"这两个坑是我们先踩进去的"）。
- **适用检查族**：C4（主）、C1、C6。**不查 C3**——本块允许清单式的干燥文字。
- **验收测试**：
  1. 独立实现者能否据本块重建实验？缺哪一项？
  2. 每一级基线是否都能指到"要归因的某一个因子"？
  3. 是否存在任一比较的预算口径未标注或只给相对倍数？
  4. 多重比较是否已说明校正方式或已声明为探索性？
  5. 本块是否出现了结果数字？（应为"否"）
- **常见病 → 处方**：设置里混入结果 → 搬去 `F-RESULT`｜基线级数多于要归因的因子数 → 合并，或说明每级只动一个因子｜口径句膨胀成三段辩护 → 压成一句 + 指针｜只报检验不报效应量定义 → 补一句定义，数字写 `<待填>` 不许编｜复现信息只写在 `%` 注释里 → 搬到读者可见处。

### 5.2 `F-RESULT` 结果、消融与负对照

- **唯一功能**：只回答卡片 C1–C3，并把每个数字收口到某条贡献。
- **篇幅**：实验部分的 50–60%。
- **必须存在**：
  1. 固定顺序：主结果 → 分解（主效应/次效应/稳健性/消融）→ 负对照或案例分析 → 外部标定（并说明它检验哪一层）；
  2. 每个结果小节四步：比较设计 → 数字 → 2–4 句解释 → 1 句收口到某条贡献；
  3. 不显著与负结果如实报告，且**就地**限定；
  4. 每个数字带口径，或明确指回设置块。
- **禁止出现**：方法中未出现过的新机制；逐格符号被当作适用区间；合计显著被写成逐条件成立；"两机制增益相乘≈端到端"被写成"证明了独立性"（那是相容，不是证明）；对负结果的多段保护；相关观察用因果动词。
- **适用检查族**：C1、C4、C6（主）；C2 只查负对照与不显著处；C3 只查"数字塞排比"这一条。
- **验收测试**：
  1. 每个小节最后一句是否收口到卡片某条贡献？
  2. 正文数字与表格是否逐一致（以表为准）？裸数字是否都能由表复算？
  3. 是否有任何结论强于该比较口径所允许的？
  4. 负对照是否写明"作为设计取舍，不作主结论"？
  5. 不显著处是否避免了"无效/该结构上不管用"的表述？
- **常见病 → 处方**：把消融链末端 vs 起点的差值归因给单一机制 → 说明未冻结的因子，或改为链式表述｜"尤其适用于 X 场景"而逐格不显著 → 删，或改为"本文不就此作主张"｜结果段里解释机制原理 → 压成 2 句，其余交给方法块。

### 5.3 `F-FIG` 图表与图注

- **唯一功能**：让每张图表脱离正文也能被正确读懂，且与正文数字同源。
- **单读测试**（只看图表 + 标题与注，逐图逐表回答 6 问；任一答不出即 `NOT_SELF_CONTAINED`）：
  1. 比较的是谁和谁？2. 什么口径（预算、样本量 n、种子数）？3. 纵轴/单元格是什么量与单位？
  2. 误差棒 / ± / 阴影代表什么？5. 显著性标注的含义与检验方法？6. 加粗或高亮按什么判定为"最优"？
- **图注纪律**：可以陈述图上看得到的东西，**不得引入正文没有的新主张**；存在性演示必须在注里自称存在性（"说明反转会发生，不是反转普遍发生"）；不写方法细节与未来工作；若该图来自与主批次不同的实验批次，注里必须自报批次归属。
- **正文接口**：每个主图主表被完整句子引用（不是"如图 3"了事）；正文数字能由该表复算；图表顺序与首次引用顺序一致；从未被引用的图表 → 删或移附录。
- **单一来源**：手打的 tabular 与脚本生成的 `\input` 表要分别标出；手打表是回归风险最高的地方，重跑实验后不会自动更新。
- **制图返工清单**（只列，不自行改脚本）：字号缩到最终尺寸仍可读；矢量、无位图化文字、无截断标签；灰度可辨且不只靠颜色区分；**图内文字语言与正文一致**（同一稿不许既有中文图又有英文图）；同类图配色与坐标范围一致；表格用 booktabs、数字按小数点对齐、单位放表头。
- **适用检查族**：C1（图注越权）、C6（数字同源）。
- **输出**：一张表（图表 / 单读 6 问逐项 ✓✗ / 图注问题 / 正文接口问题 / 制图返工项 / 可直接替换的新图注），末尾附"必须重新生成的图表清单"并标明改哪个脚本。

### 5.4 `F-METHOD` 方法与机制

- **唯一功能**：让独立实现者能重建接口与不变量。
- **篇幅**：方法部分的 55–70%。
- **必须存在**：
  1. 总览：输入 → 关键机制 → 输出的数据流；
  2. 每个模块：做什么、输入输出、与相邻模块的接口；
  3. 每个机制一句"为何这样选"（服务于接口选择，不服务于情绪）；
  4. 代价或复杂度分析——仅当代价进入实验口径时；
  5. 伪代码的前置条件、后置条件与不变量。
- **禁止出现**：没有对应实验比较的机制被写成贡献级机制（降级为实现细节，或标 `EVIDENCE_NEEDED`）；伪代码与正文互相重复；超参数罗列（移设置块或附录）；对未采用方案的长篇比较；"为了更好地……"式动机。
- **适用检查族**：C1、C5、C2、C6；C3 只查句长与术语。
- **验收测试**：
  1. 每个机制能否指到实验里的某个具体比较？
  2. 是否存在正文未定义即使用的符号？
  3. 伪代码的输入输出与正文描述是否一致？
  4. 复杂度/代价声明与实验实测的量是否同一件事？
  5. 本块的"为什么"是否都落在接口选择上？
  6. **未获净收益的机制所占篇幅**，是否与它在结论中的地位相称？（不相称即报篇幅失衡）

### 5.5 `F-MODEL` 问题、符号与假设

- **唯一功能**：把问题类、符号、假设一次性钉死，并说清"采用还是提出"。
- **必须存在**：形式化陈述；与既有问题类的关系（采用/扩展/提出，必须明说）；编号假设；符号首次定义；**与最接近工作的模型差异必须在这里讲清**（例如预约单位是边还是节点、卸料是否瞬时），不许只藏在局限块。
- **禁止出现**：从未被使用的假设；同一符号两义或同一量两符号；实验里实际依赖却没写成假设的条件；把模型差异推给局限块。
- **适用检查族**：C5（主）、C1、C6。
- **验收测试**：
  1. 每条编号假设是否在后文被用到？
  2. 是否有符号在定义前被使用？
  3. 与最接近工作的模型差异是否在本块出现（而非只在局限）？
  4. "采用/扩展/提出"是否有一句明确表态？
  5. 实验依赖但未写成假设的条件是否已补编号？

### 5.6 `F-PROOF` 形式化与证明

> 仅当论文把命题与证明**单独成章/成节**时作为独立块跑；若与 `F-MODEL` 同节，则两张卡片在同一块上依次执行。

- **唯一功能**：使每条形式化主张的强度与它的证明**恰好**相符——不多也不少。
- **必须存在**：命题编号与陈述；证明的关键步骤（或明确的文献指针）；所依赖的**编号假设**；"等价/可采纳/最优/收敛"的类型声明（见 C5）；命题在后文被用在哪里。
- **禁止出现**：把工程自检（逐位一致、若干算例验证）当作证明；命题的强度高于后文实际需要的强度；同一命题在正文、摘要、结论用三种不同强度的措辞；证明里出现未在 `F-MODEL` 定义的符号；"显然/易证"承担关键步骤。
- **适用检查族**：C5（全套执行）、C1、C6。
- **验收测试**：
  1. 每处"可证/等价/保证/可采纳/最优/收敛"是否已判定为 OK / PROOF_NEEDED / OVERCLAIM / RHETORIC？
  2. "等价"是解集等价、最优值等价还是搜索轨迹等价，是否写明？
  3. 每条命题是否在后文真的被用到？没用到的删或降为说明。
  4. 摘要与引言引用该命题时的措辞，是否不强于命题本身？
  5. 证明所用的假设是否都有编号且都在 `F-MODEL` 中出现？

### 5.7 `F-INTRO` 引言与贡献列表

- **唯一功能**：把承诺压到证据允许的范围，并让副编辑在两分钟内知道缺口是什么。
- **篇幅**：全文 15–20%。
- **必须存在**（这个顺序本身就是结构）：
  1. 从具体的失效假设切入；
  2. 一个可复算或至少可想象的最小例子，且自称存在性；
  3. 两类最接近工作各缺一半；
  4. 本文方法：先说补哪个缺口，再说机制；
  5. 主结果 + 真正决定结论成立的那 1–2 条协议；
  6. 贡献列表：与卡片贡献表逐条同构（可压缩，不可改含义或条数）；
  7. 组织：仅当结构非常规时才写，一段。
- **禁止出现**："随着 X 的快速发展"式开头；文献综述（那是 `F-RELATED`）；算法细节与超参数；次要消融；局限的预先辩护；"据我们所知/首次/填补空白"（除非卡片有可核验对照）；把实验协议扩成独立小节。
- **适用检查族**：C1、C2、C3、C6。
- **验收测试**：
  1. 贡献条数与含义是否与卡片逐条对应，不多不少？
  2. 缺口是否具体到"哪类工作缺哪一半"，而不是"相关研究较少"？
  3. 最小例子是否自称存在性演示？
  4. 是否出现了实验块没有的数字或口径？
  5. 防御性语句是否 ≤1 处？

### 5.8 `F-RELATED` 相关工作

- **唯一功能**：把引言给出的缺口落到具体文献上，并公正定位。
- **篇幅**：全文 8–12%。
- **必须存在**：按"与本文决策相关的分叉"分小节；每小节结尾一句定位（他们能做什么 → 因此不能做什么 → 本文接在哪条边）；对最接近的两项工作给出**结构级差异**而非优劣评价。
- **禁止出现**：年代式罗列；只与 claim 共享领域关键词的引用；方法预告；贬低性形容；转述他人局限或结论而无原文依据（一律插 `[ref_check]`）。
- **适用检查族**：C1、C6 + §5.13 的 `[ref_check]`。**不查 C4。**
- **验收测试**：
  1. 每个小节结尾是否有定位句？
  2. 引言声称的两半缺口，是否都能在本块找到对应文献？
  3. 实验基线声称对应的已发表工作，是否在本块被定位？
  4. 是否存在对他人工作的转述缺少原文依据？
  5. 本块是否在预告本文方法（应为"否"）？

### 5.9 `F-LIMIT` 局限（辩护瘦身主战场）

- **唯一功能**：标出每条主张的边界，使审稿人不必自己去找。**不是自我保护，也不是第二篇论文。**
- **篇幅硬约束**：≤全文 5%；4–6 条；每条 ≤4 句、最多 1 个反事实。
- **必须存在**：每条写清"威胁哪条主张 / 不威胁哪条"；每条与未来工作一一对应。
- **禁止出现**：对文献的详细模型对比（搬去 `F-MODEL`）；某公开算例为何不可用的多段论证（压成一句 + 附录）；同一件事的"两个侧面"式复述；过程性交代；把正文里删掉的辩护原封搬进来（辩护迁移）；"看上去有个自然的补法，实际没有"式的预演反驳。
- **适用检查族**：C2（全套执行）、C1。
- **验收测试**：
  1. 条数 ≤6，每条 ≤4 句？
  2. 每条是否都指明了威胁与不威胁哪条主张？
  3. 是否有某一条其实说明"摘要写过头了"？→ 去收窄摘要，而不是在这里兜底。
  4. 哪些内容应当搬去"给审稿人的回复信"？
  5. 全块字数占比 ≤5%？每条是否都有对应的未来工作？
- **处方**：超预算时按此优先级砍——预演反驳（全删）→ 文献模型对比（搬 `F-MODEL`）→ 算例不可用的论证（压一句 + 附录）→ 反事实链（留第一环）→ 复述性收尾句（全删）。

### 5.10 `F-CONCL` 结论、洞见与未来工作

- **唯一功能**：复述贡献，并给出能从本文结果推出的洞见。
- **篇幅**：≤全文 5%（含局限则 8–12%）。
- **必须存在**：与卡片同构的贡献复述；洞见（可以比具体算子更一般，但必须能从本文结果推出）；与局限一一对应的未来工作。
- **禁止出现**：第 5 条贡献；新数字；领域宣言；对局限的再次辩护；清单式开新坑的未来工作；把架构级普遍论断建立在单一负对照上。
- **适用检查族**：C1、C3、C6。
- **验收测试**：
  1. 贡献条数与引言、卡片是否完全一致？
  2. 每条洞见能否指到本文的某个结果？其普遍化程度是否超出证据？
  3. 每条未来工作是否都挂在某条局限上？
  4. 是否出现了正文没有的数字？
  5. 是否有句子比实验口径允许的更强？

### 5.11 `F-ABS` 摘要

- **唯一功能**：让副编辑在 90 秒内判断"该不该送审、送给谁"。
- **篇幅**：150–250 词 / 中文 400–700 字（以 `{{JOURNAL}}` 为准）。
- **必须存在，且只有这四段信息**：
  1. 设定 + 为何现有假设失效（2–4 句，不写文献史）；
  2. 方法的最小可复述形态（机制名 + 层间关系，不列超参数）；
  3. 主结果，每个数字带口径（相对谁、什么预算、什么检验）；
  4. 范围句（主效应/次效应、什么不主张）。
- **禁止出现**：文献史、未来工作、次要消融、自我评价（"实验充分证明"）、引用编号、未检验的适用区间、负对照的详细说明。
- **适用检查族**：C1、C3、C6。
- **验收测试**：
  1. 四段信息是否齐备且不越界？
  2. 每个数字是否都带"相对谁、什么预算、什么检验"？
  3. 是否存在会被局限块反驳的无条件主张？（若有，**改摘要**而不是加长局限）
  4. 机制名与题目、引言贡献、方法小标题、结论是否同一字符串？
  5. 字数是否在目标刊上限内？

### 5.12 `F-TITLE` 题目与关键词

- 机制型标题（命名机制或问题结构），不用"发现金句"式标题。
- 长度：中文 ≤30 字 / 英文 ≤15 词；名词堆叠 ≤3 层。短标题（`\title[...]`）与全标题的机制名必须一致。
- 测试：去掉副标题后，读者是否仍知道你做了什么？
- 关键词：覆盖机制名 + 问题类 + 方法族；每个都应在摘要或引言出现过；数量按 `{{JOURNAL}}`。
- 验收：题目里的机制名与摘要、贡献、方法小标题、结论逐字一致？题目是否承诺了实验没测的范围？

### 5.13 `F-REF` 参考文献（只处理标记处）

模型不得在未标记处增删引用。作者先通读插标记，再跑本块。

```tex
% 需要补文献
……该设定在文献中通常被写成常数。[ref_insert: claim="prior work typically treats this quantity as a constant"; type=support; recency=5; n=2]
% 核查已有引用是否真的支持这句
……其局限性小节自述该步骤只执行一次。[ref_check: key=someone2024example; claim="该工作只做一次调整且不回写决策"; need=quote]
```

`type`：`support`（通行做法）/ `contrast`（不同做法）/ `source`（问题或数据来源）/ `baseline`（实验对照原文）/ `method`（沿用其技术）。

- `**[ref_insert]` 查找标准**：真正支撑该 claim，而不是支撑同段里更弱的那句；同行评审期刊或顶会，优先近 5 年；综述只用于"通行做法"类 claim，不用于"某机制有效"；一篇文献只承担一种功能；优先已在 `{{BIB}}` 里的条目。找不到直接支持时，给最近的间接支持并说明"间接在哪一点"，或建议把正文从"普遍"收窄为"常见于……"——**不许用不相干的高引论文充数**。
- `**[ref_check]` 判定**：`SUPPORTS`（给可核对的原文句子或章节页码）/ `PARTIAL`（给应当收窄的正文改写）/ `MISATTRIBUTED`（指出应改引哪篇）/ `CANNOT_VERIFY`（明确说不能核实，不要猜）。转述他人局限性时，必须是原文有的表述，不能是对其结果的推断。
- **禁止**：编造作者、年份、期刊、DOI、卷期页码——任一项不确定就标 `UNVERIFIED`，留空；用预印本冒充已发表；为求"新"而丢掉定义该术语的经典文献（正确做法是经典 1 篇 + 近年用法 1 篇）；一句里堆 4 个以上引用；自引超过总数 15%。
- **输出**：每个标记给 2–4 篇候选，每篇含完整 BibTeX、年份与类型、它支撑 claim 的哪半句（附可核对原句，找不到则标 `UNVERIFIED`）、推荐强度、引用功能；再给含 `\cite{}` 的正文写法。新条目一律追加到 `{{BIB_NEW}}`，不直接改 `{{BIB}}`。
- **附加全局体检**：年份分布与近 5 年占比、重复条目、`{{BIB}}` 中定义却未被引用的条目、期刊名缩写是否统一、DOI 是否齐备、格式是否匹配 `{{JOURNAL}}`。

> 作者侧：模型给的每一条文献都要自己打开原文核对。标 `UNVERIFIED` 的不核实就不许进稿。

---

## 6. 检查族（被功能卡片按需引用）

### C1 主张–证据–范围

对本块每条实质性句子，先判类型再检查：**主张**（是否在卡片 C/贡献 中？证据指针在否？范围副词是否超出证据？）／**事实**（可否追溯到模型、假设、表、图、文献？）／**解释**（是否在解释已报告的结果，而非偷偷引入新主张？）／**边界**（是否一句话收口？）。

过声称的七种形式，逐条扫并给位置：

1. 合计显著 → 写成逐条件成立；
2. 存在性演示 → 写成普遍规律；
3. 消融链末端 vs 起点 → 归因给单一机制（未冻结的因子被冒领）；
4. "在本文设定下有效" → 写成"该问题的应有做法"；
5. 负对照失败 → 写成"该方向无价值"；
6. 用"因此/可见/这表明"连接并不蕴含的两句；
7. "我们没有反例" → 写成"不会发生"。

**缺口–方法–实验三角**：引言声称的缺口，方法是否真在补它？方法的每个机制，实验是否有对应比较（没有的降级为实现细节或标 `EVIDENCE_NEEDED`，不许用文字顶上）？实验的每个主要数字，是否回得去某条贡献？局限承认的每条，是否已在主张处收窄（而不是留着强主张靠局限兜底）？

**因果语言纪律**：单因子且其余冻结时才可用"使/导致/值"；观察相关用"伴随/与……同序/在本族上"；相关系数不得用因果或趋势动词转述；未检验的外推删除或改为"本文不主张"。

### C2 辩护瘦身

**A. 识别**——命中任一模式即算一处防御性语块：显式因果链（之所以 / 是因为 / 原因在于 + 第二层因果）；反事实（一旦 / 即使 / 否则 / 假如 / 若换成 / 这不等于 / 并非 X 而是 Y）；预演反驳（"看上去有个自然的补法，实际没有"、"可能有人认为……但"）；自我限定堆叠（一句里两个以上范围限定）；枚举式自辩（其一……其二……用于解释为何没做某事）；解释未做之事超过一句；替审稿人回答（"这一点不影响本文结论，因为……"）；过程性交代（"这是我们先踩进去、之后才改过来的"）。
逐块记：位置、句数、保护的是哪条主张或局限、删掉后审稿人会问什么。

**B. 判定决策树**：

1. 它保护的是一条主张，还是作者的心情？后者 → 删。
2. 审稿人真的会问吗？把它写成一条具体审稿意见试试——写不出来 → 删。
3. 会问，则归入下列之一并压缩：
  a. 比较口径为何如此 → 一句，放该比较首次出现处；
   b. 某基准/算例为何不能用 → 一句 + "详见第 X 节"，长版本进局限或附录；
   c. 某负结果的作用域 → 一句，紧跟该负结果；
   d. 与某文献的模型差异 → 一句，放 `F-MODEL` 块（那是它的位置，不是局限块）；
   e. 都不是 → 搬进"给审稿人的回复信"草稿，正文删除。
4. 同一辩护全文出现两次以上 → 只留一处，其余改交叉引用。

**C. 三种合法形态**（其余一律删）：范围限定半句（"本文不就规模外推作主张。"）／口径说明一句（"该基准没有走廊争用，故它标定的是上层搜索而非本框架。"）／指向别处半句（"理由见第 X 节。"）。

**D. 预算**：全文防御性语块 ≤卡片给定值（默认 6 处）；局限块按 §5.9 硬约束；正文其他位置每处 ≤1 句；摘要+引言+结论合计 ≤1 处。

**E. 负面结果的正确写法**：

对：「机制 M 在同预算下未获得正增益（数字）。本文将其作为设计取舍的负对照，不作为主结论。」
错：先设想 M 本应有用的三条理由，再解释为何实验不支持，再解释为何这不影响本文贡献。

**F. 与"有方法学价值的自我批评"区分**（不要误删）：记录一个失败的度量指标为何失败、一条被数据推翻的叙事为何撤回、一个参数为何不做敏感性分析——这些划定了结论的边界，属于**证据**而非辩护，判定为"留"，但仍要压到 ≤3 句。

**输出**：辩护清账表（ID / 位置 / 原句数 / 保护对象 / 判定 删·压·搬·留 / 改后句数 / 去向）+ 逐条改写 + 预算核对（改前处数→改后处数）+ 回复信草稿片段。

### C3 表达与去 AI 腔

**段落起收**：首句只做一件事——给出本段的功能命题（设定/缺口/机制/结果/范围）。禁止开头："值得注意的是 / 需要强调的是 / 需要说清楚的是 / 在这一背景下 / 随着……"，以及一句正确但无推进的综述。末句必须推进到下一功能，或收口到卡片的某条主张/不声称。禁止口号收尾。

**句子**：18–32 英文词 / 25–50 中文字；同段不得连续 3 句长句；一句只推进一个信息；从句里不再塞对比数字；主动语态优先（结果句可被动）；少用破折号插入语，括号只放符号、简短口径或交叉引用。**句长要有变化**——整段句长接近相等是机器写作最明显的指纹。

**用词**：学术性来自精确不来自大词。优先具体动词（闭合、替换、冻结、归因、稀释、标定）。删除：leverage / utilize / underscore / showcase / pivotal / seamless / intricate / realm / landscape / delve；furthermore、moreover 每段最多一次；"赋能 / 旨在 / 深入探讨 / 至关重要 / 不可忽视 / 本文创新性地提出 / 有效地 / 极大地"。

**AI 腔的十个结构特征**（逐条给"命中与否 + 位置"，比词表重要）：

1. 三元并列成瘾（"更快、更稳、更省"，尤其无数据支撑）；2. "不仅……而且""既……又"密度过高；
2. 每段都是"主题句+三句支撑+总结句"同一骨架；4. 段落长度整齐得不自然；
3. 每段以"总体而言/综上/因此可以看出"收尾；6. 对称空话对子（"既有理论价值也有实践意义"）；
4. 连接副词密度 >2 个/100 字；8. 情绪形容词替代数字（显著地、极具、令人信服地）；
5. 把"我们做了 A，因为 B"扩写成"由于 B，因此 A，这使得 C，从而 D"；
6. 完全没有取舍痕迹——真实论文会写"我们选 X 不选 Y，代价是 Z"。

**自查法**：抽 3 段，删掉所有连接词与形容词，看信息是否减少。几乎不减少，说明那些词是填充物，删。

**数字入句**：先比较设计 → 再数字 → 再统计 → 最后对主张的含义。禁止三个百分数塞进一句排比。点估计、区间、p 值、胜负次数与卡片数字账本逐字一致；不许把 18.86% 说成"约两成"后又在结论里变回精确值。

**中文稿**：少用欧化长定语，"对……的……的……"拆两句；"其/该/此"不在一段里轮换指代三个对象；不把英文腔直译成"被给定的""被证明是有效的"；四字格与文言词可用但不密集；中文正文用中文标点，数字与 LaTeX 命令周围保持半角。

### C4 统计与可归因性

**报告完整性**（逐项 有/无/位置，缺项给出"应补的一句话"，数字写 `<待填：某量>` 不许编）：样本结构与配对依据；检验名称、单双侧、配对方式；多重比较次数与校正（Holm / BH），未校正须声明为探索性；效应量（中位差、Hodges–Lehmann、相对改进、胜负次数）；离散度与 ± 的含义；主结果的置信区间；预算口径（每表每图都标，且给绝对值）；硬件与实现版本；失败或异常运行的处理。

**推断越权**（逐条扫）：合计显著→逐条件成立；逐格不显著→"该结构上无效"（不显著 ≠ 无效果，尤其功效不足时）；逐格符号→适用区间；p 值大小→效应大小；只报显著的那些比较（选择性报告）；小样本相关系数（如 n=16）未报 p 与区间却用趋势语言；消融链差值归因给单一机制；增益相乘≈端到端→"证明独立性"。

**归因结构**：每个比较实际变动了几个因子？基线的实现质量与调参预算是否与本文方法相当（非稻草人）？声称"对应已发表工作结构"的基线复现到什么程度？负对照的判定是否依赖当前实现的相对代价（若是，结论必须限定在"当前代价下"）？

**功效**：用现有离散度估算当前样本量能可靠分辨多大效应；哪些结论落在分辨能力之下——这些必须**就地限定**，不能只放在局限块。

**附加产出**：一段可复用的表格脚注模板，统一口径、检验、校正与 ± 的含义。

### C5 数学、符号与"可证"类主张

**证明义务**——文中每处出现"可证/可证明/等价/保证/最优/可采纳/收敛/上界/下界"，逐一填表并判定：
`OK`（有证明或可核查引用）／`PROOF_NEEDED`（是实质命题但无证明 → 给两个选项：补证明并列出所需引理，或降级为"在本文实现中成立"）／`OVERCLAIM`（假设不足以支撑该强度，例如剪枝"可采纳"需要下界不高估最优值的证明，验证若干算例不构成可采纳性）／`RHETORIC`（只是强调语，删该形容词即可）。

"等价"必须写清等价的是什么：**解集等价 / 最优值等价 / 搜索轨迹等价**，三者强度不同。只保证最优值不变的降本，不得写成"结果完全等价"。同一命题在摘要、引言、正文、结论的措辞强度必须一致，且**不强于命题本身**。

**符号**：同符号两义、同量两符号；每个符号的首次定义位置、是否定义前使用、是否在符号表中；上下标风格一致（`$t^P$` 与 `$t_P$` 不许混）；数学模式内的中文与单位；变量数 >15 时建议加符号表。

**假设**：编号 / 陈述 / 后文使用位置 / 若不成立影响哪条主张 / 是否与实验设置一致。从未使用的假设删除或降为说明；实验里实际依赖却没写成假设的（操作被当成瞬时、容量被当成充裕、噪声被当成独立，之类）补进编号。

**复杂度与伪代码**：复杂度声明是否给推导或计数依据；声明的量与实验测的量是否同一件事（渐近 vs 实测挂钟）；经验关系必须说明是拟合还是推导并给范围；伪代码的输入输出前置后置与不变量是否写出、与正文是否一致、是否用到未定义符号。

所有 `PROOF_NEEDED` 与 `OVERCLAIM` 一律进 `NEEDS_AUTHOR_DECISION`，**不得自行降级措辞后当作已解决**。

### C6 术语、数字与块内一致

- 卡片术语表的唯一用名，本块内不得换近义称呼（不要在"闭环/反馈/一体化/联合优化"之间跳，除非卡片把它们定义为不同概念）。
- 机制名在题目、摘要、引言贡献、方法小标题、实验、结论必须是同一字符串。
- 缩写首次给全称，之后只用缩写。
- 本块所有数字：是否引用宏？与表格是否一致（以表为准）？口径是否标注？同一比较是否混用了两种口径？符号约定（宏自带正负号 vs 正文补号）是否在本块内一致？
- 交叉引用："见第 X 节"处，那一节是否真的讲了那件事。**逐条核对，相符的也要在诊断里记一句"已核对相符"**。

---

## 7. 全文校验（S4）

逐项给 通过/失败 + 位置。**只报冲突与最小修改集，不做润色。**

1. 机制名：题目 = 摘要 = 引言贡献 = 方法小标题 = 结论（同一字符串）
2. 贡献：引言列表 vs 结论列表 vs 摘要是否暗含额外一条；条数与含义
3. 主结果数字：摘要 / 引言 / 结果正文 / 表 / 图注 / 结论，六处的点估计、比较对象、口径是否同源
4. 「不声称」清单：是否有块违反（尤其稳健性小节、图注、结论洞见）
5. 缺口：引言的两半缺口 vs 相关工作定位 vs 实验基线是否真的对应那两类结构
6. 负对照：方法如何承诺、结果如何报告、结论是否被升格为主洞见或写成失败辩护
7. 局限：是否威胁了某条已被摘要写成无条件成立的主张 → **收窄摘要**，不是加长局限
8. 图表：是否都被正文完整句子引用；正文数字能否由表复算；图注是否在做新主张；手打表与脚本生成表是否已分别标注
9. 符号与假设：两义、两符号、未使用的假设
10. 未来工作：是否都挂在某条局限上；是否夹带本应完成却没做的核心实验（若是，标为缺口而非展望）
11. 关键词：是否都在摘要或引言出现过；是否覆盖机制名与问题类
12. 交叉引用文字与实际内容是否相符
13. 手写数字 vs 宏/表：不一致以表或宏为准，列出全部手写处；会随重跑变化的量必须宏化（宏区改动由作者执行，模型只列清单）

**术语统一在本状态一次性执行**：S3 各块只登记术语漂移的出现位置，不在单块内改名——十几个块各改一次局部名字会互相打架。

**数字规则**：同一比较出现两种口径（同代数 vs 同挂钟）时，检查正文是否在该段明确切换，且没有把一种口径的数字接到另一种口径的结论上。

**产出**：一致性对照表（检查项 / 状态 / 位置 A / 位置 B / 冲突内容 / 以谁为准 / 建议改写）+ 按 §2.3 格式的最小修改集。
最后：把可判定的部分写成一个独立 Python 脚本（不改 `{{CHECK}}`，只用标准库，落盘为 `<PAPER 同目录>/{{STEM}}_selfcheck.py`），覆盖——术语禁用词命中、裸数字残留、`\label` 是否被引用、摘要字数、各块字数占比、（英文稿）术语英译一致性。**能 grep 的题交给脚本，人只审判断题。**

---

## 8. 投稿判定与评分卡（S0B 与 S5 共用同一套标尺）

本节被执行**两次**：S0B 对 `{{PAPER}}` 出基线，S5 对 `{{OUTPUT}}` 出终审。两次必须用同一套定义、同一个 12 维表，否则对比无意义。

> **先说清这个分是什么**：它是模型模拟副编辑与两名审稿人给出的判断，用于定位问题与衡量改动方向，**不是真实评审结果**，也不能预测录用。它的价值在"逐维依据"和"P 条目状态"，不在总分本身。分数只在**同一套标尺、同一份稿子的前后对比**里有意义。

### 8.1 十二维评分卡

每项 0–4，必须引用具体位置作依据。0 = 会导致拒稿；1 = 需大修；2 = 需小修；3 = 可接受；4 = 该刊上四分位。


| #   | 维度                                     | 分   | 依据（位置 + 一句话） | 到 3 分还差什么（具体动作） |
| --- | -------------------------------------- | --- | ------------ | --------------- |
| 1   | 缺口是否具体到"哪类工作缺哪一半"                      |     |              |                 |
| 2   | 新颖性相对最接近工作是否清晰且诚实                      |     |              |                 |
| 3   | 贡献是否可分离、被实验分别支撑                        |     |              |                 |
| 4   | 方法可复现到"独立实现者能重建接口与不变量"                 |     |              |                 |
| 5   | 形式化主张有证明或已被正确降级                        |     |              |                 |
| 6   | 实验可归因（单因子、口径、基线非稻草人）                   |     |              |                 |
| 7   | 统计规范（配对、校正、效应量、功效）                     |     |              |                 |
| 8   | 主张与范围匹配（无过声称、该报的负结果已报）                 |     |              |                 |
| 9   | 相关工作定位公正                               |     |              |                 |
| 10  | 图表自含且与正文一致                             |     |              |                 |
| 11  | 写作像期刊论文（篇幅、辩护密度、术语一致）                  |     |              |                 |
| 12  | 可复现材料与规范项齐备（含篇幅是否在 `{{PAGE_LIMIT}}` 内） |     |              |                 |


### 8.2 投稿判定（必须给出，且必须与分数自洽）

总分 __/48 → 判定一个：


| 判定               | 条件                           | 含义                          |
| ---------------- | ---------------------------- | --------------------------- |
| `Reject`         | 任一维 = 0（**不论总分多高**），或总分 < 24 | 存在会直接导致拒稿的硬伤；不宜投            |
| `Major Revision` | 无 0 维，总分 24–35               | 需补证据、收窄主张或重写整块              |
| `Minor Revision` | 无 0 维，且 ≤1 维 = 2，总分 36–43    | 只剩措辞、一致性与规范项                |
| `Accept`         | 无 0/1/2 维，总分 ≥ 44            | 罕见。给出此判定时必须逐条说明为何 12 维都不需要动 |


判定后紧跟一句**一句话理由**，点名压低判定的那一维（例："判定 Major Revision，压低项是第 7 维统计规范——全文无多重比较校正声明，§X 的逐格结论因此不可用"）。

另给：

- **三条最可能的拒稿理由**，各用审稿意见的口吻写 3–5 句，具体到位置与数据。任何一条你答不上来，就必须在投稿前解决，而不是留给 rebuttal。
- **分级意见**：必须修改 / 建议修改 / **不要改的地方**（已符合期刊惯例，防止下一轮又被润色坏）。

### 8.3 问题清单 P-list（S0B 产出，写入 `{{SCORE}}`）

把评分依据拆成可逐条追踪的问题。这是前后对比的真正载体——分数会波动，条目状态不会。


| P 编号 | 维度  | 位置（小节+段+首句前 8 字） | 问题（一句话，陈述事实不给改法） | 严重度 | 归属功能块 | 解决它需要什么（粗粒度动作） | 是否需作者决定 |
| ---- | --- | ---------------- | ---------------- | --- | ----- | -------------- | ------- |


- 严重度三档：`阻断`（不解决就是 0 或 1 分）/ `大修` / `小修`。
- **归属功能块**必须填 §5 的功能 ID，这样施工到该块时能自动带出待办；跨块的填 `S4`。
- 需要作者拍板的（主张强度、是否补实验、是否补文献）标 `需作者决定`，并说明选项，**不代替作者选**。
- 条数不设上限，但按严重度排序，`阻断` 排最前。
- **编号一经分配即冻结**：不复用、不重排、不因后来发现更严重的问题而插队重编。

### 8.4 终审复审表（S5 产出）

**逐维对比**：


| #   | 维度  | 基线分 | 现分  | 变化  | 变化的直接原因（`{{LEDGER}}` 改动记录号；无法指出则分数必须保持基线值） |
| --- | --- | --- | --- | --- | ------------------------------------------ |


**P-list 复审**：


| P 编号 | 基线严重度 | 现状态 | 证据（在 `{{OUTPUT}}` 中的位置，或说明为何判为已失效） |
| ---- | ----- | --- | ---------------------------------- |


现状态五选一：`已解决`（给出改后位置）/ `部分解决`（说明还差什么）/ `未解决` / `已失效`（问题所依附的文本已删除或重构）/ `已转为作者决定`（挂在某条 `NEEDS_AUTHOR_DECISION` 上）。

**新增问题**另起一表，编号 `NEW-01` 起。新增问题若源自本次改稿（例如删辩护删过头导致口径缺失），必须点名是哪一轮引入的。

**收尾必须回答一句**：`判定 <基线> → <现在>`，以及"若判定未改善，卡在哪一维、为什么这次改稿没能动它"。

### 8.5 评分通胀的防线（S5 必须逐条遵守）

模型天然倾向于让"改后"看起来更好。以下五条是硬约束，违反即本轮作废：

1. **基线不可改。** 逐字引用 `{{SCORE}}` 基线一节的分数与依据，不得改写、不得"重新理解"。
2. **涨分要有归因。** 某维分数上升，必须给出导致上升的具体改动记录号。给不出的，该维分数**保持基线值**。
3. **未触及即不动分。** 从未被任何一轮修改触及的块，其对应维度的分数原则上不变；若确要改分，必须说明是基线判错了，并同时承认基线那一处的错误。
4. **允许降分。** 若某轮改稿造成退步（辩护删过头、术语被改乱、图注被削弱），必须如实降分并指出是哪一轮引入的。**一次都没降过分的对比表是可疑的。**
5. **判定不得跳级凑数。** 只要还有 `阻断` 级 P 条目未解决，判定不得优于 `Major Revision`，无论总分算出来是多少。

---

## 9. 附录

### 9.A 语言定稿（必须在全文校验通过后做）

先按 §0.1 判定本轮属于哪一种，再执行对应部分：

- **不换语言**（`{{TARGET_LANG}}` = 同 `{{LANG}}`）→ 只做「语言定稿」：跑术语锁核对 + 母语规范清单（现稿是英文则跑下面的中式英语清单；现稿是中文则跑 C3 的中文稿条目）。**不产生新文件**，修改按 §2.6 写入 `{{OUTPUT}}`。跳过翻译与回译。
- **换语言**（中→英、英→中、或中英双版）→ 走下面的完整翻译流程。译稿**另存**为 `<PAPER 同目录>/{{STEM}}_AI-Modify-{{DATE}}_<目标语言代码>.tex`（如 `_en` / `_zh`），原语言施工稿不动。

以下条目在两种情形下都要跑的已标注「通用」。

**术语锁**（通用，先做，一次性）：用卡片术语表建立中英对照表并冻结，一词一译，含图表、算法、附录。机制名的英译一旦定下，题目、摘要、贡献、小标题、图注必须同一字符串。不确定的译法给 2 个选项交作者定。

**翻译纪律**：逐段译，不合并不拆分（结构已在中文稿阶段定好）；中文欧化长句在英文里还原为 2–3 句；不增删信息，包括程度副词；数学、符号、宏引用原样保留；图注表注算法内文字一并译；译完给**回译对照**（英译 → 回译中文 → 与原文并列，标出信息偏移处）。

**中式英语高频问题**（通用，目标语言为英文时必跑）：冠词与可数性；时态（贡献与结论现在时、实验过去时、定理现在时）；respectively 位置与滥用；such as / etc. 混用；in order to → to；due to the fact that → because；裸 This 作主语（应为 This design / This result）；the paper 做动作主语；逗号拼接；data / criteria / analyses 的单复数；名词堆叠 >3 层；`%` 前不空格、SI 单位与数字之间空格；连字符（closed-loop 作定语加、作名词不加）；小节标题大小写风格统一。

**产出**：表格（段号 / 中文首句定位 / 英译 / 回译 / 偏移风险 / 术语核对）+ 术语锁核对结果 + 需作者定夺的译法 + 建议交专业润色的段落（数学密集或论证微妙处）。

**若 `{{TARGET_LANG}}` = 中英双版，或原语言版本已投他刊**：先停下报 `NEEDS_AUTHOR_DECISION`——同一内容的中英双投会触及重复发表，两个版本是否需要实质差异属作者决定，不是翻译问题。不得未经确认就交付两个语言版本。

### 9.B 投稿前的行政关卡

- **可复现材料**：算例来源与生成器；全部超参数及其选定方式；种子集合与选法；预算与硬件；代码/数据可用性声明（多为强制项，暂不公开须写理由与获取方式）；复现命令；基线复现说明；从原始结果到表格的处理路径。
- **与前作的关系**：重复段落必须重写（自我抄袭也会被查重命中）；复用旧图必须引用并说明，仅换语言不算新图；核对图目录里是否有属于前作的图被误用；本文相对前作新增了什么，引言用一句话交代（否则会被判 salami slicing）。
- **稿件规范**：作者/单位/邮箱/ORCID 填实（占位符必须清零）；通讯作者与署名顺序、CRediT；基金与致谢编号（不要沿用前作的基金块）；利益冲突与伦理声明；收稿日期不留旧值；双盲要求下检查自引写法、仓库链接、致谢是否泄露身份；清理模板残留（shortauthors、CCS 分类、示例文献）。
- **占位符清零**：`py {{CHECK}}`，`\TBD` / TODO / 待填 / 待改 全部为 0 才可投稿。
- **投稿包**：期刊适配核对表（篇幅、结构、摘要字数、关键词数、图表数、参考文献格式、是否要 highlights 与 graphical abstract、代码政策、是否双盲）；cover letter（三段 ≤350 词：投什么且为何适合该刊 / 一句话问题+方法+主结果与相对最接近工作的差别 / 原创性与无重复投稿声明）；highlights（3–5 条 ≤85 字符，至少两条带数字与口径）；建议审稿人（3–5 位，不含合作者与同单位）。

### 9.C 回复审稿意见

每条一块：**逐字引用意见** → 我们的理解（还原为具体技术要求，多要求则拆 N.1/N.2）→ 动作（已改 / 已补实验 / 已收窄主张 / 不改）→ 改动位置（块·段·修订稿页行）并完整引出改后的句子 → 若"不改"，≤3 句理由且必须含一个可核查依据（数据、口径、文献、代价量级）。

纪律：先感谢一次，之后不再客套；不与审稿人争论主张强度——**能收窄就收窄，收窄比说服便宜**；承认对的地方要干脆，不要一边承认一边辩解；每条都要有可验证落点；两位审稿人冲突时明确指出并说明取哪一方及代价；回复信可以详细，但**不要把回复信内容搬进论文**——那是论文变臃肿的主要途径（C2 判定为"搬"的辩护，终点就在这里）。
若某条意见要求的改动与卡片冲突：停下，标 `NEEDS_AUTHOR_DECISION`，不得擅自改主张。

### 9.D 单段急救（不走 RUNBOOK，随时可用）

```text
改写下面这一段，使其可进入 SCI 期刊正文。
1. 保留全部数字宏、符号、引用键、交叉引用；宏不得展开。
2. 首句给出本段功能命题；末句接到卡片中的某条主张或范围。
3. 删除过度辩护：口径最多一句；"我们没做 X 的原因链"整段删或压成半句。
4. 句子短、词具体、术语与卡片一致；不增加新主张；字数 ≤原文 105%。
5. 若该段同时在做两件事（既报数字又讨论局限），拆段或把局限移走。
6. 若已合格，直接回答"本段通过"并说明它实现了什么功能。
先一句话说明本段当前的功能与问题，再给改写，最后报改写率与是否触及数字。
```

---

## 10. 失败模式（出现即回退，可附在任何一轮之后）

1. **一次性交付全文修改版** —— 作者无法逐条取舍。违 R1/R2。
2. **整节重写** —— 交回一整节新文本而无诊断表。
3. **润色型同义替换** —— 句子变长、信息不变。
4. **宏被展开** —— `\Foo{}` 变成 `18.86\%`（`{{CHECK}}` 会报"定义但未引用"）。
5. **越界修改** —— 卡片没列的检查族被执行（例如在实验设置块里改句长）。
6. **跳过停止点** —— 未经确认就动文件，或未出卡片就开始施工。
7. **顺序倒置** —— 从摘要或引言开始改，之后实验一收窄全部作废。
8. **向作者索要原文** —— 模型自己能读的东西却要作者贴（违 §2.5）。
9. **改错文件** —— 修改写进了 `{{PAPER}}` 而不是 `{{OUTPUT}}`（违 §2.6）。
10. **凭旧行号定位** —— 不用锚重新定位，改到了别的段落。
11. **贡献膨胀** —— 实验协议、负对照、实现技巧被升格为贡献。
12. **摘要变引言** —— 摘要里出现文献史、未来工作、次要消融。
13. **辩护迁移** —— 正文删掉的辩护原样搬进局限块，总篇幅不减。
14. **负结果小说化** —— 用一串反事实保护一个未证实的接口。
15. **口径漂移** —— 同代数的数字用来支撑同挂钟的结论。
16. **适用范围脑补** —— 逐格不显著却写"尤其适用于拥堵场景"。
17. **统计裸奔** —— 只报 p 值，不报效应量、样本量、配对方式与校正。
18. **"可证"无证** —— 写了可证/等价却没有命题与证明，或自行悄悄降级措辞。
19. **文献装饰 / 引用误置** —— 引用只与 claim 共享领域关键词；把对文献结果的推断写成该文献的自述。
20. **图注越权** —— 图注做出正文没有的主张；存在性演示不自称存在性。
21. **机制名漂移** —— 引言一个名字、方法另一个、结论第三个。
22. **AI 腔** —— 三元并列、每段同一骨架、连接词连环、每段以"总体而言"收尾。
23. **误删有价值的自我批评** —— 把失败指标记录、叙事撤回当成辩护删掉（违 C2-F）。
24. **基线通胀** —— 基线故意打低、或终审无归因地普涨，制造"改好了"的假象（违 §8.5）。
25. **给分不给位置** —— 评分卡某维只有分数与形容词，没有可核对的位置。
26. **基线阶段给改写** —— S0B 输出了改后的句子，越过了卡片与施工顺序。
27. **语言串味** —— 施工阶段擅自把正文改成 `{{TARGET_LANG}}`（违 §0.1 规则 1），或把 `{{REPLY_LANG}}` 当成正文语言（违 §0.1 规则 3），或未经确认就交付双语两版。
28. **缺自检块** —— 没报改写率与触及项，等于没交作业。

---

## 11. 作者会用到的指令词（模型必须识别并服从）


| 作者说               | 你要做的事                                                         |
| ----------------- | ------------------------------------------------------------- |
| `继续`              | 推进到下一个状态，不要重做上一个                                              |
| `装载`              | 读本手册 + `{{PAPER}}`，回填 §0 的探测与推导项（并说明推导出的文件现在是否存在），等确认，**不做别的**  |
| `执行 S0B 基线评分`     | 按 §8 对 `{{PAPER}}` 出基线评分卡 + 判定 + P-list，写入 `{{SCORE}}`，不给任何改写 |
| `卡片已确认`           | 以 `{{CARD}}` 现内容为金标准，进入 S2                                    |
| `施工 B07`          | 进入 S3，按 §2.5 自取输入包，只处理该块，只用该块的功能卡片                            |
| `应用修改 2、5、6`      | 只落盘这几条到 `{{OUTPUT}}`，其余一律不动；精确字符串替换，一条一次；每条追加一行改动记录           |
| `修改 3 我不同意，理由：……` | 撤回该条，不要换个说法再提一次                                               |
| `违约 R3，重做本轮`      | 承认违反了哪一条，回到本轮起点重做，不要在坏输出上修补                                   |
| `回滚修改 4`          | 按 `{{LEDGER}}` 改动记录号 4 反向精确替换，标记已回滚，并报告是否已复原                    |
| `回滚 F-XXX 全部修改`   | 按记录号**倒序**逐条反向替换该块的全部记录                                        |
| `恢复原样`            | 告知作者删掉 `{{OUTPUT}}` 再从 `{{PAPER}}` 重新复制即可，并列出会丢掉哪些块的成果          |
| `B07 冻结`          | 该块此后不得再改措辞，除 §7 报出与卡片冲突；同时更新 `{{LEDGER}}` 状态                  |
| `执行 S4`           | 按 §7 执行全文校验，不做润色                                              |
| `执行 S5 终审并对比基线`   | 按 §8 对 `{{OUTPUT}}` 重评，出逐维对比 + P-list 复审 + 判定变化，严守 §8.5       |
| `抽检`              | 只重评受最近几块影响的维度与 P 条目，不重出完整评分卡                                  |
| `急救` + 一段文字       | 走 §9.D，不进 RUNBOOK，不改文件                                        |


