# 闭环双层框架的五个深化方向:相关工作核查与算法设计方案

> **文档定位**:本文档基于对 `clbs/` 现有实现的结构分析,给出 A–E 五个创新方向的相关工作核查结论与详细算法设计。**本轮只做设计,不改代码**。与《建模问题梳理》(假设层)、《异构资源柔性作业车间协同调度核心算法》(规格层)并列,属于**改进设计层**;三者冲突时,假设层 > 规格层 > 本文档。

---

## 零、执行摘要

**相关工作核查的三条结论**:

1. **总体创新性成立,且比预想更稳固**。2026 年 1 月新出的直接竞品(Sensors 26(2):543)在其局限性一节**自认**其框架是"Generate-then-Refine 的两阶段顺序优化,而非真正的闭环",这句话是本文闭环主张的最佳引用锚点。
2. **单项技术大多已有出处**,创新性必须落在**组合**与**耦合机制**上,不能落在"我们用了 PBS / 我们考虑了拥堵"这类表述上。
3. **一个关键差异化资产是"RA 时间异构"**:竞品用的是固定加工时间的纯 JSP 基准(FT/LA/SWV/YN),每道工序只有一台机器,因此"拥堵驱动改派"这一机制在其问题设定下**根本不存在**。这是本文最不可替代的护城河,所有方向的设计都应围绕它组织。

**五个方向的定性判断**:

| 方向 | 现状问题 | 创新强度 | 实施成本 | 优先级 |
|---|---|---|---|---|
| A 派车感知路网 | 全框架唯一开环残余 | 中(组合新) | 低 | **最高(先做诊断)** |
| B 时空拥堵画像 | 信号无时间维、量纲失衡 | 中高 | 中 | 高 |
| C 下层升为优化器 | 贪心 PP,系统级非最优 | 高(理论卖点) | 高 | 中(第二篇主体) |
| D 扩大反馈作用面 | 反馈只回流 1/4 子问题 | 中高 | 中 | 高 |
| E 增量评价 | 无增量,无法支撑 C/D | 低(支撑性) | 中 | **地基,必做** |

---

## 一、相关工作核查结论

### 1.1 直接竞品:Sensors 2026, 26(2):543

*A New Framework for Job Shop Integrated Scheduling and Vehicle Path Planning Problem*(2025-11 投稿,2026-01-13 上线)

**它做了什么**:三层框架——调度层(改进 DQN / D3QPN)→ 任务分配层(甘特图预处理自动生成运输任务)→ 路径执行层(PBS + Space-Time A\*);建立 MIDN 模型,含点—边冲突约束;基准为 FT06、LA、SWV、YN,车队 4–5 辆。

**与本文的四条实质差异**(逐条可辩护):

1. **问题类别不同**:它是 JSP-T,原文明确"每道工序有固定加工时间",基准 FT/LA/SWV/YN 均为**每工序单机**的纯 JSP。**没有机器指派决策,没有加工时间异构** → 本文的核心机制"通往快机的路堵则改派到路顺的慢机"在其设定下不可能出现。
2. **耦合方式不同(最关键)**:原文局限性一节自述——"the proposed framework operates primarily as a **two-stage sequential optimization strategy rather than a fully dynamic, real-time closed-loop system**… relies on generating an initial high-quality schedule which is then refined via a **one-time feedback adjustment**… this 'Generate-then-Refine' approach… The global optimality of the final solution is partially dependent on the quality of the initial schedule."
   → 它是**一次性时刻表重定时**,反馈不改变任何指派决策,也不迭代。本文的评价级闭环(路由后真实完工时间作适应度,每代每个体都算)与决策级闭环(拥堵统计驱动改派)都不在其列。**引用这句话即可完成 gap 定位**。
3. **反馈内容不同**:它反馈的是"到达时刻"(用于修正时间轴);本文反馈的是"拥堵统计"(用于修正决策)。
4. **上层方法不同**:DQN/D3QPN vs GA/模因算法。这一条是中性差异,不构成优势,不必强调。

**对本文的直接影响**:方向 C 若表述为"我们用 PBS 做无冲突路由",已被抢占。必须改为"我们把冲突驱动的路由优化**嵌入适应度评价的内循环**,并处理其特有的**时序耦合**"(见 3.3)。

### 1.2 次近竞品:Sensors 2023, 23(9):4526

FJSP + 无冲突路由,两阶段:第一阶段混合 GA 排产,第二阶段基于 AGV 编码的 GA + 时间窗 Dijkstra 解冲突。**是 FJSP(有柔性),但两阶段单向、无反馈**。本文与其差异集中在"闭环 vs 两阶段"与"时间异构"两点,是 `baseline.py` 中 `two_stage_baseline` 所对标的方法族。

### 1.3 分方向的出处归属

| 技术要素 | 已有出处 | 本文可主张的增量 |
|---|---|---|
| PBS / Space-Time A\* 做车间无冲突路由 | Sensors 2026(路径执行层) | 嵌入适应度内循环 + 时序耦合下的收敛性 |
| CBS 及 makespan 目标变体(EBC、CBSm) | AAAI/IFAAMAS 2025 | 仅作小规模**下层最优性下界**参照,不主张改进 CBS |
| MAPF-LNS / LNS2 / ALNS(destroy-repair、RandomWalk、bandit 选邻域) | IJCAI 2021、AAAI 2022、SoCS 2024–2025 | 迁移到"调度适应度的增量评价",而非 MAPF 自身求解 |
| 拥堵感知派车规则(congestion function / penalty) | Electronics 2026, 15(1):139 | 其拥堵度 = 邻近 AGV 数量(空间密度、DES 仿真、纯车队调度);本文为**预约表导出的时空占用率**,且驱动**机器改派**而非仅派车 |
| AGV 派车作为染色体第三段(OS+MA+AD 三层编码) | Appl. Sci. 2025(Q-HMOABC, FJSP-MTA) | 已有,**不作为创新点**;本文若用只作对比配置 |
| 派车用规则、机器/排序用学习或搜索 | ESWA 2025(CADRL)等 | 与本文现状一致,属通行做法 |

**结论**:B 与 D 的组合(时空拥堵信号 → 驱动改派与错峰)最具原创性;A 是"补齐逻辑闭环"的必要工作,创新强度中等但审稿人易认可;C 的原创性必须落在时序耦合与收敛性论证上;E 无独立创新性,是 C/D 的地基。

---

## 二、先修地基:三处必须先改的结构性缺陷

以下三项不属于"创新",而是**不修则后续方向都建立在错误信号上**。建议作为第 0 阶段。

### 2.1 关键路径归因未覆盖运输段(最严重)

**现状**:`decoder.critical_real_ops` 从决定完工时间的最后事件反向追溯,分支只有两支——`bind=='machine'` 时跳到同机前一工序,否则跳到本工件前一道工序。**运输任务及其让行等待完全不在链上**。

**后果**:当某工序开工被"工件到达"卡住(`bind=='arrive'`)时,真正的瓶颈可能是那次运输中的某段让行等待,但算法只看到"前道工序完工晚",于是把改派机会用在了错误的工序上。**决策级反馈的定位精度从根上受限**。

**设计**:把关键路径从"工序链"扩展为"工序—运输交替链",并对 `bind=='arrive'` 的情形进一步细分归因:

```
追溯到工序 O(j,i) 且 bind == 'arrive' 时,取出其入运输任务 T:
  arrive = T.loaded_plan.arrive
  分解 arrive 的构成:
    t_load = max(空载到达, ready[j])
    若 t_load == ready[j]  → 瓶颈在上游工序(车已先到,件未好)→ 继续回溯 (j, i-1)
    若 t_load == 空载到达   → 瓶颈在车辆侧,进一步分解空载段:
         空载段 = avail[k](该车上一任务结束) + 行驶 + 让行等待
         若让行等待占比 > 阈值 θ → 关键项标记为「走廊 c 在时段 [t1,t2) 的让行」
         否则 → 瓶颈在车辆可用性 → 回溯到该车的上一个任务
    载货段的让行等待同样单独记账
```

**产出**:一条带类型标签的关键链,每个环节标注为四类之一——`机器占用`、`上游工序`、`车辆可用性`、`走廊让行(含具体走廊与时段)`。这直接决定方向 D 该派哪个算子,也是论文里定性分析(甘特图叙事)的素材。

### 2.2 拥堵项与目标量纲失衡(会在大算例上失效)

**现状**:`ga.local_search` 的评分函数为

```
score(m') = t*(pos_prev, loc(m')) + proc_time(op, m') + λ · congestion_at(loc(m'))
```

其中 `congestion_at(node)` = 该节点所有相邻走廊在**整个计划期、所有车辆**上累计的让行等待之和,λ 固定 0.5。

**问题**:前两项是**单道工序尺度**的量(示例算例中约 5 分钟量级),第三项是**全局累计量**,随算例规模、车队规模、计划期长度单调增长。在 240 工序的大算例上,累计等待可达数百,`λ·congestion` 将完全支配前两项,评分函数退化为"只挑拥堵最低的机器",丧失对加工时间的权衡——**恰好破坏了本文要主张的核心机制**。

**设计(必须做)**:改为无量纲、局部化的拥堵度量。

```
util[c][b] = (走廊 c 在时间桶 b 内被预约的时长) / Δ        ∈ [0,1]
congest(node, t) = Σ_{c ∈ 入(node)} w(c) · util[c][bucket(t)]     # w 可取 1/|入(node)| 归一
score(m') = t*(pos_prev, loc(m')) + proc_time(op, m')
            + λ · T̄p · congest(loc(m'), t_query)
```

其中 `T̄p` 为该算例的平均加工时间(实例特征参数已有),使拥堵项与前两项同量纲;`t_query` 取该工序在当前时刻表中的开工时刻(改派后时刻会变,但作为查询近似足够)。这样 λ 成为**真正无量纲的耦合强度系数**,可跨算例比较,也才有资格做敏感性分析。

### 2.3 完工时间口径分散在两处

**现状**:`decoder.decode` 取 `max(工序/伪工序 finish)`;`validator.validate` 在 `delta_return==1` 时额外并入**全部 AGV 分段**(含空载段)的结束时刻。当前算例中每车最后一段恒为载货段,两者恰好相等,测试通过;一旦引入"末次任务后空载归位"或"车辆返回停靠区"等行为,校验器立即误报。

**设计**:把口径收敛为单一定义函数(建议放在 `decoder` 中,校验器调用同一函数或独立实现但注释明确引用同一条款),并在规格文档 E1 处写死:完工时间 = 全部工件末道(伪)工序完成时刻的最大值,**不含任何空载段**。同时给 `validator` 增加一条独立检查:"是否存在晚于完工时间的载货段"(应为无)。

---

## 三、五个方向的详细设计

### 3.1 方向 A:路网感知的派车决策

**问题定位**:`decoder.dispatch_rule` 用理想最短路矩阵估算送达时刻,**完全不查预约表**。这意味着闭环方法在选车这一步仍活在"无冲突理想世界"里——是全框架唯一的开环残余,与论文主张直接矛盾,审稿人一旦读代码即会质疑。

设计三个层次,建议按 A1 → 诊断 → A2/A3 的顺序推进。

#### A1. 精确试探派车(诊断基准,先做)

对每辆候选车 k,做**试探性路由**取真实送达时刻:

```
for k in 车辆集合:
    p_empty ← route(loc[k], pickup, avail[k], commit=False)
    临时提交 p_empty 的预约                      # 需要 E1 的撤销能力
    t_load ← max(p_empty.arrive, ready[j])
    p_loaded ← route(pickup, dest, t_load, commit=False)
    est(k) ← p_loaded.arrive
    撤销 p_empty 的临时预约
k* ← argmin est(k)   # 并列取小车号,保持确定性
```

**实现要点**:`Router.route` 已有 `commit` 参数,但空载段与载货段有因果关系——评估载货段时必须先让空载段"占住"资源,否则两段可能被规划到同一走廊的同一时段。因此 A1 **依赖 E1 的预约撤销**,这也是把 E 列为地基的原因之一。

**代价**:每个运输任务从 2 次路由变为 2·NA + 2 次。示例算例 NA=2,代价约 3 倍;NA=6 时约 7 倍。配合 E2 的增量重解可部分抵消。

**用途**:先把它当**诊断实验**而非最终方法。跑一组"仅替换派车规则"的对照,量化"派车开环"到底损失多少。若在拥堵高的算例上收益显著(比如 >2%),再投入 A2/A3 做提速;若收益微弱,则本方向降级为论文里的一段说明(诚实报告"我们验证过该环节收益有限"),把精力转向 B/D。**这是一个用小成本决定后续投入的关键决策点。**

#### A2. 代理式拥堵折扣(若 A1 显示收益大但太慢)

不做真实路由,而在理想估计上加拥堵修正,复用 2.2 的时空占用率:

```
ρ(路径, t) = 该理想路径上各走廊在相关时间桶的 util 的(长度加权)均值
est(k) = max( avail[k] + t*(loc[k],pickup)·(1+α·ρ(·)), ready[j] )
         + t*(pickup,dest)·(1+α·ρ(·))
```

α 为拥堵弹性系数(可与 λ 一起做敏感性)。代价与现状同阶(只多几次查表),但估计有偏。

#### A3. 候选集截断 + 后悔值(推荐的折中)

先用 A2 的廉价估计排序,只对**前 2 名**做 A1 的精确试探,取较优者。代价固定为 4 次额外路由,与 NA 无关;质量接近 A1。这是我推荐的最终形态。

#### A4. 派车升为染色体第三段(不推荐作为主方案)

文献已有(Appl. Sci. 2025 三层编码 OS+MA+AD),不构成创新;且会破坏规格 6.1"任意染色体可解码"的保证(需额外修复算子),搜索空间显著膨胀。建议**仅作为对比配置**出现在消融表里,用来支撑"用规则 + 路网感知比直接编码更高效"这一论断。

---

### 3.2 方向 B:时空拥堵画像与自适应耦合强度

在 2.2 已给出必须做的量纲修正。此处给出进一步的创新设计。

#### B1. 时空占用张量

```
数据结构:util[corridor][bucket] 与 wait[corridor][bucket]
桶宽 Δ:建议取该算例平均加工时间 T̄p(与工序时间尺度对齐),桶数 = ⌈当前完工时间 / Δ⌉
构建时机:解码过程中在 Router 落表时同步累加,零额外遍历
查询:congest(node, t) 见 2.2
```

相比现有的"全时段标量",它回答的是"我需要通过那里的**那个时刻**堵不堵",而不是"那里整体上堵不堵"。这是把拥堵从**静态属性**变成**时空场**,也是与 Electronics 2026(空间密度)最本质的区别。

#### B2. 双信号分离:占用率 vs 让行等待

两者含义不同,应分开使用:
- `util`(占用率)是**前瞻性**信号:表示"我若此时前往,可能受阻",适合用于改派决策(方向 A、D 的评分函数);
- `wait`(实际让行)是**回顾性**信号:表示"这里已经真的堵住了",适合用于定位关键瓶颈(2.1 的归因、方向 D 选择算子)。

现有实现只有 `wait`,且被当作前瞻信号使用——这是一处概念混用,建议在设计中明确区分,并在论文里作为一个方法学要点讲(信号的因果方向)。

#### B3. 自适应耦合强度 λ

三个层次,建议做到第二层:

1. **尺度自适应(必做,见 2.2)**:λ·T̄p·congest,使 λ 无量纲。
2. **在线自适应(推荐,有创新性)**:借鉴 ALNS 的权重更新(MAPF-LNS2 用反应因子 γ=0.1)。维护若干候选 λ 值(如 {0, 0.25, 0.5, 1.0, 2.0}),按各自在近期改派中的**接受率与平均改进幅度**更新权重,每次按权重抽样选用:
   ```
   w_i ← γ · max(0, 改进量_i) + (1−γ) · w_i
   P(选 λ_i) = w_i / Σ w
   ```
   论文卖点:**耦合强度不需人工调参,由算法自行学习"当前算例该多看重拥堵"**。而且它天然产出一条可解释的曲线——在高拥堵算例上 λ 自动升高,在低拥堵算例上自动趋零(退化为纯 FJSP 逻辑),这**正好是 F2 双因子实验想证明的结论的算法内证据**,比外部对照更有说服力。
3. **多臂老虎机 / 上下文老虎机(可选)**:把算例特征(T̄t/T̄p、异构度、拥堵倾向)作为上下文。SoCS 2024–2025 的评测指出学习型邻域选择相对规则型无明显优势,故此层收益不确定,列为可选。

---

### 3.3 方向 C:把下层从贪心评估器升级为优化器

**现状的准确定性**:下层等价于 MAPF 中的**优先规划(Prioritized Planning, PP)**,且优先序被硬绑定为解码器的 OS 扫描顺序;每个任务单独取"最早到达"最优,但任务之间先到先占、不回退(`release_all` 未实现)。因此系统级路由方案既非最优,也非局部最优。

**必须讲清楚的独特难点(创新性所在)**:本问题的路由**不是标准 MAPF**。标准 MAPF 中各智能体的起点、终点、最早出发时刻都是给定的;而这里:

- 任务的就绪时刻 `ready[j]` 来自上游工序完工,而上游工序完工又取决于其入运输的到达时刻;
- 车辆的可用时刻 `avail[k]` 来自该车上一任务的结束;
- 因此**改变任一条路径,会改变后续任务的起点、最早出发时刻,甚至改变哪些任务由哪辆车执行**。

这是一个**带优先约束与时序耦合的多智能体路径问题**(precedence-coupled, time-dependent MAPF)。竞品的 PBS 用在"甘特图已定、任务清单已冻结"之后,规避了这个耦合;本文若把路由优化放进内循环,就必须正面处理它。**这正是可辩护的理论增量。**

#### C1. 解耦优先序与 OS 序 + 不动点迭代(推荐主方案)

```
给定染色体(MA, OS):
  第 0 轮:按现有解码流程得到初始时刻表 S₀(路由优先序 = OS 序)
  重复 r = 1, 2, …:
    (a) 由 S_{r−1} 提取全部运输任务及其就绪时刻、起讫点、执行车辆
    (b) 按某规则重排路由优先序 π_r
        候选规则:关键路径优先、松弛时间最小优先、等待最长优先
    (c) 清空预约表,按 π_r 依次为各任务规划无冲突路径(PP)
    (d) 用新的到达时刻前推更新时刻表,得到 S_r
        注意:若到达时刻变化导致 ready/avail 改变,需重放解码的时序部分
  直到 S_r 与 S_{r−1} 的完工时间一致(或达到轮数上限 R,建议 R=3)
```

**收敛性论证的思路**(务必写进论文,它填补规格文档十三节点出的 "Convergence / Complexity" 空缺):

- 每轮 (c) 中每个任务都取"给定已落表预约下的最早到达",故单个任务的到达时刻不劣于任意可行选择;
- 但**整体不单调**——重排优先序可能让某些任务变晚。因此不能声称单调收敛,应采用**接受判据**:仅当 `C_max(S_r) < C_max(S_{r−1})` 时接受新优先序,否则保留旧解并终止迭代。这样得到的是**单调不增序列**,且下界为该染色体在无冲突约束下的理想完工时间,故必然有限步终止。这个论证是干净可写的。
- 复杂度:每轮 O(任务数 × 单次时间窗 Dijkstra),总计 O(R × N × |E| · W log)。

#### C2. PBS 式冲突驱动优先序搜索(增强版)

把 C1 的"规则重排"换成冲突驱动的二分:发现两任务冲突时,分裂出两个子节点分别强制两种优先关系,深度优先搜索优先序空间。技术本身已被 Sensors 2026 使用,**因此论文中必须把叙述重心放在"嵌入内循环 + 时序耦合 + 增量"上**,而不是 PBS 本身。建议只在"精英个体 + 每若干代"上启用(代价高),作为可选的强化档。

#### C3. 小规模 CBS 下界(强烈建议做,性价比极高)

对示例级算例,**固定指派与排序**,用 CBS(或其 makespan 变体)求该固定上层决策下的**最优无冲突路由**,得到下层最优性下界。这回答一个审稿人必问的问题:"你的贪心路由离最优有多远?"

- 若差距很小(如 <2%),这本身是一个**强结论**:说明贪心 PP 足够好,论文可以据此论证"把算力投在上层耦合而非下层最优化是正确的设计选择"——反而支持了现有架构;
- 若差距大,则 C1/C2 的必要性被数据证明。
两种结果都有用,这是一个**无论结论如何都能写**的实验,建议尽早做。

#### C4. LNS 式局部修复(与 E4 合体)

借 MAPF-LNS2:destroy 一部分任务的路径(RandomWalk 或"共享走廊"邻域),用 PP 重规划,接受改进。注意时序耦合下"撤销不会导致不可行"(允许无限等待 + 停靠不占资源 + 图强连通),故局部修复是安全的。

---

### 3.4 方向 D:扩大决策级反馈的作用面

**现状**:反馈只作用于 MA 一段(机器指派),四个子问题中只覆盖 1 个;且依赖 2.1 未修好的归因,常常无的放矢。

**核心设计思路**:拥堵有两种缓解方式——**换地方**(空间,改派)与**换时间**(时序,错峰)。现有实现只有前者。四个算子按 2.1 的归因标签**分派**,做到"对症下药"。

| 归因标签(来自 2.1) | 应派算子 | 说明 |
|---|---|---|
| 走廊让行 | D1 改派 + D2 错峰 | 空间或时间两条路 |
| 机器占用 | 传统 OS 重排 | 与拥堵无关,常规算子 |
| 上游工序 | 沿链继续回溯 | 不在本环节施策 |
| 车辆可用性 | D3 换车 | 运力瓶颈 |

#### D1. 拥堵驱动改派(已有,按 2.2/B1 升级评分)

保留现有结构,把评分函数换成无量纲、时空索引的版本。

#### D2. 拥堵驱动错峰重排(新,推荐)

当关键项被归因为"走廊 c 在时段 [t1,t2) 的让行"时,有两个操作方向:

```
(a) 提前发起:把该工序在 OS 中前移,使其运输任务抢占更空闲的时间桶;
(b) 错峰避让:找出在同一时段争用走廊 c 的另一个任务(从预约表可直接查到冲突对手),
    把**对手**所属工序在 OS 中后移,为关键任务腾出时段。
```

(b) 尤其有价值:它是**由下层冲突信息直接指认出的一对操作对象**,不是随机变异——这正是"决策级闭环"应有的样子,也是与通用 GA 变异算子的本质区别。实现上是 OS 段的定向移位,可行性天然保持(OS 任意排列都可解码)。

#### D3. 拥堵驱动换车(新,轻量方案)

不引入完整的 AD 编码段(见 A4 的理由),而是引入**稀疏的禁派集**:染色体增加一个可选字段 `forbid[(j,i)] ⊂ 车辆集合`,仅对被归因为"车辆可用性"或"空载段严重让行"的少数任务生效。解码时 `dispatch_rule` 在候选车集合中排除禁派车。

优点:染色体仍以 MA+OS 为主体,可解码性不受影响;搜索空间只在必要处扩张;可解释性强(能说清"为什么禁止这辆车")。

#### D4. 归因扩展(见 2.1,是 D 的前提)

---

### 3.5 方向 E:增量评价与局部修复(地基)

**现状**:`local_search` 每评估一个邻居就全量 `decode()`(重建预约表、所有路由重跑);规格 5.2 写明的 `release_all` 未实现。方向 A1、C1、C4、D2 都会显著增加评价次数,**没有 E 就都跑不动**。

#### E1. 预约撤销与撤销日志

```
ReservationTable 增加:
  undo_log: List[(cid, 区间)]              # 记录每次 insert
  release_all(task)                        # 删除某任务的全部区间
  checkpoint() -> token                    # 记录当前 undo_log 长度
  rollback(token)                          # 回退到检查点
```
`rollback` 支撑 A1 的试探性路由;`release_all` 支撑 C4/E4 的局部修复。当前底层是有序列表,单次删除 O(W),可接受。

#### E2. 前缀复用的增量重解码(推荐,收益最直接)

**关键性质**(值得写进论文的复杂度分析):现有解码器严格按 OS 从左到右单向推进,状态(`free`、`pos`、`ready`、`loc`、`avail`、预约表)只向前传递,**不存在回向依赖**。因此若邻居只改动 MA 的第 x 位(或 OS 中位置 ≥ x 的部分),则位置 x 之前的全部事件与预约**逐字段不变**,可以复用。

```
decode_incremental(基准解码轨迹, 改动位置 x):
  1. 恢复到位置 x 的状态快照(状态字段浅拷贝 + 预约表 rollback 到该点)
  2. 从位置 x 起重放剩余 OS
  平均节省 ≈ 1 − x/N;若定向改动关键路径末段,节省更多
```

实现代价:解码时每隔若干位置存一个轻量检查点(或全量存 N 个状态,小算例可接受)。这是纯工程改动,但它使 A1/C1/D2 的代价从"不可承受"变为"可承受",并且给论文提供了一条干净的复杂度改进结论。

#### E3. 下界剪枝(廉价,建议一并做)

改派前先算乐观下界:假设改派后不产生任何新等待,取 `t* + proc_time` 推算的最早完工时间;若下界已不优于当前最优,直接跳过解码。零风险,纯收益。

#### E4. LNS 式修复替代全量重解(进阶,与 C4 共用)

只撤销受影响任务的路径(改派工序的入/出运输 + 与其共享走廊时段的任务),重规划这些,其余保持。由于允许等待且图强连通,局部修复必定可行。

---

## 四、依赖关系、实施顺序与论文切分

### 4.1 依赖关系

```
      ┌──────────────── 第 0 阶段:地基(必做,小改动) ────────────────┐
      │  2.1 关键路径归因扩展   2.2 拥堵项量纲修正   2.3 口径统一      │
      │  E1 预约撤销/回滚       E2 前缀复用增量解码   E3 下界剪枝       │
      └───────┬──────────────────────┬─────────────────┬──────────────┘
              │                      │                 │
      ┌───────▼────────┐   ┌─────────▼────────┐  ┌─────▼──────────┐
      │ 第 1 阶段:诊断 │   │ 第 2 阶段:创新主体 │  │ 第 3 阶段:强化 │
      │ A1 精确试探派车 │   │ B1/B2 时空拥堵画像 │  │ C1 不动点迭代   │
      │ C3 小规模CBS下界│   │ D2 错峰重排        │  │ C2 PBS 优先序   │
      │ (决定后续投入)  │   │ D3 禁派集换车      │  │ B3 自适应 λ     │
      └────────────────┘   │ A3 候选集截断派车   │  │ C4/E4 LNS 修复  │
                           └──────────────────┘  └────────────────┘
```

**关键判断点**:第 1 阶段的两个诊断实验(A1、C3)成本都很低,却决定第 2、3 阶段的投入方向:

- 若 A1 收益显著 → A3 值得做,且"派车开环"可作为论文的一个独立贡献点;若收益微弱 → 诚实报告,方向 A 降为一段讨论;
- 若 C3 显示贪心路由接近最优 → **放弃 C1/C2**,把"下层贪心已足够"作为设计论证写出来(反而是有价值的负面结论),全力做 B/D;若差距大 → C 成为第二篇的主体。

**先做诊断、再定投入**,这是本方案最重要的一条策略建议。

### 4.2 论文切分建议

不建议把五个方向塞进一篇。按创新维度切分为两篇,各有独立的理论卖点:

**论文一:闭环耦合的完整化**(现有框架 + A + B + D + 地基)
- 主张:既有工作(含 2026 年最新竞品)的"闭环"止于一次性时刻表重定时;本文把三处开环残余全部闭合——派车决策感知路网状态、拥堵信号具备时空分辨率、反馈同时作用于指派与时序。
- 对手:Sensors 2026 的 Generate-then-Refine(引用其自认局限)、Sensors 2023 的两阶段 GA、本框架去反馈的消融版。
- 核心实验:拥堵度 × 异构度双因子;λ 自适应曲线作为机制内证据;逐环节闭合的增量消融(开环 → 评价级闭环 → +决策级闭环 → +派车闭环 → +错峰算子)。这条**递进式消融链**是论文一的骨架。

**论文二:时序耦合下的无冲突路由优化**(C + E + 理论)
- 主张:调度内嵌的路由不是标准 MAPF,存在"路径改变 → 就绪时刻改变 → 任务结构改变"的耦合;提出不动点迭代与接受判据,给出有限步终止论证与复杂度分析;用增量评价使其可承受。
- 对手:MAPF 侧的 PP / PBS / LNS 在本问题上的直接移植;小规模 CBS 最优下界。
- 卖点:理论性质(终止性、复杂度)+ 下层最优性差距的量化。

### 4.3 与规格文档的衔接

本文档的设计确定后,需回写规格文档的以下位置(避免三份文档失同步):

| 本文档小节 | 应更新的规格文档位置 |
|---|---|
| 2.1 归因扩展 | 6.5 第 1 步、`critical_path` 命名映射 |
| 2.2 量纲修正 | 6.5 评分公式、第七节 λ 默认值说明 |
| 2.3 口径统一 | 二.8、第九节校验器 (f)(g) |
| 3.1 A1/A3 | 6.3(`dispatch_exact` 由"未实现"改为具体设计) |
| 3.2 B1–B3 | 6.2 `congestion_stats` 定义、第七节参数表 |
| 3.3 C1–C3 | 第五节新增"下层优化"小节;第八节基线加 CBS 下界 |
| 3.4 D1–D3 | 6.4 算子表、6.1 编码(禁派集字段) |
| 3.5 E1–E3 | 5.2 预约表接口、6.2 解码器增量接口 |

---

## 五、需要你确认的三个设计取舍

1. **A4 与 D3 的关系**:我建议不引入完整的 AD 编码段(文献已有、且破坏可解码保证),改用稀疏禁派集(D3)。若你倾向与文献对标"三层编码",则 A4 需升为主方案,届时要补一套 AD 修复算子。
2. **C 的投入时机**:建议先做 C3(小规模 CBS 下界)再决定是否做 C1/C2。若你希望论文尽早具备理论章节,也可以先做 C1 的收敛性论证(纯理论,不依赖实验结论)。
3. **两篇切分是否接受**:若必须单篇投稿,建议取"论文一"范围,C 只保留 C3 作为一个下界参照实验(一段话 + 一张表),不展开为方法贡献。

---

> 相关工作核查依据:Sensors 2026, 26(2):543(JSP-T + DQN + PBS,自认 Generate-then-Refine);Sensors 2023, 23(9):4526(FJSP + 时间窗 Dijkstra 两阶段);Electronics 2026, 15(1):139(拥堵感知 AGV 车队调度,DES);Appl. Sci. 2025, 15(20):10948(FJSP-MTA,OS+MA+AD 三层编码);ESWA 2025(CADRL,派车用规则);MAPF-LNS(IJCAI 2021)、MAPF-LNS2(AAAI 2022)、LNS 再评测(SoCS 2024–2025);CBS makespan 变体(IFAAMAS 2025)。
