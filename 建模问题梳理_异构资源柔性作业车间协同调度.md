# 工业互联网环境下异构资源协同的柔性作业车间集成调度——建模假设逐条梳理

## 0. 课题定位与问题命名

**课题**:工业互联网环境下,异构资源(加工资源为机械臂 RA,运输资源为 AGV)协同的柔性作业车间**调度与 AGV 路径规划集成**问题。

**资源异构性的准确表述**:

- AGV 之间同质(能力、速度相同);
- RA 之间"能力部分重叠、能力集内效率异构"——每台 RA 只能加工工序的一个子集(由末端执行器、负载、精度等能力约束决定,即部分柔性),且同一工序在其可用 RA 之间的加工时间可以不同(不相关并行机式的时间结构);这与课题原始表述"Robot Arm 能力不完全相同"直接对应;
- AGV 与 RA 之间构成"运输—加工"两类异构资源的协同。

**问题命名建议**:异构机械臂柔性作业车间调度与 AGV 无冲突路径规划集成问题,英文可表述为 *Integrated scheduling of flexible job shop with heterogeneous robot arms and conflict-free AGV routing*,是 FJSP_PT(Xin 等 2025 综述)向"含路径规划"一类(综述 Table 1 第三类)的扩展。

**四个耦合的决策子问题**:

1. 工序—RA 指派(job routing / machine assignment);
2. 各 RA 上的工序加工排序(operation sequencing);
3. 运输任务—AGV 指派及各 AGV 上的任务排序(transportation task assignment);
4. AGV 在车间路网上的无冲突路径规划,含节点等待/让行决策(conflict-free routing)。

其中子问题 1–3 构成标准 FJSP_PT;子问题 4 及其与 1–3 的**双向耦合**(路由结果反馈影响指派与排序)是本课题区别于既有 FJSP_PT 文献的核心。

**创新性锚点**(据以取舍假设的原则):

- 运输时间不再是常数矩阵,而是路由决策与路网占用状态的函数;
- RA 时间异构使"路网状态改变指派决策"这一权衡真正有意义(通往某 RA 的路段拥堵时,可改派到路网更通畅、加工稍慢的 RA);
- 求解框架为调度层—路由层闭环协同,而非既有工作的"先排产、后路由"单向两阶段。

> 注意:"AGV 送达后不等待、由另一台 AGV 接力下一段运输"是 FJSP_PT 自 Bilge & Ulusoy(1995)以来的标准设定(运输任务的基本单元是单次搬运,任意车辆可承接),**不能**作为创新点表述,但应作为假设明确写出(见 C4)。

---

## A. 工件与工序

### A1. 物料 = 工件,不做类别划分

**推荐描述**:待加工物料以工件(job)为基本单位,编号 j = 1, …, n;不引入物料类别集合,类别差异(若有)完全由各工件自身的工艺路线与时间参数体现。

**建模意义**:模型保持标准 FJSP_PT 结构,避免额外的类别兼容约束;基准算例(Bilge、Deroussi、Homayouni 系列)可直接改造使用。

### A2. 每个工件有预先给定的工艺路线

**推荐描述**:工件 j 由 n(j) 道工序 O_j1 → O_j2 → … → O_j,n(j) 组成,序内严格先后;不同工件的工序数量与内容可以不同(一般 job shop 结构)。

**建模意义**:对应工艺先后约束(综述附录 A 的 A3、A4 型约束);"各工件路线可不同"是更一般的设定,flow shop 为其特例。

### A3. 加工与运输均不可抢占;工件不可拆分、工序不重叠

**推荐描述**:每道工序一旦开始加工不可中断;每个运输任务(含空载段与载货段)一旦开始执行亦不可中断或改派他车中途接力;每个工件是不可拆分的加工/搬运单元,不考虑批量拆分(lot streaming)与相邻工序时间重叠(overlapping)。

**建模意义**:保证工序完成时间 = 开始时间 + 加工时间、以及运输任务时间窗的简单结构;排除拆批后"多子批并行运输"与运输中途改派带来的组合爆炸。

### A4. 静态问题:全部工件 t=0 可用,无交货期

**推荐描述**:第一阶段研究静态(off-line)问题:n 个工件在 t=0 时刻全部已知、全部位于装卸站(LU)待运;不设释放时间与交货期。计划期内信息完备(工件、工艺、加工时间、路网与车辆状态在决策时全部已知)——工业互联网提供的实时感知能力在本阶段体现为"状态可知",动态到达、信息滞后、紧急插单等作为第二阶段的滚动重调度扩展。

**建模意义**:与绝大多数 FJSP_PT 文献一致,便于建立 MILP 模型并用基准算例对标;符号系统中预留 r_j(释放时间)、d_j(交货期)以便后续扩展拖期类目标。

### A5. 工件间无外生顺序要求

**推荐描述**:不设"某工件必须先于另一工件运输/加工"的外生硬约束(A1 已不引入物料类别);所有顺序或者来自工艺先后(硬约束),或者由优化决策产生。订单紧急程度(若需要)通过目标函数(加权拖期等)体现,此时须同时引入交货期 d_j(见 A4 预留符号与 E2)。

**建模意义**:保持可行域的一般性;紧急度进目标函数而非约束,避免不必要的不可行。

---

## B. RA(加工资源)

### B1. 部分柔性:每道工序有非平凡的可用 RA 集合

**推荐描述**:每道工序 O_ji 有可用机器集合 Ω_ji ⊆ 全部 RA(部分柔性,partial flexibility),由 RA 的末端执行器、负载、精度等能力约束决定;要求绝大多数工序 |Ω_ji| ≥ 2,保证指派存在自由度;全柔性(Ω_ji = 全部 RA)为特例。

**建模意义**:这是"RA 能力异构"的形式化,比全柔性更贴近实际车间(机械臂的工具库、负载与精度等级天然限定其可加工工序集合)。|Ω_ji| ≥ 2 的底线保证"路网拥堵时改派到稍慢 RA"的创新机制有回旋余地——若大量工序 |Ω_ji| = 1,指派自由度消失,路由—指派耦合无从谈起。该设定与 Brandimarte MK 等部分柔性基准算例直接兼容,全柔性算例(Kacem 类)作为特例覆盖。

### B2. 时间异构:加工时间依赖于 RA

**推荐描述**:对 m ∈ Ω_ji,工序 O_ji 在 RA m 上的加工时间为已知常数 t^P(j,i,m),同一工序在其能力集内不同 RA 上可以不同;在能力集内,除时间外不再区分其他差异(精度、质量、能耗不在基本模型中考虑,能力差异已由 B1 的 Ω_ji 承载)。

**建模意义**:即不相关并行机(unrelated machines)时间结构,是"能力集内效率异构"的全部数学内容;与 B1 的 Ω_ji 一起构成"能力异构 + 效率异构"的双层异构表述。它使子问题 1(指派)存在真正的权衡,也是路由—指派耦合有意义的前提(见 0 节创新性锚点)。**实验设计时应定义"异构度"因子**(如同一工序各 RA 加工时间的变异系数)并作为实验因子扫描。

### B3. RA 独占且一次一件

**推荐描述**:每台 RA 同一时刻至多加工一道工序。

**建模意义**:标准设备占用约束(析取约束/大 M 约束或序变量 Y 实现)。

### B4. 缓冲区容量无限

**推荐描述**:每台 RA 设有容量充足的输入/输出缓冲区:工件可在 RA 旁排队等待加工,完工件可暂存等待 AGV 取走;装卸站 LU 的原料/成品暂存区同样容量充足;上述缓冲均不阻塞 RA 与 AGV。

**建模意义**:该假设实际提供三重保证,必须作为独立假设条款写出:

- **两个时间量可独立递推**:完工件即刻移入输出缓冲,故"RA 空闲时刻"就等于该 RA 前一工序的完工时刻,与 AGV 是否到场无关;AGV 卸货即走,故"工件到达时刻"只由车辆行程决定,与 RA 忙闲无关。于是 工序开始时间 = max(工件到达时刻, RA 空闲时刻) 中的两个自变量可分别沿机器侧与车辆侧独立算出,整张时刻表能按事件时间单向推进解出;
- **排除死锁**:单载 AGV 的同站"先卸后取"(见七节快照③)要求有放置在手工件的缓冲位;无缓冲时会出现"AGV 持货等机器空出、机器上的工件等 AGV 来取"的循环等待;
- **解码良定义**:在 D3(允许节点等待)与 D4(时间窗预约消解路网冲突)同时成立的前提下,任意"指派 + 排序 + 任务链"编码按事件推进解码,必得完工时间有限的可行方案——这是 D5 闭环框架中上层高频调用适应度评价的前提。

**辨析(若去掉 B4,模型具体变成什么)**:变为 blocking job shop + AGV 一类问题(综述中 Brucker 2012、Zeng & Tang 2014、Heger & Voss 2017 属此类),约束结构发生实质变化:(a) 无输出缓冲时,工件完工后继续占据 RA 直至被取走,需为每道工序引入"离开时刻"变量(≥ 完工时刻),同机相邻工序的析取约束右端由"前工序完工"改为"前工件离开",而离开时刻由 AGV 日程决定——机器排序与车辆调度从"性能耦合"(只影响目标值)升级为"可行性耦合"(机器能否开工取决于车辆行为);(b) 无输入缓冲时,载货 AGV 须在取放点持货等待 RA 空闲,长时间占用停靠位甚至通行弧,与 D 节路网冲突相互放大;(c) 解码可能死锁,需内置死锁检测/避免机制,现有 FJSP_PT 基准与半主动解码规则均不再适用。另辨析一个易混淆点:即便无缓冲,单看某道工序,其开始时间在形式上仍是 max(到达, 空闲);但此时"空闲"依赖前工件被运走的时刻、"到达"受 AGV 被滞留的连锁影响,两个自变量不再可独立计算,死锁场景下递推甚至无有限解。**B4 的作用不在公式形式,而在使公式的自变量可独立计算、且递推必然可行。**

### B5. 上下料不占用 RA、装卸时间忽略

**推荐描述**:AGV 在 RA 处的装卸(交接)时间忽略不计,且交接不占用 RA 的加工能力(工件进出缓冲区不需要 RA 停止加工来配合)。

**建模意义**:切断"AGV 到达需等 RA 空闲"的额外同步耦合,避免机械臂兼任上下料带来的歧义;为文献通行假设(Homayouni & Fontes 2021)。本课题中 RA 是机械臂,此条尤其必须显式声明。

### B6. 无装夹/换型时间

**推荐描述**:基本模型不单独建模装夹时间(setup),视为已并入加工时间。若扩展,参照 Pal 等(2023):同机、同工件连续加工时 setup 为 0,其余情形 setup 为独立参数。

**建模意义**:控制基本模型复杂度;扩展路径明确。

### B7. RA 无故障、无维护窗口

**推荐描述**:计划期内 RA 全程可用,不考虑故障与预防性维护(综述通行假设);设备故障归入动态扩展。

**建模意义**:保持静态模型确定性;动态扩展时以"机器不可用时段"事件触发重调度。

---

## C. AGV(运输资源)

### C1. AGV 完全同质、数量有限

**推荐描述**:车间有 NA 辆同质 AGV:速度相同、单载(一次运一个工件)、无个体差异;NA 为给定参数(不作为决策变量),实验中扫描 NA 取值。**RA 数量 NM 与 AGV 数量 NA 均为有限值**,不假设任一方"足够多"。

**建模意义**:任务分配变量对所有车辆对称定义;NA 有限是子问题 3 存在的前提——NA 过大时问题退化为"仅考虑运输时间的 FJSP"(综述 Table 1 的退化边界)。**NA/NM 比值与负载水平共同决定瓶颈落在运输侧还是加工侧**:AGV 相对少时运输成瓶颈、空载调度与路由的价值大;AGV 相对多时路网冲突加剧但任务分配退化。该比值应与 F2 的其他因子一起纳入实验设计。

### C2. 匀速行驶,弧通行时间为常数

**推荐描述**:AGV 空载与载货同速、匀速;路网中每条弧(路段)的通行时间为已知常数;加减速时间忽略或并入弧时间。

**建模意义**:单车通过一条弧的时间确定,但**端到端运输时间不再是常数**——它取决于所选路径与途中等待(见 D 节),这是与标准 FJSP_PT 的本质区别。

### C3. 装卸时间忽略、电量不建模

**推荐描述**:取放货时间忽略(或并入弧时间);基本模型不考虑 AGV 电量与充电行为(即便实际车间可能设有充电桩,路网中亦不单独建模充电节点),留作扩展。AGV 无故障。

**建模意义**:聚焦调度—路由耦合主线;工业互联网背景下电量/充电是审稿人常问点,必须显式声明"不考虑"及理由(属次要因素,可由车队规模冗余吸收)。

### C4. 运输任务以"单次搬运"为单元,任意车辆可承接

**推荐描述**:运输任务定义为"将工件 j 从其第 i 道工序的完成位置搬运到第 i+1 道工序被指派的 RA"(或从 LU 到首道工序 RA,或末道工序完成后从末道 RA 回运至 LU,见 C6);每个任务可指派给任意一辆 AGV,同一工件不同段的搬运可由不同 AGV 接力完成;AGV 卸货后即空闲,可承接任何下一任务。

**建模意义**:这是 FJSP_PT 的标准任务结构(综述附录 A 约束 A14、A15),**是既有假设而非创新点**;"多车接力、送达即走"由该结构自然涵盖。同机连续工序不产生运输任务。

### C5. 卸货后的去向:按任务链空载就位,出发时刻与停靠位置为决策量

**推荐描述**:AGV 完成卸货后立即空闲,不强制空载返回起点(**此处仅指卸货后的空载去向,不含 C6 的成品回运任务——成品回运是独立的载货运输任务**)。静态计划已为每辆 AGV 排定任务链(任务的先后序列),因此卸货后的去向由计划决定:

- 若该车已有下一任务,则空载驶向下一任务的取货点;**空载段的出发时刻为决策量**——可立即出发、提前到达取货点停靠等待工件完工(即"预先就位"),也可在当前节点停留后再出发,由路由层结合路网占用状况确定;解码时默认"尽早出发、在取货点等待",但该默认策略可被路由层为消解冲突而覆盖(见快照④推迟出发);
- 若暂无后续任务,则在当前节点的停靠位待命,直至计划中的下一任务触发;
- 所有取放点/LU/路口节点设停靠容量充足的停靠位:空闲或等待中的 AGV 占用停靠位而非通行节点/弧,不阻塞他车通行(与 D2、D3 一致;允许多车同站停靠);
- 空载段与载货段都在路网上规划路径、都受冲突约束。

**建模意义**:一次任务的时间 = 空载路由时间 + 载货路由时间,空载段起点取决于该车上一任务的卸货位置——这使子问题 3(任务排序)与子问题 4(路由)相互耦合;中途卸货后无须额外强制空载回 LU(与 C6 成品回运任务无关)。

两点辨析:

- "AGV 被调度到下一个 RA 处等待接件"**正是**本模型的行为:任务链 + 空载段 + 提前到达等待,天然实现"预先就位",无须额外假设;
- 在运输时间为常数的标准 FJSP_PT 中,"在原地等再走"与"先走到取货点等"对 makespan 完全等价(只要不迟到),故文献不区分二者;但在含路网冲突的集成问题中,两种做法占用路网的时段不同、空闲停靠位置可能影响他车通行,因此**出发时刻与停靠位置成为真正的决策变量**——这是"集成建模才能捕捉、两阶段方法捕捉不到"的又一证据,可纳入创新性论证。空闲车辆的主动重定位(repositioning)在静态确定性模型中不必单独建模(有益的重定位必然是某个空载段的前缀),动态场景(后续任务未知)中才有独立价值,留作扩展。

### C6. 初始位置与成品回运

**推荐描述**:t=0 时全部 AGV 与全部工件位于装卸站 LU;末道工序完成后成品运回 LU,基本模型已计入(为每工件追加一个末端运输任务)。是否计入回运作为算例/模型开关 δ_return ∈ {0,1}(基本模型取 1;"不回运"变体取 0)。

**建模意义**:初始条件完备。须注意:Bilge/Homayouni 等多数 FJSP_PT 基准以末道工序完工计 makespan、并不强制回运;本课题基本模型计入回运后,与既有算法对标时应统一口径——或双方均计入回运,或另报"不回运"变体下的 Cmax(见 E1、F3)。

---

## D. 车间路网与无冲突路径规划(本课题新增部分)

### D1. 路网抽象为有向图

**推荐描述**:车间布局抽象为有向图 G = (V, E):节点集 V 包含各 RA 的取放点、LU、路口/汇合点;弧集 E 为可通行路段,每条弧有已知通行时间(由长度与 AGV 速度决定)。图的拓扑与弧时间由车间布局给定,计划期内不变;并要求 G 强连通(或至少任意两取放点/LU 之间存在有向通路),保证任意合法指派下运输任务均有可行路径。

**建模意义**:这是子问题 4 的载体;"两位置间运输时间"由常数矩阵 t(d1,d2) 变为"在 G 上选路 + 等待"的决策结果,是本课题对 FJSP_PT 的核心扩展。路网规模(节点/弧数、路径冗余度)成为新的算例参数。

### D2. 资源独占式冲突定义

**推荐描述**:区分**通行占用**与**停靠占用**:同一时刻,每条通行弧至多被一辆正在行驶的 AGV 占用;每个通行节点(路口/正在穿越的取放点)至多被一辆正在通过的 AGV 占用;禁止相向冲突与通行节点冲突;同向追赶通过占用时间窗自然排除(或规定同弧同向保持安全间隔)。与此相对,各节点另设停靠位(基本模型停靠容量充足):多车可同时停靠于同一节点的停靠位,停靠不占用该节点的通行资源、不占用通行弧(与 C5、D3 一致)——从而 t=0 全部 AGV 同在 LU、以及多车同站待命均不违反独占规则。时间窗约定为半开区间 [t_start, t_end):车辆在 t_end 时刻已释放该通行资源,另一车辆可于同一时刻开始占用(见七节 t=39 在 c1 的衔接)。

**建模意义**:冲突的形式化定义,是"无冲突路径规划"约束的全部内容;用通行节点/弧的**占用时间窗**(time window)表达,与文献主流(时间窗 Dijkstra/A*)兼容。若不区分停靠与通行,则"多车初始同在 LU"将与节点独占直接矛盾。

### D3. 等待是合法的路由决策

**推荐描述**:AGV 可以在节点停靠位停留等待以让行或等件;等待占用停靠位、不占用通行弧与通行节点,等待时长为路由层决策变量。

**建模意义**:引入等待后,无冲突路由问题必定有可行解(极端时串行通行);"路径 + 出发时刻 + 途中等待"共同决定一次运输的实际耗时,这正是运输时间成为决策函数的机制;亦是 B4 解码良定义的必要配套。

### D4. 死锁避免由路由层保证

**推荐描述**:路由层采用基于时间窗预约的规划方法(如时间窗 Dijkstra):每辆 AGV 出发前在路网上预约节点/弧的占用时段,新路径只能使用空闲时段,从机制上排除死锁与冲突;不采用"先走再避"的反应式避障。

**建模意义**:保证任何调度方案解码后可执行、时间可精确计算(确定性),从而调度层适应度评价是良定义的;这也回答了"避障是否影响调度结果"——在静态模型内不影响,因为冲突在计划时已消解。

### D5. 调度层—路由层闭环耦合(方法框架假设)

**推荐描述**:求解框架为双层闭环:上层(调度层)决策工序—RA 指派、RA 上排序、运输任务—AGV 指派与排序;下层(路由层)按上层方案为每个运输任务规划无冲突路径与等待,返回各任务的**实际**起止时间;上层以此评价方案优劣并迭代修改(包括因路网拥堵而改变 RA 指派)。区别于"先排产后路由、路由不反馈"的两阶段模式。

**建模意义**:这是创新性主张的方法层载体。实现上要求:(a) 解码规则保证任意编码可解出可行方案(按事件时间推进的半主动解码,防止机器—车辆循环等待);(b) 下层求解必须高效(被上层高频调用)。

### D6. 大扰动的处理边界

**推荐描述**:静态模型中不存在计划外避障(D4 已消解);执行期的大扰动(车辆故障、临时封路、紧急插单)定义为动态事件,触发局部重调度(与 A4 的动态扩展共用机制),重调度时兼顾与原方案的偏差最小(稳定性)。

**建模意义**:划清静态模型与动态扩展的边界;动态 FJSP_PT 的响应速度与重调度稳定性是综述列出的挑战,可作为课题第二阶段。

---

## E. 目标函数

### E1. 主目标:makespan

**推荐描述**:基本模型以最小化最大完工时间 C_max 为单目标,即所有工件成品回运至 LU 完成时刻的最大值(基本模型计入回运,见 C6);采用"不回运"变体时退化为末道工序完成时刻的最大值。

**建模意义**:综述统计 90% 以上单目标 FJSP_PT 文献采用 makespan。因多数基准不计回运,与既有算法对标时须统一口径(双方均计回运,或同时报告不回运变体的 Cmax),避免数值不可比。

### E2. 扩展目标

**推荐描述**:多目标扩展候选:AGV 总空载时间/空驶率、总等待(让行)时间、总能耗(RA 加工 + AGV 行驶分段建模)、加权拖期(须同时引入交货期 d_j,见 A4);处理方式(Pareto 或加权聚合)在扩展阶段预先确定。

**建模意义**:空载与等待时间是集成路由后**新增的可观测量**,天然适合作为第二目标,能直接展示路由层决策的价值;能耗目标衔接绿色调度文献(Liu 2019、He LJ 2022、Xu 2023)。

---

## F. 实验设计与验证(建模配套约定)

### F1. 算例生成规则

**推荐描述**:在 Bilge/Deroussi/Homayouni 等基准算例基础上扩展三类结构:(a) RA 异构时间矩阵——给定基准加工时间后按可复现规则(如乘以均匀分布扰动系数)生成各 RA 时间,以变异系数刻画"异构度";(b) 车间路网——按基准布局构造图 G,以路径冗余度/瓶颈弧数量刻画"拥堵倾向";(c) 可用机器集合 Ω_ji——按目标柔性度(平均 |Ω_ji|)随机生成,或直接沿用 MK 算例自带的部分柔性结构,保证绝大多数工序 |Ω_ji| ≥ 2(见 B1)。生成规则与随机种子公开,保证可复现。

**建模意义**:现有基准不含"能力集内时间异构 + 显式路网"结构(部分柔性本身 MK 系列已有,不算新结构),自定义算例的生成规则本身是实验部分的贡献点。

### F2. 关键实验因子

**推荐描述**:至少扫描五个因子:平均运输时间/平均加工时间之比(T̄t/T̄p,综述强调的特征参数)、RA 异构度、柔性度(平均 |Ω_ji|/NM)、AGV 数量 NA(及 NA/NM 比值)、路网拥堵程度(车流密度/瓶颈)。核心实验为"拥堵程度 × 异构度"双因子:量化闭环集成方法相对解耦两阶段方法的改进幅度。

**建模意义**:正面回答审稿人对"集成收益是否值得复杂度"的必然质疑;预期结论——拥堵越重、异构度越高,集成收益越大;两者都低时退化为标准 FJSP_PT,集成无益;柔性度是收益的"使能"因子——柔性度越高,拥堵时改派的回旋余地越大,而柔性度趋近 1(平均 |Ω_ji| → 1)时指派自由度消失,改派机制失效,集成收益随之衰减。

### F3. 最优性对标

**推荐描述**:小规模算例建立 MILP/CP 模型求最优解(CP 对该类问题有效,Ham 2020),作为算法解质量的下界参照;中大规模用元启发式(或 DRL)求解,并与既有两阶段方法(如时间窗 Dijkstra + GA 类工作)对比。与文献数值对比时统一 makespan 口径(均计回运,或均用不回运变体,见 C6、E1)。

**建模意义**:没有最优性参照,算法效果无从评价;与两阶段方法的对比是创新性主张的直接证据。

---

## G. 汇总表

| **#** | **条目** | **推荐描述(要点)** |
| --- | --- | --- |
| A1 | 物料分类 | 不分类;物料 = 工件 |
| A2 | 工艺路线 | 每工件固定路线,不同工件可不同 |
| A3 | 加工/运输性质 | 加工与运输均不可抢占;不可拆分、工序不重叠 |
| A4 | 静态/动态 | 第一阶段静态,信息完备,t=0 全部可用;动态作扩展 |
| A5 | 顺序要求 | 无工件间外生顺序;紧急度进目标函数(须有 d_j) |
| B1 | RA 柔性 | 部分柔性 Ω_ji(能力异构),\|Ω_ji\| ≥ 2 为主;全柔性为特例 |
| B2 | RA 异构 | 能力集内时间异构 t^P(j,i,m);定义异构度因子 |
| B3 | RA 占用 | 一次一件 |
| B4 | 缓冲区 | RA 的 I/O 与 LU 暂存均容量充足,无 blocking |
| B5 | 上下料 | 不占用 RA、时间忽略(机械臂场景必须显式声明) |
| B6 | 装夹 | 并入加工时间;扩展时参照 Pal 2023 规则 |
| B7 | 可靠性 | RA 无故障、无维护 |
| C1 | AGV 同质 | 同速、单载;NM 与 NA 均有限;NA/NM 比值纳入实验因子 |
| C2 | 行驶速度 | 匀速,空载载货同速,弧时间常数 |
| C3 | 装卸/电量 | 忽略装卸;不建模电量充电(显式声明) |
| C4 | 任务单元 | 单次搬运,任意车可接力(标准设定,非创新点) |
| C5 | 卸货去向 | 按任务链空载就位;出发时刻可被路由覆盖;停靠位容量充足 |
| C6 | 初始/回运 | 全部在 LU;δ_return=1 计回运(基本模型);对标须统一口径 |
| D1 | 路网 | 有向图 G=(V,E),强连通(或取放点可达),弧时间已知 |
| D2 | 冲突定义 | 通行节点/弧独占;停靠与通行分离;半开时间窗 |
| D3 | 等待 | 停靠位等待合法,是路由决策变量 |
| D4 | 死锁 | 时间窗预约式规划,计划期无冲突无死锁 |
| D5 | 耦合框架 | 调度层—路由层闭环,路由时间反馈指派决策 |
| D6 | 大扰动 | 定义为动态事件,触发局部重调度 |
| E1 | 主目标 | makespan(成品回运至 LU 完成时刻) |
| E2 | 扩展目标 | 空载/等待时间、能耗、拖期 |
| F1 | 算例 | 基准 + 异构时间 + 路网 + Ω_ji,规则可复现 |
| F2 | 实验因子 | T̄t/T̄p、异构度、柔性度、NA、拥堵度;核心为拥堵×异构双因子 |
| F3 | 对标 | 小规模 MILP/CP 求最优;与两阶段对比;统一回运口径 |

---

## H. 典型任务(算例)的完整参数表示

按前述假设体系,一个"典型任务"(即一个待求解的算例/实例)由以下参数与模型开关完全确定。给定这些量后,问题即完全定义;任何不在此列的量都是决策变量或由决策导出的结果,不属于算例参数。

### 一、系统要素定义(集合与索引)

一个算例首先声明四类要素:

1. **工件集合** J = {J1, J2, …, Jn},共 n 个工件;工件 Jj 含 n(j) 道工序,第 i 道记为 O(j,i)。全部工序总数记为 N。
2. **RA 集合** M = {RA1, RA2, …, RA_NM},共 NM 台机械臂;
3. **AGV 集合** K = {AGV1, AGV2, …, AGV_NA},共 NA 辆同质 AGV;
4. **车间路网** G = (V, E):节点集 V 包含装卸站节点 v0(LU)、每台 RA 的取放节点、以及若干路口/汇合节点;弧集 E 为可通行路段。每台 RA 通过位置映射 loc(m) 对应到唯一的取放节点;v0 是所有工件与 AGV 的初始位置。

其中 n、NM、NA、|V|、|E| 是刻画算例规模的五个基本量。

### 二、工件参数(Job Parameters)

#### 2.1 工序结构

每个工件给出其工序数量与固定的先后链:O(j,1) → O(j,2) → … → O(j,n(j))。工序结构只需给出"每个工件几道工序"即可(先后关系由编号顺序隐含);不同工件的工序数可以不同。

#### 2.2 工序的可选 RA 集合

每道工序 O(j,i) 有可选机器集合 Ω(j,i)。按本课题假设(B1,部分柔性),Ω(j,i) 为全部 RA 的子集(对应加工时间矩阵中的空项),且绝大多数工序 |Ω(j,i)| ≥ 2;全柔性算例(Ω(j,i) = 全部 RA)为特例。

#### 2.3 加工时间矩阵

核心参数:N 行 × NM 列的矩阵 P,其中 P[(j,i), m] 表示工序 O(j,i) 在 RAm 上的加工时间(正整数或正实数;若 m 不在 Ω(j,i) 中则记为空/无穷大)。

**行内差异即"RA 时间异构"**:同一行(同一工序)在 Ω(j,i) 内各列取值不同。生成规则(F1):先给每道工序一个基准时间,再对能力集内每台 RA 乘以独立的扰动系数(如从某区间均匀抽取);用**行内非空项(即 Ω(j,i) 内机器)变异系数的平均值**定义该算例的"异构度",作为实例特征参数记录。

### 三、运输参数(Transportation Parameters)

#### 3.1 路网结构与弧通行时间(取代"位置间行驶时间矩阵")

注意:在集成路径规划的设定下,**端到端的"位置间行驶时间矩阵"不再是原始参数**,原始参数是路网:

- 节点清单:每个节点的类型(装卸站/RA 取放点/路口)及其停靠位属性(默认所有取放点与停靠节点均带容量充足的停靠位,停靠不占用通行节点/弧,见 D2);
- 弧清单:每条弧的起讫节点、方向(单向弧或成对的双向弧)、通行时间 τ(e)(由弧长与 AGV 统一速度换算,为已知常数);G 须满足 D1 的可达性要求;
- 独占规则(D2):通行节点/弧独占;停靠与通行分离。

由路网可**派生**一个"无冲突理想条件下的最短路时间矩阵" t*(d1,d2)(对任意两取放点求最短路),它有两个用途:其一,作为运输时间的下界参与调度层的快速评价;其二,用全体工序的平均加工时间与该矩阵的平均值之比计算实例特征参数 T̄t/T̄p。但要明确:实际运输耗时 = 所选路径的弧时间之和 + 途中等待,由路由层决定,一般不小于最短路时间。

#### 3.2 运输任务的触发规则

运输任务不是算例的直接输入,而是由工件工艺结构与指派决策**派生**的对象,算例只需给出触发规则(与 C4、C5 一致):

- 工件 Jj 的首道工序被指派到 RAm 时,产生任务"从 v0 取 Jj,送往 loc(m)";
- 工序 O(j,i) 在 RAg 完成、下道工序 O(j,i+1) 被指派到 RAm(g ≠ m)时,产生任务"从 loc(g) 取 Jj 的半成品,送往 loc(m)";
- 相邻两道工序同机时不产生任务;末道工序在 RAm 完成后,产生回运任务"从 loc(m) 取成品,送往 v0"(基本模型计入;"不回运"为简化变体,见 C6)。

由此可知:一个方案中运输任务的**数量与起讫点都依赖于指派决策**(全同机指派时工序间转运最少,每道换机时最多;首道任务与回运任务件数则固定为各 n),这是本问题区别于"任务清单事先给定"的车辆调度问题(VRP 类)的重要特征。工序间/首道任务描述为五元组:(工件, 工序号, 取货节点, 送货节点, 就绪时刻),就绪时刻 = 前道工序完工时刻(首道为 0);回运任务单独描述为四元组:(工件, 取货节点=loc(末道 RA), 送货节点=v0, 就绪时刻=末道工序完工时刻)。

#### 3.3 AGV 参数

- 数量 NA(有限,实验中扫描);
- 速度:统一并已折入弧通行时间,空载载货同速,无需单独给出;
- 载具容量:1(单载);
- 初始位置:全部位于 v0;
- 装卸时间:0(忽略);电量:不建模。

### 四、初始状态与优化目标

- **模型开关**:δ_return ∈ {0,1}(是否计入成品回运;基本模型为 1,见 C6)。
- **初始状态**(t = 0):全部工件的原料位于 v0;全部 AGV 位于 v0 的停靠位(多车同站停靠合法,见 D2);全部 RA 空闲;路网通行资源无占用。
- **优化目标**:最小化 makespan——δ_return=1 时为全部工件成品回运至 v0 完成时刻的最大值;δ_return=0 时退化为末道工序完工时刻的最大值(见 E1)。扩展目标(空载时间、能耗、拖期)按 E2 处理。

### 五、实例特征参数(建议随算例一并记录)

每个算例除原始参数外,记录五个特征量,用于实验分组与结果分析(对应 F2 的实验因子):

| **特征参数** | **定义** | **作用** |
| --- | --- | --- |
| T̄t/T̄p | 平均最短路运输时间 / 平均加工时间 | 运输与加工的相对权重(综述强调的特征量) |
| 异构度 | 加工时间矩阵行内非空项变异系数的平均值 | 能力集内效率异构的程度 |
| 柔性度 | 平均 \|Ω_ji\|/NM | 指派自由度,"拥堵时改派"机制的回旋余地 |
| NA/NM | AGV 数与 RA 数之比 | 瓶颈落在运输侧还是加工侧 |
| 路网拥堵倾向 | 路径冗余度(节点对之间不相交路径数)/瓶颈弧占比 | 冲突发生的难易程度 |

### 六、一个完整的具体示例

**算例规模**:3 个工件、3 台 RA、2 辆 AGV。

**路网**(7 个节点、双向弧,弧上数字为单向通行时间):

- 节点:v0(装卸站)、c1、c2(路口)、r1、r2、r3(分别为 RA1、RA2、RA3 的取放节点),全部带停靠位;
- 弧:v0–c1(2),c1–r1(2),c1–c2(3),c2–r2(2),c2–r3(2),r1–c2(4)。
- 由此派生的最短路时间举例:v0→r1 为 4(经 c1);r1→r2 为 6(经 c2);r1→r3 为 6;r2→r3 为 4(经 c2)。注意 c1–c2、c2 节点是多条最短路的公共部分,是天然的冲突高发段。

**工序结构与加工时间矩阵**(本例取全柔性特例以简化演示,单位:分钟;行内差异体现能力集内时间异构):

| **工序** | **RA1** | **RA2** | **RA3** |
| --- | --- | --- | --- |
| O(1,1) | 4 | 6 | 5 |
| O(1,2) | 7 | 5 | 8 |
| O(1,3) | 6 | 9 | 5 |
| O(2,1) | 5 | 4 | 7 |
| O(2,2) | 8 | 6 | 6 |
| O(3,1) | 3 | 5 | 4 |
| O(3,2) | 6 | 4 | 7 |

即工件 J1 有 3 道工序,J2、J3 各 2 道,共 7 道工序;每道工序三台 RA 都能做,但时间不同(如 O(1,1) 在 RA1 上最快)。

**其余参数**:AGV 两辆,初始与全部原料同在 v0;目标为 makespan 最小。

**该算例的特征参数**:平均加工时间约 5.7,取放点间平均最短路时间约 5.3,故 T̄t/T̄p ≈ 0.9(运输与加工同量级,集成调度价值大);NA/NM = 2/3;异构度为各行变异系数均值(本例约 0.2);柔性度 = 1(全柔性特例,每道工序 3 台 RA 均可加工)。

**一个方案(解)由什么构成**——对照说明"参数"与"决策"的边界:

1. 指派:7 道工序各派给哪台 RA,例如 O(1,1)→RA1、O(1,2)→RA2、O(1,3)→RA3、O(2,1)→RA2、O(2,2)→RA3、O(3,1)→RA1、O(3,2)→RA2;
2. 排序:每台 RA 上工序的先后,例如 RA2 上依次为 O(2,1)、O(1,2)、O(3,2);
3. 运输任务派生与分配:上述指派派生出 10 个运输任务(3 个首道任务从 v0 出发,4 个工序间转运,3 个成品回运,如"从 r1 取 J1 送 r2""从 r3 取 J2 成品送 v0");每个任务派给 AGV1 或 AGV2,并在各车上排序成任务链;
4. 路由:每个任务的空载段与载货段在路网上的具体路径、出发时刻与途中等待。例如两辆 AGV 若同时需要通过 c1–c2 弧,后到者须在 c1 停靠等待——这段等待时间进入相关工件的流转时间,可能连锁推迟后续工序,甚至使"把 O(1,2) 改派到路网上更近的 RA1(时间 7)而非 RA2(时间 5)"成为更优选择。这正是"加工时间异构 × 路网冲突"耦合的直观体现,也是该示例设计的意图。

**小结**:一个典型任务 = 工件/RA/AGV 三个集合的规模 + 工序结构 + Ω 与加工时间矩阵(含异构) + 路网(节点、弧、弧时间、连通性、通行/停靠规则) + 运输任务触发规则 + 模型开关 δ_return + 初始状态 + 目标函数;其中运输任务清单、各任务起讫点、端到端运输耗时都**不是**输入参数,而是决策的派生结果。

### 七、方案执行的关键时刻快照(字符示意图)

以下针对第六节的示例,给出一个**可行方案**(用于说明机制,未必最优)的完整时刻表,并用字符图展示六个关键时刻的车间状况。

**路网底图**(弧上数字为单向通行时间;每个节点带停靠位):

```text
 [v0]──2──[c1]──2──[r1]
 (LU)       │        │
            3        4
            │        │
 [r3]──2──[c2]──2──[r2]
(RA3)      (路口)    (RA2)      注:r1 为 RA1 取放点
```

**示例方案的完整时刻表**(推演结果,已按节点/弧独占规则消解全部冲突):

| **资源** | **时间线** |
| --- | --- |
| RA1 | 4–8 加工 O(1,1);12–15 加工 O(3,1) |
| RA2 | 9–13 加工 O(2,1);18–23 加工 O(1,2);30–34 加工 O(3,2) |
| RA3 | 17–23 加工 O(2,2);27–32 加工 O(1,3) |
| AGV1 | 0–4 载 J1(v0→c1→r1);4–8 空载返回 v0;8–12 载 J3(v0→r1);12–18 载 J1(r1→c2→r2);18–24 空载(r2→c2→r1);24–30 载 J3(r1→c2→r2);30–34 停靠 r2 待 J3 完工;34–41 回运 J3 成品(r2→c2→c1→v0);此后停靠 v0 |
| AGV2 | 0–2 在 v0 让行等待;2–9 载 J2(v0→c1→c2→r2);9–13 停靠 r2;13–17 载 J2(r2→c2→r3);17–19 在 r3 择时待发;19–23 空载(r3→c2→r2,23 时刻整点到达);23–27 载 J1(r2→c2→r3,27 时刻卸 J1、同站取 J2 成品);27–34 回运 J2 成品(r3→c2→c1→v0);34–44 空载赴 r3(其中 36–39 在 c1 停靠让行);44–51 回运 J1 成品(r3→c2→c1→v0);此后停靠 v0 |

工件加工完成时刻:J2 为 23,J1 为 32,J3 为 34(加工阶段止于 34);成品回运完成时刻:J2 为 34,J3 为 41,J1 为 51;**makespan = 51**(以最后一件成品返抵 v0 计,见 E1)。

---

**快照① t = 1:起点弧独占,AGV2 让行等待**

```text
       A1(J1)»
 [v0]━━━━━━━━[c1]────────[r1]
  A2(J2)等待   │            │
               │            │
 [r3]────────[c2]────────[r2]
```

- AGV1 载工件 J1 正行驶在弧 v0→c1 上(占用时段 0–2);
- AGV2 已装载 J2,但弧 v0–c1 同一时刻只允许一辆车,只能在 v0 停靠位等待,t=2 才出发——这是**冲突在计划阶段被消解**(D2/D4)的最小例子:等待 2 分钟是计划的一部分,不是执行期的意外;
- 三台 RA 全部空闲;J3 的原料仍在 v0 缓冲区,暂无车辆运它。

**快照② t = 8:工件在缓冲区等车,车在别处执行任务链**

```text
 [v0]────────[c1]────────[r1]
  A1(空)到达               J1✔等车
                │  RA1: 刚完工  │
                │              │
 [r3]────────[c2]━━━━━━━━[r2]
                    »A2(J2)
```

- RA1 恰在此刻完成 O(1,1),J1 进入 r1 的输出缓冲区——**但此刻没有任何 AGV 在 r1**:两辆车都被任务链安排在别处,J1 将在缓冲区等待 4 分钟(B4 缓冲区假设使这一等待可行)——本帧是典型的**工件等车**;
- AGV1 空载刚回到 v0(4–8 空载返程),即将装载 J3;
- AGV2 载 J2 正行驶在弧 c2→r2 上(7–9),即将送达 RA2;
- 对称的**车等工件**见后文 30–34(AGV1 在 r2 预先就位等待 J3 完工)。运输能力有限时(NA/NM = 2/3),两种等待都会出现,其分布由任务链决策决定。

**快照③ t = 12:同站"卸货即取货",任务链的顺路衔接**

```text
 [v0]────────[c1]────────[r1] A1: 卸J3、取J1
                │  RA1: 开工O(3,1)│
                │              │
 [r3]────────[c2]────────[r2] A2停靠
                    RA2: 加工O(2,1)中
```

- AGV1 载 J3 到达 r1:卸下 J3(RA1 随即开工 O(3,1),12–15),并**在同一停靠位直接装上已等候 4 分钟的 J1**,开始执行任务"r1→r2"——空载段长度为零,是任务链排序优化的直接收益;注意这个"先卸后取"动作本身依赖 B4:J1 完工后已从 RA1 移入输出缓冲、J3 可卸入缓冲位,若无缓冲,单载的 AGV1 无处放下 J3 也就腾不出手取 J1,正是 blocking 模型中的典型交换死锁;
- AGV2 自 t=9 起停靠在 r2(卸下 J2 后暂无任务),RA2 正在加工 O(2,1)(9–13);
- RA3 空闲,等待 J2 到来。

**快照④ t = 19:相向冲突的让行——"何时出发"是决策变量**

```text
 [v0]────────[c1]────────[r1]
                │            │
                │       «A1(空) 在r1–c2弧上
                │            │
 [r3]━━━━━━━━[c2]────────[r2]
  A2(空)»          ⚠r2–c2弧:
  19时刻出发        A1已于18–20占用(反向)
```

- AGV1 空载执行"r2→c2→r1"返程(18–24),其中弧 r2–c2 的占用时段为 18–20;
- AGV2 停靠在 r3,它的下一任务是去 r2 接 J1(J1 于 23 时刻在 RA2 完工)。若 17 时刻(卸完 J2)立即出发,将在 19–21 沿 c2→r2 行驶,与 AGV1 在同一路段**相向相遇**;路由层因此让它**推迟到 19 时刻出发**(17–19 在 r3 择时待发),21–23 通过 c2→r2,**23 时刻整点到达 r2,恰逢 J1 完工**,实现零等待交接;
- 这一帧同时展示了三件事:相向冲突的消解(D2)、等待作为合法路由决策(D3)、"预先就位/择时出发"(C5)——在两阶段方法中,出发时刻根本不是变量,这类协调无从产生。

**快照⑤ t = 34:加工阶段终态,回运阶段并行展开**

```text
 [v0]────────[c1]────────[r1]
 A2刚卸J2成品    │       RA1空闲  │
                │              │
 [r3]────────[c2]━━━━━━━━[r2]
 J1✔等回运            »A1(J3)回运出发
 RA3空闲;J1自32起在缓冲   RA2: 刚完成O(3,2)
```

- RA2 在 34 时刻完成最后一道工序 O(3,2);AGV1 自 30 时刻起停靠 r2 预先就位,此刻装上 J3 成品启程回运(r2→c2→c1→v0,34–41);
- AGV2 恰在 34 时刻把 J2 成品卸回 v0(回运段 27–34):它在 27 时刻送 J1 到 r3 时,同站"先卸 J1、后取已等 4 分钟的 J2 成品"(再次依赖 B4),直接转入回运——空载段为零;
- J1 成品自 32 时刻起滞留 r3 输出缓冲,等待下一辆空闲 AGV 来取;加工阶段至此全部结束,但 makespan 尚未确定(见 E1:以成品返抵 v0 计)。

**快照⑥ t = 51:终态(makespan)**

```text
 [v0]────────[c1]────────[r1]
 A1停靠;A2刚卸J1   │       RA1空闲  │
 全部成品已回LU     │              │
 [r3]────────[c2]────────[r2]
                三台RA空闲;路网无占用
```

- AGV2 自 v0 空载赴 r3 接 J1 时,与 AGV1 的 J3 回运在 c1–c2 弧上相向冲突:AGV1 占用该弧 [36,39),路由层令 AGV2 于 [36,39) 在 c1 停靠让行,再于 t=39 起经 c1→c2→r3(39–44)到达——按 D2 半开时间窗,t=39 时 AGV1 释放 c1–c2/进入 c1→v0、AGV2 离开 c1,不构成节点冲突;**回运阶段同样受路网冲突约束**,不是加工结束后的"自由通行";
- AGV2 于 44 时刻取走自 32 起等候的 J1 成品,44–51 回运至 v0;AGV1 已于 41 时刻卸完 J3 并停靠 v0;
- 三件成品返抵时刻:J2 = 34,J3 = 41,J1 = 51;**makespan = 51**。
- 关键路径延伸至回运:加工阶段瓶颈(J3 迟至 12 才送达 → O(3,1) → AGV1 24 才来接 → O(3,2) 30 开工、34 完工)之后,J1 成品因两车皆忙、且回程让行,又多等 12 分钟才被取走。**回运计入后,makespan 由"最后一件成品的回运完成"决定,运输侧瓶颈进一步显性化**——若增加一辆 AGV 或调整末道指派/回运任务排序,此链条即改变。

---

**B4 缓冲区在本方案中的实际使用情况**(对照说明假设与示例的关系):

- **输出缓冲被多次触发**:J1 在 r1 等车 4 分钟(8–12)、J3 在 r1 等车 9 分钟(15–24);成品侧 J2 在 r3 等回运车 4 分钟(23–27)、J1 在 r3 等回运车 12 分钟(32–44)、J3 在 r2 等车 0 分钟(AGV1 预先就位)。其中最"硬"的依赖是 t=27 的同站交换(见快照⑤叙述):AGV2 载 J1 抵达 r3 时,须先卸下 J1 再取走自 23 起等候的 J2 成品——若无输出缓冲,J2 仍占住 RA3,单载 AGV2 无处放下 J1,形成交换死锁;有缓冲则 J2 早已离机,卸 J1 与取 J2 可在同一停靠位完成。J1 成品在 r3 再等 12 分钟(32–44)直至回运,同样依赖输出缓冲;
- **输入缓冲在本方案中从未被触发**:逐一核对全部 7 道工序,每次工件送达时目标 RA 均已空闲(开始时间的 max 全部取"到达时刻"一支)。这不是巧合,而是运输侧为瓶颈(T̄t/T̄p ≈ 0.9、NA/NM = 2/3)算例的必然特征:瓶颈在车,总是"机器等件"而非"件等机器"。当 AGV 增多或 T̄t/T̄p 变小、加工侧成为瓶颈时,工件才会在输入缓冲排队;但"卸货即走"(C4/C5)在任何算例中都以输入缓冲存在为前提,它是可行性保证,与本例是否用到无关;
- 此外,快照③ t=12 的同站"先卸后取"依赖缓冲位(见该快照说明)。

**全程甘特总览**(每字符 1 分钟;A/B/C = 载运或加工工件 J1/J2/J3,e = 空载行驶,w = 等待/择时待发,· = 空闲/停靠;回运段同样用 A/B/C 标记):

```text
 时刻      0         10        20        30        40        50
           012345678901234567890123456789012345678901234567890
 RA1       ····AAAA····CCC····································
 RA2       ·········BBBB·····AAAAA·······CCCC·················
 RA3       ·················BBBBBB····AAAAA···················
 AGV1      AAAAeeeeCCCCAAAAAAeeeeeeCCCCCC····CCCCCCC··········
 AGV2      wwBBBBBBB····BBBBwweeeeAAAABBBBBBBeewwweeeeeAAAAAAA
```

从甘特图可直接读出:两辆 AGV 在加工阶段近乎满载,回运阶段仍持续占用(AGV2 的 36–39 让行、44–51 载 J1 回运);RA 自加工结束后全部空闲,而 makespan 被回运尾巴拉长至 51——印证计入成品回运后,运输由"加工完工"转为"最后一件返抵 LU",运输侧瓶颈进一步显性化。

---

## I. SCI 小论文中的假设精简写法(可直接改写入 Problem formulation)

篇幅受限时,**不要**逐条展开 A1–F3。建议在 *Problem description* / *Problem formulation* 中采用「一段总述 + 一条符号约定 + 一组编号假设(8–12 条)」,把创新相关假设写实,通行简化用半句合并。下文给出可粘贴的英文稿(SCI 常用)及中文对照;符号与正文 A–E 节一致。

### I.1 版面结构建议(约 0.5–0.8 栏)

1. **问题一句定义**(含四个子问题 + makespan 口径);
2. **Notation**(小表或行内符号,只列后文公式要用的);
3. **Assumptions**(编号列表 8–12 条;每条 1–2 行);
4. **一句界定**(相对 FJSP_PT / 两阶段方法多了什么;充电、故障、有限缓冲等不在本文范围)。

实验因子(T̄t/T̄p、异构度、柔性度等)放 *Computational experiments*,不必塞进 Assumptions。

### I.2 符号速查(论文 Notation 用,可再压缩)

| Symbol | Meaning |
| --- | --- |
| $J=\{1,\ldots,n\}$, $O_{ji}$ | jobs; $i$-th operation of job $j$ |
| $M=\{1,\ldots,N_M\}$, $\Omega_{ji}\subseteq M$ | robot arms (RAs); eligible set for $O_{ji}$ |
| $t^P_{jim}$ | processing time of $O_{ji}$ on RA $m\in\Omega_{ji}$ |
| $K=\{1,\ldots,N_A\}$ | identical unit-load AGVs |
| $G=(V,E)$, $\tau_e$ | shop floor digraph; travel time on arc $e\in E$ |
| $\mathrm{LU}\equiv v_0$, $\mathrm{loc}(m)$ | load/unload station; pickup/delivery node of RA $m$ |
| $\delta_{\mathrm{ret}}\in\{0,1\}$ | whether finished jobs return to LU (baseline: $1$) |
| $C_{\max}$ | makespan: last return completion if $\delta_{\mathrm{ret}}=1$; else last operation completion |

### I.3 英文稿(推荐直接用于 SCI 短文)

**Problem.** We study integrated flexible job-shop scheduling with heterogeneous robot arms (RAs) and conflict-free AGV routing: (i) assign each $O_{ji}$ to an RA in $\Omega_{ji}$; (ii) sequence operations on each RA; (iii) assign transport tasks to AGVs and sequence them; (iv) plan conflict-free routes (with waits) on $G$. The objective is to minimize $C_{\max}$ with $\delta_{\mathrm{ret}}=1$ (finished jobs return to LU); results with $\delta_{\mathrm{ret}}=0$ may be reported for comparison with classical FJSP_PT benchmarks.

**Assumptions.**

1. **Static & complete information.** All $n$ jobs are available at LU at $t=0$; release dates and due dates are not considered. Processing data, layout $G$, and vehicle states are known when planning.

2. **Jobs.** Each job $j$ has a fixed precedence chain $O_{j1}\to\cdots\to O_{j,n(j)}$. Operations are non-preemptive; jobs are unsplittable (no lot streaming / overlapping). No exogenous precedence among different jobs.

3. **RAs (heterogeneous, partially flexible).** Each $O_{ji}$ may be processed on any $m\in\Omega_{ji}$ (partial flexibility; $|\Omega_{ji}|\ge 2$ for most operations; total flexibility $\Omega_{ji}=M$ is a special case). Processing times $t^P_{jim}$ are machine-dependent (*unrelated*). Each RA processes at most one operation at a time; RAs are failure-free in the planning horizon; sequence-dependent setups are not modeled separately (absorbed in $t^P_{jim}$).

4. **Buffers & handling.** Input/output buffers at each RA and storage at LU have sufficient capacity (no blocking). AGV loading/unloading times are negligible and do not occupy RA capacity.

5. **AGVs.** $N_A$ identical unit-load AGVs with constant speed (empty = loaded); acceleration and battery/charging are ignored. A transport task is a single move (LU→first RA, between successive RAs if machines differ, or last RA→LU when $\delta_{\mathrm{ret}}=1$) and may be executed by any AGV (relay across segments is allowed). After unloading, an AGV need not return empty to LU; empty repositioning follows its task chain (start time may be delayed by routing).

6. **Network & conflict-free routing.** Layout is a strongly connected digraph $G=(V,E)$ with known arc times $\tau_e$. Traversing nodes/arcs are exclusive (half-open time windows); parking berths are separate and capacious enough for multiple idle AGVs. Waiting at berths is allowed. Routes are planned by time-window reservation (no reactive deadlock resolution in the static model).

7. **Solution framework (scope).** A schedule-layer solution (assignment, sequencing, AGV task chains) is evaluated by a routing layer that returns conflict-free travel/wait times; these times feed back to the schedule layer (closed loop), as opposed to one-way “schedule-then-route”.

8. **Out of scope (extensions).** Machine/AGV failures, finite buffers/blocking, energy-aware charging, and dynamic arrivals are left for future work; they do not alter the baseline model above.

*(可选收尾句)* Assumptions 1–2, 4–5 (except finite $N_A$) follow common FJSP_PT practice; the distinctive baseline choices are machine-dependent times on partially flexible RAs, explicit conflict-free routing on $G$, closed-loop schedule–route coupling, and $\delta_{\mathrm{ret}}=1$.

### I.4 中文对照(开题/中文稿可用;SCI 仍以 I.3 为准)

**问题.** 异构机械臂柔性作业车间调度与 AGV 无冲突路径规划集成:子问题为工序—RA 指派、RA 上排序、运输任务—AGV 指派与排序、路网上含等待的无冲突路由。目标为最小化 $C_{\max}$(基本模型 $\delta_{\mathrm{ret}}=1$,成品回运 LU)。

**假设.**

1. **静态完备信息:** $t=0$ 全部工件在 LU 就绪;不计释放/交货期;计划期数据已知。
2. **工件:** 固定工艺链;加工不可抢占、不可拆分、无重叠;工件间无外生顺序。
3. **RA:** 部分柔性 $\Omega_{ji}$(多数 $|\Omega_{ji}|\ge 2$);能力集内时间异构 $t^P_{jim}$;一机一事;无故障;装夹并入加工时间。
4. **缓冲与交接:** RA 的 I/O 与 LU 容量充足(无 blocking);装卸时间忽略且不占用 RA。
5. **AGV:** $N_A$ 辆同质单载、空载载货同速;不计加减速与电量;任务为单次搬运(含回运),可跨车接力;卸货后不强制空载回 LU。
6. **路网:** 强连通有向图 $G$,弧时已知;通行节点/弧独占(半开时间窗),停靠与通行分离且停靠容量充足;允许停靠等待;时间窗预约式无冲突路由。
7. **框架:** 调度层—路由层闭环,路由实际时间反馈指派/排序。
8. **本文不讨论:** 故障、有限缓冲/blocking、充电、动态到达(留作扩展)。

### I.5 再压到「一段话」时的写法(极紧篇幅)

若编辑部对列表也敏感,可用下面一段代替 I.3 的编号列表(仍建议保留 Notation 小表):

> We consider a static instance with complete information: $n$ jobs released at LU at $t=0$, each with a fixed non-preemptive operation chain. Eligible RAs $\Omega_{ji}$ encode partial flexibility and processing times $t^P_{jim}$ are machine-dependent; each RA handles one job at a time, with ample I/O (and LU) buffers and negligible handling that does not seize the RA. A fleet of $N_A$ identical unit-load AGVs moves jobs on a strongly connected layout $G=(V,E)$ with constant arc times; traversing resources are exclusive, parking is separate and sufficient, and waiting is allowed. Transport tasks are single moves (including return to LU), assignable to any AGV. Setups, failures, charging, and blocking are omitted. Routes are computed under time-window reservation and returned to the scheduling layer, so travel times are decision-dependent rather than a constant matrix. The objective is $C_{\max}$ with finished-job return ($\delta_{\mathrm{ret}}=1$).

### I.6 与正文长文档的对应(防漏项自检)

| 精简假设条 | 覆盖的正文条款 |
| --- | --- |
| 1 静态完备 | A4, A5(无外生顺序的静态部分), 工业互联网信息假设 |
| 2 工件 | A1–A3, A5 |
| 3 RA 异构/柔性 | B1–B3, B6, B7 |
| 4 缓冲与交接 | B4, B5 |
| 5 AGV 与任务 | C1–C6(含 $\delta_{\mathrm{ret}}$), A3 运输不可抢占 |
| 6 路网冲突 | D1–D4 |
| 7 闭环框架 | D5; 0 节创新锚点 |
| 8 扩展边界 | D6, E2, B6 扩展, C3 充电等 |

E2 多目标、F1–F3 实验约定**不要**写入 Assumptions;在 Experimental design 用 3–5 句说明即可。