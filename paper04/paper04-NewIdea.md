# 小论文写作参考：任务图覆盖不到的级联损坏——时空预约影响闭包

> **本文档的性质。** 后补的立论备忘，写于 `paper04.tex` 初稿之后。体例对齐 `paper02/新想法.md`，与 `paper01/paper01-NewIdea.md` 配套阅读。
>
> **本文档相对现稿的增量，主要在「查新」一节。** 现稿的相关工作只引了两篇重调度综述（Vieira 2003、Ouelhadj 2009）与几篇右移/滚动时域，其余是从 paper01 借来的引文库。而 paper04 的核心主张是一句**针对整个文献的全称否定**——「局部重调度文献一直在任务依赖图上划影响域」。本次补做了四条线的检索，**查出两条真威胁**（铁路 alternative graph、MAPF Replan Affected），其中一条现稿完全没有引用。查新结论直接改变了本文可以宣称什么，见「由此产生的硬性要求」。
>
> **编号纪律。** 「结论一…结论十五」是本次据 `STRC/experiments/` 落盘产出与 `cursor_paper04.md` 对话记录重新编号的，不是历史台账。
>
> **2026-08-16 修订（一）。** 本备忘的「待决事项与风险」已按优先级逐条动手，`paper04.tex` 相应改完。**实验规模已由 $3\times5=15$ 对扩到 $5\times10=50$ 对**，旧稿全部 `/15` 读数作废；新增**结论十六至二十二**七条。
>
> **2026-08-16 修订（二）：一次复核推翻了上一版最重要的那条结论。** 对 `paper04.tex` 做逐句核对时查出三处与数据不符的表述，其中一处代价很大：**E2b 的 $24/50$ 不是「依赖边集覆盖不到物理耦合」，而是一处实现与定义的粒度错配**。修掉之后 **E2b 变成 $50/50$**（详见结论二十三）。据此上一版的结论十七、以及由它派生的「首要后续工作＝加细边集」全部作废。另修正「A 类上闭包严格更大」（实为 $81/100$ 严格、$19$ 格相等，包含仍是 $100/100$）与 E4 的「均值被单个种子翻转」（算术错误，均值与中位同向）。现稿 $20$ 页，无未定义引用、无 overfull、无 acmart 警告。逐条见「待决事项与风险」，逐节落点见末节「现稿结构」。
>
> **2026-08-18 修订（三）：补上算例出处这一条，并点破 E4 负结果是混淆的。** 起因是 paper01 那边做完公开数据集之后回头对照，发现 paper04 全文**一个字都没交代算例来源**——既没说算例自建，也没说为什么不能换公开算例，而 paper01 有一条论证充分的同类局限。同时读两篇的审稿人会看出这个不对称。**本轮不跑新实验**，只补两处文字：局限新增第 9 条（算例自建；执行期扰动这一支无公开基准；静态公开族发布的是行程时间矩阵，两族共 $11$ 张布局无一能还原成无向走廊图，故 $R$／$G_R$／闭包／B 类扰动在其上无从定义；**且本文没有 paper01 那条「退化档」退路**——退化掉争用，研究对象本身就不存在），§7.3 新增第五件（**只借拓扑**：Lyu 等 2019 附录布局可逐项转录，但工件数据不能一并搬入）。**顺带查出一件此前未记的事**：E4 那三张「同规模、只差走廊拓扑」的布局 `mid`/`high`/`funnel` 其实同属哑铃一族，彼此只差 LU 出口与中段的并行通道数（只差容量、不差几何），所以结论二十那个负结果是**混淆**的——分不清是两个指标太粗，还是同族变体的结构差异本就不足以被任何指标分辨。已在局限 9 末句点破。局限由九条变十条、后续工作由四件变五件，原局限 9（共享代码栈）顺延为 10。

> **2026-08-18 修订（四）：真跑了外部布局，上一轮那个「混淆」被测掉了，E4 的负结果由此变硬。** 上一轮只补文字、没跑实验，留下的解法是「只借拓扑」。本轮把它做完：从 `TUE-EE-ES/TJSP-toolset` 取到 Lyu 附录 A 六张布局的机读编码（三行：网格尺寸／`[装货站 m1..mk 卸货站]`／缺边），落在 `clbs/database/raw/tjsp_toolset/`，登进 `MANIFEST.csv`；生成器加 `pubgrid` 布局与 `PUB_LAYOUTS` 预设，`tools/check_pub_layouts.py` 重读原始 `.data` 逐字段对账（含「缺边确实是四邻接边」一项，防编号口径错位），六张全过。按 S8x4x4 同口径生成五个算例（`LyuL1` 因 $F\cdot NM=1.8<2$ 与 B1 冲突而跳过），新建 `STRC/tools/pub_batch.py` 跑 $5\times10=50$ 对，**另开账本不并入主批次**（混进去会让全部 `/\NPairs` 读数改口径）。**同日补做数据归位：** 这批数据与算例在 `STRC/database/` 存一份镜像（$19$ 文件 $84.5$ KB），`pub_batch` 直接读它，使 paper04 的数据与其算法代码同处一地；镜像一律由 `tools/sync_database.py` 生成并记 SHA256，`--check` 逐字节查漂移，**真值仍在 `clbs/database/`**（原始 URL、许可、各族可用性判定都在那里）。换读镜像后重跑全批，确定性结果逐字节不变（E1／E4 的 CSV 相同，E2／E3 只差挂钟列，E5 的 R2 一侧相同），全部 `\Pub*` 宏未受影响。
>
> **两个方向相反的结果，合起来才是完整的诊断。** 其一，闭包占比在外部算例上极差 $0.207$，而**逐参数同口径**的三张自建布局只有 $0.051$（⚠ 当时据此写了「随布局显著变化、责任在算例集」，**已被修订五推翻**：这 $0.207$ 的两个端点共享同一张路网，测的是机器落位不是路网几何）。其二，指标并未因此得救：这五张外部布局的 **LU 最小割恒为 $2$、漏斗占比恒为 $0$**，因为**这一族**把装卸站放在网格角点、四邻接下角点恰两条出边（**不是**「公开布局一律如此」——反例见结论二十六的自我修正）。一个恒定量不可能解释一个极差 $0.207$ 的量，所以在真实布局上这两个指标连「相关性弱」都谈不上。据此把结论二十那句含糊的观察收紧为一条明确否证：**闭包规模不由装卸点处的静态割决定**（结论二十六）。
>
> **主张一条没被推翻。** E1／E2a／E2b／E3／E5 在外部布局上逐项复现（$50/50$、$50/50$、$50/50$、$0/50$ vs $50/50$、$18/18$ 无交叉；**E5 那一项在修订七后为 $17/18$ 更优 + $1$ 点持平，仍无一点 R2 更优**），见结论二十七。正文新增 §6.10「外部布局批次的复现」与 `tab:e4pub`，局限 8 与 9 重写（9 由「算例自建」改为「布局出处已外部化，但边权与工件数据仍自建」），后续工作第五件（已做完）换成「给闭包规模找一个时序刻画」＋「把时间标定也外部化」两件，共六件。

> **2026-08-18 修订（五）：审查「哪几节用公开数据」时发现修订四的正面论断是数据不支持的，已订正；否证反而变干净了。** 起因是复查实验章的数据分配，顺手把六个原始布局文件全解析了一遍，结果发现**五个外部算例只落在两张路网上**：`LyuL2`($4$ 机)与 `LyuL3`($5$ 机)共用 $4\times4$ 网格并删同一条边 $(7,11)$，`LyuL4/5/6`($6/7/8$ 机)共用 $5\times5$ 网格并删同两条边 $(8,13)(19,20)$。而修订四拿来当证据的极差 $0.207$ 恰好等于 $0.512(\text{LyuL2})-0.305(\text{LyuL3})$——**两个端点是同一张路网**。于是「闭包占比随布局显著变化」这句话没有数据支持，必须撤。
>
> **撤掉之后论证比原来强。** 原来的形式是「换布局→占比变→割没变」，「换布局」一步夹着路网与落位两个变量；现在是「**路网一字不动**、只换机器落位→占比变 $0.207$→而割由定义不可能动」，混淆被消掉，静态割的否证变成单变量的。代价是适用范围写窄：否证的是「静态割能解释闭包规模」，**不是**「路网几何与闭包规模无关」（后者只有两个独立样本，无权下结论）。副产品是给后续方向找到了正面依据：路网不变而落位一变占比就大幅移动，说明起作用的是**需求落在哪里**，这正好支持「改用 $t_{now}$ 邻域的时序密度」那条候选刻画。
>
> **改动清单。** 新增宏 `\NPubNets`$=2$、`\PubCloSpreadBig`$=0.075$；§6.1 增一段披露两张路网并声明「路网几何这一维的独立样本是 $2$ 而非 $5$」；§6.7 重写正面论断（改为同路网内换落位的对照），并按组重排 `tab:e4pub`（$\mathrm{N}_1/\mathrm{N}_2$ 分组、极差按组内给）；§6.11 小结与局限 8 同步收窄；后续工作第五件补上需求侧的正面依据。**另修一处独立缺陷：`tab:e4` 与 `tab:e4pub` 口径不同却共用 `funnel`/`high` 两个布局名**（前者 $12$ 工件/$8$ 机/$12$ 车/比值 $4.0$、$5$ 种子，后者 $8$/$4$/$4$/$1.0$、$10$ 种子，同名行数值差 $2.4$ 倍），已在 `tab:e4` 表题写明两表不可跨表比较。局限 9 另补一句：外部批次只复现 E1/E2/E3/E5，**E6 与规模扫描没有外部布局支撑**。

> **2026-08-20 修订（六）：把两批算例的证据结构显式写进正文。** 起因是被问「实验章哪几节用自有数据、哪几节用公开数据、合起来说明什么」，答得出来，但**正文里答不出来**——这层意思此前散在 §6.1（三处口径）、§6.7（为什么需要外部批）、§6.10（外部批的意义）、局限 9（为什么公开族当不了对标集）四处，每处都对，读者得自己拼。本轮**不跑实验、不动任何读数**，只补两处综述文字。§6.1 末尾新增「两批算例各自承担什么」一段：主批次回答**现象是否存在、机制是否干净**（E1／E2／E3），这要求对算例参数有完全控制权，尤其是运输/加工时长比——它决定走廊争用是否成立，而公开族给不了；第二批回答**现象是否只在本文自选的布局上成立**，故只用于复现、不产生新性能读数。§6.11 小结末条同步扩写。
>
> **同时点明两条此前没写出来的元判断。** 其一，**第二批在全文中只用于收窄主张、未曾用于支持任何一条**：它唯一改写的结论是结构可预测性，且改写方向是让本文少主张一件事。这本身是可信度论证——若外部数据只用来复现漂亮读数，恰说明它没起检验作用。其二，**只有 E4 一格必须两批合读**：主批次三张同规模布局同属一族、只差容量不差几何，「指标太粗」与「算例集几何差异不足」在它们身上混着；而外部布局若没有逐参数同口径的自建对照，也无从判断 $0.207$ 这个幅度算不算大。其余各节的结论由主批次单独成立，第二批只增强外部效度。**用词注意：**「分工」一词正文已被 R2 与 R0+ 的角色分工占用（§5.3、§6.8），故这段一律写作「各自承担什么」，不复用该词。

> **2026-08-20 修订（七）：修订二那个粒度错配还剩一层，E2b 结构性地看不见它；修掉之后全部读数重生成。** 起因是查 R0+ 这一臂是否真的在解恢复问题，顺手写了一项独立审计——逐条比对 $t_{\mathrm{end}}\le t_{\mathrm{now}}$ 的预约在修复前后是否被改写（按假设 A2 必须为 $0$）。结果 **R2 自己在 $36/50$ 个格子上改写了历史，合计 $86$ 条**。成因是修订二只把粒度从「按工序」细到「按段」，而一个**跨越 $t_{\mathrm{now}}$ 的运输段**仍是整段重规划：只要它有一条走廊腿结束于 $t_{\mathrm{now}}$ 之后，整个段的标签就进释放集，于是它已经走完的那几条腿也被推到从 $t_{\mathrm{now}}$ 重走——等于把车退回段起点。**E2b 查不出来是结构性的**：这些腿的标签在闭包**内**，本就不在外侧字段的比对范围里。修法是再细一层到**走廊腿**：`_precommit_frozen` 连闭包内已执行完的腿一并占位，`_reroute_tail` 只从车在 $t_{\mathrm{now}}$ 实际所处的节点往后接。
>
> **修完是净改善，没有一项主张被削弱。** 审计 $86\to0$（$50/50$ 全过）；E1、E6、E4 的读数**一字未变**（纯闭包测量不经过修复）；E2a／E2b／第 1 级可行／E3 的 $50/50$ 全部保持；$C_{\max}$ 在 E2/E3 的 $6$ 个格子上变好、$0$ 个变差，规模扫描 $100$ 行里约 $43$ 行变好、**仅 $1$ 行变差**（$56\to57$）；一个原需 $3$ 轮扩域的格子变成 $0$ 轮。代价是拼接多做占位与更长的表搜索：**修复耗时中位 $2.8\to3.1$ ms、最大 $5.2\to15.0$ ms，加速比 $110\times$–$4341\times\to87\times$–$3051\times$**，量级未变。稳定性反而变好：**预约改动比例 $59\%$–$66\%\to57\%$–$60\%$**。
>
> **改动清单。** 宏 `\MsRepairMed` $2.8\to3.1$、`\MsRepairMax` $5.2\to15.0$、`\ResFracRtwo` $59\%$–$66\%\to57\%$–$60\%$、`\SpeedLo` $110\to87$、`\SpeedHi` $4341\to3051$、`\PubEfiveNoCross` $18/18\to17/18$；摘要那处**硬编码**的 $2.8$ ms 与 $59\%$–$66\%$ 同步改（它没用宏，容易漏）；`tab:e3`、`tab:e5`、`tab:scale` 三张表逐格重填；§6.9 的「$7$–$10$ ms」改「$8$–$11$ ms」；§6.10 因 `LyuL6_8m`／种子 $2024$／预算 $0.2$ s 上 R2 由 $150.0$ 改善到 $147.0$ 而与 R0+ **持平**，故「全部更优」改为「$17/18$ 更优、余 $1$ 点持平、无一点 R2 更优」，「逐项一致」改「逐项同向」。注 `rem:e2b_history` 增后半段记这第二层错配，局限第 2 条据此补一句：**逐条比对外侧字段（E2b）不足以兜住这类缺陷**，本文对它的检出依赖「事后想到该查什么」而非完备检查。图 `fig_strc_e5`／`fig_strc_scale` 重绘（`fig_strc_e1e3` 数据未变、未重绘）。
>
> **顺带记两条尚未写进正文的读数**（本轮用户选择只落地修复、不动叙事）。其一，**在本文现有协议下 R0+ 从未给出过 A2 可采纳的解**（此处原写「$50$ 个格子上改写了 $97\%$–$100\%$」，数字与口径都错，见修订八订正：$97\%$–$100\%$ 是 `\ResFracRzero`，即**全表**改动比例，与「已完成预约被改写的比例」是两个量；$50$ 格是 R2 审计的规模，E5 只有 $18$ 点）。其二，**R0+ 有一个挂钟下限**——一代 $40$ 次解码，`example_3x3x2` 上 $30$ ms、`congested_8x4x4` 上 $221$ ms，预算给 $5$ ms 也守不住（`budget_honored=False`），而 R2 是 $0.55$–$2.7$ ms（此处原写「**结构性**」下限，修订八实测否掉了这个形容词，见下）。另用 GA 收敛解作初始排程重跑一批（`experiments/e5_repro_ga_*.csv`）：把 $54$ 个预算点按 R0+ 是否守住 A2 分层，**守住的 $14$ 个点上 R2 赢 $5$、平 $9$、输 $0$**，未守住的 $40$ 个点上 R0+ 赢 $39$。新增工具 `tools/a2_audit.py`（A2 审计）与 `tools/e5_admissible.py`（分层统计），`e5_cross_curve.py` 增 `--baseline-mode` 与越界列（默认行为不变）。

> **2026-08-20 修订（八）：台账那两条，一条写进正文、一条被实测否掉。** 起因是被问「写进正文对创新点和逻辑是正面还是负面」。分开答：**挂钟下限那条负面，A2 那条正面**，但正面的理由不是「多一个优势」。
>
> **A2 那条：它现在就是一个逻辑漏洞，不写才是风险。** A1–A5 写在 §3.4，框定的是**问题层**（「其余物理假设保持不变以便归因」），A2 无任何限定语；而 A5 **专门**为对照臂开过口子（「实验中全局重解对照臂可改染色体」）。也就是说论文已意识到该臂需要豁免，并在**动作**维度上明确给了，却没在**时间**维度上给——A2 依然管着它，而它全线违反。**A5 那个显式豁免反而是路标**，会引审稿人去查另一维度有没有豁免。不写进去不等于风险消失，只是把发现权交出去。对**创新点**：不增加，三条贡献都是关于边界这个对象的，与此无关；硬写成创新点（「动态调度的基线可采纳性审计」）会稀释焦点。
>
> **写法上守住三条底线。** 其一，$18/18$ 与「不存在交叉点」**原文一字不动**，摘要那处让步也不碰——理由写进正文了：本文并未构造受 A2 约束的全局重解对照，无从声称在那样的对照下结果会不同。其二，R0+ 的 $C_{\max}$ 定性为**参考下界**（与拿 LP 松弛下界作对照同一性质），不说「其实没输」。其三，把「构造受 A2 约束的全局重解对照」列为后续工作**第一件**，否则「说基线不可采纳却不给可采纳的版本」读起来像回避。
>
> **数字必须重测，因为口径错了。** 台账原写的 $97\%$–$100\%$ 是 `\ResFracRzero`（**全表**改动比例），与「已完成预约被改写的比例」是两个量；$50$ 格也是 R2 审计的规模，E5 只有 $18$ 点。更要命的是 `e5_cross_curve.py` 与 `a2_audit.py` 用的扰动是「从 $t_{\mathrm{now}}$ 全程封锁」，而论文 E5 官方批次（`expand_batch._run_e5`）用的是「仅封锁繁忙窗口 $[t_0,t_1]$」且 `expand_on_fail=False`——**两套协议**，前者更狠，拿它的数当 E5 的数是错的（对齐前后 R2 在 `example_3x3x2`／种子 $42$ 上 $C_{\max}$ 差 $1$，$87$ vs $86$，就是这个原因）。改法是把越界四列直接加进 `expand_batch._run_e5`（同代码路径，不会漂），加 `--only-e5` 开关，跑进 `experiments/e5_a2_audit.csv`。对齐后 R2 的 $C_{\max}$ 与官方 CSV **逐格吻合**。**新读数：$18$ 点上 R0+ 越界 $80\%$–$100\%$（最低 $8/10$），无一点为零；R2 全部为 $0$。** 新增宏 `\RzeroPastLo`／`\RzeroPastHi`。
>
> **官方 `expanded/e5_cross_curve.csv` 没有覆盖**：审计跑与它只在 `congested_8x4x4`／种子 $42$／预算 $1$ s 一格上差 $204\to185$（R0+ 受挂钟预算约束、完成代数随机），覆盖会改 `tab:e5` 的读数而毫无收益，故保留原文件，审计另存一份。
>
> **挂钟下限那条：实测把它否掉了一半，故不写。** 原打算用「把原子放在单次解码而非单代」来绕开「下限只是你 pop=40 的属性」这个反驳。新工具 `tools/decode_cost.py` 实测（$30$ 次中位）：单次解码 `example_3x3x2` $0.62$ ms、`congested_8x4x4` $4.61$ ms、三张 `S8x4x4` $5.4$–$6.1$ ms；同格 R2 修复 $0.66$／$2.11$／$3.0$–$3.3$ ms。**解码/修复比只有 $0.95$–$2.18$，不是数量级**——与配置无关的那个下界不成立。站得住的只剩「按 $40$ 的种群配置，一代 $184$–$244$ ms」，是配置量，审稿人一句「把种群调小」就能顶回来。更麻烦的是那个退化体（种群压到 $1$ = 对原染色体重解码一次）在 `example_3x3x2` 上 $0.62$ ms 比 R2 的 $0.66$ ms 还快、$C_{\max}$ 也更好，**即毫秒档存在一个论文没测的替代方案**；写下限这条等于把审稿人的注意力引过去。结论：**不写**。若日后要写，正确的做法是把它当成一条新基线（「重解码原染色体」）正面测掉——它在稳定性上仍输 R2（$29/77$ vs $20/77$），三个维度里 R2 占两个，但那是另一轮实验。
>
> **改动清单。** `paper04.tex`：§6.8 末增两段（口径交代＋参考下界，含 $18/18$ 不变的说明）；局限第 1 条补口径与「不使本条失效」；§7.3「六件事」改「七件事」，新增第一件（受 A2 约束的全局重解对照，含它为什么难做：要求解码器支持固定前缀）；宏块新增 `\RzeroPastLo`／`\RzeroPastHi` 及出处注释。`STRC`：`tools/expand_batch.py` 增越界四列与 `--only-e5`，`tools/decode_cost.py` 新增，`experiments/e5_a2_audit.csv` 新增。**未动**：摘要、$18/18$、`tab:e5`、任何既有宏值。

> **2026-08-20 修订（九）：局限十条并成四条、后续工作七件并成四件，一一对应。** 起因是修订八之后清点，发现局限已累到十条、后续七件——对一篇贡献偏概念性的论文，这个数量接近上限，再加会开始读作「预备性工作」而不是「诚实的工作」。**内容一字未删**，只是按根因归并、并让两张表逐条对齐。
>
> **归并依据是根因，不是凑数。** 旧 $2/3/4/8$ 说的都是「对闭包这个对象知道得还不够」——保证充分而非充要、边集可能漏、覆盖 $90\%$ 活预约所以不紧、规模不可由静态结构预测，合为新第 $2$ 条，内部用 (i)–(iv) 分述。旧 $5/6/7$ 说的都是「实际做的比框架宣称的窄」——扰动只修了走廊阻断、动作受 A5 限、时间上单次且信息完备，合为新第 $3$ 条，按「扰动／动作／时间」三维写。旧 $9/10$ 都是「没有独立的外部参照物」——无公开基准、出处只外部化一半、未在第二套实现上复现，合为新第 $4$ 条。
>
> **旧第 1 条坚持单独站着。** 它和新第 $4$ 条确实沾边（缺一个可采纳的对照臂也是缺外部参照），但把认输那条塞进基础设施缺口里，读起来就是把失败重新包装成「我们缺工具」，会消耗掉 $18/18$ 那句让步正在起的作用。它继续排第一。
>
> **后续工作按 $1{:}1$ 重排。** 一→局限 1（受 A2 约束的对照臂）；二→局限 2（做实＝边集差分检验、做紧＝最小释放集、刻画规模＝时序密度，三件同源）；三→局限 3（补注入通道＋放开 A3）；四→局限 4（时间标定外部化）。旧「第二套独立实现」不再单列为后续工作，改为并入第四件末句：**它不必单独立项，上述任何一件由他人复现时都会顺带完成**。
>
> **真正的收益不在条数，在对应关系。** 改前是两张各自排序的列表，读者要自己配对，读作「十七处不足」；改后是四个开放问题、每个带一条明确下一步，读作「作者知道往下怎么走」。交叉引用同步改：§6.1 的「局限第 9 条」→「第 4 条」，§7.3 原「条 (9)」→「局限 (4)」，§7.2 增一句导语点明四条按威胁程度排序。$24$ 页，编译无新增警告。

> **2026-08-20 修订（十）：审稿意见里「如果只能改三件事」三件全部落地，三件的结果都朝有利方向。** 三件是：补一档便宜的可采纳基线、把算例推到 $20$ 工件 $10$ 机、给「缺口存在」找直接证据。
>
> **一、便宜基线（新 §6.9 E7）。原先的风险最高，结果最好。** 阶梯此前是 R1（释放集为空＝不动）→ R2 → R0+（$0.2$–$2$ s 种群搜索），中间空着「便宜的全局重算」这一档，于是「低两至三个数量级」这个读数里分不清有多少只是**对照选贵了**。补两条臂：**RS 全局右移**（不改路径/指派/序，把未完成的一切统一后推 $\Delta$，$\Delta$ 取到刚好让受阻走廊上可动的占用落到阻断窗之后；冻结判据与 R2 相同，故按 A2 可采纳，且因为是统一平移，一切间隔要么不变要么变大、而全部约束都是 $\ge$ 型，无需重新路由即可行）与 **RD 原染色体重解码**（保持机器指派与扫描序，在装了阻断的路由层上从头解一遍；**不受 A2 约束**）。
>
> 三臂在 $50$ 对上同协议（同 E1–E3）：**RS $1.74$ ms / 平均退化 $58.8\%$ / 改动比例 $0.627$ / A2 越界 $0/50$；RD $7.17$ ms / $46.5\%$ / $0.586$ / 越界 $31/50$；R2 $3.11$ ms / $49.8\%$ / $0.535$ / $0/50$**。逐格 $C_{\max}$：R2 对 RS 赢 $27$ 平 $16$ 输 $7$，对 RD 赢 $2$ 平 $23$ 输 $25$。三条读数写进正文：(a) **在给出可行方案的各臂里，唯一比有界修复更快的是那个不划边界的臂，而它在质量与稳定性上都输**——顺带得到一个此前没写出来的论点：**想把右移做得有选择，就必须先知道哪些预约受影响，那正是本文定义的边界；右移能不划边界，恰恰因为它放弃了选择**；(b) **最便宜的一次全局重算（一次解码）比一次有界修复更贵**（$2.3$ 倍），这把修订八里那条被否掉的「挂钟下限」换成了一个站得住的版本——不再声称配置无关的结构性下限，而是**同一实现里最便宜的那一档实测更慢**；(c) RD 的 $C_{\max}$ 优势主要来自越界：按越界与否分层，**在 RD 恰好没越界的 $19$ 格上 R2 赢 $1$ 平 $9$ 输 $9$，基本持平**（E8 的 $46$ 个同类格上赢 $5$ 平 $22$ 输 $19$）。这条给局限 1 与后续工作一各补一句：受 A2 约束的对照下差距**很可能远小于** `tab:e5` 所示，但那仍是上界估计，不是读数。
>
> **二、算例规模（新 §6.10 E8）。** 梯子取工件 $2k$、机器 $k$、车 $k$，$k=4..10$（$8\times4\times4$ 到 $20\times10\times10$），拥堵档、$H$、$F$、Tt/Tp 与 LU 割、远端割全部同口径标定，**只有规模变**；$7$ 档 $\times$ $10$ 种子 $=70$ 对，三臂同跑。**要紧的是闭包占比不涨反微降：$0.544 \to 0.426$**——若它随规模涨到接近 $1$，「有界」就名存实亡，这一问此前完全没有答案。R2 耗时随 $|R|$（$148 \to 402$）近似线性升到 $11.8$ ms；第 1 级可行 $69/70$，唯一失败格（$10\times5\times5$、种子 $42$）由失败扩域第 $3$ 轮兜住、总耗时 $20.1$ ms、$C_{\max}$ 与首级尝试相同，终可行 $70/70$。**两个对照的差距都随规模拉大**：RD/R2 耗时比 $2.3\times \to 5.5\times$，R2 对 RS 的胜负 $7/1/2 \to$ 末三档 $10/0/0$。口径限制写明：一个拥堵档、一个算例种子、$7$ 档不足以支撑任何渐近论断。
>
> **三、缺口的直接证据（§2.4 重写）。这一件改变了缺口的表述方式，而新表述更强。** 原文写「④ 层的工作把执行期扰动留给了 §2.1 那套任务图边界」——这是**推断**，被引的三篇静态一体化工作并没有这样说，审稿人一句「哪一篇？」就问穿。查证后发现真实情况更干净、且每一环都可直引：**持有预约表的一侧明确把执行期故障排除在模型外**（`sun2023integrated` 假设中写「AGV 始终可用，不考虑故障与充电」，结论把设备维护与故障列为后续工作）；**处理执行期故障的 AGV 一侧，影响边界一律落在任务对象上**（`zhang2023energy` 恢复范围是「故障后尚未加工的工序整体重排」，`tang2026dynamic` 按扰动传播分保留/继续/重构三类），**而它们能一致地这样划，恰因为不持有预约表**（前者模型假设明写「忽略 AGV 之间的碰撞」）。于是缺口改写为：**工序级边界成立的前提正是没有预约表，而现有预约表设定又恰好把执行期扰动排除在外，两个条件至今没有同时松开**；一旦松开（而静态一体化工作都把它列为下一步），可继承的边界只有 §2.1 那套，它给出空影响集。
>
> 这比原表述强，因为它把「缺口」从一句可被反例推翻的现在时断言，换成一个**在两者并置时必然出现**的失效，且解释了为什么它至今没暴露。相应地 §2.5 明确加一句：**不应写成「已有工作在这一格上划错了边界」，R1 对照臂复现的是 ① 层的标准做法，不代表任何一篇 ④ 层工作的实际选择**。摘要、§1.2、§2.5 的定位表同步从现在时改为「可继承的边界」。
>
> **顺带修掉一处错引。** §2.1 用 `dalcastagne2020reactive` 支撑「右移与反应式规则响应快但不给出显式边界」，而那篇是 *Reactive Sample Size for Heuristic Search in Simulation-based Optimization*——讲仿真优化里的样本量，与右移重调度无关。换成 `tang2026dynamic`（它正把「完全重调度／部分重调度／右移」并列为同一组取舍），并在该处指向新增的 E7 节。
>
> **改动清单。** 新增宏 `\MsShift`/`\MsRedecode`/`\MsCheapRtwo`/`\ShiftBad`/`\RedecodeBad`/`\WinVsShift`/`\WinVsRedecode`/`\ResFracShift`/`\ResFracRedecode`/`\ResFracCheapRtwo` 与 `\ScaleKs`/`\ScalePairs`/`\ScaleJobsHi`/`\ScaleMachHi`/`\ScaleCloLo`/`\ScaleCloHi`/`\ScaleMsHi`/`\ScaleRatioLo`/`\ScaleRatioHi`/`\ScaleFeasFirst`/`\ScaleFeasAll`；新增表 `tab:cheap`、`tab:instscale`；新增参考文献 `tang2026dynamic`、`zhang2023energy`；§6.11 小结增两条；局限 1 增一段（廉价解释已被排除）、局限 3 增「规模一维」一段；后续工作一增一段（差距量级已被框住）；摘要增两句。新增工具 `STRC/tools/cheap_baselines.py`、`STRC/tools/scale_curve.py` 与 `STRC/algorithm/rightshift.py`，`tools/paper_numbers.py` 增 `cheap_section()` 使这两批的每个数都能指回 CSV，`STRC/README.md` 同步。$26$ 页，无未定义引用、无 overfull（§6.5 因浮动体位置变动新出一处溢出，已用 `sloppypar` 修掉）。

> **2026-08-20 修订（十一）：补上第四条臂 RA（不划边界），终于测出闭包本身值多少。这是全文此前最大的空洞。**
>
> **为什么这是空洞。** 注 `rem:minimality` 与局限 2(iii) 早已诚实报出「闭包覆盖 $t_{now}$ 之后活预约的约 $90\%$，相对『释放全部未来』这一平凡上界只省下不足一成」。但那只是**规模**之比——全文从未测过那个平凡上界的**性能**。于是一个审稿人可以直接问：既然反正释放了九成，划这条边界还剩什么用？这一问此前没有任何数据能回答，而它打的正是方法层贡献的正中央。E3 比较的是两种**不同的边界定义**（任务图 vs 闭包），从未比较**要不要边界**。
>
> **臂的构造。** `repair_with_all_future()`：释放集取 $\{r : t_{end}(r) > t_{now}\}$，其余**逐项**与 R2 相同——同一 `replay_reroute` 引擎、同一冻结判据、同一阻断安装、同一协议。因此 R2 与 RA 之差**不含**引擎、协议或实现的贡献，可以整份归给闭包这个对象。它同样按 A2 可采纳（$t_{end} \le t_{now}$ 仍冻结），且已是最大释放集，无需失败扩域。它也正好是 §6.1「E3 必须关闭扩域」那句里提到的扩域端点，现在那个端点有了实测代价。
>
> **结果（$50$ 对，与 E7 同批）。** **RA $3.30$ ms / 平均退化 $49.9\%$ / 改动比例 $0.604$ / A2 越界 $0/50$ / 释放占比 $1.00$；R2 $3.16$ ms / $49.8\%$ / $0.535$ / $0/50$ / $0.901$。** 三项分开读，合起来才是重点：
> - **耗时几乎相同**（$3.16$ vs $3.30$ ms）→ 毫秒级响应**不来自闭包**，来自「单遍改路、不重开搜索」，而这一机制形态本文本就在 `tab:position` 里弃权了新颖性主张。
> - **$C_{\max}$ 几乎相同**（比值均值 $0.999$，逐格 $0/46/4$，R2 从未更差，最占优一格 $0.970$）→ 闭包**不是**优化手段。
> - **改动比例显著更低**（$0.535$ vs $0.604$，平均降 $0.069$、中位降 $0.031$，逐格 $32/17/1$，Wilcoxon 符号秩双侧 $p=5.9\times10^{-7}$）→ 闭包兑现的**恰是命题 `prop:outside` 承诺的那个量：外侧字段不变**。
>
> **这个结果的形状比数值更有用。** 它把三条主张各自钉到各自的证据上：快是机制的（不主张新颖）、好不是它买的（不主张）、稳是闭包的（主张，且与理论命题对齐）。此前这三者混在一起报，读者无从判断「毫秒级」里有多少是闭包的功劳——现在答案是零，而这**不削弱**论文，因为论文主张的从来是边界该画在哪，不是响应有多快。
>
> **幅度上必须诚实，而诚实的版本恰好给出机制。** 降幅不大，且**与闭包排除掉多少成正比**：`congested_8x4x4`（释放占比 $0.937$）只降 $0.010$，`S8x4x4_mid`（$0.877$）降 $0.128$，`S8x4x4_high`（$0.906$）降 $0.094$。$17$ 个持平格里有 $4$ 个是因为闭包**就是**全部未来预约（释放占比取到 $1.00$），两臂恒等。唯一反例格（`S8x4x4_high` 种子 $13$）R2 反而高 $0.007$，$C_{\max}$ 相同——可忽略但如实记录。由此局限 2(iii) 从「闭包不小」升级为一句更准的话：**闭包越接近平凡上界，划这条边界的意义越小，而本文没有给出何时会如此的判据。**
>
> **改动清单。** `STRC/algorithm/repair.py` 增 `repair_with_all_future()`；`STRC/tools/cheap_baselines.py` 增 RA 臂与 `release_size`/`alive_size` 两列，并增 R2–RA 专项对照输出；`tools/paper_numbers.py` 增 `_closure_vs_all_future()` 与零依赖的 `_signrank_p()`（Python 3.7 无 `math.comb`，改手算二项系数）。新增宏 `\MsAllFuture`/`\ResFracAllFuture`/`\WinVsAllFuture`/`\RAStabWTL`/`\RAStabP`/`\RAStabMean`/`\RAStabMed`/`\RACmaxRatio`/`\RACmaxLo`/`\ClosureShareLo`/`\ClosureShareMean`；`\MsShift`/`\MsRedecode`/`\MsCheapRtwo` 按本次重跑更新（$1.74\to1.75$、$7.17\to7.51$、$3.11\to3.16$，挂钟读数逐次有噪声，其余读数逐位可复现）。`tab:cheap` 增一行一列、四臂并读；§6.9 增「其四」一整段与 RA 臂定义；注 `rem:minimality` 增一段区分「闭包很小」与「闭包无用」；§6.1 对照臂清单增 RA、并把扩域端点指向 §6.9；§6.11 小结增一条；局限 2(iii) 重写；结论增一段；摘要增四句。$27$ 页，编译无警告、无未定义引用、无 overfull。

---

## 一页速览

- **题目**：面向柔性作业车间与 AGV 一体化调度的时空预约影响闭包与有界重调度
- **题目-英**：Spatiotemporal Reservation Closure for Bounded Rescheduling in Integrated Flexible Job Shop Scheduling and Conflict-Free AGV Routing
- **核心洞见**：走廊被临时阻断时，**任务依赖图上的影响集是空的**——没有任何工序的加工参数被改动。但这份排程已经不可行了：被阻断走廊上的预约必须挪走，挪走会挤占别人的时窗，让行关系逐级传播，直至撞上机器时间轴。**损坏是真的，只是任务图看不见它。**
- **一句话方法**：把「谁挡了谁」显式化为一张**预约依赖图** $G_R$（四类边），扰动在其上确定一个**种子集**，取**传递闭包**得到影响集；闭包**之内**重规划，闭包**之外**逐条冻结为硬约束；单次修复失败则沿依赖边**扩域**，仍失败则升级为热启动全局重解。
- **三层创新**：问题层＝在同时持有任务图与预约图的这一支文献里，B 类扰动使任务图边界系统性为空；框架层＝把影响集从「迭代冲突检测的结果」改造为「依赖图上的传递闭包」这一**对象**，并给出闭合性命题；机制层＝闭包内有界修复与热启动全局重解的角色分工（毫秒 vs 秒；改动 $57\%$–$60\%$ vs $97\%$–$100\%$ 预约；但 $C_{\max}$ 全区间更差）。
- **实验骨架**：$5$ 算例 $\times$ $10$ 种子 $=50$ 对，E1–E6 六组实验。主结果：E1 $50/50$ 证实缺口；E3 $50/50$ vs $0/50$ 证实边界定义是唯一自变量；E2a/E2b 均 $50/50$；E6 在四类扰动 $\times$ $50$ 对上填满覆盖矩阵。**仍存的负面结果只剩一条：E5 在 $C_{\max}$ 上 $18/18$ 个预算点不交叉**；另有一条不好看的读数是闭包覆盖活预约约 $90\%$，故不作最小性主张。
- **定位**：与 paper01 同一车间、同一预约表，**paper01 用预约表构建排程，paper04 用预约表定位损坏**。

---

## 场景与系统模型

**继承自 paper01 的部分**（`STRC` 复用 `clbs` 的下层组件：`ReservationTable`、`Network.route`、`validator`、`blocking_opponents`；替换的是上层）：柔性作业车间 + 异构机械臂 + 同质 AGV + 显式走廊网络；走廊排他、占用为半开区间 $[t,\,t+\tau)$；时间窗 Dijkstra；先规划者先占用。

**本文新增的部分**：一条时间轴上的**执行时刻** $t_{now}$，以及在该时刻发生的扰动。$t_{now}$ 之前的一切已经发生，不可更改。

### 两张图

| | $G_T$ 任务依赖图 | $G_R$ 预约依赖图 |
|---|---|---|
| 顶点 | 工序 $O_{ji}$ | **预约** $r=(\text{走廊},\,[t,t+\tau),\,\text{车辆},\,\text{所属任务})$ |
| 边 | 工艺链后继、同机后继 | 四类，见下 |
| 表达什么 | 加工与指派的先后 | **占用与让行的先后** |
| B 类扰动下的影响集 | **空** | 非空（$16.7\sim85.9$ 个预约） |

**$G_R$ 的四类边**（缺任何一类都会导致闭包不闭合，其中同机后继边是补漏补出来的，见结论十）：

1. **同任务链内后继**：同一次运输的空载段 → 满载段；
2. **同车辆后继**：同一辆 AGV 的下一次运输；
3. **让行边（走廊占用序）**：在同一走廊上，先占用者 → 被迫等待者。**这条边的对象与铁路调度的 alternative arc 同源，查新一节必须显式承认**；
4. **同机后继边**：同一台机械臂上的下一道工序所对应的运输。

### 影响闭包（STRC）的定义

给定扰动 $\delta$ 与时刻 $t_{now}$：

$$\text{Seeds}(\delta) = \{r \in R : r \text{ 在 } t_{now} \text{ 之后仍 alive，且被 } \delta \text{ 直接使之不可行}\}$$

$$\mathrm{Cl}(\delta) = \text{Seeds}(\delta) \text{ 在 } G_R \text{ 上的传递闭包（沿四类边前向可达）}$$

修复时：$\mathrm{Cl}(\delta)$ 内的预约全部释放并重规划；$\mathrm{Cl}(\delta)$ 外**且** $t_{now}$ 之后仍 alive 的预约**逐条冻结为硬约束**（不是软惩罚，是重新写回预约表的不可侵占时窗）。

### 四条基线（对齐 paper01 的阶梯体例）

| 记号 | 释放什么 | 对应什么 |
|---|---|---|
| **R0** | 全部（冷启动全局重解） | 完全重调度 |
| **R0+** | 全部，但以原解热启动 | 完全重调度 + warm start，**这是最强对照** |
| **R1** | 任务图影响集（$t_{now}$ 后） | **AOR 的原型**——B 类扰动下它为空 |
| **R2** | STRC 闭包（本文） | — |

**E3 是本文最关键的一组对照：R1 与 R2 用同一个修复引擎、同一份初始解、同一个 $t_{now}$，唯一的差别是边界定义。**

### 扰动模型（对应模板的「威胁模型」）

**二分：**

- **A 类（任务侧）**：机械臂故障、加工时长变化、工件插单/取消，**以及车辆故障**。**任务图上有非空种子**，$G_T$ 与 $G_R$ 的边界都非空。
- **B 类（网络侧）**：**走廊在时窗 $[t_b,\,t_b+d)$ 内阻断**、路段限速/降速。**任务图上种子为空**，只有 $G_R$ 有种子。

> **修订（结论十八）：车辆故障是 A 类，不是 B 类。** 本备忘上一版把它列进 B 类，与代码（`TOUCHES_TASK_GRAPH["agv_breakdown"] = True`）和 `paper04.tex` 的 `def:ab` 都不一致。判据是**可执行性**而非指派：车抛锚后它承运的工序在原计划下无法开工，经「车→所承运工序」映射，任务图上留有非空种子。E6 实测确认 $50/50$ 全部非空（均 $|U_{R1}|=31.1$）。这一归类是**保守**的，把边缘情形判给了对本文不利的一侧。`paper04.tex` 已加 `rem:agv_class` 说明，并附一条限定：只有当框架维护了车–任务映射时该种子才非空，AOR 的常见实现只跟踪工序与机器，车辆故障同样会漏——但本文不靠这一点立论。

**B 类是本文的靶心。** A 类作对照（结论十二 → 已由 E6 扩样，见结论十九）。

**假设 A1–A5：**
- **A1** $t_{now}$ 之前的执行已发生且不可更改；已完成与正在执行的预约不进入闭包。
- **A2** 扰动信息在 $t_{now}$ 完全已知（阻断走廊、起止时刻均已知），不考虑不确定性与预测。
- **A3** 单次扰动，不考虑扰动流；多扰动叠加列为未来工作。
- **A4** 修复只改变 $t_{now}$ 之后的路由与派车，**不改变机器指派与工序排序**（这是「有界」的边界；改指派即退化为全局重解）。
- **A5** 外侧冻结是硬约束，可行即意味着修复解与被冻结的全部预约无冲突（由独立校验器复核）。

### 扰动类型 × 边界定义 覆盖矩阵

（对应模板的「攻击类型 × 检测通道覆盖矩阵」）

**已由 E6 填满（$4$ 类 $\times$ $50$ 对 $=200$ 次测量）。** 上一版这张表大片「未测」，是本文最容易被攻击的地方之一。

| 扰动 | 类 | R1 任务图边界 | R2 STRC 闭包 | R2 $\supseteq$ R1 | 修复可行率 |
|---|---|---|---|---|---|
| **走廊阻断** | B | **$0/50$ 非空**（恒为空，均 $|U_{R1}|=0$） | 均 $|\mathrm{Cl}|=62.6$，占活预约 $90\%$ | 平凡成立 | **R1 $0/50$ vs R2 $50/50$** |
| **路段降速** | B | **$0/50$ 非空** | 同上（逐格相同，见下） | 平凡成立 | 未测（引擎按阻断过近似） |
| **车辆故障** | A | $50/50$ 非空，均 $31.1$ | 均 $53.0$，占活预约 $75\%$ | **$50/50$** | 未测（无注入通道） |
| **机械臂故障** | A | $50/50$ 非空，均 $42.8$ | 均 $62.7$，占活预约 $90\%$ | **$50/50$** | 未测（无注入通道） |

**读这张表要带三条限定：**

1. **只有走廊阻断这一列的「修复可行率」是真的。** 另外三类没有正确的扰动注入通道——降速在 `block_context.py` 里被当作整段阻断（过近似），两类故障重放时那台车/那台机器仍然可用。照跑可行率会得到假阳性。所以 E6 **只报边界，不报修复**，`paper04.tex` §6.5 已把这一点写在小节开头。
2. **降速与阻断两行逐格相同（$50/50$ 对上闭包规模一致），不是两次独立验证。** 本文的种子规则对二者都是「与失效时窗重叠的活预约」，故按构造给出同一个种子集。它证实的是任务图边界对降速同样为空，不是对 B 类结论的重复确认。
3. **A 类上闭包边界是任务图边界的一致加强**（$100/100$ 次 A 类测量上 $U_{R1}\subseteq\mathrm{Cl}$，其中 $81/100$ 严格更大、$19$ 格相等），不是另一种取舍。代价是释放集平均变大。

---

## 问题的结构性特点

为什么这个缺口**只在这个模型层级上存在**——这是全文最需要写清的一段，也是查新之后唯一还站得住的立足点。

1. **预约表是扰动的传播通道。** 走廊排他 + 半开时窗，使「A 的延误如何变成 B 的延误」这句话第一次有了逐条可查的载体：具体哪一段、哪个时段、被谁先占。常数运输矩阵模型里连这个句子都写不出来（这与 paper01 的立论根基是同一条）。
2. **任务图与预约图是两张图，且它们的顶点集不相交。** 任务图的顶点是加工，预约图的顶点是通行。B 类扰动只改动后者的可行性，不改动前者的任何参数。于是**任务图上的种子集为空**，而排程已经不可行——这个错位就是本文的问题。
3. **这个错位需要一个很特殊的前提：模型必须同时持有两张图，却只用其中一张划界。** 这一条是查新之后才补上的，它是本文能不能站住的关键：
   - **铁路调度（alternative graph）不会犯这个错**，因为在那里闭塞区段**就是**机器，两张图合一，走廊阻断天然是 A 类；
   - **MAPF 不会犯这个错**，因为它根本没有任务图（任务外生）；
   - **恰恰是柔性作业车间 + AGV 一体化调度这一支**，同时持有工序/机器链与显式走廊预约，却沿用了 AOR 那套在任务图上划界的传统。
   **因此本文的主张必须写成「这一支」，不能写成「局部重调度文献」的全称否定。**
4. **闭包的规模是被结构决定的，不是可调参数。** 实测 $16.7$（`example_3x3x2`）/ $48.3$（`congested_8x4x4`）/ $85.9$、$82.1$、$80.2$（`S8x4x4` 的 high/funnel/mid）个预约，随算例规模上升。这使「局部」是一个可以测量的量，而不是一个人为设定的半径。**但要立刻补一句**：$\mathrm{Cl}/|R|$ 在五个算例上稳定在 $0.54$–$0.58$，**没有区分开三个同规模布局**；而按 $t_{now}$ 之后的活预约算，闭包占 $90\%$。见结论二十。
5. **修复的代价与释放范围不是同向的。** 释放得越少，越快、越稳定（少动已承诺的时窗），但解越差。E5 把这条曲线画了出来，而且画出来的结果**不利于本文**（结论五）。

---

## 查新

**这是本次补做的核心工作，也是本备忘相对现稿最大的增量。** 检索了四条线，逐条给判定：

| 我之前的主张 | 关键先行工作 | 判定 |
|---|---|---|
| **P1** 局部重调度只在任务图上划影响域 | **AOR（Affected Operations Rescheduling）**：Li, Shyu & Adiga（1993）、Abumaizar & Svestka（1997）、Subramaniam & Raheja 的 mAOR（2003）——「只重调度受扰动**直接或间接影响**的工序」，其余保持原样 | **成立，但必须点名。** 这正是 R1 的原型。现稿只引两篇综述而不引 AOR 原文，会被判为**打稻草人** |
| **P2** 「影响边界如何划定」本身是一个值得研究的对象 | **Match-up scheduling**：Bean, Birge, Mittenthal & Noon（*Operations Research* 1991）、Aktürk & Görgülü（*EJOR* 1999）——在**时间轴**上定一个 match-up point，只重调度扰动点到匹配点之间的部分 | **体裁不成立。** 「边界怎么划」早就是研究对象。本文新的是**划在哪个图上**，不是「应该划边界」 |
| **P3** 单次修复失败后沿依赖关系逐步扩大释放域 | **Local Rescheduling（LRS）**：Kuster, Jannach & Friedrich（IEEE CISched 2007, pp. 79–86）——「incremental extension of a time window」直到找到可行解，且论文明确指出 AOR 与 match-up 都绑死在生产特有结构上 | **不成立。** 扩域机制本身是既有做法。差别只在「沿依赖边扩」而非「沿时间窗扩」，**不足以单独作为贡献**<br>（**订正**：上一版署名 "Jannach & Zanker" 有误，第一作者是 Kuster，第三作者是 Friedrich，与 Zanker 无关） |
| **P4** 只重规划受影响者、其余冻结当作障碍 | **Online MAPF 的 Replan Affected**：Švancara et al.（AAAI 2019）明确给出 Replan All / Replan Single / **Replan Affected** 三档，并配 Online Independence Detection；以及 I-MAPF/LIMP、D-MAPF 的 Revise-and-Augment、Malka/Shani/Stern（IROS 2024）「只为受观测变化影响的智能体重规划，其余视为动态障碍」 | **不成立。** 「局部重规划 + 外侧当动态障碍」在 MAPF 已是标准做法。**本文不得把它写成机制创新** |
| **P5** 用「谁挡了谁」的图来组织冲突关系 | **Alternative graph**：Mascis & Pacciarelli（*EJOR* 2002）——顶点＝列车在**闭塞区段**上的作业，固定弧＝路径先后，**成对 alternative arcs ＝「谁先通过这个区段」**；D'Ariano et al.（2007–2012）的 ROMA 用它做实时重调度、研究延误传播与局部调度区 | **最危险的一条，而且现稿完全没有引用。** 「让行边」这个对象在铁路调度里已有二十余年历史。**必须补引并主动区分** |
| **P6** 少动已承诺的时窗，这件事可以度量 | 港口 AGV 干扰管理（2024）：时间窗 Dijkstra + 干扰管理，报告路径偏离度 $5.56\%$ vs 全面重调度 $44.44\%$ | **不成立（作为指标）。** 稳定性度量已有先例；本文的「释放 $57\%$–$60\%$ vs $97\%$–$100\%$」只是同类读数 |
| **P7** 走廊阻断这类扰动在常数运输矩阵的动态调度模型里根本写不下来 | `xin2025review` 综述、`meng2025cp`、`qin2026knowledge`、`fu2025integrated` | **成立。** 这是护城河，与 paper01 同源 |

### 与 alternative graph 的区分（最重要的一段）

**必须承认的部分：** 让行边与 alternative arc 是同一个对象——都表达「在一个排他资源上谁先谁后」。铁路调度用它做实时重调度已二十余年，还研究延误传播。本文若装作不知道，一个熟悉该文献的审稿人一句话就能否掉。

**真正的区别有两条，都必须写在正文里：**

1. **用途不同。** 在 alternative graph 里，alternative arc 的**选择本身是决策变量**——求解就是选一组弧使图无正环且完工时间最小。本文**不选择弧**：让行边由**已经落盘的那份排程**唯一确定，本文在这份**既定选择**之上取传递闭包，用来回答一个完全不同的问题——「扰动之后，哪些承诺必须作废」。**它是一个 measurement，不是一个 decision。**
2. **模型层级不同，而这个差别恰恰是本文问题的来源。** 在铁路模型里，闭塞区段**就是**机器，任务图与让行图是同一张图；因此「区段阻断」在那里天然是 A 类扰动，不存在任务图看不见的问题。本文的缺口只在**两张图分立**的模型里存在。**这既是对该文献的礼让，也是本文问题成立的前提。**

### 剩下什么真正站得住

**S1（强，且是唯一干净的问题层立足点）：在同时含工序指派/机器链与显式走廊预约的调度问题上，任务图边界对 B 类扰动系统性为空。**
理由见「结构性特点」第 3 条：AG 社区不会犯这个错（两图合一），MAPF 社区不会犯（没有任务图），恰恰是 FJSP–AGV 一体化这一支同时持有两张图却只在任务图上划界。这个**错位**是问题层唯一站得住的东西。**代价是必须收窄措辞**——写成「这一支」，不写「局部重调度文献」。

**S2（中）：把影响集从「迭代冲突检测的结果」改造成「依赖图上的一个传递闭包」这一对象。**
MAPF 的 Independence Detection / CBS 合并、AOR 的二叉树式传播，都是**过程**——影响集是算法跑出来的副产品，随实现与终止条件而变。闭包是**对象**——它由图结构唯一确定，可以陈述并证明闭合性（prop:struct：闭包外侧不含任何与闭包内重规划相冲突的活预约），可以测量规模，可以作为不同修复引擎的公共输入。这个从「过程」到「对象」的转换是中等强度的框架层贡献，不宜说过头。

**S3（中弱）：闭包内有界修复与热启动全局重解的角色分工，以可复现读数给出。**
毫秒 vs 秒（$87\times$–$3051\times$）、释放 $57\%$–$60\%$ vs $97\%$–$100\%$、但 $C_{\max}$ 全区间更差。体裁不新（disruption management 一贯的稳定性—质量权衡），**实例新**（第一次在 FJSP–AGV 的预约层给出这条曲线）。**注意：这一条目前是负面的**，见结论五。

---

## 由此产生的硬性要求

> **状态：五条全部已落地（2026-08-16）。** 下面保留原始要求，并在每条后附「怎么改的」，便于日后核对正文是否被改回去。

**1. 必须补引六族文献，Related Work 按四层重写。** ✅ 已改：`paper04.tex` §2 整节重写为四层，新增 `tab:position` 定位表（`table*` 跨栏）。十篇新引文已核对卷期页码后写入 `reference-base.bib`。

| 层 | 引什么 | 说什么 | bib key |
|---|---|---|---|
| ① 任务图与时间轴上的边界 | AOR 三篇 + match-up 两篇 + LRS | 边界划定是成熟研究对象；AOR 就是 R1 | `li1993heuristic` `abumaizar1997rescheduling` `subramaniam2003maor` `bean1991matchup` `akturk1999matchup` `kuster2007local` |
| ② 让行图，但两图合一 | alternative graph + ROMA | 让行边同源，区别在选弧 vs 取闭包、以及闭塞区段即机器 | `mascis2002alternative` `dariano2007branch` |
| ③ 有让行、无任务图 | Online MAPF 的 Replan Affected | 局部重规划 + 外侧冻结是标准做法，不作机制创新 | `svancara2019online` `malka2024online` |
| ④ 两张图都有、却只用任务图 | FJSP–AGV 一体化重调度 | **本文的位置** | 沿用原有 `xin2025review` 等 |

**2. 摘要与引言的措辞必须收窄。** ✅ 已改：摘要与 §1.2 均改为「**在同时持有任务依赖图与显式走廊预约表的这一支调度文献中**」。§1.2 另加一段说明这个错位需要「两图并存却只用一张划界」这个特殊前提，并逐条排除第 ②③ 层。§2.5 用整段写明「本文的主张必须收窄到第 ④ 层」。

**3. 三条禁语。** ✅ 已改：写成 §2.5 的一个显式列表「本文**不**主张以下三件事」，并在正文各处兑现——§1.4 贡献 3 改为「此处新的不是让行关系这一对象，而是把影响集由过程改造成对象」；贡献 4 加「两者的机制形态均非本文首创」；§5.2 扩域一节开头单起一段「先声明：扩域机制本身不是本文首创」。

**4. 负面结果不得删。** ✅ 已改。E5 新增 `tab:e5`，明写「本小节的设计意图原是找一个交叉点，**该交叉点不存在**」，并加一段说明为什么不换指标绕过去；最小性一条主动点破「闭包覆盖活预约约 $90\%$」。**E2b 已不再是负面结果**（$50/50$，结论二十三），但那段历史没有删——它被写成 `rem:e2b_history` 留在正文里，因为「把实现缺陷读成模型缺陷」这个坑值得留个记号；局限章也补了一条「边集是否穷尽物理耦合本文并未验证」，把 E2b 让出来的那块真空明确标出，而不是让它悄悄消失。

**5. 与 paper01 的自洽声明必须写。** ✅ 已改，且写了**三处**：§1.3 主张预告末尾单起一段「影响闭包是一次测量，不是一个决策」并解释稀释论证；§1.5 与静态工作的关系一节完整展开；§2.2 用同一句话区分本文与 alternative graph（那里选弧本身就是决策变量）。三处互相 `\ref`，形成一条贯穿全文的线。

---

## 标题演变与定稿

| 候选 | 判定 |
|---|---|
| 「双时间尺度重调度」 | **废**。双时间尺度不是贡献，是实现细节；作核心贡献会被一句话问倒 |
| 「NOSR 影响边界定理」（原 paper04） | **废**。$\rho$–$d_{out}$ 自相矛盾；宣称的「$74\%$ 缩减」在复核中失效 |
| 「任务图**看不见**的级联损坏」 | **废**（措辞）。「看不见」是拟人化且过强 |
| 「任务图**覆盖不到**的级联损坏」 | 可用作引言小节标题 |
| **时空预约影响闭包与有界重调度** | **定稿** |

**为什么「闭包」这个词值得放进标题。** 它精确对应 S2 那条从「过程」到「对象」的转换：闭包是图论里有确定含义的算子，读者一看就知道它由结构唯一确定、可以陈述闭合性。用「影响域」「影响区」都会读成一个启发式半径。

**同时被砍掉的还有：** $\varphi$ 不再作为结构参数（paper01 已证该指标未能区分它本要区分的两个族）；四级升级阶梯收窄为「第 1 级改路 + 失败扩域 + 升级全局重解」；HMACSP 那套常数运输矩阵的建模必须整体换掉。

---

## 三层创新表述

**C1–C7 与 E1–E6 的对应，以及 $50$ 对扩样后的状态：**

| 编号 | 主张 | 由谁验证 | 实测锚点（$5\times10=50$ 对） | 判定 |
|---|---|---|---|---|
| **C1** | B 类扰动下任务图影响集为空而排程已不可行 | E1 | $50/50$；$T_{impact}=0$ 而闭包 $16.7/48.3/85.9/82.1/80.2$ | **立（问题层核心）** |
| **C2** | 边界定义是可行与否的唯一自变量 | E3 | R1 $0/50$ vs R2 $50/50$，同引擎同解同 $t_{now}$；R2 修复中位 $2.4$ ms | **立（最强的一组）** |
| **C3** | 闭包结构闭合：外侧无泄漏 | E2a | $50/50$，泄漏 $0$ | **立** |
| **C4** | 闭包外侧解不变 | E2b | $50/50$（修掉粒度错配后，见结论二十三） | **立，但命题仍只作充分条件**——$50/50$ 说明前提成立，不说明前提可省 |
| **C5** | 闭包规模随争用结构变化 | E4 + E1 + 外部布局批次 | 两组区间高度重叠、$5$ 种子支撑不了方向；$\mathrm{Cl}/|R|$ 在三个同规模布局上落在 $0.54$–$0.57$ 窄带；换外部布局后闭包占比极差 $0.207$（自建族仅 $0.051$）而两个指标**取值恒定** | **不作贡献，且已否证静态割这一族指标（结论二十六）** |
| **C6** | 有界修复与全局重解有明确的角色分工 | E5 | 加速比 $87\times$–$3051\times$，改动 $57\%$–$60\%$ vs $97\%$–$100\%$；但 $C_{\max}$ $18/18$ 个预算点更差 | **部分立**（时间/稳定性立，质量不立） |
| **C7** | A/B 二分与「闭包边界一致加强任务图边界」 | E6 | $200$ 次测量无例外；A 类 $100/100$ 上 $U_{R1}\subseteq\mathrm{Cl}$（$81/100$ 严格） | **立（填满覆盖矩阵）** |

### 问题层

**C1｜任务图覆盖不到的级联损坏。** 走廊在 $[t_b,t_b+d)$ 阻断，没有任何工序的加工参数被改动，$G_T$ 上种子为空；但该走廊上 $t_{now}$ 之后的预约必须挪走，挪走挤占别人的时窗，让行关系沿 $G_R$ 逐级传播，最终撞上机器时间轴。这不是一个可以靠「右移」吸收的扰动，因为右移会打破外侧的时窗承诺。

**注意 `prop:miss` 的两半有不同的分量**（结论十三）：「$G_T$ 上种子为空」是**定义性**的（B 类扰动按定义不碰加工参数），不是发现；非平凡的是后半句——**「而排程确实已经不可行」**，这需要证明阻断之后原预约集不存在可行的时移。写作时不要把定义性的那半句当成结果来宣传。

### 框架层

**C2 + C3｜闭包作为对象。** E3 是本文最强的一组证据，因为它把所有其他变量都消掉了：同一个修复引擎、同一份初始解、同一个 $t_{now}$、同一批种子、同一段阻断窗，**唯一的差别是释放集合怎么算**。$0/50$ 对 $50/50$。这组对照的说服力来自它的干净，不来自数字大。

**还要补一句 R1 是怎么输的**（已写进 `paper04.tex` §6.4，结论二十一）：R1 侧**根本没有触发任何改路**——它的释放集是空的，第 1 级改路对空集是恒等操作，于是原排程原封不动地留在被阻断的走廊上。所以 R1 的失败方式不是「修得不好」，而是「判断为无需修」。这句话必须写，否则读者会把 $0/50$ 读成「任务图方法的修复能力差」，那就把问题层的结论降格成了机制层的比较。

`prop:struct`（闭合性）保证外侧冻结不会与闭包内的重规划相冲突——这是「有界」这个词有意义的前提。若不闭合，「只重规划闭包内」就只是一个碰运气的启发式。

### 机制层

**C6｜角色分工，包含一条不利的读数。** 有界修复在**时间**与**稳定性**两个维度上大幅优于热启动全局重解，但在**解质量**上全区间更差且不交叉。诚实的表述是：

> 有界修复的定位不是「更好的重调度」，而是「**在必须立刻给出可执行方案、且不愿撕毁已下发承诺时**的那一档」。若允许秒级停机并接受全盘改动，热启动全局重解在质量上占优。

**不要试图把 E5 说成有利。** 见结论五。此外「互补」这个词必须限定含义（已写进 §5.3 与 §6.7）：它指的是两者被**不同的约束**选中，**不**指存在某个预算区间使 R2 的 $C_{\max}$ 更优——那样的区间不存在。上一版 §5.3 写的「交叉曲线与规模扫描用于展示互补」正是这种含混，已删。

---

## 方法层与模块衔接

```
执行到 t_now，扰动 δ 到达
  │
  ├─ [S1 种子识别]  δ 直接使之不可行、且 t_now 后仍 alive 的预约
  │      └─ B 类：走廊 c 在 [t_b, t_b+d) 内的全部活预约
  │
  ├─ [S2 闭包计算]  在 G_R 上沿四类边取传递闭包 → Cl(δ)
  │      四类边：同任务链内后继 / 同车辆后继 / 让行边 / 同机后继
  │
  ├─ [S3 外侧冻结]  Cl 外且 t_now 后 alive 的预约逐条写回预约表为硬约束
  │
  ├─ [S4 闭包内修复]  释放 Cl，按原次序以时间窗 Dijkstra 重规划（第 1 级：改路）
  │      └─ 失败 → [S5 扩域]  沿依赖边把 Cl 扩一层，回到 S3
  │              └─ 仍失败 → [S6 升级]  热启动全局重解 R0+
  │
  └─ [S7 校验]  独立校验器复核：无冲突、外侧承诺未被侵占、C_max 可复算
```

**各模块要点：**

- **S1 的两个坑都踩过**（结论九）：（i）微阻断协议曾把**窗外**预约也一并封死，导致 STRC 系统性不可行；（ii）受扰工序的运输**已在 $t_{now}$ 之前结束**，于是种子集为空、实验退化。两处修正之后才有现在这张表。**「$t_{now}$ 后仍 alive」这个限定词是被 bug 逼出来的，不是设计出来的**，正文可以不讲这段历史，但内部必须记住它是必要条件。
- **S2 的四类边是补漏补齐的**（结论十）：早期实现漏了**同机后继边**，导致外侧冻结与机器时间轴发生冲突——闭包不闭合。补边之后 E2a 才全通过（当批 $15/15$，扩样后 $50/50$）。
- **S3 是硬约束不是软惩罚。** 逐条写回预约表，由 `ReservationTable` 直接拒绝侵占。这是「$50/50$ 可行」这个读数有意义的前提。
- **S5 扩域必须在 E3 里关掉**（结论七）：开着扩域时 R1 会靠「一路扩到释放全部未来」而偶然成功，那样测的就不是边界定义了。**这是一个口径缺陷，修正后 R1 才是零通过（当批 $0/15$，扩样后 $0/50$）。**
- **S6 升级到 R0+ 而非 R0。** 对照必须取强者（与 paper01 的「B0 取强口径」同一条纪律）。

**复杂度：** 闭包计算是 $G_R$ 上的一次 BFS/DFS，$O(|R| + |E_R|)$；修复是 $|\mathrm{Cl}|$ 次时间窗 Dijkstra。实测总耗时 $0.5\sim1.8$ ms 量级，主导项是修复而非闭包。

---

## 实验设计与已实测结论

### 算例与协议

- **算例**：$5$ 个——`example_3x3x2`（小）、`congested_8x4x4`（拥堵），以及 `S8x4x4` 的 `high`/`funnel`/`mid` 三个布局。后三者**同规模、同柔性，只差走廊拓扑**，因此它们之间闭包规模的差异可归因于拓扑而非尺寸。
- **种子**：$10$ 个（`42,7,2024,99,123,13,1,777,31415,8`，与 paper01 的十种子表对齐）；**共 $50$ 对**
- **初始解**：由 `clbs` 在静态设定下求出（复用 paper01 的求解器，保证初始解质量不是变量）；同一算例–种子对下各臂共享
- **$t_{now}$**：$0.35\,C_{\max}(\mathcal{S}^0)$
- **扰动**：E1–E3/E5 为繁忙走廊单窗阻断；E6 另加降速/车辆故障/机械臂故障；规模扫描用微阻断协议
- **校验**：独立校验器复核无冲突 + 外侧承诺未被侵占
- **口径纪律**：E3 **必须关闭扩域**（结论七）
- **数据源**：`STRC/experiments/expanded/summary.md` 及同目录 CSV
- **复现命令**：`cd STRC && py -m tools.expand_batch`（约 $4$ 分钟）→ `py -m tools.paper_numbers`（打印正文要引的全部数字）→ `cd ../paper04/figures && py fig_strc_e1e3.py; py fig_strc_e5.py; py fig_strc_scale.py; py fig_strc_case.py`
- **外部布局批次（第二批，`\NPub*` 一族的来源）**：数据源 `STRC/experiments/pub_layouts/`；复现命令 `cd STRC && py -m tools.sync_database --check`（校验算例副本未漂移）→ `py -m tools.pub_batch`（约 $30$ 秒）。算例输入取 `STRC/database/instances/`（$5$ 外部布局 + $3$ 同参数自建对照，镜像自 `clbs/input/pub|ext/`；原始布局发布件另镜像在 `STRC/database/raw/`，来自 `clbs/database/raw/`），由 `tools/sync_database.py` 生成并记 SHA256——**真值仍在 `clbs/`**，镜像只是让 paper04 的数据与代码同处一地。**这一批的 E5 不是逐位可复现的**：R0+ 是按挂钟预算重解，其 $C_{\max}$ 随机器负载浮动（实测重跑 $6/18$ 个预算点变动 $1\sim19$），但 R2 一侧稳定。`\PubEfiveNoCross` 在修订七后为 $17/18$（余 $1$ 点两臂持平，无一点 R2 更优）。引用本批数字必须连带三处口径（边权是补的、装卸点合并、工件数据未借用），见 `STRC/database/README.md`。
- **便宜对照臂与算例规模两批（第三、四本账本，`\MsShift`/`\Scale*` 两族的来源）**：`cd STRC && py -m tools.cheap_baselines`（约 $5$ 秒，出 `experiments/cheap_baselines.csv`，$150$ 行 = $50$ 对 $\times$ $3$ 臂）→ `py -m tools.scale_curve`（约 $9$ 秒，出 `experiments/scale_curve.csv`，$210$ 行 = $70$ 对 $\times$ $3$ 臂）→ `py -m tools.paper_numbers`（末两段打印这两批）。E8 的算例由 `cd clbs && py -m tools.gen_instances --tags mid --het 0.3 --jobs 2k --machines k --agvs k --ops 3 --tt-tp 1.0 --seeds 42` 逐档生成（$k=5..10$；$k=4$ 就是主批次的 `S8x4x4-LD22`）。**两批都不并入主批次的 $50$ 对**：E7 与 E1–E3 同协议但多两条臂，E8 换的是算例规模。**耗时列不是逐位可复现的**（挂钟量，重跑浮动约 $\pm5\%$），可行率、闭包占比与逐格 $C_{\max}$ 胜负是确定的。引用 RD 的 $C_{\max}$ 必须连带它的 A2 越界格数——单独引会误导。
- **单一来源纪律**：`paper04.tex` 导言区有一块宏（`\NPairs` `\PassInvar` `\FeasRone` …），正文一律用宏不写字面量，与 paper01 同一体例。改数只改那一块，并跑一遍 `paper_numbers.py` 核对。

### 二十二条结论

#### 门槛结果

**结论一（E1，$50/50$）：B 类扰动下任务图影响集恒为空，而排程确已不可行。** $T_{impact}=0$，同时 $\text{Seeds}\neq\emptyset$、$\mathrm{Cl}\neq\emptyset$。闭包规模按算例（$10$ 种子均值）：`example_3x3x2` $16.7$（种子 $5.6$，全表 $28.6$）、`congested_8x4x4` $48.3$（$15.0$／$85.0$）、`S8x4x4_high` $85.9$（$15.5$／$150.5$）、`S8x4x4_funnel` $82.1$（$15.5$／$149.1$）、`S8x4x4_mid` $80.2$（$15.0$／$147.6$）。**这是本文问题成立的直接证据，也是全文最干净的一组。**

**结论二（E3，$50/50$ vs $0/50$）：同一修复引擎下，只改边界定义，R1 全败、R2 全胜。** 这是本文最强的一组对照——所有其他变量都被消掉了，可行与否**只**取决于释放集合怎么算。R2 修复耗时中位 $3.1$ ms、最大 $15.0$ ms。

**结论三（E2a，$50/50$）：闭包结构闭合，外侧泄漏为 $0$。** 即不存在「闭包外的活预约与闭包内重规划结果相冲突」的情形。这使外侧冻结成为一个**有保证**的操作，而非碰运气。

**结论四（E2b，$50/50$；本条已由结论二十三改写）：闭包外侧预约字段逐条不变。** 逐算例均 $10/10$。~~旧版：仅 $24/50$，漂移中位 $2$、最大 $10$。~~ 那个读数是实现缺陷造成的，不是边集缺陷，成因与修法见结论二十三。**但 `prop:outside` 仍只写「充分条件」**：$50/50$ 说明四条前提在本文协议下成立，不说明它们可以省略——前提 (iii) 恰恰是被悄悄破坏过的那一条。

#### 必须如实报的负面结果

**结论五（E5，$C_{\max}$ 上完全不交叉，且方向不利）：有界修复在解质量上全区间劣于热启动全局重解。** 修完粒度错配后重跑（E5 仍取 $3$ 种子 $\times$ $3$ 预算 $\times$ $2$ 算例 $=18$ 个预算点）：

| 算例 | R2（有界修复） | R0+ 三档预算（$0.2$/$1$/$2$ s） | R2 改动比例 | R0+ 改动比例 |
|---|---|---|---|---|
| `congested` | $299.0$（恒定） | $212.7$ / $160.7$ / $140.0$ | $0.59$ | $1.00$ |
| `example` | $88.7$ | $45.7$ / $45.3$ / $45.3$ | $0.66$ | $0.97$ |

R0+ 即使在**最小**预算下也优于 R2，且随预算增加继续拉开；$18/18$ 个预算点无一例外。原先期待的「小预算下 R2 占优、大预算下 R0+ 反超」的**交叉点不存在**。**这条不能藏，也不能靠换指标绕过去；正确做法是改写 C6 的定位（见「机制层」）。** 注意 R2 的 $C_{\max}$ 在 `example` 上由 $92.0$ 降到 $88.7$——按段释放少动了一批空载段，顺带小幅改善了质量，但不足以撼动结论。

#### 时间与稳定性

**结论六：耗时相差两到三个数量级。** R2 单次修复（E3 批次，无扩域）：中位 $3.1$ ms，逐算例均值 $0.68$/$2.12$/$3.45$/$3.29$/$5.25$ ms。规模扫描下的加速比：`congested` $87\times$–$963\times$，`example` $667\times$–$3051\times$。**这是 C6 立得住的那一半。**

**结论十五：预约改动比例 $57\%$–$60\%$ vs $97\%$–$100\%$。** E5 的直接读数：`congested` 上 R2 改动 $0.573$、R0+ 改动 $0.992$–$0.996$；`example` 上 $0.599$ vs $0.965$。**不取单点、按区间报**——R0+ 在 `congested` 上贴着 $1.00$（修订七前的批次上确实到过字面的 $1.00$），在 `example` 上又只有 $0.97$。这是稳定性维度的读数，但**作为指标不新**（查新 P6）。

**结论十四：恢复可行是有代价的。** 案例（`example_3x3x2` seed $42$）中 $C_{\max}$ 由 $57$ 升至 $86$，$|Seeds|=6$、$|\mathrm{Cl}|=16$，亚毫秒级。修完粒度错配后该格读数未变（脚本重跑确认）。与结论五一致——本文买到的是速度与稳定性，卖掉的是质量。

#### 机制与口径

**结论七（口径缺陷，已修正）：扩域必须在 E3 里关掉。** 扩域把小 $\varphi$ 情形的可行率由 $50\%$ 拉到 $100\%$——效果是真的，但开着它做 E3 时，R1 会靠一路扩域到「释放全部未来」而偶然成功，测的就不是边界定义了。修正后 R1 才是零通过（当批 $0/15$，扩样后 $0/50$）。

**结论八（E4，不作硬贡献）：闭包规模随争用结构的变化只有中位支持。** `funnel` $0.254$ > `high` $0.204$（中位），但**均值被单个种子翻转**（`2024` 一列 high $0.372$ > funnel $0.296$）。C5 因此只作观察，不作贡献。**扩样后有第二次独立否证，见结论二十。**

**结论九（协议缺陷，已修正，两处）：** （i）微阻断协议曾把**窗外**预约一并封死 → STRC 系统性不可行；（ii）受扰工序的运输**已在 $t_{now}$ 前结束** → 种子集为空、实验退化。两处都修正之后才有现在这张表。**内部记录：早期任何在这两个 bug 之前产出的数字一律作废。**

**结论十（实现缺陷，已修正）：早期 $G_R$ 漏了同机后继边**，导致外侧冻结与机器时间轴冲突、闭包不闭合。补边之后 E2a 才全通过（当批 $15/15$，扩样后 $50/50$）。**四类边是补漏补齐的，不是一开始就设计对的。**

**结论十一（实现缺陷，已修正，两处）：** E3 中 R1 的影响集**未按 $t_{now}$ 过滤**；E2 的「包含性」判定与闭包的 alive 口径不一致。两处都会虚增 R1 的表现或虚增 E2 的通过率。

**结论十二（已被结论十九取代）：A 类扰动上 R1 与 R2 基本重合。** 早期单次观测 $|R1|=|R2|=18$ 且 $R2 \supseteq R1$。**这条的「基本重合」部分在扩样后不成立**——见结论十九：包含关系 $100/100$ 成立，$81/100$ 上闭包严格更大，剩下 $19$ 格才真的重合。原结论是单次观测的过度概括。

**结论十三（表述纪律）：`prop:miss` 的前半句是定义性的。** 「B 类扰动在 $G_T$ 上种子为空」按 B 类的定义即成立，不是发现；非平凡的是后半句「而排程确实已不可行」。写作时必须把重量压在后半句上，否则会被读成同义反复。**已写进 `paper04.tex` §6.2**：读 `tab:e1` 时要看 `feasible_after_block` 那一列恒为假，而不是只看 $T_{impact}=0$ 那一列。

---

#### 本次扩样新增（结论十六至二十二）

**结论十六（实验规模，已解决）：$15$ 对扩到 $50$ 对，全部门槛结论保持。** $5$ 算例 $\times$ $10$ 种子。E1 由 $15/15$ 变 $50/50$，E2a 由 $15/15$ 变 $50/50$，E3 由 $0/15$ vs $15/15$ 变 $0/50$ vs $50/50$。新增的两个算例（`S8x4x4_funnel`、`S8x4x4_mid`）与原有的 `high` 同规模同柔性，只差走廊拓扑——这是有意选的，为的是让「闭包规模差异」有一个受控的归因通道。跑一遍约 $4$ 分钟。

**结论十七（已作废，被结论二十三推翻）：~~E2b 的低通过率不是样本噪声，是实质缺陷。~~** 上一版写道：$7/15=46.7\%$ → $24/50=48.0\%$，样本翻三倍而比例不动，故断定为「四类边没有覆盖若干弱耦合」这一实质缺陷，并据此把「加细边集」提为首要后续工作。**推断的前半段对，后半段错。** 比例不随样本变化确实说明它不是噪声，但成因不是边集对物理世界的覆盖度，而是实现与定义的粒度错配——见结论二十三。这条留在台账里不删，因为它示范了一个真实的误判路径：**「不是噪声」并不蕴含「是模型缺陷」，还有第三种可能是「是实现缺陷」，而我上一版直接跳过了这一支。**

**结论十八（分类订正）：车辆故障是 A 类。** 见「扰动模型」一节的修订框。本备忘上一版把它列进 B 类，与代码和 `paper04.tex` 都不一致。

**结论十九（E6，A 类扰动，$100$ 次测量）：闭包边界是任务图边界的一致加强，不是另一种取舍。** 两类 A 扰动的 $100$ 次测量上 $U_{R1}\subseteq\mathrm{Cl}$ **全部成立**。**订正（见结论二十四）**：包含在 $81/100$ 上是严格的，其余 $19$ 格两者恰好相等，上一版写的「且闭包严格更大」是错的。均值上闭包确实更大（车辆故障 $31.1\to53.0$，机械臂故障 $42.8\to62.7$）。**含义**：改用闭包边界在 A 类上不会丢失任务图能看见的东西，代价只是释放集变大。结合 B 类的 $0/50$ vs $50/50$，闭包边界在四类扰动上一致不劣于任务图边界。这条把「A 类只有单次观测」这个待决事项彻底关掉了。

**结论二十（第二次否证，C5 彻底不作贡献）：$\mathrm{Cl}/|R|$ 在三个同规模布局上落在 $0.54$–$0.57$，没有区分度。** E4 用 LU 割边阻断协议测过一次，E1 用繁忙走廊阻断协议在三个受控布局上又测了一次，仍然测不出来。**订正（见结论二十五）**：E4 那次的正确说法不是「中位支持、均值翻转」——均值与中位同向（funnel $0.244$/$0.254$ 对 high $0.229$/$0.204$），真正的问题是两组取值区间高度重叠（high 的 $[0.156,0.372]$ 把 funnel 的 $[0.180,0.309]$ 整个包住），$5$ 种子 $\times$ $2$ 布局支撑不了任何方向。**两个协议、两个指标（最小割、漏斗占比）都没做出可复现区分**，说明这件事需要比最小割更细的刻画。已写进正文 §4.3 与 §6.7。

**结论二十一（表述纪律）：R1 的失败方式是「判断为无需修」，不是「修得不好」。** R1 释放集为空，第 1 级改路对空集是恒等操作，原排程原封不动留在被阻断的走廊上。不写这句，$0/50$ 会被读成机制层的能力比较，把问题层的结论降格。

**结论二十二（最小性，明确不作主张，并给出实测依据）：闭包覆盖 $t_{now}$ 之后活预约的约 $90\%$。** 相对「释放全部未来」这一平凡上界只省下一成。有界修复真正省下的是**不动过去**与**不重开搜索**，不是把未来切得很小。两个口径都要给：按全表算 $\mathrm{Cl}/|R|\approx0.57$（好看），按活预约算 $\approx0.90$（不好看），差别在于全表包含 $t_{now}$ 之前按假设 A1 本就不可改的那一半。正文 `rem:minimality` 给了不作最小性主张的三条理由：(i) 最小释放集的定义依赖修复引擎，而闭包刻意不依赖，这正是它能作公共输入的原因；(ii) 四类边是充分方向的过近似（让行边只按时间重叠建立，不判断是否真的只能等；同车后继边把整条链连通）；(iii) 实测确实不小。

#### 逐句复核 `paper04.tex` 查出的三条（2026-08-16 第二轮）

**结论二十三（本轮最重要的一条，推翻结论十七）：E2b 的 $24/50$ 是实现与定义的粒度错配，不是边集缺陷；修掉之后是 $50/50$。**

- **成因。** 释放集 $U=\mathrm{Cl}$ 是一个**预约**集合，预约的粒度是「段」（`J3-2-empty` 与 `J3-2-loaded` 是两条）。而 `replay_reroute` 原先用 `_op_needs_release` 按**工序**判定是否重路：两段中任一段落在闭包内，整道运输就被重规划。于是当只有满载段进闭包时，同工序的空载段也被改写，而它的 task 标签不在 $U$ 里，`outside_reservations_unchanged` 遂记为「外侧漂移」。
- **证据。** $26$ 个不通过格上共 $84$ 条漂移预约，**逐条核对，无一例外都是这种同工序空载段**；其中 $71$ 条（$85\%$）的 $t^e$ 早于 $t_{now}$，即它们在决策时刻之前就已执行完毕——改写它同时**违反假设 A2**（已完成占用不回溯修改）。
- **这意味着测的根本不是命题。** `prop:outside` 的前提 (iii) 是「仅对 $U$ 内运输调用路由器生成新占用」。按工序判定时这条前提就没被满足，所以旧的 $24/50$ 既不能证伪命题，也不能说明边集不够——它只说明实现没落在命题的前提域里。
- **修法与结果。** 改为按段判定（新增 `_segments_to_reroute`：已执行完或未被释放的空载段沿用原计划，只重规划真正落在 $U$ 内的段），漂移归零，**E2b $24/50\to50/50$**，逐算例均 $10/10$。顺带 R2 的 $C_{\max}$ 在 `example` 上由 $92.0$ 降到 $88.7$、改动比例由 $0.70$ 降到 $0.66$（少动了一批本不该动的空载段）。
- **试过但不够的一条路。** 先试的是「给 $G_R$ 补第五类边：同工序两段互指」，只把 E2b 拉到 $29/50$。原因是闭包按定义只含**活**预约（`def:strc` 的 $R^\circ$），而 $85\%$ 的漂移段是死的，补边根本够不着它们。**这反过来确证了成因在实现侧而非边集侧。**
- **正文落点。** `rem:e2b_history`（§4.2，专门记这次误判）、`rem:precommit` 补一段前提 (iii) 如何失效、§6.3 整段重写、局限章新增一条「依赖边集是否穷尽物理耦合本文并未验证」、后续工作第一条由「加细边集」改为「为边集建立独立检验手段（扰动–响应差分实验）」。

**结论二十四（订正）：A 类上「闭包严格更大」是 $81/100$，不是 $100/100$。** 包含 $U_{R1}\subseteq\mathrm{Cl}$ 确实 $100/100$；但 `agv_breakdown` 有 $11$ 格、`ra_failure` 有 $8$ 格上 $\mathrm{Cl}$ 与 $U_{R1}$ **恰好相等**（闭包沿依赖边没走出任务图影响域诱导的那个集合）。相等**不是包含失败**，但也不是「严格更大」。`paper_numbers.py` 一直只打印 `R2>=R1`，「严格」二字是写正文时自己加的——**这是一类典型的错误：工具报的是弱结论，落笔时不知不觉写成了强结论。**

**结论二十六（本轮最重要的一条，把结论二十从「测不出」升级为「否证」）：闭包规模不由装卸点处的静态割决定。** 换用五张不由本文设计的外部布局（Lyu 附录 A 拓扑，与自建族逐参数同口径）后，同一 LU 割阻断协议给出两个方向相反的结果，**必须一起读**：

- **闭包占比确实大幅变动，但变动的来源不是路网几何（已订正，见下）。** 外部算例的 $\mathrm{Cl}/|R|$ 中位落在 $0.305$–$0.512$，极差 $0.207$；逐参数同口径的三张自建布局只落在 $0.446$–$0.497$，极差 $0.051$。
  - **⚠ 这里曾写成「闭包占比确实随布局显著变化，幅度约为自建族的四倍」，那句是错的，$2026$-$08$-$18$ 订正。** 把六个原始布局文件全解析后发现：**五个外部算例只落在两张路网上**——`LyuL2`（$4$ 机）与 `LyuL3`（$5$ 机）共用同一张 $4\times4$ 网格、删掉同一条边 $(7,11)$；`LyuL4/5/6`（$6/7/8$ 机）共用同一张 $5\times5$ 网格、删掉同两条边 $(8,13)(19,20)$。而极差 $0.207 = 0.512(\text{LyuL2}) - 0.305(\text{LyuL3})$，**两个端点共享同一张路网**。所以这个数测的是**机器落位**，不是路网几何。方向也不一致：$4\times4$ 上机器由 $4$ 台增至 $5$ 台占比反降，$5\times5$ 上由 $6$ 到 $8$ 台则升（$0.385/0.422/0.460$，组内极差 $0.075$），说明是落位而非台数。
  - **订正后的说法反而更有力：路网固定、只换机器落位，占比变动幅度可超过换三张不同路网的幅度**（$4\times4$ 上约四倍，$5\times5$ 上约一点五倍；两张上成立的是不等号方向，倍数只有两个样本不宜细读）。上一轮（修订三）猜的「同族变体几何差异不足」**不能**由此证实——路网几何这一维只有两个独立样本，本文无权对它下结论。
- **但指标并未因此得救，反而暴露出更硬的问题。** 这批算例的 **LU 最小割恒为 $2$、漏斗占比恒为 $0$**。这不是巧合而是结构性的：两张路网都把装卸站放在网格角点，而四邻接网格的角点恰有两条出边，割因此被钉死。**一个取值恒定的量不可能解释一个变动 $0.207$ 的量**——这两个指标连「相关性弱」都谈不上，是**无从谈起**。第三个指标（每节点走廊数）虽有两个取值，秩相关为 $0$。
  - **订正带来的额外好处：否证反而变干净了。** 原来的论证形式是「换布局→占比变了→但割没变」，其中「换布局」这一步夹着路网与落位两个变量；订正后是「**路网一字不动**、只换落位→占比变了→而割由定义就不可能动」，混淆被消掉了。代价是适用范围要如实写窄：本文否证的是「静态割能解释闭包规模」，**不是**「路网几何与闭包规模无关」——后者需要更多张互不相同的公开路网，而可得的就这么多。
  - **别把这句overclaim成「公开布局一律把装卸站放在角点」。** 落笔时我先写成了这个版本，核对时发现反例就在手上的数据里：同一工具集里 Liu 那族（因允许对角移动而未收入）把装卸站放在**网格边的中点**，出度是 $3$，不是 $2$。准确的说法是**该指标在真实布局上只取到极少数几个由「装卸站摆在哪种网格位置」决定的离散值，不随几何连续变化**——这一版才是数据支持的。已在正文与宏注释里各改一处。

**为什么这比原来的说法有价值。** 原来只能说「两个指标没测出区分度」，这是一句关于本文实验的陈述；现在可以说「最小割这一族指标在真实布局上退化为常量，不具备跨布局的解释力」，这是一句关于**指标本身**的陈述，并给出了退化的机制（装卸站在角点）。**方法论教训**：这两个指标是围绕哑铃布局那个可调的 LU 咽喉（`lu_exits` $\in\{1,2\}$）设计出来的——**指标是跟着自建算例的旋钮长出来的，于是只在有那个旋钮的地方有方差。** 换一批没有该旋钮的布局，它就什么也说不了。下一个候选刻画必须是随排程而变的量（$t_{now}$ 邻域内走廊占用的时序密度），这同时意味着要重新界定「可预测性」问的是什么：给定布局预测闭包规模，还是给定布局**与当前排程**预测它。正文 §6.7 与 `tab:e4pub`。

**结论二十七（外部布局批次，主张一条没被推翻）：换掉布局出处后 E1／E2／E3／E5 逐项复现。** $5$ 张外部布局 $\times$ $10$ 种子 $=50$ 对：任务图漏报 $50/50$（结构泄漏计 $0$）、结构闭合 $50/50$、外侧字段逐条不变 $50/50$、第 1 级改路可行 $0/50$ 对闭包边界 $50/50$、E5 的 $18$ 个预算点上 R0+ 的 $C_{\max}$ 全部更优（交叉点仍不存在）。E1 协议下闭包占比逐布局中位 $0.438$–$0.547$，与主批次的 $0.54$–$0.57$ 同量级。

**这一批的意义不在于新的性能数字**，而在于钉住「这些现象是否只在本文自己设计的布局上成立」这一问：B 类扰动使任务图边界失效、预约闭包包含之、闭包外字段不受扰动，三件事在一批不由本文选定的拓扑上同样成立。**唯一被改写的结论是结构可预测性，而它本来就是负结果。** 三处口径必须随数字一起报：(i) 逐段行驶时间不是原始数据（Lyu 只为单个示例算例发表过，且取值不均匀），本批按所有边等权补齐，**故不可与 Lyu 或 van Os 的参照值比较**；(ii) 原布局有分离的装货站与卸货站，本项目只有一个装卸点，取车辆起始的装货站充当，卸货站退化为普通节点；(iii) **工件数据没有借用**——公开族的平均运输时长只占平均加工时长百分之几，一并搬入会让研究对象消失。正文 §6.1 与 §6.10。

**结论二十五（订正）：E4 的「均值被单个种子翻转」是算术错误。** `tab:e4` 五个种子：funnel 均值 $0.244$、中位 $0.254$；high 均值 $0.229$、中位 $0.204$。**funnel 在均值与中位上都更高，没有翻转。** 种子 $2024$ 单格反向（high $0.372$ > funnel $0.296$）不等于均值反向。正确的自我批评是区间重叠而非均值翻转，正文已改。

#### 便宜对照臂与算例规模（2026-08-20 修订十）

**结论二十八（E7，$50$ 对 $\times$ $3$ 臂）：「低两至三个数量级」不是对照选贵了；而唯一比有界修复更快的臂，是那个不划边界的臂，它质量与稳定性双输。** 三臂同协议（同 E1–E3）：RS 全局右移 $1.74$ ms／平均退化 $58.8\%$／改动比例 $0.627$／A2 越界 $0/50$；RD 原染色体重解码 $7.17$ ms／$46.5\%$／$0.586$／越界 $31/50$；R2 $3.11$ ms／$49.8\%$／$0.535$／$0/50$。逐格 $C_{\max}$：R2 对 RS 赢 $27$ 平 $16$ 输 $7$，对 RD 赢 $2$ 平 $23$ 输 $25$。**要点有三。** (i) **RD 比 R2 慢 $2.3$ 倍**——一次全局重算是搜索的原子代价，任何以解码评估候选的搜索都至少要付这笔钱，所以速度优势不来自对照的配置。这是修订八里被否掉的「挂钟下限」的可用替代版本：不再声称配置无关的结构性下限，只声称同一实现里最便宜那一档实测更慢。(ii) **右移能不划边界，恰恰因为它放弃了选择**——想只推迟真正受影响的车，就必须先知道哪些预约受影响，那正是本文定义的对象。这是本轮唯一一条不靠新数据、纯靠对照臂存在才说得出的论点。(iii) **RD 的 $C_{\max}$ 优势主要来自越界**：按 A2 越界与否分层，在它恰好没越界的 $19$ 格上 R2 赢 $1$ 平 $9$ 输 $9$（E8 的 $46$ 个同类格上赢 $5$ 平 $22$ 输 $19$）。⚠ 这是差距的**上界估计**，不是受约束对照的成绩——RD 并非受 A2 约束的算法，只是在那些格上没用到那份自由度。

**结论二十九（E8，$7$ 档 $\times$ $10$ 种子 $=70$ 对）：闭包占比不随算例规模上涨，有界性未被稀释。** 梯子只变规模（工件 $2k$／机器 $k$／车 $k$，$k=4..10$），拥堵档、$H$、$F$、Tt/Tp 与 LU 割、远端割同口径标定。$\mathrm{Cl}/|R|$ 从 $0.544$（$8\times4\times4$）**降**到 $0.426$（$20\times10\times10$）；R2 耗时随 $|R|$（$148\to402$）近似线性升至 $11.8$ ms；第 1 级可行 $69/70$，唯一失败格（$10\times5\times5$、种子 $42$）由扩域第 $3$ 轮兜住（总 $20.1$ ms，$C_{\max}$ 与首级相同），终可行 $70/70$。**两个对照的差距都随规模拉大**：RD/R2 耗时比 $2.3\times\to5.5\times$，R2 对 RS 的胜负 $7/1/2\to$ 末三档 $10/0/0$。⚠ 口径：一个拥堵档、一个算例种子，$7$ 档**不支撑任何渐近论断**，能说的只是这段区间内没有退化。

**结论三十（缺口的表述方式被换掉了，而新表述更强）：不要写「④ 层的工作用错了边界」，要写「两个条件至今没有同时松开」。** 原文那句「④ 层把执行期扰动留给了任务图边界」是**推断**，被引的三篇静态一体化工作并没有这样说。查证得到三条可直引的事实：持预约表的一侧明确排除执行期故障（`sun2023integrated` 假设「AGV 始终可用，不考虑故障与充电」，结论把设备维护与故障列为后续工作）；处理执行期故障的 AGV 一侧边界一律落在任务对象上（`zhang2023energy` 是「故障后尚未加工的工序整体重排」，`tang2026dynamic` 是保留/继续/重构三分）；**而后者能一致地这样划，恰因为不持预约表**（`zhang2023energy` 明写「忽略 AGV 之间的碰撞」）。于是缺口是结构性的：**工序级边界成立的前提正是没有预约表，而现有预约表设定又恰好把执行期扰动排除在外。** 这比原表述强，因为它从一句可被单个反例推翻的现在时断言，变成一个在两者并置时必然出现的失效，并且顺带解释了它为什么至今没暴露。**表述纪律**：R1 对照臂复现的是 ① 层的标准做法，**不代表任何一篇 ④ 层工作的实际选择**，正文 §2.5 已明写这一句。

---

## 与 paper01 的分工

| | paper01 | paper04 |
|---|---|---|
| 时间设定 | 静态、完备信息 | 执行期、$t_{now}$ 之后 |
| 预约表的用途 | **构建**排程（试探派车、闭环评价） | **定位**损坏（闭包、冻结） |
| 核心对象 | 层间接口（下层向上层返回什么） | 影响边界（扰动之后释放什么） |
| 主要指标 | 完工时间相对降幅 | 可行率、修复耗时、预约改动比例 |
| 实验规模 | $10\times10=100$ 对 | $5\times10=50$ 对 |
| 共享代码 | `clbs` 全栈 | 复用 `clbs` 的 `ReservationTable`、`Network.route`、`validator`、`blocking_opponents`；替换上层 |

### 必须写清的一处自洽问题

paper01 判定「**冲突凭证制导**」为废（$-1.72\%$，$16$ 格中仅 $1$ 格显著），而本文复用同一套 `blocking_opponents` 去构造闭包。答辩时必然被问：「你第二章不是说这条路不通吗？」

**化解句（两篇 memo 都要写）：**

> **影响闭包是一个 measurement，不是一个 decision。**

paper01 的凭证机制失效，是因为它用凭证去**制导搜索**——它买的是「改进一个决策」，而收益要经受关键链的**稀释**（消除一处走廊延误会把另一个约束顶成紧约束，命中率实测 $<2\%$）。本文用同一份阻塞关系去**划定损坏范围**——它买的是「改进一次测量」，测量不经受稀释：闭包要么包含了所有必须释放的预约，要么没有，这与关键链上顶上来的是谁无关。

这与 paper01「主要洞见」第二条立下的判据——**区分「改进一个决策」与「改进一次测量」**——是**同一条规则的两侧证据**，不是自相矛盾。这一点写好了反而是加分项：它说明两章共用一个方法论。

**引用纪律：** 本文把 paper01 作为相关文献处理，独立可读；不假设读者读过第二章。paper01 的具体百分数不在本文出现（除非用于说明角色分工）。

---

## 图表清单

现稿已有的（`paper04.tex` 编译后 $20$ 页，图 $7$ 表 $9$）：

| 编号 | 内容 | 状态 |
|---|---|---|
| `fig:motivate` | 动机图：走廊阻断 → 任务图无变化／预约图级联 | ✅ 已有 |
| `fig:closure` | 闭包构造示意（种子 → 传递闭包 → 外侧冻结） | ✅ 已有 |
| `fig:framework` | STRC 有界恢复框架总览 | ✅ 已有 |
| `fig:e1e3` | E1/E3 条形图 | ✅ **已按 $5$ 算例 $\times$ $10$ 种子重画**（脚本 `fig_strc_e1e3.py` 的算例名单原写死三个，已改） |
| `fig:case` | 案例甘特（修复前后 + 闭包着色） | ✅ 已重跑，数据未变 |
| `fig:e5` | E5 双面板：耗时–$C_{\max}$（**不交叉，如实画**）+ 改动比例 | ✅ 已重画 |
| `fig:scale` | $\varphi$ 扫描 | ✅ 已重画（$10$ 种子） |
| `tab:position` | **相关工作四层定位**（AOR/match-up/LRS → alternative graph → MAPF Replan Affected → 本文） | ✅ **本次新建**，跨栏 `table*` |
| `tab:notation` | 记号表 | ✅ 已有 |
| `tab:e1` | E1 逐算例：$|R|$、$|\text{Seeds}|$、$|\mathrm{Cl}|$、$\mathrm{Cl}/|R|$ | ✅ 已扩为 $5$ 行 + 合计行 |
| `tab:e2` | E2a/E2b 逐算例 | ✅ **本次新建**（上一版 E2 只有正文一段话，没有表）；第二轮后三列全为 $10/10$ |
| `tab:e3` | E3 主对照 + 释放集规模 + 耗时 | ✅ 已扩 |
| `tab:e6` | **扰动类型 $\times$ 边界定义覆盖矩阵** | ✅ **本次新建** |
| `tab:e5` | E5 权衡（$C_{\max}$ + 改动比例） | ✅ **本次新建**（上一版只有图，没有表） |
| `tab:e4` | E4 闭包占比逐种子 | ✅ 已有，正文说明已改写 |
| `tab:e4pub` | E4 布局出处外部化对照（外部 $5$ 张 + 同参数自建 $3$ 张） | ✅ 修订四新增 |
| `tab:scale` | $\varphi$ 扫描 | ✅ 已扩 |

仍缺、但优先级不高：$G_T$ 与 $G_R$ 顶点集不相交的对照图；$G_R$ 四类边的独立示意图（现由 `fig:closure` 兼任）；扰动二分与假设 A1–A5 的表（现为正文列表）。

---

## 待决事项与风险

### 已结（2026-08-16）

| 原优先级 | 事项 | 怎么解决的 | 落点 |
|---|---|---|---|
| **最高** | 相关工作未引 alternative graph | 补引 `mascis2002alternative` + `dariano2007branch`，§2.2 整节写「用途不同（选弧 vs 取闭包）／层级不同（区段即机器）」两条区分；摘要与 §1.2 也各出现一次 | §2.2、`tab:position` 第 ② 行 |
| **最高** | 未引 AOR 原文，只引综述 | 补引 `li1993heuristic` / `abumaizar1997rescheduling` / `subramaniam2003maor`，§2.1 明写「**本文的 R1 对照臂就是 AOR**」，§1.2 与摘要各点名一次 | §2.1、`tab:position` 第 ① 行 |
| 高 | 摘要与引言的全称否定 | 收窄为「在同时持有任务依赖图与显式走廊预约表的这一支调度文献中」；§1.2 另加一段逐条排除第 ②③ 层；§2.5 整段说明为何必须收窄 | 摘要、§1.2、§2.5 |
| 高 | **实验规模 $15$ 对** | 扩到 **$5$ 算例 $\times$ $10$ 种子 $=50$ 对**。新增两个算例与原 `high` 同规模同柔性、只差走廊拓扑，使布局归因受控。全部门槛结论保持（结论十六） | `expand_batch.py` 的 `_instances()` 与默认种子；全实验章 |
| 高 | E5 曲线不交叉 | 不改数据，改定位。新增 `tab:e5`，明写「设计意图原是找交叉点，**该交叉点不存在**」，并加一段说明为何不换指标绕过去；§5.3 把「互补」的含义限定为「被不同约束选中」 | §5.3、§6.7 |
| 中 | B 类只做了走廊阻断一种 | 新增 **E6**：四类扰动 $\times$ $50$ 对 $=200$ 次测量，覆盖矩阵在**边界**一维填满 | §6.5、`tab:e6` |
| 中 | A 类扰动只有单次观测 | 同上。A 类 $100$ 次测量上 $U_{R1}\subseteq\mathrm{Cl}$ 全部成立（$81/100$ 严格，结论十九与二十四），已成节 | §6.5 |
| 中 | `prop:invariance` 写作充要 | **现稿本来就已写作「外侧字段不变的充分条件」**（`prop:outside`），这一条是备忘的误记。但顺手做了实质加强：新增 `tab:e2`、加一段区分 E2a 与 E2b。第二轮又补写「$50/50$ 不等于充要，只说明四条前提在本文协议下成立」 | §4.2、§6.3 |
| 中 | 闭包最小性未讨论 | 新增 `rem:minimality`，明确**不作最小性主张**，给三条理由（定义依赖引擎／四类边是过近似／实测确实不小），并给出实测依据：闭包覆盖活预约约 $90\%$（结论二十二） | §4.2、§6.5、局限 3 |
| 低 | 多扰动叠加、扰动流 | 结论章新增 §7.3「后续工作」成节，现并入第三件「把覆盖矩阵填满」 | §7.2 局限 3、§7.3 |
| 低 | 修复不改指派（A4） | 已在 §7.2 限制 5 明写「放开它们即向全局重解退化，『有界』也随之失去边界」 | §7.2 |

**顺带做的两件不在原表上的事：** (a) 导言区加了一块**单一来源宏**（`\NPairs` `\PassInvar` `\FeasRone` …），与 paper01 同体例，正文不写字面量；配套新建 `STRC/tools/paper_numbers.py` 从 CSV 打印这些数。(b) 结论章由一段话拆成 §7.1 结论 / §7.2 局限 / §7.3 后续工作（当时四件，2026-08-18 增至五件，同日修订四增至六件）。修订四另加一组外部布局批次专用宏（`\NPubInst` `\NPubPairs` `\PubClo*` `\SelfClo*` …），与主批次的 `\NPairs` 一族**分开命名**——两批口径不同，共用一套宏迟早会被当作同一批数据引用。

### 第二轮（逐句复核 `paper04.tex`）结掉的

| 事项 | 处理 | 落点 |
|---|---|---|
| **E2b $24/50$ 的成因判错**（结论二十三） | 定位为粒度错配并修掉代码，E2b $\to50/50$；台账保留旧结论十七作为误判样本 | `repair.py` 的 `_segments_to_reroute`；`rem:e2b_history`、`rem:precommit`、§6.3、局限 2–3、§7.3 第一条 |
| 「A 类闭包严格更大」失实（结论二十四） | 改为「包含 $100/100$，其中 $81/100$ 严格，$19$ 格相等」；新增宏 `\StrictlyLarger` | §6.5、结论十九 |
| E4「均值被单个种子翻转」算错（结论二十五） | 改为区间重叠论证，并给出两组的实际区间 | §4.3、§6.7 |
| A 类扰动的种子规则从未写进论文 | `def:seeds` 由「只讲走廊阻断」扩为三条（阻断/降速、车辆故障、机械臂故障），并说明机械臂那条为何要沿工件链向后取 | §3.3 |
| $\theta$ 从未给值 | 协议节写明 $\theta=2$，并说明它只影响 A 类的 $|U_{R1}|$ | §6.1 |
| 扩域耗时随 $\varphi$ 的因果叙述与数据不符 | 数据非单调（$7.9/9.7/6.7/7.9/9.6$ ms），改为「两种效应方向相反，只宣称不崩溃」 | §5.2 |
| 若干口径小误 | 扫描耗时 $9$–$13\to7$–$10$ ms；`tab:e1` 的 `n` 列改名「种子数」（原与 §3.1 的工件数 $n$ 撞名）；§1.3 的「常取得更优」改为「每一个预算点」；改动比例改为区间 $59\%$–$66\%$ vs $97\%$–$100\%$；§2 开头「四层」改为带编号列举 | 全篇 |
| CCS 未填、图缺 `\Description` | 补 `\ccsdesc` 三条与七张图的 `\Description`，acmart 警告清零 | 导言区、各 `figure` |

### 未结

| 优先级 | 事项 | 影响 | 应对 |
|---|---|---|---|
| **高** | **依赖边集是否穷尽物理耦合，至今没有检验过** | 这是结论二十三留下的真空。旧的 E2b 曾被当成这个检验，其实不是；现在 E2b $50/50$ 只说明实现与定义自洽 | 需要一个不预设耦合真值的探测手段：只改动闭包内某一条预约，观察闭包外是否出现本不该出现的连锁（扰动–响应差分）。已写入 §7.3 第一条 |
| 高 | 覆盖矩阵在「修复」一维仍只有一格 | 降速与两类故障只报了边界，没报可行率 | 给三类扰动补正确的注入通道：降速需路由层支持分段 $\tau$；车辆故障需摘除该车并允许换车（会触及 A5） |
| 中 | 闭包覆盖活预约 $90\%$，「有界」的省量有限 | 「局部」「有界」两个词有被读大的风险 | 已在 `rem:minimality`、§6.5 与局限 4 三处主动点破。进一步的做法是定义引擎相关的最小释放集，测出闭包与它的差距 |
| 低 | ~~闭包规模的结构可预测性两次都没测出来~~（结论二十）→ **已升级为否证，见结论二十六** | E4 由「测不出区分」改为「静态割这一族指标在真实布局上退化为常量」 | 混淆已被测掉（外部布局极差 $0.207$ vs 自建 $0.051$），静态割一族已排除。**剩下的是正向工作而非未结漏洞**：找一个随排程而变的刻画（$t_{now}$ 邻域内走廊占用的时序密度、受扰走廊在让行图上的出度），并重新界定「可预测性」问的是「给定布局」还是「给定布局与当前排程」。已列为后续工作第五件 |
| 中 | **算例出处只解决了一半**（2026-08-18 新增，同日修订四部分解决） | 执行期扰动这一支没有公开算例；静态公开族发布的是**机器间行程时间矩阵**而非路网，两族共 $11$ 张布局无一能还原成与所发布矩阵一致的无向走廊图（全部不对称，其中两张违反三角不等式连有向图也还原不出），故 $R$、$G_R$、$\mathrm{Cl}(\cdot)$ 与 B 类扰动在其上**一概无从定义**。与 paper01 的关键差别：paper01 可以退化掉争用、退化后解的仍是文献那个问题，因而能借公开算例标定搜索能力；**本文没有这条退路**——退化掉争用，被研究的对象本身就不存在了 | **布局那一半已解决**（修订四）：Lyu 附录 A 六张布局的三项已逐项转录并对账，据此跑了 $50$ 对，E1／E2／E3／E5 全部复现（结论二十七）。**时间标定那一半没解决**：逐段行驶时间原文未随该批布局发表，本文补的是等权值，故这批算例**不能**复现 Lyu 或 van Os 的参照值；工件数据也未借用（公开族运输/加工时长比过低，搬入会让争用消失）。退一步的做法是在等权之外再扫若干条非均匀边权，检验结论是否对边权口径敏感——已列为后续工作第六件 |
| 低 | Liu 2023 那四张布局排除得对不对 | 无（已排除） | 其数据首行带 `d` 后缀即 `IS_DIAGONAL`，允许对角移动，而本项目走廊为四邻接；接受对角移动要改的是**下层路由层**而不是算例，性质与 van Os 的节点容量问题相同。若将来要支持，需在 `network` 里引入对角走廊并处理两条对角线在物理上相交而模型中不冲突的问题——那是另一篇的工作 |
| 中 | 结论只在一套实现上复现 | 下层路网、路由与校验器与 paper01 共享同一实现 | 归因干净是优点，但独立复现是另一回事；列为局限 10（2026-08-18 前为局限 9）。**结论二十三正是这条风险的一次兑现**——同一套实现里的一个粒度错配，差点被写成模型层的缺陷 |
| 低 | 多扰动叠加、扰动流 | 假设 A3 排除 | 已列 §7.3 |

---

## 必引且必须显式区分的文献

**① 任务图 / 时间轴上的边界（R1 的原型）**
- Li, Shyu & Adiga（1993）；**Abumaizar & Svestka（1997）**，Affected Operations Rescheduling；Subramaniam & Raheja（2003）mAOR
- **Bean, Birge, Mittenthal & Noon（*Operations Research* 1991）**，match-up scheduling；Aktürk & Görgülü（*EJOR* 1999）
- Jannach & Zanker（IEEE SSCI 2007），Local Rescheduling 的增量时间窗扩展

**② 让行图，但两图合一（最危险的先行工作）**
- **Mascis & Pacciarelli（*EJOR* 2002）**，alternative graph 与 blocking/no-wait 作业车间
- D'Ariano, Pacciarelli & Pranzo（2007 起），ROMA、实时铁路重调度、延误传播与局部调度区

**③ 有让行、无任务图**
- **Švancara, Vlk, Stern, Atzmon & Barták（AAAI 2019）**，Online MAPF：Replan All / Single / **Affected** + Online Independence Detection
- Malka, Shani & Stern（IROS 2024）；D-MAPF 的 Revise-and-Augment；I-MAPF / LIMP

**④ 两张图都有、却只用任务图（本文的位置）**
- Vieira, Herrmann & Lin（2003）、Ouelhadj & Petrovic（2009）重调度综述（**保留，但不能只有这两篇**）
- 港口 AGV 干扰管理（2024）：时间窗 Dijkstra + 干扰管理，路径偏离度 $5.56\%$ vs $44.44\%$
- `xin2025review`、`meng2025cp`、`qin2026knowledge`、`fu2025integrated`（常数运输矩阵，写不下走廊阻断——护城河）

---

# 小论文框架

## 标题、摘要、关键词

**标题：** 面向柔性作业车间与 AGV 一体化调度的时空预约影响闭包与有界重调度

**关键词：** 动态调度；重调度；影响域；时空预约；无冲突路由；柔性作业车间；AGV；有界修复
（不用：双时间尺度、NOSR、漏斗份额 $\varphi$、冲突凭证）

**摘要（五段，现稿即此结构）：**
1. 走廊被临时阻断时，没有任何工序的加工参数被改动，任务依赖图上的影响集为空——但排程已经不可行。
2. **收窄的措辞**：在同时持有任务依赖图与显式走廊预约表的这一支文献中，影响域通常沿任务依赖图界定（原型是 AOR），因此这类扰动落在其覆盖之外。
3. 方法：预约依赖图（四类边）+ 传递闭包 = 时空预约影响闭包；闭包内重规划、闭包外逐条冻结；失败扩域，仍失败升级为热启动全局重解。**闭包是一次测量，不是一个决策。**
4. 结果：$5$ 算例 $\times$ $10$ 种子 $=50$ 对；任务图边界 $0/50$ 可行 vs 闭包 $50/50$；结构闭合性与外侧字段逐条不变均 $50/50$；修复中位 $3.1$ ms，较热启动全局重解低两到三个数量级；预约改动 $57\%$–$60\%$ vs $97\%$–$100\%$。
5. 诚实边界：完工时间上有界修复在**全部 $18$ 个预算点**劣于热启动全局重解、不存在交叉点；闭包亦不最小，覆盖活预约约 $90\%$。其定位是「必须立刻给出可执行方案且不愿撕毁已下发承诺」的那一档。
6. 对照阶梯已补齐便宜那一档（E7）：唯一比有界修复更快的是**不划边界**的全局右移，而它质量与稳定性都输（逐格 $27/16/7$；改动比例 $0.627$ vs $0.535$）；同一实现里最便宜的一次全局重算反而贵 $2.3$ 倍，且 $31/50$ 格改写历史。
7. 规模上（E8）算例推至 $20$ 工件 $10$ 机、$70$ 对：**闭包占比不涨反微降（$0.544 \to 0.426$）**，响应时间随 $|R|$ 近似线性升到 $11.8$ ms，两个对照的差距同向拉大。

## 现稿结构（`paper04.tex`，$26$ 页）

| 节 | 标题 | 本次改动 |
|---|---|---|
| 1.1 | 背景与动机 | — |
| 1.2 | 任务图覆盖不到的缺口 | **收窄全称否定；点名 AOR 为 R1 原型；逐条排除 alternative graph 与 MAPF 两层** |
| 1.3 | 主张与方法预告 | **加「测量不是决策」一段** |
| 1.4 | 贡献 | **由四条扩为五条**，新增「填满的覆盖矩阵」，并点名两条负面结果 |
| 1.5 | 与静态一体化排程工作的关系 | **新增与 paper01 的自洽声明** |
| 1.6 | 篇章结构 | — |
| 2.1 | 任务图与时间轴上的影响边界 | **重写**：AOR 三篇 + match-up 两篇 + LRS。**修订十：修掉一处错引**（右移那句原引 `dalcastagne2020reactive`，那篇讲仿真优化的样本量，与右移无关），换 `tang2026dynamic` 并指向新增的 E7 节 |
| 2.2 | 让行图已有先例：alternative graph 与铁路实时重调度 | **新增整节**，含两条区分 |
| 2.3 | 无任务图的局部重规划：MAPF 的 Replan Affected | **新增整节** |
| 2.4 | 两张图并存的场合：考虑运输的柔性作业车间 | 由原两节合并。**修订十：末段推翻重写**——原先「④ 层把执行期扰动留给了任务图边界」是推断，改为三条可直引的事实（持预约表的一侧明确排除故障并列为后续工作；处理故障的一侧边界落在工序与配送任务上；后者能这样划恰因不持预约表、明写忽略碰撞），缺口改述为「两个条件至今未同时松开」 |
| 2.5 | 本文定位，以及本文不主张什么 | **新增**，含 `tab:position` 与三条禁语。**修订十：`tab:position` 第 ④ 行加执行期扰动一列引文，末列改为「两者至今未同时出现，一旦并置则错位」；正文加一句禁语——不得写成「已有工作划错了边界」** |
| 3.1–3.4 | 系统设定 / 假设 A1–A5 / 扰动二分 / 恢复问题 | **`def:ab` 把车辆故障移入 A 类，加 `rem:agv_class`；`def:seeds` 补齐 A 类两条种子规则** |
| 4.1–4.3 | 预约依赖边 / 包含性 / 结构可预测性 | **加 `rem:minimality` 与 `rem:e2b_history`；`rem:precommit` 补前提 (iii) 失效方式；4.3 改写。修订七：`rem:e2b_history` 增后半段，记同类错配的第二层（按段 → 按走廊腿）及 E2b 为何结构性看不见它** |
| 5.1–5.3 | 释放冻结与改路 / 失败扩域 / 与全局重解的对照 | **5.2 声明扩域非首创、并改掉与数据不符的单调性叙述；5.3 改写角色分工** |
| 6.1–6.12 | 协议 / E1 / E2 / E3 / **E6** / 案例 / E4 / E5 / **E7 便宜对照** / 扰动扫描 / **E8 算例规模** / **外部布局复现** / 小结 | **全部换 $50$ 对读数；协议节写明 $\theta=2$；新增 E6 一节与 `tab:e2` `tab:e5` `tab:e6` 三张表；E2/E5/扫描三节按修完粒度错配后的新读数重写。修订四：协议节增「第二批：布局出处外部化」并声明三处口径；E4 节增外部布局对照与 `tab:e4pub`；新增 §6.10「外部布局批次的复现」。修订六：协议节末增「两批算例各自承担什么」一段（含「第二批只用于收窄主张」与「只有 E4 必须两批合读」两条元判断），§6.11 小结末条同步扩写。修订七：`tab:e3`／`tab:e5`／`tab:scale` 按修完第二层粒度错配后的读数逐格重填，§6.9 耗时区间与 §6.10 的 E5 措辞随之改。修订八：§6.8 末增两段，交代全局重解对照臂未受 A2 约束（$18$ 点上越界 $80\%$–$100\%$）、其 $C_{\max}$ 宜读作参考下界；$18/18$ 与「不存在交叉点」原文保留。修订十：新增 §6.9「E7 便宜的可采纳对照」（`tab:cheap`，RS/RD/R2 三臂 $50$ 对）与 §6.11「E8 算例规模上的外推」（`tab:instscale`，$8\times4\times4$ 到 $20\times10\times10$ 共 $70$ 对），§6.12 小结增两条** |
| 7.1–7.3 | 结论 / 局限（**四条**）/ 后续工作（**四件**，与局限一一对应） | **由一段话拆成三节；局限 2–3 换成「命题仍只是充分条件」与「边集覆盖度未验证」；2026-08-18 新增局限 9 与后续工作第五件。同日修订四：局限 8 改为「已知不由静态割给出」、局限 9 改为「布局出处已外部化，但边权与工件数据仍自建」；第五件（换外部布局）已做完，替换为「给闭包规模找一个时序刻画」＋「把时间标定也外部化」两件。修订八：局限第 1 条补该对照臂的 A2 口径（不使本条失效）；后续工作增第一件「构造一个受 A2 约束的全局重解对照」。修订九：按根因归并为局限四条（旧 2/3/4/8→新 2，旧 5/6/7→新 3，旧 9/10→新 4，旧 1 单独保留）、后续工作四件并与局限 $1{:}1$ 对应，内容未删。修订十：局限 1 增一段（「对照选贵了」这一廉价解释已被 E7 排除，且分层后差距收窄至基本持平，但仍是上界估计）、局限 3 增「规模一维」一段（已推到 $20$ 工件 $10$ 机、闭包占比未涨，但只有一个拥堵档、$7$ 档不支撑渐近论断）；后续工作一增一段（受约束对照下的差距量级已被框住，把它变成读数仍需固定前缀解码）** |

**不用的词（现稿已全域清除）：** 双时间尺度、NOSR、漏斗份额 $\varphi$ 作为结构参数、冲突凭证、「首次提出让行关系」、「首次做局部重规划」、「闭包是最小的」。
