# 施工台账 · paper01/paper.tex

> 由 `PaperRefinePrompt.md` 的 S2 生成。两张表：**块清单**（施工单）与**改动记录**（撤销日志，初始为空）。
> 顺序依据 §4「自下而上、先证据后承诺」，与章号次序无关。
> **定位锚是本台账的主键，行号只是参考值**：每轮开工前用锚重新定位，不要相信上一轮记下的行号——行号会随施工漂移。锚失配即报 `ANCHOR_LOST` 并停下。
> 关联 P 条目取自 `paper01/paper-score.md` 基线一节；对应主张取自 `paper01/paper-card.md`（金标准）。
> 改动预算按 `{{BUDGET_PER_100}}` = 12 处 / 100 行计算并向上取整；压缩性删减不占该预算，但须在 §2.4 自检块单独报「原 __ 字 → __ 字」。

**当前状态**：`{{DATE}}` = **26-08-22**（首次进入 S3 之日，一经生成不再变）。`{{OUTPUT}}` = **`paper01/paper_AI-Modify-26-08-22.tex`**，已于 2026-08-22 从 `{{PAPER}}` 整份复制并在首行插入来源注释；此后 `{{PAPER}}` 只读。

已完工 15 块（B01a 9/12 条、B01b 6/9 条、B02a 7/10 条、B05a/b/c/d 全组、B04b、B04c、B04a 2/6 条、B07 8/26 条、B08、B09、B10 8/12 条、**B11 4/5 条**）＋ B03、B04d/e/f/g 部分完工，其余 15 块 `未开工`。**序 11 `F-ABS` 已完工**：摘要 853 → **697 汉字**，四段信息齐备且不越界，卡片 §5.11 的五项验收测试全部通过；决定 AA·A（文献史压成半句）与 BB·A（保留全族口径并自报）已落地，X31、X25 随之了结，新增 X33（「低开销」跨块措辞漂移，留 S4）、X34（题目 33 字超限，转 B12）。**序 12 `F-TITLE` 亦已完工**（记录 106–107）。

**遗留项清账轮（记录 97–107）**：对台账全部「待兑现」越界项做了一次集中兑现。**已了结 11 条**——X14、X15、X17（核查后判定零改动）、X21、X22、X26、X27、X28、X32、X33、X34。**X23 随后亦已了结**（记录 108–109）：回填成功且**推翻了原句**——早停阈值 $400$ 每次都在开环配置上触发（$421$–$688$ 代、只用掉预算的 $12\%$–$29\%$），两个闭环则每次跑满 $90$ s，代数上限从未触发；原文「两项均放宽至不构成约束、以确保预算被真正用尽」为假，已改写为如实交代，并顺带修正 `1825` 把「单次评价成本之比（$21$–$42$ 倍）」误安到「评价次数之比（实测 $2.5$–$13.3$ 倍）」上的量级错误。**余 2 条不可由模型执行**：X9（图内中英混用，作者暂缓）、X12（图内文字溢出，须重排版面而非调字号，作者侧）。**新增 X35 亦已同轮了结**（记录 110–111，作者取选项甲）：`fig:protocol` 原把「等挂钟双曲线」画在 $54$ s（B0 的 $17.6$ s 与闭环的 $90$ s 平均出来的、不存在的预算），图注「各配置只能沿它移动」与改后的 §5.1.2 直接矛盾；现灰线改画在名义上限 $90$ s，B1／B2 贴线而 B0 显式落在线下方并注明是收敛自停。同时修掉一个独立缺陷：各档标记点由算术平均改为几何平均，否则 `ms_per_eval` 与 `decodes` 跨算例的强负相关会把 B2 画到 $120.9$ s 的双曲线上。此轮后**未开工的只剩新 S3 轮次与全文级步骤**：B01c、B02b–B02g（F-RESULT 八个子块）、B03 三项残余（图内中英文、`fig:theta` 图注、已由记录 101 部分兑现）、B04d/e/f/g 的剩余额度、B13（须作者插引用标记）、S4 / S9A / S5。**序 6 `F-PROOF` 已整体完工**：C5 全套在四个宿主块上执行完毕（B04b、B04c、B04e、B05c），并**另补跑一次证明义务的全文扫描**（正文 130 处逐处判定，查出 OVERCLAIM 1 处、§709 措辞不一致 5 处，均已落地；见记录 50–52 与 §五）。宿主块四轮无未决项：B04e 的 P14 等价类型经核为搜索轨迹等价（`prop:speedup_equiv` 明写「所选车辆、所提交的预约以及最终时间表逐位相同」），三种等价中最强且正文未越强。四条形式化资产的后文使用逐条核过，无一是摆设；摘要与 §1 未援引任何一条，C5 验收测试 4 不存在强度倒挂。**序 5 `F-MODEL` 整组一轮完工**，卡片 §5.5 的五项验收测试与两条禁止出现项全部转为通过（详见序 5 表与记录 31–41）。B01a 第 8 条经 X5 核实后**撤回不改**（见 §三）；F-METHOD 轮的 P07 收窄改写**已批准但挂起**，须与 §5.6.2、§6.2 同轮落地（X13）。P 条目进度：**P26 已了结**（记录 39）；**P02 已了结**（记录 61，依卡片取 4 条贡献，解开 B09／B10／B11 的基准依赖）；**P01 已了结**（记录 53–54），全文最要害的口径缺口由此闭合，X15 随之解封。核 P01 时发现台账 X8 原记的预算三元组「$90$ 秒／代数 $200$／早停 $30$」**把阶梯批次与公开库批次混成了一个从未跑过的配置**，已在 X8 行更正——照原记写进论文即为事实错误。

**最近一次体检与编译**（2026-08-22 `F-PROOF`／B04b+B04c 轮后，对 `{{OUTPUT}}`）：`check_tex.py` **无新增告警，再减一条既有告警**——宏 120 个（不变）/ 引用 141 个共 588 处；引用但未定义 0、定义但未引用 0、花括号净差 0、环境不配对 0、不存在的 label 0、`\TBD` 0、待填数值 0。「定义却无人引用的 label」由 9 条降至 **8 条**（`eq:dur`、`eq:lbcut`、`sec:arms`、`sec:assumptions`、`sec:complement`、`sec:contributions`、`sec:insights`、`sec:main`）：记录 48 新增的 `\ref{sec:upper}` 消掉一条。`xelatex` 两趟构建通过，**0 致命错误 / 0 未解析引用 / 0 未解析文献**，页数 39 不变。§4.2 由 928 字增至 989 字、§4.3 由 955 字增至 1148 字，§4 由 7444 字增至 **7798 字**；方法部分合计 11447 字，F-METHOD 占比 **68.1%**，仍在 55–70% 区间内。

> 前两轮体检留档：F-MODEL 轮——无人引用 label 由 11 条降至 9 条（记录 39 消 `prop:np_hard` 即 P26、记录 33 消 `sec:future_work`），页数 39。F-METHOD 轮——宏 +1（`\MsPricedRef`），无人引用 label 由 13 条降至 11 条，页数 39。

> 上一轮（F-METHOD）体检留档：宏 120 个（+1 `\MsPricedRef`），无人引用 label 由 13 条降至 11 条（记录 24 的 `\ref{sec:overview}`、记录 25 的 `\ref{sec:casestudy}` 各消一条），页数 39。

---

## 一、块清单

### 序 1 · `F-SETUP` 实验设置与口径（卡片 §5.1 · 依赖：卡片）

F-SETUP 合计 213 行 / 4575 汉字。三个子块共用卡片 §5.1，**不得**把 §5.2 之外的卡片规则套过来。

| 块 ID | 功能 ID | 本文位置（标题） | 定位锚 | 起止行（参考） | 行数·字数 | 对应卡片哪条主张 | 关联 P 条目 | 风险标注 | 阻断项 | 改动预算 | 状态 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| B01a | `F-SETUP` | §5.1.1 受控算例族 | `\subsubsection{受控算例族}`（无 label；父节锚 `\label{sec:instances}`） | 原稿 1660–1752；施工稿 1668–1770 | 93 行 / 1994 字 → 约 103 行 / 2043 字 | 支撑 C1–C3 的可归因性；卡片 §5.1 必须存在项 1、2 | P06 **已解决**、P13 **部分**、P28 **已解决** | 公开算例不可用的论证已压缩（523→379 字），与 B09 的重复更显眼待 B09 处理；`tab:instances` 由脚本生成不得手改 | 无 | 12（已用 9） | **已完工**（记录 1–9 已应用；第 8 条经 X5 核实后撤回——§5.5 并未承载该读数，删除即信息损失） |
| B01b | `F-SETUP` | §5.1.2 比较协议 ＋ §5.1.3 实现与运行环境 | `\label{sec:protocol}` | 1754–1822 | 69 行 / 1663 字 | 钉死全文所有数字的口径 | **P01**, **P04**, P05, P12, P20 | **全文最要害的一块**：同挂钟预算的绝对值缺失于此；本块含 8 条「不要改的地方」中的第 1、2、8 条，删减对象必须绕开 `1779–1810` | **P01** — 预算三元组（挂钟秒数 / 代数上限 / 早停阈值）与 Python 及依赖版本，模型无从读出。注释 `95`/`127` 的 `--budget 90` 是复现命令示例，**不得**径直当作主批次实际取值。**P01 已了结**：$90$ 秒经 `baseline_ladder_10seeds.log:3` 确证（同日志的种子表与论文 `1714–1715` 逐位相同，批次归属无歧义），代数 $2000$／早停 $400$ 出自 `baseline_ladder.py:89`，Python $\ge 3.8$ 且零第三方依赖出自 `requirements.txt:1–2`；CPU 原就在 §5.1.3。见记录 53–54 | 9（已用 6） | **完工**：记录 53–58。卡片 §5.1 的七项必须存在项全部齐备，五条验收测试全通过；越界项 X21–X23 |
| B01c | `F-SETUP` | §5.2 四级基线阶梯 | `\label{sec:arms}` | 1823–1873 | 51 行 / 918 字 | 卡片 §5.1 必须存在项 3（每级对应什么结构、哪几级对应已发表工作、复现到什么程度） | 无直接 P | 归入 F-SETUP 而非 F-RESULT 是 S0 的判定（依据：卡片 §5.1 必须存在项 3 的内容在此不在 §5.1）；含手打表 `tab:arms`；`63.00/78.00`（`1869`）为裸数字 | 无 | 7 | 未开工 |

> **本组篇幅体检**：F-SETUP 占实验部分（§5 全章 15566 字）的 **29.4%**，卡片阈值为 15–25%。但图注字数被计入本组与 F-RESULT 内部、而 F-FIG 不是独立连续区间，故该百分比不能直接与阈值比对。可执行的判据是二者之比 **29.6 : 70.4**（卡片隐含中值比约 26.7 : 73.3），即本组偏大但幅度有限。P12、P13 的删减落地后须复算一次。

### 序 2 · `F-RESULT` 结果、消融与负对照（卡片 §5.2 · 依赖：序 1）

F-RESULT 合计 640 行 / 10861 字，远超 `{{BLOCK_LINES}}` = 120，按小节与 `\paragraph` 边界切为 9 个子块，共用卡片 §5.2。**固定顺序**（卡片 §5.2 必须存在项 1）：主结果 → 分解 → 负对照 → 外部标定，与下表次序一致。

| 块 ID | 功能 ID | 本文位置（标题） | 定位锚 | 起止行（参考） | 行数·字数 | 对应卡片哪条主张 | 关联 P 条目 | 风险标注 | 阻断项 | 改动预算 | 状态 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| B02a | `F-RESULT` | §5.3 主结果 | `\label{sec:main}` | 原稿 1874–1950；施工稿 1894–1976 | 77 行 / 1042 字 → 83 行 / 约 1197 字 | **C1**（主）、C2 | 无直接 P | 本块承载 `tab:main` 全部合计读数；口径切换点 K1、K3 在此交汇；`\GainProbeLoop` 的合计显著不得写成逐条件成立（改前即已防住，修改 4 further 收紧）。**修改 1 的预算指回待 B01b 落地后可换成绝对值** | 无 | 10（已用 7） | **已完工**（7 条修改 / 记录 10–15，修改 6＋7 合为记录 15） |
| B02b | `F-RESULT` | §5.4 主效应、次效应与不可替代性 | `\label{sec:complement}` | 1951–1983 | 33 行 / 512 字 | **G4**（角色分工） | 无直接 P | 卡片禁止项正中此块：「两机制增益相乘≈端到端」不得写成"证明了独立性"（`1958–1962`） | 无 | 4 | 未开工 |
| B02c1 | `F-RESULT` | §5.5 引语 ＋ `tab:bytag` 区 | `\label{sec:whichcongestion}` | 1984–2003 | 20 行 / 414 字 | C1、C2 | 无直接 P | 与 B02c2、B02c3 同属 §5.5，三块之间不得互相改对方管的句子 | 无 | 3 | 未开工 |
| B02c2 | `F-RESULT` | §5.5「能看清什么：两个效应的逐格分辨率相差悬殊」 | `\paragraph{能看清什么:两个效应的逐格分辨率相差悬殊。}` | 2004–2115 | 112 行 / 1943 字 | **C2**（后半：不可定位） | P24 | 含「不要改的地方」第 4 条（`2087–2098`、`2103–2109` 两处自我更正，属 C2-F 保护项）；口径切换点 K4（车臂比族引用校正前 $p$）在此 | 无 | 14 | 未开工 |
| B02c3 | `F-RESULT` | §5.5「来自另一批自建算例的独立印证」＋「异质度不是收益的放大器」 | `\paragraph{来自另一批自建算例的独立印证。}` | 2116–2163 | 48 行 / 974 字 | C1、N4 | P28（回指） | 口径切换点 K2（降本前 −0.74% vs 降本后 +2.45%）与 K4（矩阵批次未校正）均在此；`fig:theta` 注自报"降本之前" | 无 | 6 | 未开工 |
| B02d | `F-RESULT` | §5.6 消融与受控负对照（含 §5.6.1/5.6.2/5.6.3） | `\label{sec:pricing}` | 2164–2269 | 106 行 / 2276 字 | C3（反面）、G3 | **P07**（一处，`2225–2228`） | 卡片 §5.2 要求负对照写明"作为设计取舍，不作主结论"（`2168–2170` 已有）；架构级普遍论断在 `2225–2228`；`fig:protocols` 注自报"降本之前" | 无 | 13 | 未开工 |
| B02e | `F-RESULT` | §5.7 代价律与降本效果 | `\label{sec:costlaw}` | 2270–2359 | 90 行 / 1386 字 | **C3**（主） | 无直接 P | 口径切换点 K1、K2 的正面陈述均在此；`tab:prune` 由脚本生成；36 组逐位等价自检（`2339–2341`）是 C3 的实证补强，不得删 | 无 | 11 | 未开工 |
| B02f | `F-RESULT` | §5.8 案例分析：甘特图与关键链归因 | `\label{sec:casestudy}` | 2360–2409 | 50 行 / 697 字 | 解释性，不承载主张 | P15（图侧） | 本块唯一实质风险是 `fig:case_gantt` 图内英文，属 B03 处理；正文侧只需保证不引入新主张 | 无 | 6 | 未开工 |
| B02g | `F-RESULT` | §5.9 外部标定：公开基准上的退化对标 | `\label{sec:external}` | 2410–2513 | 104 行 / 1617 字 | **N8**（检验上层搜索而非本框架） | P20, P22 | 含手打表 `tab:external`（20×7 全数字，回归风险最高）；口径切换点 K5（三种分母的间隙百分数并排）；`2451` 的 `5.45\%` 与 `2496` 的 `12.93\%`/`12.85\%` 为裸数字，其中一个与 `\PubGapMax` 重复 | 无 | 13 | 未开工 |

> **本组篇幅体检**：占实验部分 **69.8%**（卡片阈值 50–60%），同 F-SETUP 的口径说明，与 F-FIG 重叠计入，不作为独立超限项处理。

### 序 3 · `F-FIG` 图表与图注（卡片 §5.3 · 依赖：序 2）

| 块 ID | 功能 ID | 本文位置（标题） | 定位锚 | 起止行（参考） | 行数·字数 | 对应卡片哪条主张 | 关联 P 条目 | 风险标注 | 阻断项 | 改动预算 | 状态 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| B03 | `F-FIG` | 9 图 ＋ 9 表 ＋ 3 算法，散在全文 | **非连续块**，逐个用各自的 `\label`：`fig:motivating` `fig:framework` `fig:convergence` `fig:prediction3` `fig:theta` `fig:protocol` `fig:protocols` `fig:case_gantt` `fig:case_chain`；`tab:related` `tab:notation` `tab:instances` `tab:arms` `tab:attrib` `tab:main` `tab:bytag` `tab:prune` `tab:external`；`alg:route` `alg:decode` `alg:ls` | 634–2486 | 非连续 | 全部（图表是 C1–C3 的证据载体） | **P15**, P19, P23 | 本块输出形式由卡片 §5.3 规定为**一张表**（单读 6 问逐项 ✓✗ + 图注问题 + 正文接口问题 + 制图返工项 + 可直接替换的新图注），不按行数计预算；4 张 `\input` 表不得手改，5 张手打表须逐一标注单一来源 | **P15** — `fig:case_gantt` 图内英文，重出需改图脚本，属作者侧 | 按输出表计，不设行预算 | **部分完工**：单读测试 18 个浮动体全做完（3 个判 `NOT_SELF_CONTAINED`：`fig:theta`、`tab:main`、`tab:prune`）；R2 离散度、R3 加粗统一、P1/P2 单位与字号已落地（记录 16–21 ＋ 脚本改动表）。**未做**：图内文字中英混用（作者暂缓）、`tab:external` 改为脚本生成、`fig:theta` 图注补批次与误差棒 |

### 序 4 · `F-METHOD` 方法与机制（卡片 §5.4 · 依赖：序 2）

§4 全章 457 行 / 7451 字，按小节切 7 个子块。**`F-PROOF`（卡片 §5.6）在 B04b、B04c、B04e 三块上叠加执行 C5 全套**——见序 6。

| 块 ID | 功能 ID | 本文位置（标题） | 定位锚 | 起止行（参考） | 行数·字数 | 对应卡片哪条主张 | 关联 P 条目 | 风险标注 | 阻断项 | 改动预算 | 状态 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| B04a | `F-METHOD` | §4.1 框架总览：两条反馈通路 | `\label{sec:overview}` | 1195–1244 | 50 行 / 800 字 | C1、C2 的机制定义 | 无直接 P | 卡片 §5.4 必须存在项 1（输入→机制→输出数据流）落在此块；`fig:framework` 为示意图无数据；`1219–1220` 已声明负对照不作主结论，属保留项 | 无 | 6（已用 2） | **完工**：记录 59–60，X16 就地了结。本块是三度重复辩护中**被保留**的那一处（记录 24、26 均改为指回此处），故 `1234–1238` 的负对照说明按原样保留 |
| B04b | `F-METHOD` ＋ `F-PROOF` | §4.2 下层：时间窗预约与无冲突路由 | `\label{sec:router}` | 1262–1329 | 68 行 / 989 字 | C1 | 无直接 P | C5 全套已执行。`lem:label` 的**结论与强度相符**（限定「相对当前预约表」、只谈单段），缺陷在两处：证明用 A6 却不标编号；「松弛关于 $t$ 单调不减」是裸断言，而它正是标签设定式成立的关键步骤。`1324` 的「可采纳下界」**经核通过**——§4.5 `1490–1491` 给了论证且此处已 `\ref` 指向 | 无 | 8（已用 3） | **完工**：记录 42、43、49 |
| B04c | `F-METHOD` ＋ `F-PROOF` | §4.3 上层：编码与事件驱动解码 | `\label{sec:upper}` | 1330–1415 | 86 行 / 1148 字 | C1 | 无直接 P | C5 全套已执行，实得三处：`prop:decodable` 陈述含「无冲突、**无死锁**、完工时间有限」三项，而证明只覆盖前后两项；「评价是确定性的」依赖并列打破规则，派车侧写在 120 行后的 `prop:speedup_equiv` 里、路由侧全文未写；`alg:route` 的 `\Require` 有五个参数而 `alg:decode` 的调用只传三个。两条资产的**后文使用均充分**（`lem:label` 五处、`prop:decodable` 四处），摘要与 §1 未援引二者，故不存在强度倒挂 | 无 | 10（已用 4） | **完工**：记录 44–47 |
| B04d | `F-METHOD` | §4.4 预约表感知的车辆派遣（含 §4.4.1 层间接口的三种选择） | `\label{sec:dispatch}` | 1390–1462 | 73 行 / 1667 字 | **C2**（机制定义） | **P07**（一处，`1444–1446`） | 卡片 §5.4 验收测试 6：未获净收益的机制（定价，`1432–1452` 约 21 行）所占篇幅是否与其在结论中的地位相称；`lem:label` 在 `1448–1451` 被用作定价失效的技术根源 | 无 | 9（已用 1） | **部分完工**：记录 26。**P07 的收窄改写已备妥、作者已批准，但按建议挂起**——同源全称量词另在 §5.6.2 与 §6.2，须三处同轮落地，见 X13 |
| B04e | `F-METHOD` ＋ `F-PROOF` | §4.5 降本：可采纳下界剪枝与胜者路径复用 | `\label{sec:speedup}` | 1463–1515 | 53 行 / 966 字 | **C3**（主） | **P14** | 含 `prop:speedup_equiv` 及其证明（`1489–1504`）→ 叠加 C5，且 P14 的等价类型声明就落在此处；口径切换点 K1 的首次出现（`1466–1468`） | 无 | 7（已用 1） | **部分完工**：记录 29（决定 I·A）。P14 的等价类型措辞经本轮核对与 `prop:speedup_equiv` 一致（命题声称「所选车辆、所提交的预约以及最终时间表逐位相同」，强于最优值等价，正文未越强），未改动 |
| B04f | `F-METHOD` | §4.6 冲突制导局部搜索（含 §4.6.1/4.6.2/4.6.3） | `\label{sec:ls}` | 1516–1610 | 95 行 / 1212 字 | 负对照二（不承载主张） | 无直接 P | **篇幅失衡的主嫌**：未获净收益的机制占 95 行，卡片 §5.4 验收测试 6 的直接对象；含裸超参 `L=5`（`1556`）、阈值 `0.5`（`1529`）、`10\%`（`1519`）；`1567–1571` 关于 $\lambda$ 量纲不可比的自陈属 C2-F 保护项 | 无 | 12（已用 5） | **部分完工**：记录 24、25、27、28、48。**「篇幅失衡的主嫌」这一判断本轮须修正**——本节 1212 字里 397 字（§4.6.1＋`tab:attrib`）是 §5.8 共用的基础设施、162 字（$\lambda$ 记录）是 C2-F 保护的证据，真正冗余的仅约 220 字，已清。裸超参转 X15（依赖 B01b） |
| B04g | `F-METHOD` | §4.7 外层遗传算法 ＋ §4.8 复杂度与代价分析 | `\subsection{外层遗传算法}`（无 label） | 1611–1649 | 39 行 / 917 字 | C3（代价侧） | 无直接 P | 口径切换点 K6（跨批次绝对毫秒不可相除）在 `1629–1634`；含「不要改的地方」第 6 条（`1642–1648` 不收敛的主动声明） | 无 | 5（已用 1） | **部分完工**：记录 30。K6 经核对陈述正确（正文主动声明两批绝对毫秒不可相除），保留；§4.7 的超参罗列转 X15 |

> **本组篇幅体检**：方法部分（F-METHOD + F-MODEL + F-PROOF）= §4 全章 7451 字 ＋ §3 全章 3450 字 = 10901 字。F-METHOD 占 **68.4%**，卡片阈值 55–70%，**在区间内**。施工后 §4 = 7431 字，占 **68.3%**，仍在区间内。
>
> **卡片 §5.4 验收测试 6 专项（本组的核心发现）**：未获净收益的两个机制所占篇幅与其结论地位**不相称，判篇幅失衡**。
>
> | 机制 | 净收益 | §4 篇幅（改前→改后） | 结论中的地位 |
> |---|---|---|---|
> | 预约表感知派遣 | 有 | 533 → 523 字 | 贡献 (2) 主体，240 字 |
> | 降本（剪枝＋复用） | 有 | 966 字 | 贡献 (2) 后半 |
> | 走廊时段定价 | **无** | 547 → 547 字 | 洞见 1 整条，223 字 |
> | 冲突凭证 ＋ 局部搜索 | **无** | 1508 → 1447 字 | 洞见 2 主要实例，215 字 ＋ 洞见 3 一从句 |
>
> 关键在于**两个失败机制的结论地位其实相当**（各约 220 字、各占一条洞见），篇幅却相差一倍有余。计量须扣除一项：§4.6.1「关键链的归因」及 `tab:attrib`（397 字）**被 §5.8 案例分析共用**（2441、2451 行按该表给关键链上色），不能全记在失败机制账上。扣除后为 **1050 对 547，倍率 1.92**（改前 2.03）；折成「每单位结论地位所占方法篇幅」：定价 2.45，凭证/局搜 4.88（改前 5.17）。
>
> **处置＝决定 H 选项 A（维持并明示）**。理由：失衡的成因有一半是共用基础设施被误记在失败机制账上，属记账问题而非篇幅问题；选项 B（把 §4.6.2 移附录）要付出拆散 §5.8 所依赖的 `eq:reassign` 的代价，比它买到的对称性更贵。故记录 25 只加一句明示，不作压缩——**倍率仍为 1.92，失衡未消除，S5 复评须复看本条**。
>
> ⚠ **一处自我更正**：本轮初次计量把 §4.6 整节 1212 字全记在失败机制账上，得倍率 2.76；核查 `tab:attrib` 的引用后发现其中 397 字为 §5.8 共用，真实倍率 2.03。失衡结论不变，但量级差三分之一，且该发现**直接否掉了「大幅压缩 §4.6」这条看似最自然的改法**。

### 序 5 · `F-MODEL` 问题、符号与假设（卡片 §5.5 · 依赖：序 4）

| 块 ID | 功能 ID | 本文位置（标题） | 定位锚 | 起止行（参考） | 行数·字数 | 对应卡片哪条主张 | 关联 P 条目 | 风险标注 | 阻断项 | 改动预算 | 状态 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| B05a | `F-MODEL` | §3.1 问题描述 ＋ §3.2 建模假设 ＋ §3.3 决策变量与派生的运输任务 | `\label{sec:assumptions}`（§3.2；§3.1、§3.3 无 label） | 948–1051 | 104 行 / 1419 字 | G1（问题层） | 无直接 P | 验收测试 1 逐条核毕：**A1–A6 均有编号回指**（`1022–1023` 点名 A1/A2/A4/A5 为标准假设、A3/A6 为塑造本文的两条），**A7、A8 全文零回指**——A7 与 `prop:decodable` 是同一句话（假设即结论），A8 内容在 §6.4 兑现却不互指。验收测试 5 命中：车辆初始条件只在 `alg:decode` 里 | 无 | 13（已用 4） | **完工**：记录 31（决定 J·A 删 A7）、32、33、34 |
| B05b | `F-MODEL` | §3.4 时序关系与可行性条件 ＋ §3.5 与 FJSP\_PT 的关系 | `\subsection{时序关系与可行性条件}`（无 label） | 1052–1123 | 72 行 / 785 字 | G1 | 无直接 P | 本块原挂两条风险，**其中一条须撤销**：`eq:dur` 无人单独 `\ref` 不是缺陷，`1121` 用的是区间引用 `\eqref{eq:assign}--\eqref{eq:obj}`，中间各式必须带编号。另一条属实且更重——§3.5 只做了 FJSP\_PT 退化说明，既无"采用/扩展/提出"明说（明说在 §1.2 `676`，不在本块），也无与最接近工作的模型差异 | 无 | 8（已用 3） | **完工**：记录 35（$\theta\to\eta$）、37（立场表态）、38（决定 K·A 模型差异） |
| B05c | `F-MODEL` ＋ `F-PROOF` | §3.6 计算复杂度 | `\subsection{计算复杂度}`（无 label；块内 `\label{prop:np_hard}`） | 1124–1149 | 26 行 / 424 字 | G1 | **P26** | C5 全套已执行，**证明本身通过**：两条特例规约（$|\Omega_{ji}|=1$ 退化经典 JSP、$\Omega_{ji}=M$ 且 $n_j=1$ 退化无关并行机）均给出且引文到位，「完全柔性不使问题变容易」由第二条特例直接支撑，未越强。缺陷只在援引侧：全文检索「NP」只落在 §3.6 内部，命题无人引用 | 无 | 4（已用 1） | **完工**：记录 39，**P26 已了结** |
| B05d | `F-MODEL` | §3.7 算例特征的刻画 | `\label{sec:features}` | 1150–1209 | 60 行 / 1012 字 | 定义 $H/F/\bar{T}_t/\bar{T}_p$ 与争用强度，被 F-SETUP 大量引用 | P21 | 「不要改的地方」第 5 条（$\varphi$ 指标失败记录）**已核，未动**。本轮实得两处：首句把 §6.3(1) 明确弃权的「结构而非规模」当前提断言；$H$ 条目对 §5.5 的前指强于 §5.5 自称。`eq:lbcut` 无人 `\ref` 属实但保留编号（它是 $\mathrm{LB}_{\text{cut}}$ 的定义式），不占额度 | 无 | 8（已用 2） | **完工**：记录 40、41 |

> **序 5 篇幅体检**：§3 施工前 3450 字、施工后 **3649 字**（净 +199：模型差异约 130、立场表态约 40、车辆初始条件约 15，A7 删除与其结论化改写相抵）。§4 因记录 36 增 13 字至 7444 字。方法部分合计 11093 字，F-METHOD 占 **67.1%**（前值 68.3%），仍在卡片 55–70% 区间内。
>
> ⚠ **两处自我更正**：（一）台账原记 B05b「`eq:dur` 定义后无人 `\ref`」是误判——`1121` 的区间引用要求中间各式带编号，该条撤销。（二）台账原记模型差异「须由 B09 搬入本块」，但序 5 先于序 9，照此执行会让 F-MODEL 的验收测试 3 当场不通过；改为本轮在 §3.5 就地补写、§6.3 的重复段留给 B09 删（X17）。这对 B09 是净减负——它是全流程唯一以删为主的块，2074 字已超 5% 硬约束。

### 序 6 · `F-PROOF` 形式化与证明（卡片 §5.6 · 依赖：序 5）

| 块 ID | 功能 ID | 本文位置（标题） | 定位锚 | 起止行（参考） | 行数·字数 | 对应卡片哪条主张 | 关联 P 条目 | 风险标注 | 阻断项 | 改动预算 | 状态 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| B06 | `F-PROOF` | **不独立成块**（论文未把命题与证明单独成章/成节） | 四项资产各自的 label：`prop:np_hard`（B05c）、`lem:label`（B04b）、`prop:decodable`（B04c）、`prop:speedup_equiv`（B04e） | 1110–1126 / 1262–1272 / 1372–1385 / 1489–1504 | 分散 4 处 / 约 63 行 | C3（主）、C1 | **P14**, P26 | 依卡片 §5.6 首句："仅当论文把命题与证明单独成章/成节时作为独立块跑；若与 `F-MODEL` 同节，则两张卡片在同一块上依次执行"。故本行**不单独排轮次**，C5 全套在 B04b、B04c、B04e、B05c 四块上各执行一次。**四块进度：B05c ✓（P26 了结）、B04b ✓（余复杂度一项待决，X20）、B04c ✓、B04e 无未决项**（P14 的等价类型经核为搜索轨迹等价且明写，未越强）。四条资产的后文使用逐条核过：`prop:np_hard` 1 处（记录 39 补）、`lem:label` 5 处、`prop:decodable` 4 处、`prop:speedup_equiv` 3 处，**无一是摆设**；摘要与 §1 未援引任何一条，故 C5 验收测试 4 不存在强度倒挂。**另补跑一次 C5 证明义务的全文扫描**（宿主块四轮只覆盖资产周边约 27 处，而该条款的范围是「文中每处」，且 S4 的 13 项检查不含此项，不能留到 S4）：全文 134 处、正文 130 处逐处判定，查出 OVERCLAIM 1 处（记录 50）与 §709 措辞不一致 1 组 5 处（记录 51），均已落地；PROOF_NEEDED 与 RHETORIC 各 0。判定明细与典范用法见 §五 | 无 | 计入宿主块预算 | **完工**（X20 已收口；全文扫描无余项） |

### 序 7 · `F-INTRO` 引言与贡献列表（卡片 §5.7 · 依赖：序 1–6 全部完成）

| 块 ID | 功能 ID | 本文位置（标题） | 定位锚 | 起止行（参考） | 行数·字数 | 对应卡片哪条主张 | 关联 P 条目 | 风险标注 | 阻断项 | 改动预算 | 状态 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| B07 | `F-INTRO` | §1 引言全章（§1.1–§1.6） | `\label{sec:Chapter_I}` | 602–815 | 214 行 / 4782 字 | G1–G4 全部，须与卡片 §3 逐条同构 | **P02**, P08, P11 | **必须整块**（`{{BLOCK_LINES}}` 对引言不适用），214 行是全流程最大的单块；三处结构病同时在场：贡献列表嵌在 §1.5 之内、实验协议被扩成独立小节、`744–758` 承担的其实是 F-SETUP 的口径论证（S0 标为「位置错配 / 功能串味」）；`628–632`、`647` 的动机算例常量未走宏 | **P02 已了结** — 决定 R·A 依卡片取 4 条（记录 61），B09、B10、B11 的基准由此确定 | 26（已用 8） | **完工**：记录 61–68。卡片 §5.7 七项必须存在项全部齐备，五条验收测试全通过；三处结构病（贡献列表嵌在协议小节内、协议扩成独立小节、`751–766` 实为 F-SETUP 口径论证）一并解决。越界项 X24–X27 |

### 序 8–10 · `F-RELATED` / `F-LIMIT`（依赖：序 7）

| 块 ID | 功能 ID | 本文位置（标题） | 定位锚 | 起止行（参考） | 行数·字数 | 对应卡片哪条主张 | 关联 P 条目 | 风险标注 | 阻断项 | 改动预算 | 状态 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| B08 | `F-RELATED` | §2 相关工作全章（§2.1–§2.3，含 `tab:related`） | `\label{sec:Chapter_II}` | 795–920 | 126 行 / 3717 字 | 卡片 §4 的两半缺口须在此落到具体文献 | **P16** | 121 行超 `{{BLOCK_LINES}}` 仅 1 行，按整块处理；若严格执行则切为 §2.1–2.2（`823–860`）/ §2.3（`861–935`）。占全文 6.8%，卡片阈值 8–12%，**偏低**；`tab:related` 为手打表；`867` 的 `10\%` 系转述文献 | 无 | 15 | **已完成**（用 8 条 / 15；见记录 69–76）。篇幅 6.1%→6.3%，仍低于卡片 8–12%，按**决定 U·A** 不硬填文献；§2.1+§2.2 由 1227 字增至 1488 字，与 §2.3（1521）基本持平，分布失衡已解。17 处转述挂 `[ref_check]` 交 F-REF。不查 C4（作者指令） |
| B09 | `F-LIMIT` | §6.3 局限 | `\label{sec:limitations}` | 2638–2691 | 54 行 / 1398 汉字 | 标注 C1–C3 各自的边界 | **P10**, P13, P21 | **辩护瘦身主战场**，且是全流程唯一以删为主的块：2074 字占全文 5.4% 超硬约束 ≤5%，五条中四条超「每条 ≤4 句」（实测 10/5/10/5/3）；局限(1) 内的文献模型对比须搬去 B05a（卡片 §5.9 明令）；CF-4（未来工作「动态运行」无对应局限）在此与 B10 交汇 | 无 | 9（压缩性删减不占此额，另报字数比） | **已完成**（用 6 条 / 9；见记录 77–82）。C2 全套 A→F 执行。字数 2074→**1398 汉字**（5.10%→**3.50%**，硬约束 ≤5%）；句数 33→**22**，六条实测 4/4/4/4/3/3 **全部合规**（改前 10/5/10/5/3）。条数 5→6（**决定 W·A** 新增「静态假设」），**CF-4 与 X18 同时了结**。三处判定「搬」的辩护已出回复信草稿，未进正文 |

### 序 10–13 · `F-CONCL` / `F-ABS` / `F-TITLE` / `F-REF`

| 块 ID | 功能 ID | 本文位置（标题） | 定位锚 | 起止行（参考） | 行数·字数 | 对应卡片哪条主张 | 关联 P 条目 | 风险标注 | 阻断项 | 改动预算 | 状态 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| B10 | `F-CONCL` | §6 引语 ＋ §6.1 结论与贡献 ＋ §6.2 主要洞见（前段）；§6.4 未来工作（后段） | 前段 `\label{sec:Chapter_VI}`（§6.1 `\label{sec:contributions}`、§6.2 `\label{sec:insights}`）；后段 `\label{sec:future_work}` | 2514–2575 ＋ 2648–2687 | 102 行 / 2567 字 | G1–G4 复述 ＋ 洞见 | **P07**（主）, P27 | **非连续块**：被 B09 从中间劈开。卡片要求结论整块，故本块两段同轮处理，两段之间的 §6.3 属 B09，**不得在本轮触碰**；卡片 §5.10 禁止"第 5 条贡献"，与 P02 的裁决直接相关；`2555–2560` 的架构级论断是 P07 的主发生地 | **P02**（贡献条数）＋ B09 须先完成（依赖 7,9） | 12 | **已完成**（用 8 条 / 12；见记录 83–90）。**P07（大修·阻断）与 X13、X24 同轮了结**。贡献 3 条→**4 条**，与引言、卡片 G1–G4 同构。验收测试 4（新数字）判过：21 个数值宏与 3 个裸数字在正文均有落点。篇幅按**决定 Y·A** 采「含局限 8–12%」口径，§6 整章 **9.91%** 合规（F-CONCL 单独 6.42%） |
| B11 | `F-ABS` | 摘要 | `\begin{abstract}` | 513–542 | 30 行 / 697 字 | 四段信息，覆盖 C1–C3 与范围句 | P02（摘要侧）, P14（"可证等价"）, P18 | **必须整块**。850 字超卡片 §5.11 的中文 400–700 字区间（TD-6：以 `{{JOURNAL}}` 实际规定为准）；末句列举两个负对照的名称与用途，属卡片"禁止出现：负对照的详细说明"的越界候选 | 依赖 B07、B10 完成 | 5 | **已完成**（动刀 4 处 / 5；见记录 91–96）。**853 → 697 汉字**（220/201/162/114），落进卡片 400–700。四段信息齐备且不越界：段 1 删文献史三句（**决定 AA·A** 压成半句缺口陈述）、段 4 删负对照整句与三个重复数字。五项验收测试全通过：段 3 补「共用单一规模」＋段 4 补「不就机制如何随规模变化作主张」，消除与局限 (1) 的无条件主张冲突（**收窄摘要，局限块未动一字**）；补入题目机制名「路由内嵌评价」「低开销」；摘要内简称漂移统一为全称。**X31 兑现、X25 摘要侧了结**（决定 BB·A 保留 `\ContentionHi` 并自报「后者为全族最大值」） |
| B12 | `F-TITLE` | 题目 ＋ 短标题 ＋ 关键词 | `\title[`（`512`）；`\keywords{`（`567`） | 512 ＋ 567–568 | 3 行 | 机制名全文唯一性 | **P18** | 全标题 40 字超卡片 §5.12 的中文 ≤30 字；导言区注释 `478–499` 记录三处刻意选择（不用主副标题、用"低开销"而非"降本"、"路由内嵌评价"的确切所指），改动不得回退 | 依赖 B11 完成 | 1 | **已完成**（见记录 106–107，含术语前置条件 103）。题目 **35 → 27 汉字**、堆叠 **4 → 3 层**，两条硬约束同时满足；短标题「闭环调度」→「路由内嵌评价闭环双层框架」（原短标题的机制名全文不存在）。关键词 8 → 9 个，三个不合格项（异构机械臂、AGV 无冲突路由、预约表感知派遣，前两者 exact 0 处、后者为简称）已换，原为空的**方法族**一栏补「时间窗预约」。**验收测试 4 现真通过**：题目两个机制名在摘要、引言贡献、方法小标题、结论四处逐字一致（记录 103 改了 §4.1／§4.5 小标题与结论 (2)(3) 标题）。导言区三处刻意选择未回退，第 2 条改写为记录「低开销为何降到关键词」 |
| B13 | `F-REF` | `\bibliography{reference-base}` ＋ 全文标记处 | `\bibliography{`（`2695`） | 2695 ＋ 标记处 | 14 键 / `{{BIB}}` 100 条 | —— | **P17** | 卡片 §5.13 首句：**模型不得在未标记处增删引用**；86/100 条未被引用（前作遗留库）；新条目只追加到 `{{BIB_NEW}}` = `paper01/reference-suggested.bib`，不动 `{{BIB}}` | **作者未插 `[ref_insert]` / `[ref_check]` 标记前本块不能开工**；P16 已识别 B08 中至少一处需插 `[ref_check]` | 按标记数计 | 未开工 |

### 序 14–16 · 全文级（不属任何功能块）

| 块 ID | 功能 ID | 内容 | 定位锚 | 关联 P 条目 | 阻断项 | 状态 |
|---|---|---|---|---|---|---|
| S4 | — | 全文校验（§7 一致性矩阵 + 脚本化检查） | 全文 | **P03**, P09, P25 | 依赖全部块冻结 | 未开工 |
| S9A | — | 语言定稿（§9.A）。`{{LANG}}` = `{{TARGET_LANG}}` = 中文，**不翻译**，降级为语言定稿轮：只跑术语锁核对 | 全文 | P18（复核） | 依赖 S4 通过 | 未开工 |
| S5 | — | 终审与前后对比（§8）。对 `{{OUTPUT}}` 重出 12 维评分卡，追加逐维对比与 P-list 复审表，写入 `{{SCORE}}` 的"终审"一节 | 全文 | 全部 28 条复审 | 依赖 S9A | 未开工 |

### `MISSING_FUNCTION` / `NEW_FUNCTION`

无。§1.6 论文组织归 `F-INTRO` 第 7 项；§4.7、§4.8 归 `F-METHOD`；§3.5 归 `F-MODEL`；均落在既有卡片内，未自造卡片。

---

## 二、改动记录（撤销日志）

> 每应用一条修改立即追加一行。记录号全局递增、永不复用。
> 回滚：作者说 `回滚修改 N` → 按记录号做反向精确替换（改后串 → 原文串），并把"是否已回滚"标为是；替换前须确认改后串在 `{{OUTPUT}}` 中仍然唯一，不唯一则报 `AMBIGUOUS_ROLLBACK` 并停下。作者说 `回滚 F-XXX 全部修改` → 按记录号**倒序**逐条反向替换该块全部记录。
> 中文稿注意：正文为半角逗号 + 全角句号混排，替换字符串必须逐字匹配，**每次替换前重读该处原文**，不得凭上一轮的引文拼字符串。

**格式说明（对 §2.6 表式的一处必要偏离）**：逐字原文串含换行与 LaTeX 命令，无法放进 Markdown 表格单元。故本表只作索引，**逐字串全文见其后的「记录明细」**，每条一个代码块。回滚仍按记录号执行，两处内容一一对应。

| 记录号 | 块 ID | 本轮条号 | 一句话摘要 | 是否已回滚 |
|---|---|---|---|---|
| 1 | B01a | 修改 1 | 压缩「公开算例不可用」论证，523→379 汉字，两条硬性质与三段结论全留 | 否 |
| 2 | B01a | 修改 10 | 补 $H$ 不随 $F$ 漂移的生成机制（标准化扰动使行内 CV 与 $\lvert\Omega_{ji}\rvert$ 无关） | 否 |
| 3 | B01a | 修改 2 | 补生成器路径、生成种子 $42$、运行种子十个，并声明两类种子不同 | 否 |
| 4 | B01a | 修改 3 | A 族枚举按构造序而非争用序，声明 `low` 高于 `mid`，改正"递降"的误读 | 否 |
| 5 | B01a | 修改 9 | 删元叙述「这里必须由我们自己指出而不是留给读者发现的是:」 | 否 |
| 6 | B01a | 修改 4 | 补 A 族"为何能检验"（B/C 族原有，A 族缺） | 否 |
| 7 | B01a | 修改 7 | 区分 $F$ 目标值 $0.6$ 与实测值 $0.61$，给出随机化取整的机制 | 否 |
| 8 | B01a | 修改 6 | 钉死争用强度口径（初始随机种群 $20$ 个体、种群种子 $42$）并说明为何不取优化后的解 | 否 |
| 9 | B01a | 修改 5 | 补 $H$ 外推检验的批次口径边界（`\MatrixCells` 格独立批次、降本之前） | 否 |
| 10 | B02a | 修改 1 | 引导段三合一：补预算口径指回 §5.1.2、改正 Holm「惰性」的不精确表述、声明置信区间的符号约定 | 否 |
| 11 | B02a | 修改 2 | 「数倍于闭环的评价次数」补指回 §5.7 单次评价成本 | 否 |
| 12 | B02a | 修改 3 | 闭环段补收口句（收到「争用入目标本身值得，两种派车下都成立」） | 否 |
| 13 | B02a | 修改 4 | 就地补「十格中两格符号为正」（车臂比 $1.0$/$2.0$ 上 B2 慢于 B1） | 否 |
| 14 | B02a | 修改 5 | 试探段补收口句（收到「派遣有独立价值但小得多，不足以单独承担收益」） | 否 |
| 15 | B02a | 修改 6＋7 | 端到端胜负计数补「$1$ 平」并加 `\NPairs` 使 $94+5+1$ 当场可验；同段补收口句（收到「两条主张合起来的总量，降约五分之一」）。两条落在相邻两句，为一次原子替换，回滚不可拆 | 否 |
| 16 | B03 | `tab:external` | 手打表补 `$C_{\max}$(时间单位)` 跨列表头 ＋ `\cmidrule(lr){4-6}` | 否 |
| 17 | B03 | `tab:main` 注 | 声明全文统一的加粗约定（表头加粗＝本文配置，数值一律不加粗），并就地点出 B2 非每格最优 | 否 |
| 18 | B03 | `tab:prune` 注 | 补「关/开」列义、加粗说明、Wilcoxon 检验方法、两个比值列的分母方向（开/关 与 关/开） | 否 |
| 19 | B03 | `tab:bytag` 注 | 补检验方法（双侧 Wilcoxon）；「精确等于」软化为「在舍入误差内等于」——`B1→B2` 列按表内一位小数复算得 $-2.46\%$，主表为 $-2.45\%$ | 否 |
| 20 | B03 | `fig:prediction3` 注 | 图注与 `\Description` 补误差棒定义（\NSeeds{} 个种子上逐对相对增益的均值标准误），并指出试探那条曲线的误差棒逐格跨零 | 否 |
| 21 | B03 | `fig:convergence` 注 | 图注与 `\Description` 补分布带定义（种子间全距，并说明为何不用四分位）＋ 批次自报「三算例三种子、说明性、不承载统计主张」（了结 X6） | 否 |
| 22 | B03 | `fig:theta` 注 | 了结 X11。图注与 `\Description` 补：误差棒＝SEM、检验＝按种子配对的双侧 Wilcoxon 符号秩（对 $\theta=0$，据 `sweep_theta.py:166`）、批次自报（$8\times4\times4$，非 A/B/C 三族；左 10 种子右 5 种子；降本之前）。并点破图例陷阱：`high`/`funnel` 是布局标签，与 A 族同名两格并非同一算例 | 否 |
| 23 | B03 | `tab:external` 表体 | 了结 X10。手打的 20 行 $\times$ 7 列改为 `\input{tab/tab_external.tex}`，由脚本生成；原表体整段删除，`\caption`/`\label`/`\small` 保留在 `.tex` 内 | 否 |
| 24 | B04f | §4.6 引语 | 删去「构造上站得住却不划算…读者可以直接看到它的代价」这句辩护的**第三次**出现，改为指回 §4.1；同时去掉 C3 禁用开头「需要提醒读者的是」（C2-B4、C3） | 否 |
| 25 | B04f | §4.6 引语 | **决定 H·A**：补一句明示 §4.6.1 的关键链归因是 §5.8 案例分析共用的基础设施，并非只服务于这个失败算子（缓解验收测试 6 判定的篇幅失衡） | 否 |
| 26 | B04d | §4.4.1「回报冲突凭证」末 | 删去同一辩护的**第二次**出现（其尾句「会自己设计出它的读者可以直接看到它的代价」与 §4.1 第 1228 行逐字相同）。全文自此只剩 §4.1 一处（C2-B4） | 否 |
| 27 | B04f | §4.6.3 接受准则 | 删去对 `alg:ls` 第 2–13 行的逐行复述，只留伪代码表达不了的那件事——为何每轮都要重新提取关键链（卡片 §5.4 禁止出现 2） | 否 |
| 28 | B04f | `alg:ls` | 补 `\Ensure`（三条伪代码中唯一缺后置条件者；`alg:route`、`alg:decode` 均已有）（卡片 §5.4 必须存在 5） | 否 |
| 29 | B04e | §4.5 末段 | **决定 I·A**：声明 `\SpeedupProbe`/`\RouteCallCut`/`\CostMultAfter` 三数为剪枝与复用**两项合计**，实验中作为一个整体开关度量（`tab:prune`），本文不分别归因（卡片 §5.4 验收测试 1） | 否 |
| 30 | B04g | §4.8 ＋ 导言区 | 新增 `\newcommand{\MsPricedRef}{15.05}` 并替换 §4.8 的同名裸数字。此后 §4 内两个倍数（约 37、约 4.8）的**输入项全部为宏**，可随时复算（C6） | **是**（新增一个宏，未改动任何既有宏的值） |
| 31 | B05a | §3.2 假设表 | **决定 J·A**：删除 A7「构造性的无死锁」整条，原 A8 顺延为 A7。该条把 `prop:decodable` 的结论写成了建模假设，而其模型层内容已被 A4（缓冲充裕）与 A6（走廊排他＋停靠位）完全覆盖，剩下的全是算法层结论（卡片 §5.5 验收测试 1 ＋ C5） | 否 |
| 32 | B05a | §3.2 表后段（原 `1026–1028`） | 记录 31 的配套：补一句「无死锁不在上列之中，因为它不是一条假设」，指明它是 A4 与 A6 在 §4.2 时间窗预约构造之下的结论、由 `prop:decodable` 证明。删掉的信息一条不丢，但改挂在结论一侧 | 否 |
| 33 | B05a | A7（原 A8） | 「均留待后续工作」补 `(第~\ref{sec:future_work}~小节)`。此前 A8 全文零回指，其四项内容在 §6.4 兑现却两边不互指（验收测试 1） | 否 |
| 34 | B05a | A5 车辆 | 补「全部车辆在 $t=0$ 时空载停于 $v_0$ 且立即可用」。该条件此前只活在 `alg:decode` 的初始化行里（$\mathrm{loc}_k\gets v_0$、$\mathrm{avail}_k\gets 0$），A1 只覆盖工件（验收测试 5） | 否 |
| 35 | B05b | §3.4 `eq:walk`／`eq:excl` | 走廊进入时刻 $\theta\to\eta$ 共 9 处，解除同符号两义（另一义为定价强度，见记录 36）。改 §3 一侧而非定价侧：定价 $\theta$ 有 `fig:theta` 的文件名、`\label`、图内轴标签与 10 处正文，成本高一个数量级；$\eta$ 全文未占用（$\sigma$ 已是段起始时刻、$s$ 是 `alg:route` 的起点节点） | 否 |
| 36 | B04d | §4.4.1 `1442` | 记录 35 的另一半，了结 X19：定价 $\theta$ 首次出现处补「协调强度 $\theta\ge0$ 是该方案唯一的旋钮」。此前该符号直到 §5.6.2 `2225` 才被命名 | 否 |
| 37 | B05b | §3.5 首段末 | 补「本文因此是*采用* FJSP\_PT 这一既有问题类并在其上增加一族约束，而不是提出一个新的问题类」。与 §1.2 `676` 同强度，不新增主张（卡片 §5.5 必须存在项：采用/扩展/提出，验收测试 4） | 否 |
| 38 | B05b | §3.5 新增第二段 | **决定 K·A**：把与最接近工作的模型差异提进本块——`vanos2026exact` 预约*节点*、本文预约*边*；`lyu2019approach` 冲突语义同构但另要求节点单车容量，本文模型是其*松弛*；并保留「双向走廊＋边排他在文献中已有先例」的正面意义，末句指向 §6.3 的不可比论证。§6.3(1) 本轮不动，重复段的压缩登记为 X17 交 B09（解除卡片 §5.5「不许把模型差异只藏在局限块」，验收测试 3） | 否 |
| 39 | B05c | §3.6 规约段 | 「该规约刻意关闭了争用」→「命题~`\ref{prop:np_hard}` 的规约……」。**P26 就地了结**：`check_tex` 的「定义却无人引用的 label」名单中 `prop:np_hard` 已消失。全文检索「NP」此前只落在 §3.6 内部 5 行，命题无人援引 | 否 |
| 40 | B05d | §3.7 首句 | 「由于本文所研究机制的效果取决于算例的*结构*而非*规模*」→「本文只在固定规模上变化算例的*结构*，规模的影响不在考察范围之内（第 6.3 小节）」。原句把 §6.3(1) `2614`「我们不就机制如何随规模变化作任何主张」明确弃权的东西当作前提断言（C1 内部矛盾） | 否 |
| 41 | B05d | §3.7 $H$ 条目 | 「实测的逐 $H$ 收益**证实了**这个区别」→「**与这个区别相容**」。被引的 §5.5 自称只支撑否定式（`2183`「不足以断言」）、对 $H=0$ 一档只说「与该定义是一致的」（`2191`），前指句强于被引处（C6 措辞强度一致） | 否 |
| 42 | B04b | `lem:label` 证明 | 补 A6 编号；并把「每次走廊松弛关于 $t$ 单调不减」这句裸断言换成有据的表述——查询时刻越晚、可行时窗集合只减不增，故 $t\mapsto R.\textsc{EarliestEntry}(e,t,\tau_e)+\tau_e$ 单调不减。该性质是标签设定式搜索成立的**唯一**依据，原文未给理由（C5 禁止「显然/易证」承担关键步骤 ＋ 验收测试 5） | 否 |
| 43 | B04b | `alg:route` | 三处补全（C5 伪代码条）：`\Ensure` 补「若 *commit* 为真，这些区间同时写入 $R$」的副作用；标签队列键由「时间」改为「(时间, 节点标识)」并注明松弛只在严格更早时进行；队列耗尽处补 `\Return 失败` 分支——原伪代码在不可达情形下无出口（实现为抛异常，`network.py:637`） | 否 |
| 44 | B04c | §4.3「结构性性质其一」 | 确定性主张补两处并列打破规则：路由标签队列的次键、派车按车辆编号升序保留首个严格更优者（后者原只写在 120 行后的 `prop:speedup_equiv` 里，前者全文未写）。规则据实现核定（`network.py:622/632/635` 堆键为 `(arr, node)` 元组、松弛用严格 `<`）。并点明这是全文可复现性的来源 | 否 |
| 45 | B04c | `prop:decodable` 证明 | 补「无死锁」的证明步骤：走廊是唯一被争用资源（A6）、等待只发生在容量充裕且不占通行资源的停靠位、已提交占用都在有限时刻过期，故不存在彼此等待的车辆环。**原证明覆盖了终止与无冲突，命题里的「无死锁」三字在证明中没有对应步骤**——决定 J·A 删掉 A7 后，「A4＋A6 ⇒ 无死锁」这条链只剩此处承载，故必补 | 否 |
| 46 | B04c | `alg:decode` 两次 `Route` 调用 | 补齐 $R$ 与 *commit* 实参并注明空载段同样占用走廊。原调用只传 3 个实参，而 `alg:route` 的 `\Require` 有 5 个，两张伪代码接口对不上，读者无法判断空载段是否提交预约（实现 `commit` 默认为真，`network.py:586/610`） | 否 |
| 47 | B04c | `alg:decode` 的 `\Return` | 补「逐走廊的让行统计量」。`\Ensure` 承诺三项而 `\Return` 只给两项，让行量在算法体内累计（`1372`）后无出口 | 否 |
| 48 | B04f | §4.6 末（`1665`） | 「整个过程是可复现的」原与另三件事并列挂在 `prop:decodable`／`lem:label`／`prop:speedup_equiv` 三条资产名下，但**没有一条保证可复现性**——它的真正来源是 §4.3 那条未编号的确定性性质。改为「再加上第 4.3 小节的确定性解码，整个过程是可复现的」（C5 验收测试 4；与记录 44 同源） | 否 |
| 49 | B04b ＋ B04g | §4.2 `1323` ＋ §4.8 `1654` | **决定 L·A**，了结 X20。单次路由调用 $O(\lvert E\rvert W\log\lvert V\rvert)\to O(\lvert E\rvert(W^{2}+\log\lvert V\rvert))$ 并补计数依据（至多 $\lvert E\rvert$ 次松弛；每次枚举 $O(W)$ 个候选进入时刻、每个候选再 $O(W)$ 判重叠；每次 $O(\log\lvert V\rvert)$ 堆操作），另加一句说明换成有序结构可降至 $O(\lvert E\rvert(\log W+\log\lvert V\rvert))$、本文实现用的是朴素扫描。§4.8 复合界同步改为 $O((N+n)N_A\lvert E\rvert(W^{2}+\log\lvert V\rvert))$。**原式不是有效上界**：`earliest_entry` 实为两重扫描（`network.py:435/444/422`），$W>\log\lvert V\rvert$ 时 $O(\lvert E\rvert W^{2})$ 超出所声称的界。无下游数字受影响——§4.8 的 37 倍与 4.8 倍均来自实测毫秒宏，`1655` 关于剪枝的论述只涉及因子 $N_A$ | 否 |
| 50 | B06 全文轮 | §5.5 `2033–2034` ＋ `2025` | **决定 M·A**，C5 证明义务全文扫描查出的唯一 OVERCLAIM。「第二阶段能挽回的东西有一个由第一阶段无知所框定的**上界**；要突破这个上界……」把一个未证、未测遍的界断言为既成事实，并推广到整条「先排产、后解冲突」文献路线，实证支撑只有本文算例族上的一个点（$\GainProbeOpen$ 对 $\GainLoopRule$）、一套实现。改为「我们把这个落差读作第一阶段的无知所留下的天花板；但天花板的高度是本文算例族上测得的一个点，不是一个已被证明的界」。同段 `2025`「**最多**拿到约 $\GainProbeOpen$」的「最多」同属界断言，改为「只拿到约」。收紧时顺手删掉了初稿里与 `2025–2026` 重复的一遍数字复述（否则正文由 39 页涨到 40 页） | 否 |
| 51 | B06 全文轮 | 摘要 `527`、§1 `706`／`788`、§2 `914` | **决定 N·A**，C5 §709 措辞一致。`prop:speedup_equiv` 证的是「所选车辆、所提交的预约以及最终时间表逐位相同」，全文却有三套说法：摘要与 §2 作「可证**等价**」（未写明等价类型，违反 C5「等价必须写清是解集／最优值／搜索轨迹」），§1 两处作「可证**保持最优选择**」（严格**弱于**命题——只提选车，漏了预约与时间表也逐位相同），§4.5 `1499` 与 §7 `2593` 作「可证**不改变输出**」（与命题相符）。四处统一到「可证不改变输出」，命题本身不动。改后全文十处同一措辞（`527/706/788/914/1500/1657/2343/2368/2414/2596`），无一处越强 | 否 |
| 52 | B06 全文轮 | — | C5 证明义务的**全文扫描本身**（此前四轮只覆盖形式化资产周边约 27 处）。「可证／等价／保证／最优／可采纳／收敛／上界／下界」全文命中 134 处，其中 4 处为导言区宏定义行尾注释（`386/387/403/404`，不渲染）不计。正文 130 处判定：OK 124、OK 但违反 §709 措辞一致 5（记录 51）、OVERCLAIM 1（记录 50）、PROOF_NEEDED 0、RHETORIC 0。**判定明细见 §五「C5 证明义务全文扫描」** | 否 |
| 53 | B01b | §5.1.2 `1816–1819` | **P01 了结**（卡片 §5.1 必须存在 4）。「各配置在同一算例上共享同一预算」补出绝对值：主批次 $90$ 秒、代数上限 $2000$ 代、早停阈值 $400$ 代。另按**决定 P·A** 补一句总则，声明其余读数来自另外四个批次、各有自己的预算与种子集、在首次引用处标注。出处：`clbs/output/baseline_ladder_10seeds.log:3`（UTF-16）「每算例每臂预算 90s, 种子=[42, 7, 2024, 13, 1, 99, 123, 777, 31415, 8]」，种子表与论文 `1714–1715` 逐位相同，可确证批次归属；代数与早停出自 `clbs/tools/baseline_ladder.py:89` | 否 |
| 54 | B01b | §5.1.3 `1862` | 补 Python 版本与依赖口径：「纯 Python」→「纯 Python（$\ge 3.8$），仅用标准库、无任何第三方依赖，亦不依赖任何求解器」。原句只否认了求解器依赖，**弱于实情**——`clbs/requirements.txt:1–2` 明写「纯 Python 标准库…无第三方依赖」「要求 Python >= 3.8」，零依赖对复现性是更强的陈述 | 否 |
| 55 | B01b | §5.1.2 `1825–1827` | 卡片 §5.1 必须存在 5 要求「把乘式写出来」。补 $\NCases$ 格 $\times$ $\NSeeds$ 种子 $=$ $\NPairs$ 配对；原文只分别给了 $\NSeeds$ 与 $\NPairs$，乘式写在 `1921`（属 F-RESULT，位置不对） | 否 |
| 56 | B01b | §5.1.2 `1854` 末 | 卡片 §5.1 必须存在 7 要求在本块一次性钉死离散度符号。补「图中误差棒为逐对相对增益的均值标准误，阴影带为种子间全距，表中不报 $\pm$」。原先该定义散在图注里（`2120` 标 SEM、`1973` 标全距），表格不报 $\pm$ 则从未说明 | 否 |
| 57 | B01b | §5.1.2 `1801–1802` | **决定 O·A**。删去「因为其中两个是我们先踩进去、之后才改过来的」——卡片 §5.1 禁止出现项逐字点名「过程性交代（"这两个坑是我们先踩进去的"）」。**保留**「三个陷阱会使朴素的比较近乎无意义」。同类自曝在 `2403`（「正是我们在前一版稿件里犯过的错」）经 C5 判为典范而予保留，两者的区别是：§5.1 是口径定义区，自曝放在论证现场才有力量 | 否 |
| 58 | B01b | §5.1.2 `1801–1802` | 顺带修 C6：原文说「三个陷阱」，其下却有五个 `\paragraph`（统一执行器／同挂钟／种子与配对／多重比较／正确性）。补「下面前三段各对应一个陷阱，后两段是本章其余数字的口径声明」 | 否 |
| 59 | B04a | §4.1 `1216–1219` | **了结 X16**（卡片 §5.4 必须存在 1：输入→机制→输出数据流）。原文只说「把四组决策分配到两层」，上层与下层各一句，**中间的事件驱动解码器、算例输入与最终输出三者全缺**——完整的数据流句此前只存在于 `fig:framework` 图注（`1248–1249`「中间的事件驱动解码器把上层的一个候选解翻译成一串运输任务」），即图注在承担正文的功能。补为「输入是一个算例：工件的工序链、机器的可选集与工时、车队规模与走廊网络……中间由一个事件驱动的解码器把上层的一个候选解翻译成一串带先后约束的运输任务……输出是一张可执行的无冲突时间表及其完工时间」。图注原句保留，图注自足是卡片 §5.3 的要求，两者不冲突 | 否 |
| 60 | B04a | §4.1 `1240–1241` | 删去与 `fig:framework` 图注近乎逐字的重复：「通路一每个候选解触发一次，通路二每个运输任务触发一次」在图注 `1250–1252` 已完整给出，「进入上层的位置不同」两处亦重复。正文只保留图引用与图注没有的那半句解读（「这正是它们代价不同的根源」）。此删减同时抵消记录 59 的新增——记录 59 单独落盘时正文由 39 页涨至 40 页，删后回落 | 否 |
| 61 | B07 | §1.5 贡献 5（原 `801–806`） | **决定 R·A，了结 P02／CF-1／TD-1**（卡片 §5.7 必须存在 6）。原文列 5 条贡献，卡片贡献表判 4 条（G5「实验装置」已划掉并明写「不计入 ≤4 的贡献名额」）。删去独立的贡献 5，内容按承重位置并入：「统一执行器＋同挂钟口径」并入贡献 3 末句，「四级基线阶梯＋两个受控负对照」并入贡献 4 末句并注明「是这些结论所依赖的实验装置，而非独立的贡献」。「争用强度可控的算例生成」原就在贡献 1 有对应表述（「补充一个可控的争用强度刻画」），不重复。结论 §6.1 本就把它并入第 3 条处理，改后引言与结论对齐 | 否 |
| 62 | B07 | §1.5（原 `751–766`）→ §1.4 末 | 卡片 §5.7 **禁止出现**项逐字点名「把实验协议扩成独立小节」。删「实验协议」小节，按必须存在 5「主结果 ＋ 真正决定结论成立的那 1–2 条协议」压成一段并入 §1.4：只留同挂钟而非同代数、以及每次只改一个因子两条，指针指向 §5.1.2。删去的种子与 Wilcoxon 属通例，算例设计一段属 F-SETUP（台账 S0 已标该处「位置错配／功能串味」）| 否 |
| 63 | B07 | §1.5 | 贡献列表原嵌在「实验协议」小节之内，副编辑找贡献会先撞上协议叙述。升为独立小节 `\subsection{贡献}`，并删去多余的引导句「本文的贡献如下。」。改后 §1 小节序列为 背景动机／差距／本文方法／主要结果／贡献，与卡片必须存在项 1–6 的顺序一致 | 否 |
| 64 | B07 | 贡献 2、贡献 3 标题 | 卡片第 326 行要求四个机制名在**题目、短标题、摘要、引言贡献、方法小标题、结论**中为同一字符串，而引言贡献里四缺三：贡献 3 写「预约表**探测式派车**」（全文其余 19 处写「预约表感知的车辆派遣」），「路由内嵌评价」与「低开销」则一次都没出现（仅见于题目区 `509`）。贡献 2 标题改为「闭环双层框架，以及*路由内嵌评价*的价值被定量」并在正文补该名的定义性同位语；贡献 3 标题改为「预约表感知的车辆派遣，以及使它*低开销*的两项优化」。四名现已齐备 | 否 |
| 65 | B07 | §1.3（原 `709–717`） | 卡片 §5.7 禁止「次要消融」。第三条接口（关键链归因）原占 9 行，连五类成因标签 `工序/机器/上游/车辆/走廊` 与两族算子名（改派、错峰）一并写进引言——这是 §4.6 与 §5.6 的内容。压成两句，只留「另实现并考察了两条更具雄心的接口……均未获净收益，作为受控负对照见 §5」。**同时了结重复防御句**：原 `805–806`「这些负对照不是本文的结论，而是…正当理由」与此处近乎逐字重复，随记录 61 删贡献 5 一并消失，全块防御性语句由 4 处降至 1 处（卡片验收测试 5 限 ≤1）| 否 |
| 66 | B07 | §1.6（原 `810–819`） | 卡片必须存在 7：「组织：**仅当结构非常规时才写**」。本文结构为 相关工作→模型→方法→实验→结论，完全常规，313 字路线图无信息量，删。**连带处理**：删后 `sec:Chapter_II` 与 `sec:Chapter_VI` 失去唯一引用。前者已在 §1.2 末补一句自然指引「第~\ref{sec:Chapter_II}~节把这两半逐篇落到具体文献上」——缺口指向相关工作章本就是卡片要的；后者接受为良性孤儿，见 X27 | 否 |
| 67 | B07 | §1.4 `737` | 卡片 K3（双口径自报）。同一块内两个争用强度上限：§1.4 用 `\ContentionAHi`（$31.9\%$，布局族内最大）、贡献 5 用 `\ContentionHi`（$33.5\%$，全族最大，来自一格 C 族读数）。**两处各自都对**，问题是并排出现而不自报口径。贡献 5 随记录 61 删除后块内冲突自动消失，§1.4 仍补三字「该族内」使口径显式。剩余的跨块差异（摘要 `530` 用 Hi、§1.4 用 AHi）转 X25 | 否 |
| 68 | B07 | §1.3 `697–699`、贡献 2 | **决定 S·A，了结 TD-2**。卡片 §5.7 禁止「据我们所知／首次／填补空白」除非有可核验对照，而卡片给不出该对照；卡片 G2 自身的标签写的也是「闭环的价值被**定量**」。三处全部降级：`698`「据我们所知，此前没有工作量化过这笔交换是否划算」→ 改为指向可核验事实「\cite{li2026framework} 的作者本人把『将该机制扩展为完全迭代的回路』列为未来工作，但没有给出把它做成回路要付多少、又能换回多少」；贡献 2 标题「被**首次**量化」→「被定量」；正文「从未有人量过它值多少」→「这笔开销换回多少并无现成读数」。三处均由全称断言改为对单篇文献的可核验陈述 | 否 |
| 69 | B08 | §2.1 末（新增段，`825–828`） | 卡片 §5.8 必须存在 2：每小节结尾一句定位（他们能做什么 → 因此不能做什么 → 本文接在哪条边）。**§2.1 原以综述引文收尾，完全没有定位句**，验收测试 1 判否。补一段，逐字对齐引言 §1.2 两半缺口的措辞「一类具备机器柔性却在常数运输时间矩阵上排产，看不见拥堵」：「这一支具备机器柔性，却在常数运输时间矩阵上排产……本文接在这条边上——保留其柔性建模与元启发式的求解框架，只把运输时长从一次矩阵读数换成一次可能被别的车推迟的实际通行。」不点任何机制名，故不触犯「禁止方法预告」 | 否 |
| 70 | B08 | §2.2 末（新增段，`856–858`） | 同上。§2.2 原结尾只说到「反馈能携带什么」，有分析无定位。补一段对齐引言另一半「具备真正的无冲突路由却没有柔性」：「任务从别处来，指派不在它的决策范围之内。本文接在这条边上——不改动其无冲突路由的正确性保证，而是让产生任务的那一层能够读到路由过程中实际发生的让行。」 | 否 |
| 71 | B08 | §2.2 `852`（原 `838`） | **决定 V·A，卡片禁止「方法预告」第 1 处**。原句「……才能指名『哪一段、哪个时段、被谁先占』**，后者才是上层据以决定『是把工序换个地方，还是仅仅换个时间』所需的信息**」——加粗部分把改派与错峰两族算子预告进了相关工作。删。保留的前半只陈述代理量与具体让行事件的区别，是定位所需 | 否 |
| 72 | B08 | §2.3 `869–870`（原 `847–849`） | **决定 V·A，第 2 处，卡片两条要求的边界所在**。原句前半「本文采用这一问题类，并把上述『由不可行成因反馈到可行动决策』的思路移到启发式区间」是必须存在 2 要的「本文接在哪条边」，**保留**；后半「第~\ref{sec:Chapter_IV}~节的预约表试探正可视为该思路在启发式框架中的对应物」点名机制并前指方法章，是预告，**删**。删前已核 `sec:Chapter_IV` 在 `1068` 另有引用，不产生孤儿标签（`check_tex` 孤儿列表删前删后同为 8 条） | 否 |
| 73 | B08 | §2.3 `896–897`（原 `876–878`） | 卡片 §5.8 唯一功能：本块只做「把引言给出的缺口落到具体文献上」。原文在算例设计段末尾追加「并对实验协议提出一项要求：当各机制的单次评价成本相差达两个数量级时，在相同迭代次数下比较所测得的并非其所声称的东西……」——这是 F-SETUP 的论证。**核查后发现它是全文第四份**：引言 `747`、§4.8 `1632–1637`、§5.1.2 `1784–1794`（整段 `\paragraph{同挂钟，而非同代数。}`）已各有一份。直接删，不搬运，故 X29 不成立、无挂账。省 130 字 | 否 |
| 74 | B08 | §2.1 `813–815` | 卡片禁止「只与 claim 共享领域关键词的引用」。Qin 与 Fu 两句原本只描述各自方法（QD 存档＋Q 学习、改进遗传算法），与本块主线「常数矩阵」的连接全靠段末总结句一句兜底。各补半句连回本文决策：Qin 补「——但其关键路径仍是在矩阵行驶时间上算出的」（同时为 §2.3 `885` 的关键链诊断埋下对照），Fu 补「其『一体化』指两类决策被同时求解，而非运输时长随排程而变」 | 否 |
| 75 | B08 | `tab:related`（`\label` 后） | 卡片 §7 第 8 项：手打表须与脚本生成表分别标注。`tab:related` 全表手打，但源码无任何标记，与 `tab/` 下由 `gen_tables_ladder.py` 生成的表在源码里长得一样。加源码注释「手工维护表：无生成脚本；任一行改动须同步 §2.3 正文」，仅源码可见，不进 PDF、不占篇幅 | 否 |
| 76 | B08 | §2 全块 17 处 | 卡片 §5.8「转述他人局限或结论而无原文依据（一律插 `[ref_check]`）」＋作者指令「插标记而不下判断」。17 处全部按 §5.13 格式 `[ref_check: key=…; claim="…"; need=quote]` 挂出，**一字未改措辞、一处未降级**。**执行细节偏离卡片字面**：§5.13 示例是行内明文，那样会渲染进 PDF（17 处，影响版式与页数）；改为紧跟对应段落末尾的 `%` 注释行，PDF 干净且 `grep '\[ref_check'` 可全数取出，交 F-REF 块处理。分布：§2.1 六处、§2.2 五处、§2.3 六处。其中两处是全块仅有的**零出处全称断言**——`806–809`「常数行驶时间矩阵是占主导地位的建模约定」（句末无 cite）与 `840–841`「拥堵被公认会在远未达到容量上限时就已损害吞吐」（无任何出处），若 F-REF 核不出原文依据，须改写为「常见于……」或补 `[ref_insert]`。`li2026framework` 的三处引文（`876–877`）**不标记**：原文已明说取自「该文自己的局限性小节」且三句均带引号，属有依据的直接引用 | 否 |
| 77 | B09 | 局限 (1) `2640–2664`→`2640–2650` | **C2 主删项**。原 10 句 1012 字压到 4 句约 400 字，删掉的**全部是别处已有的内容**，无一数字、无一范围限定丢失：`(1).7` 公开矩阵不对称／非最短路闭包（212 字）与 §5.2.1 `1690–1701`（12 行）是同一论证的两遍 → **决定 X·A**，长版本留在使用点 §5.2.1（那里它是「为何自建算例」的设计依据），此处压成一句加交叉引用（C2-B-4 ＋ §5.9 明令「压成一句」）；`(1).8` 退化比较如实报告与 §5.2.1 `1699–1701` 逐义重复 → 删；`(1).9` 的 `vanos2026exact`／`lyu2019approach` 模型差异（297 字）与 §3.5 `1113–1117` 重复 → 删（C2-B-3d，**B05 已把它搬进 §3.5**，此处是搬运后未清理的旧址），但**保留不可比的结论一句**，因为 §3.5 `1118` 前指本节，删净会使前指悬空；`(1).10`「并非为本文方便而设」与 `1116–1117` 逐字重复 → 删。`(1).4` 后半「需在网格上另做容量旋钮」属「解释未做之事」→ 搬回复信 | 否 |
| 78 | B09 | §3.5 `1104` | 记录 77 的交叉引用需要落点，而 §3.5「与 FJSP\_PT 的关系」原**无 `\label`**。新增 `\label{sec:fjsppt}`。至此 §3.5 与 §6.3 互指闭合：§3.5 讲模型差异并前指本节问不可比，本节给不可比结论并回指 §3.5 问差别 | 否 |
| 79 | B09 | 局限 (2) `2666–2675`→`2652–2660` | 5 句 → 4 句。合并前两句（功效数字与量级 CI 本是同一论证）；末句删掉「我们把这一条单列，是因为在同类文献中……是常见做法」这个自辩框架，**保留 C2-F 保护项③ 的证据内核**「本文前一版正是按逐格符号叙述柔性族的适用区间，被逐格 $p$ 值的族内校正挡了下来」。卡片明令这五条保护项是证据不是辩护、不得作删减对象 | 否 |
| 80 | B09 | 局限 (3) `2677–2697`→`2662–2673` | 10 句 → 4 句，822 字 → 约 400 字。删两处：`(3).5`「量级只是旁证，且我们不夸大它……并非高出一个量级」属自我限定堆叠 → **搬回复信**；`(3).6`「这与局限 (2) 是同一件事的**两个侧面**」正是 §5.9 明令禁止的复述形态 → 改为「（另见局限 (2)）」。`(3).4`「不威胁主效应」是 §5.9 必须存在项，留，只删括号内旁支（该问题未来工作已有）。**移出的四个宏 `\PubLBSlack`／`\PubLBTotal`／`\PubGapMean`／`\GainLoopRule` 经核仍在 §5.10 `2528–2570` 首报处**，局限块原本只是在复述它们；`check_tex` 的「定义但未引用」仍为 0 | 否 |
| 81 | B09 | 局限 (4) `2699–2703`→`2675–2679` | 5 句 → 4 句。本条是五条里质量最好的——已符合 C2-E 的负结果正确形态（直接给作用域，没有「先设想三条本应有用的理由再逐一解释」），唯一越界是 `(4).4`「这正是本文降本一节的意义所在」的自我推销，压成交叉引用半句并入末句 | 否 |
| 82 | B09 | 局限 (5) `2681–2684`、新增局限 (6) `2686–2691`、未来工作「动态运行」`2712` | **决定 W·A，同时了结 CF-4 与 X18**。§5.9 必须存在项「每条与未来工作一一对应」原本两头都缺：局限 (5) 收敛性无对应未来工作（卡片标 `MISSING`），未来工作「动态运行」无对应局限。修法：(5) 补半句「这是主动放弃的主张，故本条不开对应的未来工作」（N3 一致）；新增局限 (6)「静态假设」3 句，引 **A7**（「不在建模范围内」，B05 renumber 后的编号，明列机器与车辆故障、动态到达）并指向「动态运行」；「动态运行」首句改为「本条对应局限 (6) 所划的建模范围」。条数 5→6，仍在 4–6 内 | 否 |
| 83 | B10 | §6 引语 `2579–2582`、§6.1 `2588–2594` | **了结 X24**。B07 把引言贡献由 5 条改为 4 条后，结论仍是按**旧的 5 条**合并出的 3 条（映射 G2→(1)、G3→(2)、G4→(3)，**缺 G1 问题层**），卡片 §5.10 验收测试 1 判否。补贡献 (1)「一个在最接近的两类工作中都无法表述的权衡」，原 (1)(2)(3) 顺延为 (2)(3)(4)。**较诊断有一处微调**：诊断说「不另写引语」，落盘时保留了压缩后的引语——原引语后半「当预算固定、任何信息都要用放弃掉的评价次数来支付时，哪一种层间信息值得它的代价」是全文论点句，删净会丢；其前半（问题设定）才是 G1 的内容，已并入贡献 (1)。引语由 4 行压到 3 行 | 否 |
| 84 | B10 | §4.4.1 `1474–1477`、§5.6.2 `2303–2307`、§6.2 `2620–2622` | **决定 Z·A，了结 P07（大修·阻断）与 X13**。三处架构级全称断言强度递增：§4.4.1「在**任何**……框架中，很可能是有害的」→ §6.2「任何……都具有这一性质」→ §5.6.2「不只是无益，**而是严格有害的**」，证据只有定价这一个负对照、2 个算例族、1 套实现。三处**同轮同强度**落地（X13 明记只改一处「反而更糟」）：全称量词「任何……都」一律改为条件句「只要……就」，机制的可迁移性保留而不再断言穷尽；§4.4.1 的「架构层面」改「机制层面」；§5.6.2 的「严格有害」降为「在本文的口径下系统性地有害」；§4.4.1 与 §6.2 各补一句证据基础自报「本文只在定价这一个接口上验证了这一点」。全文核验「任何以完整无冲突解码」「严格有害」均为 0 处 | 否 |
| 85 | B10 | §6.1 (4) `2612`、末句 `2617` | 卡片 §5.10 禁止「第 5 条贡献」。原标题「主效应与次效应的分工，**以及支撑该结论的实验协议**」把已撤销的 G5 抬进标题，且无引言 G4 那句免责。标题改为与引言 G4 逐字一致的「角色分工，以及一条关于分辨率的负结论」，实验协议内容留正文并补免责句「它们是这些结论所依赖的实验装置，而非独立的贡献」 | 否 |
| 86 | B10 | §5.5 `2024`、§1.4 `733`、§6.1 `2614`、导言区注释 `191` | 验收测试 5 ＋ 卡片 G4「**不主张正交互**」。「无正交互」是断言不存在，实测只支持「未检出」。**四处同改**——诊断只点了 §6.1 与 §1.4 两处，落盘时全文复扫又发现首报处 §5.5 `2024` 与宏定义注释 `191`；只改结论会造成结论已收窄而数据小节仍作强断言 | 否 |
| 87 | B10 | §6.1 (3) `2607–2609` | 验收测试 5 ＋ 卡片 G3「合计口径为正；**逐格不可兑现**」。「本文对这一机制的主张是完整的：**它有效**」是无口径裸断言，而同段前文刚说逐格校正后无一格显著。改为「它在合计口径上有效（逐格不可兑现，见局限 (2)）」 | 否 |
| 88 | B10 | §6.4 `2709` | C6 内部一致。「按**上一小节**的比值框架」——§6.4 的上一小节是 §6.3 局限，而比值框架在 §6.2 洞见 3。改为 `\ref{sec:insights}`。**连带收益**：`sec:insights` 由此脱离孤儿 label 名单（`check_tex` 孤儿数 8→7） | 否 |
| 89 | B10 | §6.4 `2706`、`2711`、`2718` | 卡片必须存在「每条与未来工作一一对应」＋验收测试 3。五条中「增量式评价」「链可替代性」未明写挂靠，「把绝对水平在两端测准」的挂靠散在段中。三条统一补「本条对应局限 (N)」，与「把次效应的适用区间测出来」（局限 (2)）、「动态运行」（局限 (6)，记录 82）一致。五条挂靠现为 (4)/(1)/(3)/(2)/(6)，**六条局限中除 (5) 外全部有对应**，而 (5) 已按记录 82 就地声明不开对应坑 | 否 |
| 90 | B10 | §6.4 `2718–2725` | 卡片禁止「清单式开新坑」＋ C2 残留。「把绝对水平在两端测准」原 430 汉字占未来工作 40%，内含 B09 刚按 C2-B-3e 清理过的同类辩护：「可行的做法不是把本文方法搬到对方算例上（冲突语义不同构，见局限 (1)），而是把该精确方法的冲突模型改写为走廊排他的版本后……故这是一项独立工作而非一次补充实验」。删该段辩护，保留全部数字与两个落点。**篇幅净效应为零**：本条省下的字数恰被记录 83 新增的贡献 (1) 抵消，F-CONCL 由 6.41% 到 6.42%——按决定 Y·A 这不构成问题，合规判据是含局限的 9.91% | 否 |
| 91 | B11 | 摘要段 1 `514–519` | 卡片 §5.11 段 1 明令「不写文献史」。原段 5 句 310 汉字，末三句「该权衡在两类最接近的工作中均无法表述:一类具备机器柔性,但上层在常数运输时间矩阵上排产,看不见拥堵;另一类具备真正的无冲突路由,却沿用每道工序仅一台可选机器的经典基准,指派根本不成为决策」是 §2 的定位内容。按**决定 AA·A** 压成半句「而这一权衡在两类最接近的工作中都无法表述」并入第三句末尾——保留缺口感、不展开文献分类。段 1 由 310 → 220 汉字 / 4 句（卡片限 2–4） | 否 |
| 92 | B11 | 摘要段 2 `521–526` | 卡片验收测试 4：机制名须与题目逐字一致。题目含三个机制名（低开销 / 路由内嵌评价 / 闭环双层框架），改前摘要**只有「闭环双层框架」**，另两个各 0 处（全文各 5 处），摘要改用「评价通路」与「可证不改变输出的降本」描述同一事物。补入 `\emph{路由内嵌评价}`（替换「评价通路把……定义为」的迂回说法）与 `\emph{低开销}`（替换「付得起」），三个机制名一律加 `\emph` 与全文机制名格式对齐。「降本」→「优化」以免与目标函数混读（导言区注释 `501–502` 记录的刻意选择，未回退） | 否 |
| 93 | B11 | 摘要段 3 `528–536` | 三项补口径。**其一**（验收测试 3）补「共用单一规模」——局限 (1) 明写「全部算例格共用一个规模，本文不就机制如何随规模变化作任何主张」，改前摘要仅说「NCases 个受控算例」，读者会读成覆盖多种规模，构成会被局限反驳的无条件主张；按指令**收窄摘要，局限块未动一字**。**其二**（决定 BB·A，了结 X25 摘要侧）保留 `\ContentionHi`（33.5%，全族最大）并自报「后者为全族最大值」，与 §1.4 已自报的 `\ContentionAHi`（31.9%，布局族内最大）互不冲突——摘要描述的本就是全部 NCases 格，用全族口径才对。**其三**（验收测试 2）`\GainLoopRule` 的「相对谁」原靠「把拥堵搬进搜索目标」隐含，补写「相对开环阶梯」。另删「少数例外在正文逐处标明」（§5.1 已有完整落点，摘要内属噪声） | 否 |
| 94 | B11 | 摘要段 4 `538–541` | 卡片禁止「负对照的详细说明」，且段 3 / 段 4 重报了同一批数字。删末句「本文并以两个受控负对照(走廊时段定价、冲突凭证制导的局部搜索)说明为何在若干更具雄心的接口设计中选定了这一个」（两个负对照在正文 §5.6 各有完整落点，摘要内 0 处）。删段 4 重复的 `\GainLoopRule`、`\GainProbeLoop`、`\GainEndToEnd`（段 3 已报）与属「次要消融」的 `\GainLoopProbe`、`\GainProbeOpen`、`\PProbeOpen`——三者在正文仍有 3 / 7 / 3 处引用，`check_tex` 无「定义但未引用」告警。段 4 由 190 → 114 汉字，成为纯范围句 | 否 |
| 95 | B11 | 摘要 `538–541`、机制名三处 | **X31 兑现**。记录 86 把全文「无正交互」四处改为「未检出正交互」，摘要用的是「呈近加性**而非**正交互」这一不同字符串故被漏过，改前**全文仅剩的旧措辞就在摘要**。改后「而非正交互」全文 0 处、「未检出正交互」5 处。同轮统一摘要内机制名简称漂移：改前「预约表感知的车辆派遣」1 处 ＋「预约表感知派遣」2 处并存（验收测试 4 直接命中），改后全称 3 处、简称 0 处。全文层面的 12 处简称仍按 X26 留 S4 | 否 |
| 96 | B11 | 摘要全块 `513–542` | 篇幅收口。删除项落地后实测 760 汉字，仍超卡片上限 700，故追加一轮无损压缩：段 1「建模为→设为」「这等于→即」「有限规模的车队→有限车队」并删与「不再是输入数据」语义重叠的「,而非布局的固有属性」；段 2「每一代每个候选解→每个候选解」「使……,而不再是→……而非」「决策通路上承重的机制是→决策通路上承重的是」；段 3「全算例族内的最大值→全族最大值」「其中前两级分别对应→其前两级对应」；段 4 删段 3 已建立的 `\NCases` 复指、两个「不就……作主张」合并顿号。**最终 697 汉字**（220 / 201 / 162 / 114），四段结构与全部实质内容不变 | 否 |

| 97 | X15 | §5.1.3 `1861–1868`；§4.6 `1545`／`1609`；§4.7 `1641`／`1643` | 卡片 §5.4 禁止方法章罗列超参。§5.1.3 加 `\label{sec:impl}` 并一次列全：种群 $60$、$p_c=0.8$、$p_m=0.2$、精英 $5$、二元锦标赛、局搜作用于前 $10\%$、$L=5$、至多三轮（源：`clbs/algorithm/ga.py:26–35`；种群 $60$ 出自 `tools/baseline_ladder.py:89` 的覆盖值，**不是** `GAConfig` 默认的 $100$）。§4 侧删纯配置数（`10\%`、「至多三轮」、「二元锦标赛并保留五个精英」→「锦标赛并保留少量精英」）并在 §4.7 末尾指回 `sec:impl`；**保留** §4.6 的 `0.5`（归因阈值，属机制定义）与 `L=5`（`L+2=7` 依赖它）。台账警示已核：`10\%` 是 `top_ls` 而非变异率，未写反 | 否 |
| 98 | X17 | —— | **核查后判定无需改动**。X17 记「§6.3(1) 约 330 字与 §3.5 模型差异重复，应压成一句指回」，但 B09 记录 77 重写局限 (1) 时已带掉：现文只剩「带显式路网的两项工作则因预约单位不同而不可直接比较，差别见第~\ref{sec:fjsppt}~小节」一句，正是 X17 要求的形态。**已了结，零改动** | —— |
| 99 | X14 | §5.6.2 `2325–2327` | §4.6.2 `1605`「于是邻域最多包含 $L+2=7$ 个候选,且每一个都是由下层指名的」与此处近乎逐字重复。保留 §4.6.2（机制定义的所在地），把 §5.6.2 改为指回：「该邻域按构造既小又有指向(第~\ref{sec:ls}~小节)」。论证链不变——低命中率的前提仍在，只是不再重述数字 | 否 |
| 100 | X21 | §5.6.2 `2190–2193` | 原句「\MatrixCells 个算例格 $\times$ $10$ 种子、**同挂钟预算**」会被读成与主批次同为 $90$ 秒。删「同挂钟预算」四字，另起一句写明该批次预算与种子集均与主批次不同：挂钟按算例标定于 $30.2$–$72.9$ 秒（`clbs/experiments/meta.json:48–65`），批次内各臂共享同一标定值故臂间比较仍同预算，种子集另取十个 | 否 |
| 101 | X22 | `fig:convergence` 图注、`fig:theta` 图注 | 按决定 P·A 在首次引用处标预算。`fig:convergence` 补「本图的挂钟预算为 $46.3$ 秒,与主批次的 $90$ 秒不同」（源 `clbs/output/convergence.log`）；`fig:theta` 补「挂钟预算分别为 $48.9$ 秒与 $60.7$ 秒,均与主批次的 $90$ 秒不同」（源 `clbs/output/theta_wall.log`），接在已有的种子数之后、「降本之前」批次声明之前 | 否 |
| 102 | X28 | §1.5 贡献 1 `754` | `sun2023integrated` 被写成「具备柔性但用常数运输时间矩阵排产的模型」，而 `tab:related` 标它「车辆争用：无冲突」、§2.3 `872–873` 写它用时间窗 Dijkstra 消解冲突。并列阅读像是自相矛盾。改为「具备柔性但**排产阶段**用常数运输时间矩阵的模型」——Sun 的排产确实用矩阵，冲突消解在第二阶段，加两字即消歧。§1.2 `682` 与 §2.1 `827` 的同类表述**未动**：它们说的是「这一支」整体而非具名文献，不产生并列矛盾 | 否 |
| 103 | X33 | §4.1 标题 ＋ `1216`；§4.5 标题；§1.3 `703`；§1.5 贡献 2 `769`；§2.3 `904`；§6.1 (2)(3) | 机制名跨块统一。**「低开销」**：§4.5 标题「降本:…」→「低开销:…」；结论 (3) 标题「使它付得起的降本」→「使它\emph{低开销}的两项优化」（与引言贡献 3 逐字同）；§1.3 `703`「付得起…降本」→「\emph{低开销}…优化」（与摘要逐字同）；§2.3 `904`、§1.5 `769` 同步。**「路由内嵌评价」**：§4.1 标题「框架总览:两条反馈通路」→「框架总览:路由内嵌评价与决策通路」，正文 `1216` 的 description 项改为「评价通路,即\emph{路由内嵌评价}。」；结论 (2) 标题「一个双反馈通路的闭环双层框架」→「闭环双层框架,以及\emph{路由内嵌评价}的价值」（与引言贡献 2 同构）。**未动**行文中的「降本」约 25 处（「降本之前/之后」「代价律与降本效果」）——那是动作名词不是机制名，替换成「低开销之前」不成话 | 否 |
| 104 | X26 | 正文 8 处 | 「预约表感知派遣」简称统一为全称「预约表感知的车辆派遣」：`777`、`1225`、`1993`（`\paragraph` 标题）、`2230`、`2398`、`2619`、`2671`、`2734`。改后正文简称 0 处、全称 18 行。导言区三处注释（`170`／`370`／`491`）保留简称，不影响渲染 | 否 |
| 105 | X32 ＋ X27 | `716`／`729`／`2625`；`\section{结论}` | X32：`$2\times2$` 三处统一为带空格的 `$2\times 2$`，与 `1895`／`1903`／`1936` 一致，全文现 6 处同形。X27：`sec:Chapter_VI` 删除——它是全文唯一无人引用的章级 label，而 §6 的三个子节 label（`sec:contributions`、`sec:insights`、`sec:limitations`、`sec:future_work`）都各有引用，章级锚点冗余。`check_tex` 对 `sec:Chapter_*` 前缀有盲区，此项须人工核 | 否 |
| 106 | X34 / B12 | `498–509` 导言区注释 ＋ `\title` | **F-TITLE 落地，取候选 C**。全标题 35 字（堆叠 4 层）→「柔性作业车间调度与 AGV 无冲突路由的路由内嵌评价闭环双层框架」**27 汉字、堆叠 3 层**，两条硬约束同时满足。短标题「低开销的路由内嵌评价闭环**调度**」→「路由内嵌评价闭环双层框架」——原短标题的「闭环调度」不是全文任何一处使用的机制名，违反 §5.12「短标题与全标题的机制名必须一致」。舍「低开销」的实质理由不是长度：摘要与贡献 3 里「低开销」修饰的是\emph{预约表感知的车辆派遣}，入题后成了框架的定语，范围被悄悄放大。导言区注释第 2 条据此改写（记录选择理由，未回退「用低开销而非降本」这一口径），英文题名同步去掉 Low-Overhead | 否 |
| 107 | X34 / B12 | `\keywords` `564–565` | 关键词重排为 9 个，逐个在摘要或引言有落点：柔性作业车间调度（引言 2）、无冲突路由（引言 8）、一体化调度（摘要）、走廊争用（引言 1）、闭环双层框架（摘要＋引言）、路由内嵌评价（摘要＋引言）、预约表感知的车辆派遣（摘要 3 ／引言 3）、低开销（摘要＋引言）、时间窗预约（摘要＋引言）。**删三个不合格项**：「异构机械臂」摘要与引言均 0 处（摘要作「能力重叠、效率互异的机械臂」＋「异构性」）、「AGV 无冲突路由」同样 exact 0 处（引言 8 处作「无冲突路由」）、「预约表感知派遣」是简称。**补方法族一栏**（原为空）：「时间窗预约」是唯一合格候选——「memetic 算法」「遗传算法」在摘要与引言均 0 处，「局部搜索」虽有 2 处但在本文是受控负对照，做关键词会误导检索。「完工时间」让位给方法族 | 否 |
| 108 | X23 | §5.1.2 `1816–1823` | **X23 回填，改写「预算被真正用尽」一句**。原句：「代数上限 $2000$ 代与早停阈值 $400$ 代均放宽至不构成约束，以确保预算被真正用尽，每次运行都报告它是耗尽预算还是提前收敛。」证据链：(a) `ladder_diag.py:153` 与 `baseline_ladder.py:89` 的 `GAConfig` 逐字段相同（`pop=60, max_gen=2000, stall_gen=400, time_budget_sec=90`），诊断批次的 3 算例 3 种子均为主批次 10×10 的子集，故可外推；(b) `ladder_cost.csv` 的 `runtime_sec` 逐次读数——B0 九次全部 $10.97$–$26.17$ s（均值 $17.63$），B1 九次全部 $90.01$–$90.26$，B2 九次全部 $90.13$–$91.24$；(c) `ladder_convergence.csv` 每组行数即代数——B0 为 $421/440/446/485/635/688$ 代，全部 $<2000$，故 B0 停于 `stall` 而非 `max_gen`；(d) 主批次 `baseline_ladder_10seeds.log` 每格耗时 $2044$ s（十格 $1958$–$2096$），按诊断分布外推 $10\times(17.63+90.12+90.56)=1983$ s，相符到 $3\%$ 以内，证明该分布在主批次上同样成立。改后如实交代：代数上限从未触发、早停阈值每次都在开环上触发、开环只用掉预算的一到三成、两个闭环每次跑满。**并补一句「这一不对称对基线是有利的」**——开环不是被掐断而是搜索到收敛为止，且其评价次数与 B1 相当或更多（九次诊断里八次 B0 更多，故写「相当或更多」而非「最多」）。停机原因的出处与主批次只留总耗时这一事实一并写明，不假装主批次有该列 | 否 |
| 109 | X23 连带 | §5.1.2 `1825` | 核 X23 时顺带查出的**量级错误**：「对齐时间…反过来给便宜的配置多送几十倍搜索次数」。实测 B0/B2 的评价次数比为 $2.5$–$13.3$ 倍（`ladder_cost.csv` 九对），不是「几十倍」。「几十倍」在 `1815` 是对的——那里说的是**单次评价成本**之比（B0 $0.28$–$0.44$ ms 对 B2 $5.6$–$19.6$ ms，$21$–$42$ 倍），两个量被混用了。改为「多送数倍乃至十余倍搜索次数」。注：若 B0 不早停而跑满 $90$ s，次数比确会进到几十倍——但它早停了，故按实测写 | 否 |
| 110 | X35 | `fig/fig_protocol.py` | **取选项 (甲) 重画 `fig:protocol`**。三处改动：(1) 灰线由 `budget = mean(全部 36 行 runtime_sec)`$=53.98$ s 改为模块常量 `BUDGET = 90.0`（§5.1.2 声明的名义上限），图例 `equal wall clock (54 s)` → `budget cap (90 s)`；改后 B1／B2 精确贴线、B0／B0$^+$ 明显落在线下方，正是记录 108 要讲的事实。(2) 各档标记点由算术平均改为**几何平均**——对数轴上只有几何平均满足 $\mathrm{gm}(x)\cdot\mathrm{gm}(y)=\mathrm{gm}(xy)=\mathrm{gm}(\text{挂钟})$，故点必落在该档真实挂钟的双曲线上；原算术平均因 `ms_per_eval` 与 `decodes` 跨算例强负相关，把 B2 画到了 $120.9$ s 的双曲线上（无任何一次运行对应）。降本消融的两个点同步改用几何平均。(3) 新增淡色小点显示三算例逐个读数，并把「B0 停在 $17$ s——是收敛不是被掐断」并入左下脚注（初版做成内联注记，与 B1 散点相撞，故改并入）。模块 docstring 补记为何不能用实测均值画线。`mean` 与 `by` 两个 import 随之移除 | 否 |
| 111 | X35 | `fig:protocol` 图注 ＋ `\Description` ＋ §5.7 `2372–2374` | 图注补三句与新图一致的口径：灰线是 $90$ 秒上限、两个闭环贴线而开环落在线下方并给出原因（约 $17$ 秒自停，回指 `\ref{sec:protocol}`）、淡色小点与几何平均的关系。`\Description` 同步重写（原文称 four configurations lying along an equal wall-clock hyperbola，与实测不符）。正文 `2373` 的「因此在同挂钟下便宜的配置能搜得多得多」改为「因此便宜的配置能搜得多得多——开环用掉不到两成挂钟就完成了最多的评价次数」：原句默认各档同挂钟，恰是记录 108 推翻的前提。编译 39 页不变、`check_tex` 零新增告警、`Overfull` 0 条 | 否 |

### 记录明细（逐字串，回滚用）

**记录 1**（修改 1）— 原文串：

```tex
FJSP\_PT 基准的通行做法是把行驶时间给成一张"位置对--时长"矩阵,而这恰是本文要去掉的那条假设。
一张矩阵本身不足以定义车辆争用:争用发生在具体的通道上,而矩阵只记下端点之间的总时长。于是
问题不在于"矩阵够不够",而在于能否从矩阵反推出一张与之一致的通道网络。我们对实际取用的那一族
做了检验,答案是不能,而且理由比"文献没有附网络"更硬:
在 Homayouni 与 Fontes~\cite{homayouni2021production} 所用的 \PubNLayout{} 个布局中,
\PubNLayoutAsym{} 个矩阵\emph{全部不对称}(同一对位置往返时长不等,这是单向导轨的必然结果),
而其中 \PubNLayoutBad{} 个还\emph{违反三角不等式}——存在直达时长严格长于绕行时长的位置对。
这两条性质各自排除一种还原:本文的走廊是无向的、每条只有一个通行时间 $\tau_e$,故由它算出的
$t^{*}$ 必然对称,\PubNLayoutAsym{}/\PubNLayout{} 张矩阵因此都不可能是本文任何一张路网的
$t^{*}$;而任何图的全对最短路矩阵必然满足三角不等式,故那 \PubNLayoutBad{} 张矩阵\emph{根本不是
任何图的最短路闭包}——即便允许有向图也还原不出来。若强行为它们反推一张网络,得到的行驶时间
在那些位置对上必然\emph{严格更短}(绕行路径本身就成了更短路),那已经是\emph{另一个}算例,而
不再是文献发布并给出参考值的那一个,于是参考值也不再可比。因此我们生成一族受控算例,其中网络
是显式的,且
第~\ref{sec:features}~小节判定为关键的那些量可以逐个单独变化。这不排除在公开算例上做另一种
比较——把本文的争用约束删去、退化到与文献同一个数学问题上比对完工时间;
第~\ref{sec:external}~小节报告该比较,它标定的是上层搜索的强度,而非本文的框架。
```

改后串：

```tex
FJSP\_PT 基准的通行做法是把行驶时间给成一张"位置对--时长"矩阵,而这恰是本文要去掉的那条假设:
争用发生在具体的通道上,矩阵却只记下端点之间的总时长。于是问题不在于"矩阵够不够",而在于能否
从矩阵反推出一张与之一致的通道网络;对实际取用的那一族,答案是不能。
在 Homayouni 与 Fontes~\cite{homayouni2021production} 所用的 \PubNLayout{} 个布局中,
\PubNLayoutAsym{} 个矩阵\emph{全部不对称}(单向导轨的必然结果),而其中 \PubNLayoutBad{} 个还
\emph{违反三角不等式}。前一条排除了本文这类无向走廊网络——每条走廊只有一个通行时间 $\tau_e$,
由它算出的 $t^{*}$ 必然对称;后一条更强:任何图的全对最短路闭包都满足三角不等式,故那
\PubNLayoutBad{} 张矩阵\emph{根本不是任何图的闭包},即便允许有向图也还原不出来。强行反推只会
得到\emph{另一个}算例,文献发布的参考值随之不再可比。因此我们生成一族受控算例,其中网络是显式
的,且第~\ref{sec:features}~小节判定为关键的那些量可以逐个单独变化。这不排除在公开算例上做另
一种比较——把本文的争用约束删去、退化到与文献同一个数学问题上比对完工时间;
第~\ref{sec:external}~小节报告该比较,它标定的是上层搜索的强度,而非本文的框架。
```

---

**记录 2**（修改 10）— 原文串：

```tex
矛盾的主要方面,这正是本文问题设定的用意所在。在此核心之上变化三族因子
(表~\ref{tab:instances})。
```

改后串：

```tex
矛盾的主要方面,这正是本文问题设定的用意所在。$H$ 在十格上同为 $0.3$ 而不随柔性漂移:生成器
对每道工序的 $|\Omega_{ji}|$ 个加工时间先抽独立扰动、再标准化为零均值单位方差后乘以 $H$,故
行内变异系数与 $|\Omega_{ji}|$ 无关,C 族改变 $F$ 不会带动 $H$(取整引入的小偏差在生成后实测
并随算例记录)。在此核心之上变化三族因子
(表~\ref{tab:instances})。
```

---

**记录 3**（修改 2）— 原文串：

```tex
并随算例记录)。在此核心之上变化三族因子
(表~\ref{tab:instances})。
```

改后串：

```tex
并随算例记录)。在此核心之上变化三族因子
(表~\ref{tab:instances})。这些算例由 \texttt{tools/abc\_matrix.py} 的 \texttt{build()} 构造,
生成参数即上述三族因子,生成种子固定为 $42$、十格共用。生成种子与运行种子是两回事:后者指
第~\ref{sec:protocol}~小节所述每格 \NSeeds{} 次重复各自的种子,取 $42$、$7$、$2024$、$13$、
$1$、$99$、$123$、$777$、$31415$、$8$——其首个与生成种子同值,但二者角色不同。
```

> **回滚依赖**：记录 3 的原文串是记录 2 的改后串尾部。**若要回滚记录 2，须先回滚记录 3**；单独回滚记录 2 会报 `AMBIGUOUS_ROLLBACK`。

---

**记录 4**（修改 3）— 原文串：

```tex
\texttt{low}、\texttt{scatter},争用强度由 \ContentionAHi{} 递降到 \ContentionLo{}(全族最拥堵的
一格 \ContentionHi{} 不在本族,它是同样建在 funnel 布局上、柔性拉满的 C 族那一格)。这一族
由两段构成,须分开交代,因为只有前一段是纯容量扫描。
```

改后串：

```tex
\texttt{low}、\texttt{scatter},此处按构造顺序列出(哑铃三档在前、网格两档在后);争用强度的
跨度为 \ContentionAHi{} 至 \ContentionLo{},但\emph{不}沿此顺序单调——\texttt{low} 高于
\texttt{mid},逐格值见表~\ref{tab:instances}。全族最拥堵的一格 \ContentionHi{} 不在本族,它是
同样建在 funnel 布局上、柔性拉满的 C 族那一格。这一族
由两段构成,须分开交代,因为只有前一段是纯容量扫描。
```

---

**记录 5**（修改 9）— 原文串：

```tex
这里必须由我们自己指出而不是留给读者发现的是:第二段换了拓扑,行驶距离随之变长。量化它最
```

改后串：

```tex
第二段换了拓扑,行驶距离随之变长。量化它最
```

---

**记录 6**（修改 4）— 原文串：

```tex
会把这处拼接计入——它正是那里两处逆序之一的成因,而另一处发生在哑铃内部,才是真正需要解释
的那一处。
```

改后串：

```tex
会把这处拼接计入——它正是那里两处逆序之一的成因,而另一处发生在哑铃内部,才是真正需要解释
的那一处。本族在全篇中的用处是让闭环效应能够对照争用结构来读:争用强度是本文唯一直接刻画
拥堵的量,本族沿它取到最宽的跨度,故第~\ref{sec:whichcongestion}~小节对闭环适用范围的判断
主要建立在这一族上。
```

---

**记录 7**（修改 7）— 原文串：

```tex
\emph{C 族:柔性 $F$}。在同一布局上取 $F\in\{0.3,\,0.6,\,1.0\}$,即每道工序的可选机械臂
比例,其中 $0.6$ 同样是那个共用基点。柔性决定改派算子有多少可动的余地,也决定拥堵信息能在
```

改后串：

```tex
\emph{C 族:柔性 $F$}。在同一布局上取目标值 $F\in\{0.3,\,0.6,\,1.0\}$,即每道工序的可选机械
臂比例,其中 $0.6$ 同样是那个共用基点;$|\Omega_{ji}|$ 由随机化取整使其期望等于 $F\cdot N_M$,
故基点一档的实测值为 $0.61$(表~\ref{tab:instances} 列的是实测值,另两档目标值与实测值相同)。
柔性决定改派算子有多少可动的余地,也决定拥堵信息能在
```

---

**记录 8**（修改 6）— 原文串：

```tex
第~\ref{sec:whichcongestion}~小节读这两族时必须把基点那一格算进去,漏掉它会让扫描看上去比
实际单调。
```

改后串：

```tex
第~\ref{sec:whichcongestion}~小节读这两族时必须把基点那一格算进去,漏掉它会让扫描看上去比
实际单调。

争用强度虽由算例结构决定,其读数仍取自实际运行,故口径在此一并钉死:本文取初始随机种群
($20$ 个个体,种群种子 $42$)上争用占比的均值,逐格值列于表~\ref{tab:instances}。不取优化后
的解,是因为那量的是解而不是算例——优化器本就会绕开拥堵走廊,收敛解上的争用趋近于零,反而
抹掉算例之间的差别。该量是第~\ref{sec:whichcongestion}~小节给算例排序的依据。
```

---

**记录 9**（修改 5）— 原文串：

```tex
后两项都是外推,但性质不同,需分开交代。$\bar{T}_t/\bar{T}_p$ 那一项是\emph{有意为之},
且正是本文问题设定的前提:运输若只占加工时间的百分之几,它就不是矛盾的主要方面,车辆争用
也就不值得建模——通行的 FJSP\_PT 算例族恰好落在那一侧,这是本文另立算例的理由之一,而不是
取值失当。异质度那一项则是一处需要举证的外推:$H$ 若是收益的使能因子,把它定在文献区间之上
就会\emph{抬高}本章的主效应,那样各档之间的差值就有一部分是取值造出来的。这不能靠解释打消,
只能测。第~\ref{sec:whichcongestion}~小节用一批横跨四个 $H$ 水平的独立数据做了这个检验,
结论是收益并不随 $H$ 上升,且 $H$ 取在公开中位附近的那一档与取 $0.3$ 的那一档给出的增益几乎
相同;故这处外推没有把主效应抬高。我们把该检验放在那里而不是此处,是因为它需要先有增益的
定义才能读。
```

改后串：

```tex
后两项都是外推,但性质不同。$\bar{T}_t/\bar{T}_p$ 那一项是\emph{有意为之},且正是本文问题
设定的前提:运输若只占加工时间的百分之几,车辆争用就不值得建模——通行的 FJSP\_PT 算例族恰好
落在那一侧,这是本文另立算例的理由,而非取值失当。异质度那一项则需要举证:$H$ 若是收益的使能
因子,把它定在文献区间之上就会\emph{抬高}本章的主效应,那样各档之间的差值就有一部分是取值
造出来的。这不能靠解释打消,只能测。第~\ref{sec:whichcongestion}~小节用一批横跨四个 $H$ 水平
的独立数据做了这个检验,结论是收益并不随 $H$ 上升。该检验的口径边界需一并声明:那批数据来自
\MatrixCells{} 格的独立批次,其基线口径与本章主批次不同,且采集于第~\ref{sec:speedup}~小节
两项降本\emph{之前};故它支持的是"$H$ 未抬高主效应"这一方向性判断,不构成主批次口径下的
敏感性分析。
```

---

---

**记录 10**（B02a 修改 1）— 原文串：

```tex
$\NPairs$ 个配对下的结果,每格同挂钟预算,全部运行均通过独立校验。以下三段分别读出析因的
两个主效应与端到端的合计。表中 $p$ 值已按这五个对比做 Holm 校正;此处校正是惰性的——五个
$p$ 值逐个与校正前相同,最大者仍为 $p\PProbeLoop$——因此下面各段的判定不依赖于是否校正。
```

改后串：

```tex
$\NPairs$ 个配对下的结果,每格同挂钟预算(口径见第~\ref{sec:protocol}~小节),全部运行均通过
独立校验。以下三段分别读出析因的两个主效应与端到端的合计。表中 $p$ 值已按这五个对比做
Holm 校正;此处校正是惰性的——最大的那个乘数为 $1$、校正前后精确相同($p\PProbeLoop$),其余
四个各乘 $5$ 至 $2$ 后仍低于 $10^{-4}$——因此下面各段的判定不依赖于是否校正。正文一律以
\emph{降幅}报伪中位数与置信区间,端点按幅度由小到大列;表~\ref{tab:main} 同一区间带负号、
按数值升序列,二者是同一个区间的两种写法。
```

依据：卡片 §5.2 必须存在 4（每个数字带口径或指回设置块）、验收测试 2（正文与表逐一致）；检查族 C4、C6。三条并入一处因同属引导段口径交代，拆开会把一段切成四截。Holm 五对比的乘数为 $5/4/3/2/1$，四个小 $p$ 校正后数值确实变了，仅仍在 $10^{-4}$ 显示阈以下——原句「逐个与校正前相同」对那四个不成立。符号约定：`\CILoopRule` = `[14.68%, 21.95%]`（正值幅度升序）与 `tab_main` 的 `$[-21.95,-14.68]$`（负值数值升序）为同一区间，原文「区间下端」在两处指相反端点。

---

**记录 11**（B02a 修改 2）— 原文串：

```tex
的评价次数。它搜得更狠却仍然输,原因是它在优化一个错误的目标,只有当决策已被冻结之后才发现
```

改后串：

```tex
的评价次数(逐配置的单次评价成本见第~\ref{sec:speedup}~小节)。它搜得更狠却仍然输,原因是它
在优化一个错误的目标,只有当决策已被冻结之后才发现
```

依据：卡片 §5.2 必须存在 4。「数倍于闭环的评价次数」是本块唯一无口径、无指回的定量主张，而 §5.7 已有逐配置的单次评价成本数据。

---

**记录 12**（B02a 修改 3）— 原文串：

```tex
可以正当主张;但它必须与下一段的机制贡献\emph{分开}报,不得合并成一个数字。
```

改后串：

```tex
可以正当主张;但它必须与下一段的机制贡献\emph{分开}报,不得合并成一个数字。这一级单独兑现
了本文关于闭环的那条主张:把车辆争用移入搜索目标是值得的,且这个判断在两种派车方式下都
成立,不依赖本文的派遣机制。
```

依据：卡片 §5.2 必须存在 2（每小节四步的第四步）、验收测试 1。改前三段末句分别是纪律声明、前向指针、自我指涉，无一收口。「两种派车方式下都成立」由 `tab_main` 第 30、31 行（$-18.28$ 与 $-18.00$，两者 CI 均不跨零）支撑。

---

**记录 13**（B02a 修改 4）— 原文串：

```tex
不到一个百分点;逐格则在按族校正后\emph{无一格}显著(校正前为 \CellsSigProbe{} 格)。因此
```

改后串：

```tex
不到一个百分点;逐格则在按族校正后\emph{无一格}显著(校正前为 \CellsSigProbe{} 格),且十格中
有两格的符号为正——车臂比 $1.0$ 与 $2.0$ 两档上 B2 反而慢于 B1(表~\ref{tab:main})。因此
```

依据：卡片 §5.2 必须存在 3（不显著与负结果如实报告，且**就地**限定）。`tab_main` 第 14 行 B1 $47.50$ → B2 $49.80$、第 15 行 $42.20$ → $44.20$。§5.4 虽提「被大车队两格拖成」，但报出 $-\GainProbeLoop$ 合计值的地方须自己说明。注意反转格是 $1.0$ 与 $2.0$，**不是**「最大的两档」——$1.5$（即 `A funnel`）未反转。

---

**记录 14**（B02a 修改 5）— 原文串：

```tex
这才是下一小节要钉死的判断。
```

改后串：

```tex
这才是下一小节要钉死的判断。两段合起来兑现的是本文关于派遣机制的那条主张:查预约表选车
确有独立于闭环的价值,开环与闭环下各自显著;但那是一份小得多的价值,不足以单独承担本文的
收益。
```

依据：同记录 12。措辞刻意不预告 §5.4 的不可替代性论证（那问的是闭环能否被派遣替代，方向相反），只收口到「有独立价值但量级小得多」这一本段已证之事。

---

**记录 15**（B02a 修改 6＋7，一次原子替换）— 原文串：

```tex
($p\PEndToEnd$,$\WinEndToEnd$ 胜 $\LoseEndToEnd$ 负)。这是一个可以正当主张的端到端数字,
前提是它被拆开报告过——上面两段就是那个拆解。
```

改后串：

```tex
($p\PEndToEnd$,$\NPairs$ 个配对中 $\WinEndToEnd$ 胜 $\LoseEndToEnd$ 负 $1$ 平)。这是一个
可以正当主张的端到端数字,前提是它被拆开报告过——上面两段就是那个拆解。它兑现的不是单独
某条主张,而是两条合起来的总量:从文献的两阶段开环结构走到本文的闭环加预约表派遣,完工时间
降低约五分之一。
```

依据：修改 6（验收测试 2）与修改 7（验收测试 1）落在相邻两句，合为一次替换。`tab_main` 第 35 行为 `94/5/1`；改前写「$94$ 胜 $5$ 负」，$94+5=99\neq\NPairs$，读者会以为漏了一对。改用 `\NPairs` 打头使加和当场可验。

---

### B03 脚本改动（回滚方式不同，另表）

> 这些改的是 `fig/*.py` 与 `tab/*.py`，不是 `{{OUTPUT}}`，**不适用字符串反向替换**。它们都在 git 跟踪下，回滚用 `git checkout -- <路径>`，随后重跑该脚本重出产物。

| 文件 | 改了什么 | 触发的重出 |
|---|---|---|
| `fig/_style.py` | 新增 `sem()`（均值标准误）与 `span()`（全距）两个共享助手；新增字号常量 `FS_MIN`/`FS_FOOT`/`FS_LEG`/`FS_ANNOT`/`FS_TICK_SM`＝7.0 pt，作为终版式下的可读下限 | 全部图 |
| `fig/fig_prediction3.py` | `cell_effect()` 改为同时返回逐种子相对增益的 SEM；每格加误差棒（画在标记之下，不夺实心/空心的可读性）；页脚补误差棒说明；字号常量化 | `fig_prediction3.pdf` |
| `fig/fig_convergence.py` | `step_envelope()` 改为返回 `(t, 均值, 最小, 最大)`；新增 `band()` 画种子间全距（α=0.13，不与「乐观幅度」箭头争视觉）；标题标出带的含义；图例左移让开落点标记；字号常量化 | `fig_convergence_closedloop.pdf` |
| `tab/gen_tables_ladder.py` | `tab_main`/`tab_bytag`/`tab_prune` 加 `$C_{\max}$(时间单位)` 跨列表头；`tab_instances` 的复合下界列补单位；`tab_prune` 两个比值列在表头写明分母方向；**删去 `tab_main` 的 B2 数值加粗与 `tab_prune` 的开列数值加粗**，改为只加粗表头（R3） | 四张 `\input` 表 |
| `fig/fig_case_chain.py` | 字号常量化；新增 `set_ylim` 留白——7 pt 下四条图例会压住 B2 柱内的数字标签 | `fig_case_chain.pdf` |
| `fig/fig_case_gantt.py`、`fig_protocol.py`、`fig_protocols.py`、`fig_theta.py` | 页脚、图例、刻度、独立标注的字号统一到 7 pt 下限 | 各自 PDF |
| `tab/gen_tables_ladder.py`（X10 轮） | 新增 `tab_external()` 生成 `tab_external.tex`、`macro_external()` 打印十个 `\Pub*` 宏；新增 `pub_name`/`n_ops`/`num`/`median`/`external_rows` 助手。**两处共享改动**：`read()` 由 `utf-8` 改 `utf-8-sig`（`gap_ideal.csv` 带 BOM，否则首列名成 `\ufeffinstance`）；`write()` 增 `src` 形参以按表写溯源注 | `tab_external.tex` |

**验证 1**（R3 轮）：`gen_tables_ladder.py` 重跑后打印的 55 个导言区宏与改动前**逐字相同**，确认只动了表头与加粗、未触任何数字。

**验证 2**（X10 轮，两处共享改动的隔离证明）：`read()`/`write()` 动过之后，四张阶梯表的 MD5 与改动前**完全一致** —— `tab_instances` `8707D8E1…`、`tab_main` `1CAB66AA…`、`tab_bytag` `B06DF28A…`、`tab_prune` `EBE9D25B…`。过程中曾两度改坏又复原：`"%%…" % src` 把 `%%` 折成单个 `%`（须写 `%%%%`），以及默认溯源串漏掉「，见脚本首部」；两次都由哈希比对当场发现。

**验证 3**（X10 轮，数据可复算）：20 个「工序」数由 `len(proc_time)` 从 `clbs/database/json/hf/*.json` 现算，与原手打表**无一不符**；十个 `\Pub*` 宏与导言区手打值逐位相同，其中 `\PubLBSlack`/`\PubLBTotal` 的分母（参照值／本方法最好解，均只取 16 个可证最优算例）系反推得出而仍精确命中，可视为对该二宏口径的独立确认。

---

## 三、越界发现台账

> S3 各轮中发现、但按 R5 不属本块卡片管辖的问题。**只记录不执行**，到归属块开工时兑现。

| 编号 | 发现于 | 归属块 | 内容 | 状态 |
|---|---|---|---|---|
| X1–X4 | B01a | 见对话轮次 | （B01a 轮次内已随修改一并处置或转入下列条目） | — |
| X5 | B01a | B02c2（§5.5） | 核实 §5.5 是否已载「两档网格争用强度最低而完工时间最高」，以决定 B01a 修改 8 是否执行 | **已了结**：§5.5 无此读数，仅用行驶距离（`\LegGrid` $18.0$ 对 `\LegDumbbell`）解释 `low`/`mid` 逆序。该句在 §5.1.1 是「距离变长」的唯一实测佐证，删除即信息损失且削弱「容量＋拓扑两段拼接」论证。**修改 8 撤回** |
| X6 | B02a | B03（`F-FIG`） | `fig:convergence` 的批次归属（只取三算例三种子、不承载统计主张）只写在 `%` 注释里，图注正文没有。**与 `paper-score.md` 第 10 维的举证有出入**——该处把「注中自报说明性」记作图注纪律的正面证据，实际读者看不见。基线不改，S5 复评按 §8.5 第 3 条处理 | **已了结**（记录 21）；S5 仍须处理基线举证 |
| X7 | B02a | B03（`F-FIG`） | `tab:main` 的 B2 列整列加粗，而十格中两格 B2 并非最优（车臂比 $1.0$/$2.0$）。表注未说明加粗表示「本文方法」而非「最优值」；`tab:arms` 写的是 `\textbf{B2}(本文)`，本表缺此限定 | **已了结**：数值加粗已从生成器删除（R3），全文统一为「表头加粗＝本文配置」，记录 17 在注中写明 |
| X9 | B03 | 作者侧 | **图内文字中英混用**：`fig:motivating` 为中文，其余八张全英文，中文稿不得如此。`fig/fig_framework_CN.py` 已存在中文版但 `.tex` 引用的是英文版。作者本轮明确暂缓 | 暂缓 |
| X10 | B03 | 作者侧 | `tab:external` 是手打的 20 行数字，却在注里标注数据源 CSV，读者会误以为是生成的；重跑退化对标后不会自动更新 | **已了结**（记录 23）：新增 `tab_external()`＋`macro_external()` 于 `gen_tables_ladder.py`（**非** `gen_tables.py`，后者仍用已废弃的 twostage/opendispatch 档位词汇）。生成表与原手打表 140 个数**逐格相同**，十个 `\Pub*` 宏与导言区**逐位相同** |
| X11 | B03 | B03 续轮 | `fig:theta` 图注不提误差棒、不提检验方法、不自报批次 | **已了结**（记录 22）。**原判过重须更正**：图\emph{自身页脚}早已印有 `runs per point ... mean ± s.e.`，故 Q2/Q4 本可答；图上无显著性标记，Q5 应判 N/A 而非 ✗。真正缺的只有批次归属一项 |
| X12 | B03 | 作者侧 | **图内文字溢出**：`fig_case_gantt.py:102` 的条内工序标签为 5.2 pt 且已溢出条形，`fig_framework.py` 的节点号与边标为 6.4 pt。这三处是图形\emph{内部}的文字，提字号会直接撑破条形与方框，属版面重排而非调字号，故本轮未动 | **待兑现**（作者侧，需重排版面，非调字号可解） |
| X8 | B02a | B01b | 记录 10 加的是「口径见第~\ref{sec:protocol}~小节」。B01b 已把预算三元组写进 §5.1.2（记录 53），此处可考虑改为绝对值；指回本身无论如何保留。**⚠ 本行原记的「$90$ 秒 / 代数 $200$ / 早停 $30$」是错的**：$90$ 秒属阶梯批次（`baseline_ladder.py`，代数上限 $2000$、早停 $400$），而 $200$／$30$ 属公开库批次（`experiments_database/meta.json:9-10`，`budget: "p100g200s30"`，另有 pop $=100$、种子 $1$–$10$）。两者被混成了一个从未跑过的配置。正确值见记录 53 | **已兑现**（记录 53） |
| X13 | B04 | B02d（§5.6.2）、B10（§6.2） | **本轮唯一挂起的修改所依赖的前提**。§4.4.1 的架构级全称量词「在\emph{任何}以完整无冲突解码作为适应度的框架中……」（C1 形式 2 过声称，实测支撑仅 2 算例、1 套实现）在另两处同源出现：§6.2 洞见 1「任何以完整无冲突解码作为适应度的框架都具有这一性质」，以及 §5.6.2（台账原记 2225–2228）。C5 要求同一命题各处强度一致，故**三处须同强度落地**；只改 §4 会造成方法章已收窄、结论章仍作全称断言的内部矛盾，反而更糟。诊断中的「修改 5」已备妥改写文本，作者已批准但按建议暂缓 | **已了结**——见记录 84：B10 轮三处同强度落地（决定 Z·A），全称量词改条件句，§5.6.2 的「严格有害」同时降级，P07 一并了结 |
| X14 | B04f | B02d | §4.6.2 末句「于是邻域最多包含 $L+2=7$ 个候选，且每一个都是由下层指名的」与 §5.6.2（2294）「至多 $L+2=7$ 个候选，且每一个都由下层指名」近乎逐字重复 | **已了结**（记录 99）：保留 §4.6.2 的机制定义，§5.6.2 改为指回 `\ref{sec:ls}`，数字不再重述 |
| X15 | B04 | B01b 完成后 | §4 内散落裸超参：$10\%$、阈值 $0.5$、$L=5$、最多三轮、五个精英与二元锦标赛。卡片 §5.4 禁止超参罗列，应移设置块或附录。**但落点（§5.1.3）本身受 P01 阻断尚未定稿**，此时搬运等于搬进一个还会变的位置，故本轮不动。**已解封**：P01 于记录 53 了结，落点定稿。搬运时须核对语义——这些数在 `clbs/algorithm/ga.py:31–35` 全部对得上（`elite=5`、`tournament=2`、`top_ls=0.10`、`L_ls=5`、`ls_rounds=3`），但**「$10\%$」是 `top_ls`（每代对排名前 10% 个体做局部搜索），不是变异率**；变异率为 `pm=0.2`（`ga.py:30`），若写反即为事实错误 | **已了结**（记录 97）：§5.1.3 加 `sec:impl` 并一次列全七项超参，§4 侧删纯配置数、保留 `0.5` 与 `L=5` 两个机制定义所需的数。台账这条警示确实拦下了一次事实错误——写入时按 `top_ls` 表述，未写成变异率 |
| X16 | B04a | B04a 续轮 / B03 复评 | 卡片 §5.4 必须存在 1（输入→机制→输出数据流）**最完整的一句在 `fig:framework` 图注里**（「中间的事件驱动解码器把上层的一个候选解翻译成一串运输任务」），正文 1206–1209 只说了「分配到两层」。图注承担了正文该承担的功能。**已了结**：见记录 59，正文补齐输入、解码器与输出；图注原句保留（§5.3 要求图注自足）| **已了结** |
| X17 | B05b | B09（§6.3(1)） | **决定 K·A 的另一半**。记录 38 已把模型差异写进 §3.5，§6.3(1) 现 `2631–2638` 约 330 字与之重复，应压成一句指回 §3.5，只留「因此两边算例不可直接比较」的可比性论证。这笔删减计入 B09 的 5% 字数体检（B09 现 2074 字，删后约 1750 字，占比由 5.4% 降至约 4.5%，硬约束由此可解）| **已了结，零改动**（记录 98）：B09 记录 77 重写局限 (1) 时已带掉，现文只剩指回 §3.5 的一句，正是本条要求的形态 |
| X18 | B05a | B10（§6.4） | 记录 33 只补了 A7→§6.4 的单向回指。反向一句（§6.4「动态运行」指回 A7 所划的建模范围）留给 B10，与 CF-4（未来工作「动态运行」无对应局限）同轮处理 | **已了结**（提前）——见记录 82：B09 新增局限 (6) 引 A7 并指向「动态运行」，「动态运行」首句反指局限 (6)，双向闭合，无需留待 B10 |
| X19 | B05b | B04d | 定价 $\theta$ 首次出现处无定义。**已就地了结**——见记录 36，占 B04d 余额 1 条 | **已了结** |
| X20 | B04b | B04b 续轮 ＋ B04g（§4.8 `1642`） | **本轮唯一挂起项，须作者决定（C5 复杂度条）**。§4.2 `1319` 声称单次路由调用为 $O(\lvert E\rvert W\log\lvert V\rvert)$，原文未给计数依据。补依据时发现该式与实现不符：`earliest_entry` 是「候选进入时刻 × 重叠计数」两重扫描（`network.py:435/444/422`，候选集 $O(W)$、每个候选再 $O(W)$ 判重叠），单次查询最坏 $O(W^2)$ 而非 $O(W)$。当 $W>\log\lvert V\rvert$ 时 $O(\lvert E\rvert W^2)$ 严格超出所声称的界，故现声明不是有效上界。§4.8 `1642` 的复合界 $O((N+n)N_A\lvert E\rvert W\log\lvert V\rvert)$ 复用同一表达式，**两处必须同轮改**。**已了结**：作者选定选项 A，见记录 49 | **已了结** |
| X21 | B01b | B02d（§5.6.2 `2184`） | `2184` 写「\MatrixCells 个算例格 $\times$ $10$ 种子、**同挂钟预算**」。该句本身不假（该批次内部各臂确实共享挂钟），但读者会理解成与主批次同为 $90$ 秒，实际它是 `--budget auto` **按算例标定**的 $30.21$–$72.93$ 秒（`clbs/experiments/meta.json:48–65`、`instances.csv` 的 `budget_sec` 列），且**种子集与主批次不同**（$\{3,7,11,19,23,31,42,47,53,2024\}$ 对 $\{1,7,8,13,42,99,123,777,2024,31415\}$）。按决定 P·A，具体数值落在此处而非 §5.1。另：该行的裸 `10` 宜改 `\NSeeds` 或保持裸数并注明是另一组种子 | **已了结**（记录 100）：删「同挂钟预算」四字，另起一句明示挂钟按算例标定于 $30.2$–$72.9$ 秒、种子集另取十个；裸 `10` 保留，已由新句注明它是另一组种子 |
| X22 | B01b | B03（`fig:theta`／`fig:convergence` 图注） | 两图的挂钟预算未在图注声明：`fig:theta` 为 $48.9$ s／$60.7$ s（`clbs/output/theta_wall.log`），`fig:convergence` 为 $46.3$ s（`clbs/output/convergence.log`）。按决定 P·A 应在首次引用处标注。`fig:theta` 图注已有「批次归属须一并声明」一句，补预算是同一处的自然延伸；`fig:convergence` 图注已声明三个种子（与 `ladder_convergence.csv` 的 $\{7,42,2024\}$ 相符），只缺预算 | **已了结**（记录 101）：两图图注各补一句挂钟预算，并同时点明与主批次 $90$ 秒不同 |
| X23 | B01b | 作者侧（需重跑或补列） | **决定 Q·A 的挂账项**。§5.1.2「每次运行都报告它是耗尽预算还是提前收敛」这句，在主批次上**无数据支撑**：`clbs/output/baseline_ladder.csv` 的表头只有 `case,contention,arm,seed,makespan`，既无 `budget` 列也无 `stopped_by` 列。该声明在矩阵批次（`experiments/runs.csv` 有 `stopped_by`，取值 `budget`）与公开库批次（`stopped_by` 恒 `stall`，§5.9 `2555–2556` 已据此论证）上都成立。$90$ 秒本身由日志确证，故本轮照写；但若审稿人索要停机原因分布，主批次需补跑或从 `baseline_ladder_10seeds.log` 回填 | **已了结**（记录 108–109）：回填成功，且**回填结果推翻了原句**。`baseline_ladder_10seeds.log` 本身只有每格累计耗时，但同预算、同超参、算例与种子均为主批次子集的**成本诊断批次**留下了逐次 `runtime_sec`（`ladder_cost.csv`）与逐代时刻（`ladder_convergence.csv`），足以逐次判定停机原因。结论：早停阈值 $400$ **每一次都在 B0 上触发**（$421$–$688$ 代、$11.0$–$26.2$ s，即预算的 $12\%$–$29\%$），B1／B2 **每一次都跑满**（$90.0$–$91.2$ s），代数上限 $2000$ 从未触发。原句「两项均放宽至不构成约束，以确保预算被真正用尽」为**假**，已改写 |
| X24 | B07 | B10（§6.1） | **CF-1 的另一半**。贡献由 5 条改为 4 条（记录 61）后，结论 §6.1 现复述的 3 条是按**旧的 5 条**合并重组来的（把引言原 1、4、5 并成一条），须按新基准重新对齐为 4 条同构。这也是 B10 开工的前置 | **已了结**——见记录 83：补 G1 后结论为 4 条，与引言、卡片逐条同构 |
| X31 | B10 | B11（摘要） | 记录 86 把「无正交互」全文改为「未检出正交互」，摘要若含同类措辞须同步（现已核：摘要无该措辞，但贡献覆盖仍须按四条新基准重核，与 X25 合并处理） | **已了结**——见记录 95。B10 当轮的核查结论「摘要无该措辞」**是错的**：摘要用的是「呈近加性**而非**正交互」，字符串不同故未被 `无正交互` 的检索命中，改前全文仅剩的旧措辞正在摘要。现「而非正交互」全文 0 处 |
| X32 | B10 | S4 | `$2\times2$`（`720`／`2609`，无空格）与 `$2\times 2$`（`1892`／`1900`／`1933`，有空格）两种写法并存。渲染相同，属排版一致性，留 S4 | **已了结**（记录 105）：三处无空格版统一为带空格版，全文 6 处同形 |
| X25 | B07 | B11（摘要） | 摘要暗含贡献 4「角色分工」但不含贡献 1、原贡献 5，条数与含义须随本轮新基准重核。**同时并入卡片 K3 的跨块口径差**：摘要 `530` 用 `\ContentionHi`（全族 $33.5\%$）、§1.4 `737` 用 `\ContentionAHi`（布局族 $31.9\%$），两处各自成立但需在摘要处也自报口径 | **已了结**——见记录 93。口径差按**决定 BB·A** 处理：摘要保留 `\ContentionHi` 并自报「后者为全族最大值」。贡献覆盖已按四条新基准重核：摘要段 2 覆盖 G2、G3，段 3 覆盖 G1（相对开环阶梯的完工时间下降），段 4 覆盖 G4（角色分工），四条齐备 |
| X26 | B07 | S4（术语统一） | 机制名逐字一致的**全文**统一。本轮只改了贡献列表内的那一处（记录 64，属卡片同构的硬要求）；全文另有 13 处「预约表感知派遣」简称漂移（`535/539/568/716/783/795/1233/1993/2228/2394/2608/2672/2742`），与全称「预约表感知的车辆派遣」并存。按协议 §7「S3 各块只登记术语漂移的出现位置，不在单块内改名」，留 S4 一次性处理 | **已了结**（记录 104）：提前执行，正文 8 处简称统一为全称，简称在正文归零；导言区三处注释保留简称，不影响渲染 |
| X27 | B07 | S4 | 删 §1.6 后 `sec:Chapter_VI`（结论章）无人引用。**`check_tex` 未报此项**——其「定义却无人引用的 label」检查似乎放过 `sec:Chapter_*` 前缀，8 条告警数删前删后未变，故不能依赖该脚本发现同类孤儿。良性，不影响编译；S4 的自检脚本宜补上章级 label 的引用检查 | **已了结**（记录 105）：删除 `sec:Chapter_VI`——§6 的四个子节 label 各有引用，章级锚点冗余。「S4 补章级 label 检查」这一条建议仍有效 |
| X28 | B08 | B10（§6.1 同轮）或 S4 | 引言贡献 1 把 `sun2023integrated` 写成「具备柔性但用**常数运输时间矩阵**排产的模型」，而 `tab:related` 同一篇标的是「车辆争用：无冲突」，§2.3 `872–873` 也写它用时间窗 Dijkstra 消解 AGV 冲突。**不是硬矛盾**（Sun 的排产阶段确实用矩阵，冲突消解在第二阶段），但两处并列阅读会让审稿人以为其中一处错了。修法：引言处改为「排产阶段用常数矩阵」。**不在本块改**——B07 已完成，且改的是引言不是相关工作 | **已了结**（记录 102）：贡献 1 改为「排产阶段用常数运输时间矩阵」，加两字即消歧；§1.2 `682`、§2.1 `827` 说的是「这一支」整体而非具名文献，未动 |
| X29 | B08 | —— | 原拟：§2.3 的实验协议要求属 F-SETUP，须搬去 B01a。**核查后取消**：该论证在引言 `747`、§4.8 `1632–1637`、§5.1.2 `1784–1794` 已有三份，§2.3 这份是第四份，记录 73 直接删除，无内容需要搬运 | **不成立** |
| X30 | B08 | —— | 原拟：`tab:related` 手打表未标注，转 B03。**已就地了结**——见记录 75，占 B08 余额 1 条，无需转块 | **已了结** |
| X33 | B11 | S4（与 X26 同批） | 「低开销」这一机制名的**跨块措辞漂移**。记录 92 只保证摘要与题目逐字一致；全文另有三种说法指同一对象：引言贡献 3 用「使它\emph{低开销}的两项优化」、结论 (3) 用「使它付得起的降本」、§4.8 小标题用「低开销」。卡片 §5.11 验收测试 4 与 §5.12 都要求逐字一致，但按协议 §7「S3 各块只登记术语漂移，不在单块内改名」，留 S4 | **已了结**（记录 103）：与 X26 同批提前执行。「低开销」四处、「路由内嵌评价」三处统一，方法小标题与结论均已对齐；行文中作动作名词用的「降本」约 25 处未动（「降本之前／之后」不能替换成「低开销之前」） |
| X34 | B11 | B12（`F-TITLE`） | 全标题「面向柔性作业车间调度与 AGV 无冲突路由一体化的低开销路由内嵌评价闭环双层框架」33 字，超卡片 §5.12 的中文 ≤30 字；名词堆叠 4 层（低开销／路由内嵌评价／闭环／双层框架），超 ≤3 层。B11 已把三个机制名全部写进摘要，B12 删减标题时**不得删到摘要没有对应的程度**，两块须联动核对 | **已了结**（记录 106–107）：取候选 C，题目 27 字 / 堆叠 3 层，双约束同时满足；短标题与关键词同步落地 |
| X35 | B01b（X23 轮） | B03（`fig:protocol` ＋ `fig/fig_protocol.py`） | **X23 的直接连带，等级高于一般排版项——图与刚改过的 §5.1.2 现在互相矛盾**。`fig:protocol` 的灰线是「等挂钟双曲线」，脚本 `fig_protocol.py:42` 取 `budget = mean(全部 36 行 runtime_sec)` $=53.98$ s，图例遂写 `equal wall clock (54 s)`——**没有任何一次运行是 54 秒**，它是 B0 的 $17.6$ s 与 B1／B2 的 $90$ s 两个不同区间被平均出来的产物。图注称「各配置只能沿它移动」，`\Description` 称四档 lying along an equal wall-clock hyperbola，而实测 B0／B0$^+$ 落在 $0.20\times$ 该线上（对数轴上偏离约 $0.43$ 个十倍程，肉眼可见）。脚本自己的注释写着 "Drawn from the measured budget rather than assumed, so a mismatch would be visible"——错配确实存在，却被平均抹平了。**另有第二个独立缺陷**：脚本对三个算例先取 `mean(ms_per_eval)` 与 `mean(decodes)` 再画点，而两者跨算例强负相关，乘积不等于均值挂钟——B2 的绘制点隐含 $120.9$ s，同样无任何一次运行对应。修法须作者定：(甲) 灰线改画在**名义预算 $90$ s** 并如实标注，让 B0 显式落在线下方，图注改说「B0 提前收敛故停在线下，其余各档贴线」——与记录 108 的新正文同调，且强化「基线未被掐断」这一点；(乙) 按算例分面或画三条线；(丙) 只改图注不动图（最弱，仍留 54 s 这个不存在的数）。**推荐 (甲)** | **已了结**（记录 110–111）：作者取 (甲)，同轮落地 |

---

## 四、施工顺序摘要

```
序1  B01a → B01b → B01c        F-SETUP     ← P01 未解决则 B01b 空转
序2  B02a → B02b → B02c1 → B02c2 → B02c3 → B02d → B02e → B02f → B02g   F-RESULT
序3  B03                        F-FIG
序4  B04a → B04b → B04c → B04d → B04e → B04f → B04g   F-METHOD（B04b/c/e 叠加 C5）
序5  B05a → B05b → B05c → B05d  F-MODEL（B05c 叠加 C5）
序6  B06                        F-PROOF  = MERGED，不单独排轮
序7  B07                        F-INTRO     ← P02 未裁决则不得开工
序8  B08                        F-RELATED   ← 已完成；17 处 [ref_check] 交 B13
序9  B09                        F-LIMIT     ← 已完成
序10 B10                        F-CONCL     ← 已完成
序11 B11                        F-ABS       ← 已完成；697 字，四段齐备
序12 B12                        F-TITLE     ← 已完成；题目 27 字 / 3 层
序13 B13                        F-REF       ← 作者插标记后方可开工；B08 已交来 17 处 ref_check
序14 S4 → 序15 S9A → 序16 S5
```

共 **31 个可施工块**（B01 组 3 ＋ B02 组 9 ＋ B03 ＋ B04 组 7 ＋ B05 组 4 ＋ B07–B13 共 7；B06 为 MERGED 不计，S4/S9A/S5 为全文级）。

---

## 五、C5 证明义务全文扫描

B06 虽为 `MERGED`，但 C5 证明义务的措辞是「文中**每处**」，范围是全文而非宿主块。此前四轮（B04b/c/e、B05c）只覆盖四条形式化资产周边约 27 处，故补跑一次全文扫描。**S4 全文校验的 13 项检查里没有这一项**（第 9 项只管符号与假设），所以它不能留到 S4。

检索词：`可证|等价|保证|最优|可采纳|收敛|上界|下界`。全文命中 **134 处**，其中 4 处为导言区宏定义的行尾注释（`386/387/403/404`），不渲染进正文、不计入判定。**正文 130 处**。

| 判定 | 处数 | 去向 |
|---|---|---|
| OK | 124 | — |
| OK · 违反 C5 §709 措辞一致 | 5 | 记录 51（决定 N·A，已落地） |
| OVERCLAIM | 1 | 记录 50（决定 M·A，已落地） |
| PROOF_NEEDED | 0 | 上一轮两处已补证，见记录 44、45 |
| RHETORIC（该删的形容词） | 0 | — |

**分章分布**：摘要与前言区 11（含 4 处宏注释）、§1 引言 12、§2 相关工作 8、§3 模型 5、§4 框架 22、§5 实验 50、§6–§7 结论 26。§5 与 §6–§7 合计 76 处是此前从未逐处扫过的部分，也是本轮唯一 OVERCLAIM 的所在。

### 判定为 OK 的典范用法（备查，供 S0B/S5 评分与审稿答复引用）

这批不是「勉强通过」，是主动自我设限，凡涉及同类质疑可直接指过去：

| 位置 | 原文要点 | 为何是典范 |
|---|---|---|
| `1207`、`2686–2687` | 复合下界「是松的……作用是数量级核查与回归防护，不是最优性证书」 | 两处口径一致，主动拒绝把下界当解质量证书 |
| `1672–1673`、`2701–2702` | 「交换乘子或价格的双层方案有时可被证明收敛到不动点；本框架不能，我们也不作此主张」 | 两处口径一致，主动放弃收敛性主张 |
| `2150`、`2157`、`2160`、`2400`、`2403` | 记录了一条**被撤回**的主张：原稿写过「$N_A/N_M\ge 1$ 即为适用上界」，补回中间基点后叙事不成立；并点名「把前者当作后者的证据，正是我们在前一版稿件里犯过的错」 | C1 形式 2 的反向示范 |
| `2521` | 「可证最优值是硬地板，若我们『赢』了它，那不是结果好，而是转换或口径有错」 | 外部对标的正确态度 |
| `2593–2594` | 「并\emph{证明}它们不改变输出（命题 3），又以逐位等价性自检加以实证」 | **C5 头号禁止项（把工程自检当证明）的反面**：证明与自检分列 |
| `1816`、`1857` | 每次运行报告是耗尽预算还是提前收敛；未过校验或低于下界的运行单独报告、绝不并入均值 | 协议层面的自证 |
| `2566–2568`、`2690–2692` | 公开算例上的下界分解「不能直接搬回自建算例（那里存在争用，而该下界完全忽略争用，只会更松）」 | 主动限制结论的可迁移范围 |

### 与 X13 的关系

记录 50 的 `2033–2034` 与 X13（§4.4.1／§5.6.2／§6.2 的架构级全称量词）**属同类缺陷但非同一命题**：X13 是「在\emph{任何}以完整无冲突解码作为适应度的框架中……」这一显式全称量词，`2033–2034` 是「存在一个由第一阶段无知框定的上界」这一存在性断言，推广对象也不同（前者是解码框架类，后者是两阶段文献路线）。故按决定 M·A **本轮独立落地**，不并入 X13 的三处同轮兑现，X13 范围维持三处不变。
