\documentclass[manuscript,screen,review]{acmart}

\usepackage{algorithm}
% Use algorithmicx + algpseudocode: provides \Require, \Ensure, \State, and \Comment
\usepackage{algorithmicx}
\usepackage{algpseudocode}
\usepackage{tikz}
% Load common tikz libraries used in figures (positioning provides the "of" syntax)
\usetikzlibrary{positioning,arrows.meta,calc,shapes,shadows}
% color support for \textcolor used inside algorithms
\usepackage{xcolor}
% Math packages required for \text and bold symbols inside math
\usepackage{amsmath}
\usepackage{bm}
\usepackage{multirow}
% Enable list customization (leftmargin, itemsep, etc.) used in the document
\usepackage{enumitem}
% Ensure header height is sufficient for fancyhdr
\setlength{\headheight}{20.48303pt}
% Visible placeholder for numbers still awaiting the controlled matrix run.
% Every occurrence must be gone before submission: grep for \TBD.
\newcommand{\TBD}[1]{\textcolor{red}{\textbf{[TBD:} #1\textbf{]}}}
% theorem environments used in the document
\usepackage{amsthm}
\newtheorem{assumption}{Assumption}
\newtheorem{problem}{Problem}
% Define remark environment (was missing and caused compilation errors)
\newtheorem{remark}{Remark}
% Source environment names stay prop / lem; printed labels are English.
\newtheorem{prop}{Proposition}
\newtheorem{lem}{Lemma}

\renewcommand{\figurename}{Figure}
\renewcommand{\tablename}{Table}
\floatname{algorithm}{Algorithm}
\renewcommand{\proofname}{Proof}

%% ===================== Rebuild order for figures and tables =====================
%% Sequential. Steps 1--2 run in clbs/; steps 3--4 at the repository root.
%%
%%  1. Main result (~6 h; every headline number below comes from this):
%%       py -u -m tools.baseline_ladder --budget 90 \
%%          --seeds 42,7,2024,13,1,99,123,777,31415,8      -> output/baseline_ladder.csv
%%  2. Three companion batches (after 1; no dependence among themselves):
%%       py -u -m tools.prune_ablation --budget 90         -> output/prune_ablation.csv
%%       py -u -m tools.ladder_diag  --budget 90           -> output/ladder_cost.csv
%%                                                            output/ladder_convergence.csv
%%                                                            output/case_study/*.json
%%     Do not run these in parallel with step 1: all use a wall-clock budget and
%%     would steal evaluations from each other, which breaks the equal-wall-clock
%%     protocol of Section 5.1.
%%  3. Tables and macros (seconds):
%%       py paper01/tab/gen_tables_ladder.py
%%     Writes tab/*.tex (Chinese paper) and tab/*_EN.tex (this file), and prints
%%     the macro block below for pasting. Tables and macros share one source.
%%  4. Figures (seconds; needs matplotlib):
%%       py paper01/fig/render_all.py         (skip a figure if its data is missing)
%%     This manuscript includes the English PDFs (no _CN suffix).
%%     CLBS_FIG_PNG=1 also writes png previews.
%%  5. Self-check (seconds; no TeX engine):
%%       py paper01/check_tex.py paper01/paper_EN.tex
%%     Undefined macros, hard-coded numbers, brace/environment pairing,
%%     dangling cross-refs, leftover TODOs.
%%
%% Data completeness: the five figures cited in this manuscript have data on disk
%% (motivating, framework, prediction3, protocol, case).
%% fig_convergence / fig_theta / fig_protocols / the unmerged case pair are no longer cited.

%% ===================== Single source for headline numbers =====================
%% Prose always cites the macros below; never hard-code a percentage.  Source:
%% clbs/output/baseline_ladder.csv from clbs/tools/baseline_ladder.py
%% (in clbs/: py -u -m tools.baseline_ladder --budget 90 --seeds ...).
%% Convention: 10 instances x 10 seeds = 100 pairs, equal wall-clock budget,
%% paired Wilcoxon signed-rank.  Regenerated by tab/gen_tables_ladder.py.
%%
%% Narrative that must not be reverted to a 4-seed reading:
%%  (1) open-loop dispatching is significant (4.59%, p<1e-4);
%%  (2) end-to-end 21.35% < independent product 22.6% --- near-additive, not a
%%      positive interaction;
%%  (3) B NA/NM 0.5, B0->B1 is negative (closed loop still helps); large-fleet
%%      B cells have B1->B2 positive.  Do not rewrite (3) as an applicability
%%      upper bound --- that claim was withdrawn after the 2026-08-16
%%      per-instance check (see the vehicle-to-arm-ratio block below).
%%
%% tab_bytag uses the same estimator as this block: mean of pairwise relative
%% gains.  The ten-instance mean then equals the pooled macros.  The gain
%% column cannot be recovered by dividing the two means on the same row.
\newcommand{\NCases}{10}
\newcommand{\NSeeds}{10}
\newcommand{\NPairs}{100}
% Closed loop itself (dispatching rule fixed; only whether delay enters search)
\newcommand{\GainLoopRule}{18.86\%}      % B0  -> B1 , rule-based
%% Every P-macro is Holm-adjusted over the five contrasts of tab:main.
%% The correction is inert on those five (largest p has multiplier 1).
%%
%% !! Each P-macro carries its own relation ("<" or "=").  Write $p\PLoopRule$
%% !! with no extra equals.  Wilcoxon reports 0.0000 below 1e-4; "p=0.0000"
%% !! would claim a zero p.  If the relation lived in the body, a re-run that
%% !! pushed a p under the floor would render "p=<10^{-4}".  pmac() in
%% !! gen_tables_ladder.py chooses the relation.
\newcommand{\PLoopRule}{<10^{-4}}
\newcommand{\GainLoopProbe}{17.75\%}     % B0+ -> B2 , reservation-table-aware
\newcommand{\PLoopProbe}{<10^{-4}}
% Reservation-table-aware dispatching (closed-loop factor fixed)
\newcommand{\GainProbeOpen}{4.59\%}      % B0  -> B0+, open-loop, now significant
\newcommand{\PProbeOpen}{<10^{-4}}
\newcommand{\GainProbeLoop}{2.45\%}      % B1  -> B2 , in the closed loop
\newcommand{\PProbeLoop}{=0.0054}
% End-to-end: literature structure -> this paper
\newcommand{\GainEndToEnd}{21.35\%}      % B0  -> B2
\newcommand{\PEndToEnd}{<10^{-4}}
\newcommand{\WinEndToEnd}{94}
\newcommand{\LoseEndToEnd}{5}
%% ---- Effect size: Hodges--Lehmann pseudomedian and 95% CI ----
%% The Gain macros are means of pairwise relative gains and carry no precision;
%% they are a different source from the Wilcoxon p (ranks).  The HL pair is the
%% estimator of that test; interval from stats.py hodges_lehmann_ci
%% (Hollander--Wolfe; n=100 uses the normal approximation).  Both macros are
%% absolute values (a reduction is positive); endpoints are ordered by
%% magnitude.  Only the two contrasts quoted in the prose are supplied.
\newcommand{\HLLoopRule}{18.28\%}
\newcommand{\CILoopRule}{$[14.68\%,\ 21.95\%]$}
\newcommand{\HLProbeLoop}{3.03\%}
\newcommand{\CIProbeLoop}{$[0.94\%,\ 5.19\%]$}
% End-to-end if the two mechanisms were independent.  Observed is slightly lower.
\newcommand{\GainIndepProduct}{22.6\%}
%% How many of the ten instances are significant on their own.  Section 5.5
%% reads signs per instance, i.e. ten tests in a ten-instance family, so the
%% family must be corrected.  Family = the ten instances of one effect (two
%% families, not twenty).  Holm controls FWER; BH agrees (see the generator).
%% Closed loop: 9/10 raw and 9/10 Holm (largest Holm p=0.0401).
%% Dispatching: 2/10 raw (Family C), 0/10 Holm.  Prose uses the Adj counts;
%% raw counts stay so Section 5.5 can state the difference.  Do not revive
%% "Family C is the only family that supports a conclusion" from the raw 2/10.
\newcommand{\CellsSigLoop}{9}
\newcommand{\CellsSigLoopAdj}{9}
\newcommand{\CellsSigProbe}{2}
\newcommand{\CellsSigProbeAdj}{0}
%% ---- Vehicle-to-arm-ratio family (funnel layout), full four-point B1->B2 ----
%% !! None of the four is significant (p = 0.4121 / 0.2011 / 0.1386 / 0.3313),
%% !! and the signs alternate:
%% !!   0.5 -4.67% | 1.0 +5.04% | 1.5 -4.63% (= A funnel) | 2.0 +5.71%
%% !! Do not narrate a fleet-size threshold.  The generator warns if signs
%% !! are not monotone.
%%
%% Per-instance macros carry their own sign (negative = lower makespan = better).
%% Quote them bare.  Pooled Gain macros are absolute; the prose carries direction.
%% \GainProbeStarve is the one exception: the body writes $-\GainProbeStarve$.
\newcommand{\GainLoopStarve}{-2.0\%}     % B NA/NM 0.5, B0->B1 (closed loop n.s.)
\newcommand{\GainProbeStarve}{4.7\%}     % B NA/NM 0.5, B1->B2 (gain direction; n.s.)
\newcommand{\GainProbeFleetBase}{-4.6\%} % A funnel (N_A/N_M=1.5), B1->B2 (n.s.)
\newcommand{\GainProbeFleetOne}{+5.0\%}  % B NA/NM 1.0, B1->B2 (n.s.)
\newcommand{\GainProbeFleetTwo}{+5.7\%}  % B NA/NM 2.0, B1->B2 (n.s.)
%% ---- Remaining per-instance readings quoted in Section 5.5 ----
%% Same cell_gain / cell_p as tab_bytag.  Each carries its own sign.
\newcommand{\GainLoopFunnel}{-21.3\%}    % A funnel , B0->B1
\newcommand{\GainLoopHigh}{-26.6\%}      % A high
\newcommand{\GainLoopMid}{-14.4\%}       % A mid
\newcommand{\GainLoopLow}{-10.3\%}       % A low
\newcommand{\GainLoopScatter}{-8.1\%}    % A scatter
\newcommand{\GainLoopFleetOne}{-15.6\%}  % B NA/NM 1.0
\newcommand{\GainLoopFleetTwo}{-37.7\%}  % B NA/NM 2.0
\newcommand{\GainLoopFlexLo}{-29.7\%}    % C F=0.3
\newcommand{\GainLoopFlexHi}{-22.9\%}    % C F=1.0
\newcommand{\GainProbeFlexLo}{-8.2\%}    % C F=0.3 , B1->B2 (largest of ten)
\newcommand{\GainProbeFlexHi}{-7.2\%}    % C F=1.0 , B1->B2
%% Four fleet p-values are uncorrected on purpose: even raw they miss the threshold.
\newcommand{\PProbeFleetHalfRaw}{=0.4121}
\newcommand{\PProbeFleetOneRaw}{=0.2011}
\newcommand{\PProbeFleetBaseRaw}{=0.1386}
\newcommand{\PProbeFleetTwoRaw}{=0.3313}
%% Flexibility is the only family whose verdict moves under correction.
\newcommand{\PProbeFlexLoRaw}{=0.0105}
\newcommand{\PProbeFlexLoHolm}{=0.1052}
\newcommand{\PProbeFlexLoBH}{=0.1052}
\newcommand{\PProbeFlexHiRaw}{=0.0321}
\newcommand{\PProbeFlexHiHolm}{=0.2885}
\newcommand{\PProbeFlexHiBH}{=0.1603}
%% Largest still-significant closed-loop Holm p: the family's verdict does not
%% hinge on whether a correction is applied.
\newcommand{\PLoopCellMax}{=0.0401}
% Contention-intensity range (min/max of the 10-instance contention column)
\newcommand{\ContentionLo}{6.6\%}
\newcommand{\ContentionHi}{33.5\%}
%% Layout family's own upper end, not the global max (that is C F=1.0, same
%% funnel layout at full flexibility).  Use this macro for the span of Family A
%% (Section 5.1.1 and the introduction).  Abstract generation range and the
%% common-executor optimistic gap stay \ContentionHi (global).
\newcommand{\ContentionAHi}{31.9\%}
%% Evidence that the layout family is spliced: LU-to-farthest-arm unimpeded
%% haul is identical on the three dumbbell cells and identical on the two grid
%% cells.  Section 5.1.1 is therefore "capacity sweep + topology contrast",
%% not "capacity only".  The generator asserts within-segment equality.
%% Do not use the composite LB of tab:instances as a proxy for distance.
\newcommand{\LegDumbbell}{10.0}          % funnel/high/mid, identical
\newcommand{\LegGrid}{18.0}              % low/scatter, identical

% Gap between the composite lower bound and the best known on this family.
%% Source: clbs/tools/lb_gap.py (2026-08-16).  Bound = simple_lower_bound
%% (max of job_chain, machine_load, lu_cut).  Best known = min makespan over
%% all arms x seeds in baseline_ladder.csv (validator-passed).
%% gap = (best known - bound)/best known; ten-instance range 27.3%--57.9%,
%% median 41.8%.  Tightest C F=1.0 (27.3%); loosest B NA/NM 0.5 (57.9%).
%% lu_cut wins 8 cells, job_chain 2; machine_load never.
\newcommand{\LBGapLo}{27\%}
\newcommand{\LBGapHi}{58\%}
\newcommand{\LBGapMed}{42\%}

%% ---- Two speed-ups (Sections 4.5 / 5.7) ----
%% Source: clbs/output/prune_ablation.csv (2026-08-08, 6 instances x 10 seeds
%% x two settings, 90 s).  Equivalence self-check: 6x6 chromosomes bit-identical.
%% Router calls 19448/36940 -> cut 47.4% (per-instance 42.2%--61.2%; A low
%% highest, B NA/NM 2.0 lowest).
\newcommand{\RouteCallCutLo}{42.2\%}     % largest fleet (B NA/NM 2.0)
\newcommand{\RouteCallCutHi}{61.2\%}     % least contended (A low)
%% \SpeedupProbe = off/on ms_per_eval (2.33x); same-budget eval count 2.23x.
%% \GainPrune = off->on paired relative gain (compute only).
%% CostMultAfter = B2/B1 ms_per_eval in ladder_cost (closed loop).
%% CostMultBefore = After x SpeedupProbe (relative to rule-based, pruning off).
%% ladder_cost.csv (3 instances x 3 seeds); B2 14.15 ms, B1 2.70 ms.
\newcommand{\SpeedupProbe}{2.33}
\newcommand{\RouteCallCut}{47.4\%}
\newcommand{\GainPrune}{5.60\%}
\newcommand{\PPrune}{<10^{-4}}
\newcommand{\WinPrune}{50}
\newcommand{\LosePrune}{0}
\newcommand{\EvalRatioPrune}{2.23}
\newcommand{\CostMultBefore}{12.2}
\newcommand{\CostMultAfter}{5.23}

%% ---- Absolute cost per evaluation (Section 4.8) ----
%% From clbs/tools/eval_cost.py (2026-08-16) on ladder_cost.csv and
%% theta_sweep.csv.  The first three are the ladder batch (3 x 3);
%% \MsPriced is a different batch (pricing sweep, before the speed-ups).
%% Compare it only to the same-batch theta=0 reading (15.05 ms), about 4.8x;
%% do not divide by \MsOpen --- absolute milliseconds are not comparable
%% across batches.
\newcommand{\MsOpen}{0.38}
\newcommand{\MsLoopRule}{2.70}
\newcommand{\MsLoopProbe}{14.2}
\newcommand{\MsPriced}{72.9}
\newcommand{\MsPricedRef}{15.05}         % theta=0 in the pricing batch

%% ---- Pre-speed-up matrix batch (Sections 5.5, 5.6) ----
%% clbs/output/matrix/p3/summary.json and matrix/lowmid/summary.json:
%% 16 instances x 10 seeds x equal wall-clock = 1280 runs, invalid empty.
%% Run 08-03/08-05, before pruning and winner-path reuse (in decoder.py only
%% from 08-07).  That dating is what lets Section 5.6 join this batch to the
%% ladder.  Checked against mechanism_gain / integration_gain in summary.json.
\newcommand{\MatrixCells}{16}
\newcommand{\MatrixRuns}{1280}
\newcommand{\GainIntegMatrix}{5.52\%}    % two-stage -> closed loop, 16-instance mean
\newcommand{\SigIntegMatrix}{11}         % significant instances out of 16
\newcommand{\GainIntegMatrixLo}{0.28\%}
\newcommand{\GainIntegMatrixHi}{9.28\%}
\newcommand{\GainProbePre}{-0.74\%}      % dispatching ablation before speed-ups, 0/16
\newcommand{\GainCertMatrix}{-1.72\%}    % yield-record feedback ablation
\newcommand{\GainStaggerMatrix}{-0.16\%} % staggering-operator ablation
%% Integration gain by H (same 16, four groups of 4).  Mean of the four
%% group means is \GainIntegMatrix{}.  Direct evidence that H=0.3 does not
%% inflate the closed-loop gain.
\newcommand{\GainIntegHZero}{6.12\%}
\newcommand{\GainIntegHLow}{5.62\%}      % target H=0.15, measured \MatrixHMeasLow{}
\newcommand{\GainIntegHMid}{5.95\%}      % target H=0.3, measured 0.288--0.313
\newcommand{\GainIntegHHigh}{4.38\%}     % target H=0.5, measured 0.500--0.507
\newcommand{\MatrixHMeasLow}{0.139}      % measured H at target 0.15
\newcommand{\RhoIntegH}{-0.21}           % Spearman(integration gain, H) on 16, weak neg.

%% ---- Public-benchmark feature anchors (Section 5.1.1) ----
%% clbs/tools/pub_features.py -> experiments_database/pub_features.csv,
%% from clbs/database/json/hf/*-ideal.json (20 instances), via the same
%% feature_params as the self-generated instances, so F/H are comparable.
%% H = mean, over all operations, of the CV of processing times in Omega;
%% |Omega|=1 counts as 0 (skipping them would raise public H; median 0.148).
\newcommand{\PubFMed}{0.38}              % median F, measured 0.3810 (0.292--1.000)
\newcommand{\PubHMed}{0.135}             % median H, measured 0.1349 (0.038--0.258)
\newcommand{\PubHMax}{0.258}             % max H, 0.2575 (sfjs01)
%% Rounded against this paper: measured max 0.1176 (sfjs08) -> 0.12.
\newcommand{\PubTtTpHi}{0.12}            % max public Tbar_t / Tbar_p

%% ---- Same comparison under two budget conventions (4.5, 5.7; numbers in prose) ----
%% clbs/experiments/protocols.csv, baseline = opendispatch (dispatching only);
%% 4 instances x 10 seeds; before the speed-ups.
%% Equal generations: rel_gain 0.02904, p 0.02688;
%% equal wall-clock: rel_gain 0.00026, p 0.87989.
\newcommand{\GainProbeSameGen}{2.90\%}
\newcommand{\PProbeSameGen}{=0.0269}
\newcommand{\GainProbeSameClock}{0.03\%}
\newcommand{\PProbeSameClock}{=0.8799}

%% ---- Degenerate public comparison (Section 5.9, limitation (3)) ----
%% gap_ideal.csv (default p100g200s30, 20 x 10) and
%% p100g1000s100/budget_effect.csv (high budget, 10 mfjs).
%% Converted by convert_public.py from Homayouni & Fontes (2020) set 1;
%% delta_return=0, num_agvs=2.  16 MILP proven optima + 4 best-known.
%% All 200 runs: never below a proven optimum, never below the self-computed
%% bound, all passed the validator.
\newcommand{\PubNInst}{20}
\newcommand{\PubNOpt}{16}                 % instances with a MILP proven optimum
\newcommand{\PubNMatch}{12}               % best solution bitwise-identical to the ref
\newcommand{\PubGapMean}{2.68\%}          % mean gap of best-of-10
\newcommand{\PubGapMed}{0.00\%}
\newcommand{\PubGapMax}{12.93\%}
\newcommand{\PubGapBig}{9.96\%}           % mean of the four largest (32--48 ops)
\newcommand{\PubGapMeanSeed}{5.29\%}      % mean-across-seeds convention
%% Budget p100g200s30 -> p100g1000s100; measured wall-clock x2.5
\newcommand{\PubBudgetRatio}{2.5}
\newcommand{\PubGapLoBudget}{5.37\%}      % 10 mfjs, low budget
\newcommand{\PubGapHiBudget}{4.53\%}      % same, high budget
\newcommand{\PubBudgetGainPP}{0.84}       % difference, percentage points
\newcommand{\PubBudgetNoGain}{5}          % of 10, gain exactly 0
%% Composite-bound slack on the 16 proven-optima only (using best-known as
%% denominator would overstate slack).
\newcommand{\PubLBSlack}{10.3\%}          % median bound-to-proven-opt (16)
\newcommand{\PubLBTotal}{10.8\%}          % median bound-to-our-best (same 16)
\newcommand{\PubLBResidMed}{0.0}          % median search shortfall, pp
\newcommand{\PubLBResidMax}{4.1}          % max of the same, pp
\newcommand{\PubCvBig}{$2.3\%$--$4.5\%$} % seed sd / mean, four largest
%% ---- Public-layout fidelity (clbs/experiments_database/fidelity.csv) ----
\newcommand{\PubNLayout}{7}
\newcommand{\PubNLayoutAsym}{7}           % asymmetric layouts
\newcommand{\PubNLayoutBad}{2}            % not a shortest-path closure

%%
%% \BibTeX command to typeset BibTeX logo in the docs
\AtBeginDocument{%
  \providecommand\BibTeX{{%
    Bib\TeX}}}

%% Rights management information.  This information is sent to you
%% when you complete the rights form.  These commands have SAMPLE
%% values in them; it is your responsibility as an author to replace
%% the commands and values with those provided to you when you
%% complete the rights form.
\setcopyright{acmlicensed}
%% Year matches the data (main result 2026-08-08).  The three \received lines
%% stay placeholders until the venue fills them; do not invent dates.
\copyrightyear{2026}
\acmYear{2026}
\acmDOI{XXXXXXX.XXXXXXX}

%% These commands are for a PROCEEDINGS abstract or paper.
%%  \acmConference[Conference acronym 'XX]{Make sure to enter the correct
%% 	conference title from your rights confirmation email}{June 03--05,
%% 	2018}{Woodstock, NY}

%%
%%  Uncomment \acmBooktitle if the title of the proceedings is different
%%  from ``Proceedings of ...''!
%%
%%\acmBooktitle{Woodstock '18: ACM Symposium on Neural Gaze Detection,
%%  June 03--05, 2018, Woodstock, NY}
%%\acmISBN{978-1-4503-XXXX-X/2018/06}


%%
%% Submission ID.
%% Use this when submitting an article to a sponsored event. You'll
%% receive a unique submission ID from the organizers
%% of the event, and this ID should be used as the parameter to this command.
%%\acmSubmissionID{123-A56-BU3}

%%
%% For managing citations, it is recommended to use bibliography
%% files in BibTeX format.
%%
%% You can then either use BibTeX with the ACM-Reference-Format style,
%% or BibLaTeX with the acmnumeric or acmauthoryear sytles, that include
%% support for advanced citation of software artefact from the
%% biblatex-software package, also separately available on CTAN.
%%
%% Look at the sample-*-biblatex.tex files for templates showcasing
%% the biblatex styles.
%%

%%
%% The majority of ACM publications use numbered citations and
%% references.  The command \citestyle{authoryear} switches to the
%% "author year" style.
%%
%% If you are preparing content for an event
%% sponsored by ACM SIGGRAPH, you must use the "author year" style of
%% citations and references.
%% Uncommenting
%% the next command will enable that style.
%%\citestyle{acmauthoryear}


%%
%% end of the preamble, start of the body of the document source.
\begin{document}

%%
%% The title names the construction, not a finding.  Do not revert to:
%%  - a colon / discovery title ("Evaluate, Don't Coordinate...");
%%  - reservation-table-aware dispatching as the grammatical subject
%%    (aggregate only \GainProbeLoop{}, 0/10 after Holm);
%%  - "low-overhead" as a modifier of the framework (it modifies dispatching).
%% Routing-in-the-Loop Evaluation = fitness from the realized conflict-free
%% makespan (the evaluation path), not "an evaluation of the router".
\title[Closed-Loop Bilevel Framework with Routing-in-the-Loop Evaluation]{A Closed-Loop Bilevel Framework with Routing-in-the-Loop Evaluation for Flexible Job Shop Scheduling and Conflict-Free AGV Routing}


%% Every percentage in the abstract is a preamble macro.
\begin{abstract}
Integrated scheduling of processing and transport in flexible job shops almost always treats travel times as a location-indexed constant matrix.
Once a finite fleet shares a sparse corridor network, travel time is the outcome of conflict-free path planning---routing, in this paper---because one job's occupancy delays others.
This paper studies a class in which each operation may use one of several robotic arms that are homogeneous in capability and heterogeneous in efficiency, while AGVs must travel conflict-free.
Machine flexibility then becomes an operational trade-off: when the corridor to a fast arm is congested, a slower arm with better connectivity may be the better assignment.

A \emph{closed-loop bilevel framework} connects the two decisions along an evaluation path and a decision path.
The upper level assigns machines and sequences operations; the lower level generates conflict-free paths by time-window reservation.
\emph{Routing-in-the-loop evaluation} takes the fitness of each candidate as the makespan after that routing, so yielding wait enters the search objective.
\emph{Reservation-table-aware vehicle dispatching} chooses a vehicle by a two-leg trial query on the reservation table, not by unimpeded travel time.
Two optimizations---admissible lower-bound pruning and winner-path reuse---do not change the selected vehicle or the timetable.

On \NCases{} controlled instances of one size, with contention from \ContentionLo{} to \ContentionHi{}, a four-level ladder, \NSeeds{} seeds, and an equal wall-clock budget, the closed loop reduces makespan by \GainLoopRule{} ($p\PLoopRule$) relative to the open loop;
within the closed loop, reservation-table-aware dispatching reduces it by a further \GainProbeLoop{} ($p\PProbeLoop$);
end-to-end the reduction is \GainEndToEnd{} (\WinEndToEnd{} wins, \LoseEndToEnd{} losses, $p\PEndToEnd$), close to the independent product \GainIndepProduct{}.
The closed loop is the larger effect and remains significant on \CellsSigLoopAdj{} instances after correction; dispatching is smaller and significant only in the aggregate.
The two are near-additive; no positive interaction is detected.
\end{abstract}

%%
%% The code below is generated by the tool at http://dl.acm.org/ccs.cfm.
%% Please copy and paste the code instead of the example below.
%%
%% Previous CCS block described a different paper; left commented.
%% concept_id must come from dl.acm.org/ccs.cfm, not be typed.  If submitting
%% to ACM, regenerate; suggested concepts:
%%   Theory of computation~Scheduling algorithms            (significance 500)
%%   Applied computing~Industry and manufacturing           (significance 300)
%%   Computing methodologies~Planning and scheduling        (significance 300)
%% Drop the block for a non-ACM venue.
%
% \begin{CCSXML}
% <ccs2012>...</ccs2012>
% \end{CCSXML}
% \ccsdesc[500]{Theory of computation~Scheduling algorithms}

%%
%% Keywords. The author(s) should pick words that accurately describe
%% the work being presented. Separate the keywords with commas.
\keywords{flexible job shop scheduling, conflict-free routing, closed-loop bilevel framework, routing-in-the-loop evaluation, reservation-table-aware vehicle dispatching}

%% ===================== Authors =====================
%% acmart errors with "No author given" and empties the running head if this
%% block is missing.  Confirm names and order before submission.  For double
%% blind, use \documentclass[...,anonymous]{acmart}; do not delete the block.
\author{Qi Yu}
%%\authornote{.}
%%\authornotemark[1]
\email{230240012@seu.edu.cn}
\orcid{0009-0008-7843-6345}
%%\authornote{corresponding author}
\affiliation{%
	\institution{School of Cyber Science and Engineering, Southeast University}
	\city{Nanjing}
	\state{Jiangsu}
	\country{PR China}
}
\author{Wei Li}
\orcid{0009-0001-0949-329X}
\email{xchlw@seu.edu.cn}
\authornote{corresponding author}
\affiliation{%
	\institution{School of Computer Science and Engineering, Southeast University}
	\city{Nanjing}
	\state{Jiangsu}
	\country{PR China}
}

%% Running-head short authors.  Unset, acmart falls back to "Trovato et al."
\renewcommand{\shortauthors}{Qi et al.}



%\Acknowledgements{This work was supported partly by Key Laboratory of Computer Network and Information Integration (Southeast University) of Ministry of Education of China under Grants NO. 93K-9 and Jiangsu Provincial Key Laboratory of Network and Information Security under Grants No.BM2003201.}


%% Venue fills the three received dates.  Do not keep the old 2025 dates.
\received{XX XX 2026}
\received[revised]{XX XX 2026}
\received[accepted]{XX XX 2026}

%%
%% This command processes the author and affiliation and title
%% information and builds the first part of the formatted document.
\maketitle
\section{Introduction}
\label{sec:Chapter_I}

\subsection{Motivation and gap}

Flexible job shop scheduling with transportation has long treated travel time as a constant, read from a matrix indexed by location pairs---the dominant convention~\cite{xin2025review}, still widely used~\cite{meng2025cp,qin2026knowledge,fu2025integrated}.
The assumption is reasonable when vehicles rarely meet.
Real shops are not built that way: a few vehicles share a sparse corridor network, and a single-lane segment cannot be used in both directions at once; once fleet density is high enough, the fleet's own traffic becomes the main delay~\cite{kim2026congestion}.
Travel time is then the outcome of two decisions---which path a vehicle takes (conflict-free path planning; routing, in this paper) and who first reserved the contended segment---not the distance between two points.

That coupling can be optimized only if the scheduler can change the assignment.
When an operation may use any arm in an option set, and the processing time depends on the arm, the assignment selects both the destination and the duration.
Under a constant matrix the comparison is arithmetic (shorter processing plus longer travel, or the reverse).
With corridor contention the wait on the path to a fast arm is invisible to the matrix, so a slower arm with better connectivity may be better---and the ranking may flip again at a different congestion level.
The trade-off needs both ingredients: heterogeneous optional arms, and distinct paths on which vehicles contend.

Figure~\ref{fig:motivating} shows the reversal on a recomputable minimal instance: the same two assignments rank the fast arm better under the constant matrix and the slow arm better under conflict-free routing.
Neither decision has changed; only the evaluation has.
The heterogeneity is already the smallest value that allows the reversal: with $\tau=6$ and $t^P_{\text{fast}}=3$, choosing the fast arm on the unimpeded matrix requires $t^P_{\text{slow}}\ge 14$, not a value chosen to amplify the effect.

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{fig/fig_motivating.pdf}
\Description{Two panels. The left panel is a small shop network in which the only
route to a fast arm passes through an exclusive trunk corridor, while a slow arm
hangs on its own branch. The right panel shows makespan for the same two machine
assignments evaluated twice: under a constant travel-time matrix the fast arm
wins, under conflict-free routing the slow arm wins, so the ranking reverses.}
\caption{Motivating instance: the same assignment ranked in opposite order by two evaluations.
(a)~The only path to the fast arm M1 is an exclusive trunk ($\tau=6$); the slow arm M2 sits on a short corridor that avoids it.
(b)~Under the constant matrix the fast arm wins $41$ to $54$; conflict-free routing reverses the ranking to $95$ versus $60$.
An \emph{existence demonstration}: of $1200$ nearby parameter combinations, $205$ ($17\%$) reverse.}
\label{fig:motivating}
%% From fig/fig_motivating.py and clbs/output/motivating.json
%% (clbs/tools/motivating.py --sweep; same decoder as the paper).
%% Panel (b) numbers are not schematic: unimpeded matrix =
%% decode(conflict_free=False), conflict-free routing = decode(conflict_free=True).
%% Instance: 4 jobs / 3 arms / 6 nodes / 5 corridors / 2 AGVs, delta_return=1,
%% rule-based dispatching.  Do not reuse fig/fig_intro_problem_new.pdf
%% (previous paper, proximity-vs-availability, not corridor contention).
\end{figure*}

Existing work typically treats flexible-job-shop scheduling or conflict-free travel, not both; when they are combined, the assignment is usually fixed first and routing then absorbs the contention, so delay cannot revise the schedule.
An exact method already writes both into one problem and solves small instances to proven optimality~\cite{vanos2026exact}; this paper adopts that setting, and the contribution is the solver, not the problem definition.
Beyond that exact size, heuristics still schedule then resolve~\cite{sun2023integrated}, or write realized durations back onto the time axis of the current solution only, on a benchmark with one machine per operation~\cite{li2026framework}.
The trade-off in which flexibility, heterogeneous efficiency, and corridor contention are present at once, and every candidate is ranked by the makespan after yielding wait, is therefore still open at heuristic size.
The literature position is in Section~\ref{sec:Chapter_II}.

\subsection{Approach and contributions}

This paper connects the two levels along an evaluation path and a decision path, and measures each under an equal wall-clock budget.
The upper level assigns machines and sequences operations; the lower level generates conflict-free paths by time-window reservation.

The \emph{evaluation path} takes the fitness of each candidate as the makespan after conflict-free routing, so yielding wait enters the search objective.
One such decode costs about an order of magnitude more than a lookup of the unimpeded matrix.
\cite{li2026framework} lists a fully iterative loop as future work, but does not report that cost or its effect on makespan.

The \emph{decision path} uses \emph{reservation-table-aware vehicle dispatching}: a two-leg trial query on the reservation table (empty pickup, then loaded delivery; the query does not write the timetable) selects the vehicle with the earliest feasible delivery, replacing the idle-first rule of~\cite{li2026framework}.
Two optimizations that do not change the output---admissible lower-bound pruning and winner-path reuse---keep each evaluation low-overhead.
A surcharge on occupied corridor--time slots, and reassignment or staggering from the yield record, were also implemented; neither yields a stable gain under the same budget (Section~\ref{sec:Chapter_V}).

A four-level ladder separates the two paths.
\textbf{B0} is open-loop planning plus actual execution~\cite{li2026framework};
\textbf{B0+} replaces only vehicle assignment on a frozen schedule~\cite{sun2023integrated};
\textbf{B1} is closed-loop planning plus rule-based dispatching;
\textbf{B2} is the method of this paper.
Every schedule is executed by the same conflict-free planner; what is compared is the realized makespan.
Moving congestion into the search reduces makespan by \GainLoopRule{} ($p\PLoopRule$), the larger effect;
within the closed loop, reservation-table-aware dispatching reduces it by a further \GainProbeLoop{} ($p\PProbeLoop$), a smaller but still significant effect;
end-to-end the reduction is \GainEndToEnd{} (\WinEndToEnd{} wins, \LoseEndToEnd{} losses, $p\PEndToEnd$).
The two speed-ups give a factor of \SpeedupProbe{}, cut router calls by \RouteCallCut{}, and press the cost multiple from \CostMultBefore{} to \CostMultAfter{}.
The two factors are near-additive (\GainEndToEnd{} close to \GainIndepProduct{}) and are not substitutes: open-loop dispatching is only \GainProbeOpen{} ($p\PProbeOpen$).
After Holm correction by family, the closed loop remains significant on \CellsSigLoopAdj{}/\NCases{} instances; dispatching is significant on none of the instances on its own (before correction, \CellsSigProbe{}).
With \NSeeds{} seeds per instance the sample is not sufficient to identify on which class of instances the dispatching gain is stable.
The protocol is in Section~\ref{sec:protocol}; per-instance readings are in Section~\ref{sec:whichcongestion}.

The contribution is the solver~\cite{vanos2026exact}: a controllable characterization of contention intensity; a measurement of routing-in-the-loop evaluation; reservation-table-aware dispatching with two output-preserving speed-ups; and a separate reading of the two effects---the closed loop larger and per-instance robust, dispatching significant only in the aggregate.

The notation of the paper is collected in Table~\ref{tab:notation}.



\section{Related work}
\label{sec:Chapter_II}

Two strands sit next to this paper: flexible job shop scheduling with transportation times, and conflict-free routing of exogenous tasks.
Work that writes both into one problem is scarce; Table~\ref{tab:related} places the present solver against that literature.

\subsection{Two strands}

FJSP\_PT extends FJSP by bringing material movement into the schedule~\cite{xin2025review,dauzereperes2024fjsp}.
Public benchmarks are collected by Homayouni and Fontes~\cite{homayouni2021production}.
The dominant convention is a constant travel-time matrix: a vehicle needs a fixed $t(a,b)$, independent of corridor occupancy, so vehicles enter only as capacity resources.
Meng et al.~\cite{meng2025cp} close 29 previously open benchmarks and improve 27 best-known solutions; Qin et al.~\cite{qin2026knowledge} guide a local search by AGVs and a critical path still computed on matrix times; Fu et al.~\cite{fu2025integrated} decide production and logistics jointly, without making transport duration depend on the schedule.
These assumptions ``reduce the complexity of the problem and even change its nature''~\cite{xin2025review}: when the fleet is large enough, arrival times are affected by congestion, and a plan that ignores that dependence need not be feasible on the actual corridors.
This strand has machine flexibility, but no yielding wait.
% [ref_check: key=xin2025review; claim="review covers progress through 2023; constant matrix is dominant; the matrix reduces complexity and can change the nature of the problem"; need=quote]
% [ref_check: key=meng2025cp; claim="closes 29 previously open benchmarks and improves 27 best-known solutions"; need=quote]

The other strand takes corridor contention as the object.
Optimal multi-agent path finding is NP-hard~\cite{gao2023mapf}; Safe Interval Path Planning extends single-vehicle routing to continuous-time road networks~\cite{kasaura2022sipp}.
Zhang et al.~\cite{zhang2024guidance} steer lifelong agents with offline-learned edge weights; Kim and Kim~\cite{kim2026congestion} give density-based congestion-aware dispatching.
Surveys list dispatching as independent of path planning~\cite{lu2022agv} and task allocation as adjacent to it~\cite{fragapane2021planning}.
Release times and endpoints are inputs: the router cannot change the assignment that produced the tasks.
Congestion is usually a proxy---local density~\cite{kim2026congestion} or offline edge weights~\cite{zhang2024guidance}---not the yield that names a corridor, an interval, and the transport that occupied it first.
This strand has genuine conflict-free routing, but no flexibility.
% [ref_check: key=zhang2024guidance,kim2026congestion; claim="release times and endpoints are exogenous; congestion is density or offline edge weights"; need=quote]

\subsection{Integration and our position}

van Os and Basten~\cite{vanos2026exact} formalize FJSP with conflict-free transportation: optional machines, location-dependent processing times, and conflict-free travel.
A logic-based Benders solver (constraint-programming master, integer-programming routing subproblem, conflict-driven cuts) solves most benchmarks to proven optimality and improves about half of them by at least $10\%$ over prior heuristics, within the size the exact decomposition can reach.
This paper adopts that setting; the contribution is a heuristic that, beyond that size, sends routing delay back into every scheduling candidate.
% [ref_check: key=vanos2026exact; claim="most benchmarks solved to proven optimality; about half improve makespan by at least 10% over prior heuristics"; need=quote]

Sun et al.~\cite{sun2023integrated} still fix assignment and sequence first, then resolve conflicts with a genetic AGV encoding and time-window Dijkstra; the second stage cannot revise the first.
Li et al.~\cite{li2026framework} write realized durations back onto the current solution only, on a one-machine-per-operation benchmark.
The remaining gap is how the routing result enters the schedule: whether every candidate is evaluated by actual routing, and whether the assignment can change from that evaluation.
% [ref_check: key=sun2023integrated; claim="second-stage conflict resolution does not revise first-stage schedule or assignment"; need=quote]
% [ref_check: key=li2026framework; claim="feedback is time only; the experimental benchmark has one optional machine per operation"; need=quote]

Critical-chain local search is mature~\cite{otala2021graph,tamssaouet2023neighborhood,qin2026knowledge}, but a chain on a constant matrix cannot name a corridor; after time-window reservation it can.
On instance design, \cite{xin2025review} notes that published instances ``differ mainly in size'' and lists $\bar{T}_t/\bar{T}_p$ as likely to affect performance; this paper also varies how much of the congestion is assignment-related (Section~\ref{sec:Chapter_V}).
% [ref_check: key=xin2025review; claim="instances differ mainly in size; T_t/T_p is listed as likely to affect performance"; need=quote]

\begin{table}[t]
  \caption{Related work versus this paper.
Machine heterogeneity: optional machines with machine-dependent processing times.
Coupling frequency: how often, and in which direction, the routing result enters the schedule.}
\label{tab:related}
%% Hand-maintained table: no generator.  Any row change must match Section 2.2.
\footnotesize
\begin{tabular}{@{}lclll l@{}}
\toprule
Work class & Ref. & Machine het. & Vehicle contention & Coupling freq. & Feedback content \\
\midrule
FJSP\_PT metaheuristics & \cite{meng2025cp,qin2026knowledge,fu2025integrated}
  & yes & constant matrix & --- & --- \\
Lifelong MAPF & \cite{gao2023mapf,zhang2024guidance}
  & --- & conflict-free & tasks exogenous & edge weights \\
Congestion-aware dispatching & \cite{kim2026congestion}
  & no & density proxy & one-way & congestion penalty \\
Two-stage FJSP + routing & \cite{sun2023integrated}
  & yes & conflict-free & one-way & none \\
RL scheduling + PBS & \cite{li2026framework}
  & no & conflict-free & one-shot correction & arrival times \\
Exact LBBD & \cite{vanos2026exact}
  & yes & conflict-free & master--subproblem & conflict-driven cuts \\
\textbf{This paper} &
  & yes & conflict-free & every candidate & realized time + trial query \\
\bottomrule
\end{tabular}
\end{table}


\section{Problem statement and model}
\label{sec:Chapter_III}
\subsection{Problem statement}

An instance is a set of jobs, robotic arms, and vehicles on a shop-floor graph (Table~\ref{tab:notation}).
Each job is a fixed operation chain; each operation has an option set of arms with machine-dependent processing times.
Arms occupy fixed nodes; the fleet moves jobs between them.
Four decisions are taken at once:
(i)~assign each operation to an optional arm;
(ii)~sequence each arm;
(iii)~assign and sequence the induced transport tasks on vehicles;
(iv)~plan a conflict-free path for every move, waits allowed.
The objective is to minimize $C_{\max}$.

\begin{table}[t]
\caption{Notation.}
\label{tab:notation}
\footnotesize
\renewcommand{\arraystretch}{0.95}
\begin{tabular}{@{}ll@{}}
\toprule
Symbol & Meaning \\
\midrule
$J=\{1,\dots,n\}$, $O_{ji}$ & jobs; $i$-th operation of job $j$, $i\le n_j$ \\
$M=\{1,\dots,N_M\}$, $\Omega_{ji}\subseteq M$ & robotic arms; option set of $O_{ji}$ \\
$t^P_{jim}$ & processing time of $O_{ji}$ on arm $m\in\Omega_{ji}$ \\
$K=\{1,\dots,N_A\}$ & homogeneous unit-load vehicles \\
$G=(V,E)$, $\tau_e$ & shop-floor graph; traversal time of corridor $e$ \\
$v_0$, $\mathrm{loc}(m)$ & load/unload station; node of arm $m$ \\
$t^{*}(a,b)$ & shortest-path time $a\to b$ in $G$, ignoring contention \\
$\delta_{\mathrm{ret}}\in\{0,1\}$ & finished job returns to $v_0$ (baseline $1$) \\
$S_{ji}$, $C_{ji}$, $A_{ji}$, $C_{\max}$ & start, completion, arrival; makespan \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Modelling assumptions}
\label{sec:assumptions}

\begin{enumerate}[label=\textbf{A\arabic*.},leftmargin=*,topsep=2pt,itemsep=1pt]
\item \textbf{Static instance and complete information.} All $n$ jobs are at $v_0$ at $t=0$; no release or due dates. Processing data, $G$, and vehicle states are known.

\item \textbf{Jobs.} Fixed precedence chain per job. Processing and transport are non-preemptive; jobs cannot be split; no exogenous precedence among jobs.

\item \textbf{Robotic arms: partial flexibility and heterogeneous times.} $O_{ji}$ may run on any $m\in\Omega_{ji}$ (most operations have $|\Omega_{ji}|\ge 2$; $\Omega_{ji}=M$ is a special case). Times $t^P_{jim}$ are machine-dependent and need not be proportional across arms. An arm processes one operation at a time, does not fail, and sequence-dependent setup is folded into $t^P_{jim}$.

\item \textbf{Buffers and loading.} Ample input/output buffers and $v_0$ storage, so no blocking. Loading and unloading are negligible and do not occupy the arm.

\item \textbf{Vehicles.} $N_A$ homogeneous unit-load vehicles at constant speed, empty or loaded; no acceleration or battery. All stand empty at $v_0$ at $t=0$. A task is a one-way move---$v_0$ to the first arm, between successive distinct arms, or, when $\delta_{\mathrm{ret}}=1$, last arm back to $v_0$; any vehicle may take any task. After unloading, a vehicle is not forced back to $v_0$; routing may postpone its departure.

\item \textbf{Road network and conflict-freedom.} $G$ is strongly connected; each $\tau_e$ is a known constant. Corridors are \emph{exclusive}: both directions of $e$ share one reservation, so at most one vehicle occupies $e$ at a time (no head-on, no overtaking). Occupancy is $[t,\,t+\tau_e)$; a second vehicle may enter at $t+\tau_e$. Nodes have ample berths, separate from the traversal resource; waits occur at berths and are always permitted.

\item \textbf{Out of scope.} Machine and vehicle failures, finite buffers and blocking, battery charging, and dynamic arrivals~\cite{gui2023dynamic,xiong2024dynamic} are left to future work (item~(5) of Section~\ref{sec:future_work}).
\end{enumerate}

A1, A2, A4, and A5 are standard in FJSP\_PT~\cite{xin2025review}.
What shapes this paper is A3 (machine-dependent times on partially flexible arms) and A6 (contention as an exclusive-resource constraint, not a constant matrix): transport duration is an output of routing, not a fixed input.
Deadlock-freedom is not assumed.
A4 and A6, with Proposition~\ref{prop:decodable} in Section~\ref{sec:router}, imply that any assignment and sequence decodes to a conflict-free, deadlock-free timetable, so the search need not repair infeasible solutions.

\subsection{Decision variables and induced transport tasks}

Let $x_{jim}\in\{0,1\}$ indicate that $O_{ji}$ is assigned to robotic arm $m$; write $\mathrm{m}(j,i)$ for the chosen arm and $d_{ji}=\mathrm{loc}(\mathrm{m}(j,i))$ for the node of that arm.
When $\delta_{\mathrm{ret}}=1$, a zero-duration dummy operation $O_{j,n_j+1}$ at $v_0$ is appended so that the return move uses the same task notation, and $C_{\max}$ is always the completion of each job's last operation.

Transport tasks are not an input; they are \emph{induced} by the assignment:
\begin{equation}
\label{eq:tasks}
\mathcal{T}(x)=\Bigl\{\,(j,i)\ :\ d_{j,i-1}\neq d_{ji}\,\Bigr\},
\qquad d_{j0}\equiv v_0 ,
\end{equation}
where task $(j,i)$ moves job $j$ from $d_{j,i-1}$ to $d_{ji}$ and becomes ready at time $r_{ji}=C_{j,i-1}$ (with the convention $C_{j0}=0$).
If two successive operations are placed on the same arm, no transport task is created.
Thus both the \emph{number} and the \emph{endpoints} of transport tasks are consequences of decision group~(i): an assignment that keeps a job on the same arm throughout needs only two moves, whereas an assignment that changes arm at every operation needs $n_j+1$ moves.
Assignment of tasks to vehicles is indicated by $y_{tk}\in\{0,1\}$, with $\sum_{k\in K}y_{tk}=1$ for each $t\in\mathcal{T}(x)$; tasks given to the same vehicle are ordered into a sequence.

\subsection{Timing relations and feasibility}

Write $p_{ji}=\sum_{m\in\Omega_{ji}}x_{jim}t^P_{jim}$ for the processing time on the chosen arm. Assignment, precedence, and the unit-capacity of each arm give
\begin{align}
\sum_{m\in\Omega_{ji}} x_{jim} &= 1, & \forall j,i,
\label{eq:assign}\\
C_{ji} &= S_{ji}+p_{ji}, & \forall j,i,
\label{eq:dur}\\
S_{ji} &\ \ge\ A_{ji}, & \forall j,i,
\label{eq:material}\\
S_{j'i'}\ \ge\ C_{ji}\ \ \vee\ \ S_{ji} &\ \ge\ C_{j'i'}
& \forall (j,i)\neq(j',i')\ \text{on the same arm},
\label{eq:disj}
\end{align}
where $A_{ji}$ is the time at which the job actually appears at $d_{ji}$.
If $(j,i)\notin\mathcal{T}(x)$, the job is already there and $A_{ji}=C_{j,i-1}$.
Otherwise $A_{ji}$ is produced by the vehicle that carries task $(j,i)$: write $a^{\mathrm{E}}_{ji}$ for the time at which that vehicle reaches the pickup node after its empty leg, and set
\begin{equation}
\label{eq:load}
\ell_{ji}=\max\bigl(a^{\mathrm{E}}_{ji},\ r_{ji}\bigr),
\qquad
A_{ji}=\text{arrival time of the loaded leg departing at }\ell_{ji},
\end{equation}
i.e.\ the vehicle may wait for the job, and the job may wait for the vehicle.
Whether empty or loaded, each leg is a walk $(e_1,\dots,e_q)$ in $G$ whose corridor entry times $\eta_1,\dots,\eta_q$ satisfy
\begin{equation}
\label{eq:walk}
\eta_1 \ \ge\ \sigma, \qquad
\eta_{l+1}\ \ge\ \eta_l+\tau_{e_l}, \quad l=1,\dots,q-1,
\end{equation}
where $\sigma$ is the earliest start of the leg; a gap $\eta_{l+1}-\eta_l-\tau_{e_l}$ is a wait at a berth.
The leg ends at $\eta_q+\tau_{e_q}$.
Any two distinct occupancies of the same corridor $e$---regardless of direction or vehicle---must be disjoint:
\begin{equation}
\label{eq:excl}
\bigl[\eta,\ \eta+\tau_e\bigr)\ \cap\
\bigl[\eta',\ \eta'+\tau_e\bigr)\ =\ \emptyset .
\end{equation}
The objective is
\begin{equation}
\label{eq:obj}
\min\ C_{\max},\qquad
C_{\max}=\max_{j\in J}\ C_{j,\,n_j+\delta_{\mathrm{ret}}} .
\end{equation}

Two facts are used in Section~\ref{sec:Chapter_IV}.
In any semi-active schedule, \eqref{eq:material} and \eqref{eq:disj} tighten to
\begin{equation}
\label{eq:bind}
S_{ji}=\max\bigl(A_{ji},\ F_{\mathrm{m}(j,i)}\bigr),
\end{equation}
where $F_m$ is the completion of the preceding operation on arm $m$; later sections attribute a postponed start to material or to the arm by which argument is larger.
And \eqref{eq:excl} is the only constraint that couples jobs through \emph{space} rather than a shared machine.

\subsection{Relation to FJSP\_PT}
\label{sec:fjsppt}

After \eqref{eq:excl} is removed, each leg may take a shortest path, durations collapse to $t^{*}(a,b)$, and the model becomes FJSP\_PT---the two problems differ only in corridor exclusivity; $t^{*}$ is later a lower bound and the open-loop arrival estimate.
van Os and Basten~\cite{vanos2026exact} reserve \emph{nodes}; this paper reserves \emph{edges}.
Lyu et al.~\cite{lyu2019approach} use edge exclusivity and also require that a node hold at most one vehicle; this paper does not, and waits occur at ample berths.
Why the instances cannot be compared directly is limitation~(1) of Section~\ref{sec:limitations}.

\subsection{Problem hardness}

\begin{prop}
\label{prop:np_hard}
The problem defined by \eqref{eq:assign}--\eqref{eq:obj} is strongly NP-hard; it remains so when every operation satisfies
$\Omega_{ji}=M$.
\end{prop}

\begin{proof}
Restrict to $\tau_e=0$ for every $e$, each arm on a dedicated corridor from $v_0$, and $N_A=n$.
Every leg then has duration zero, \eqref{eq:excl} holds identically, and $A_{ji}=C_{j,i-1}$.
What remains is \eqref{eq:assign}--\eqref{eq:disj} with $A_{ji}=C_{j,i-1}$, i.e.\ FJSP with unrelated machines.
Two special cases are already strongly NP-hard: $|\Omega_{ji}|=1$ is the classical job shop (already with three machines); $\Omega_{ji}=M$ and $n_j=1$ is makespan on unrelated parallel machines, strongly NP-hard when the number of machines is part of the input.
FJSP itself is strongly NP-hard~\cite{dauzereperes2024fjsp}.
Each restriction is a polynomial reduction, so the general problem is strongly NP-hard; full flexibility does not make it easy.
\end{proof}

The reduction of Proposition~\ref{prop:np_hard} excludes vehicle contention, so the problem is already strongly NP-hard without it.
Fixing decisions~(i)--(iii) still leaves a conflict-free multi-agent routing problem~\cite{gao2023mapf}; routing adds constraints on top of the scheduling hardness.
The running time of one decode is reported with the framework (Section~\ref{sec:complexity}), not here.

\subsection{Instance features}
\label{sec:features}

Instance size is held fixed and only structure is varied; the effect of size is limitation~(1) of Section~\ref{sec:limitations}.
Four quantities are standard in the scheduling literature; Xin et al.~\cite{xin2025review} note that $\bar{T}_t/\bar{T}_p$ in particular is likely to affect performance.
$\bar{T}_t/\bar{T}_p$ is mean shortest-path travel among arm nodes over mean processing time.
Heterogeneity $H$ is the coefficient of variation of each operation's processing times over its optional arms, then averaged (singleton option sets contribute $0$); $H=0$ means reassignment changes no processing time, only the corridor.
Flexibility $F=\operatorname{mean}_{ji}|\Omega_{ji}|/N_M$ is the scope of reassignment.
$N_A/N_M$ is the vehicle-to-arm ratio (smaller: transport bottleneck more likely).

Two further quantities exist only on an explicit road network; what matters is how much congestion reassignment can avoid.
Contention intensity is the share of travel time that is yielding wait on the initial random population---read off the structure, not supplied as input---and is used to order instances (Table~\ref{tab:instances}).
Let $\mathrm{cut}(v_0)$ be a smallest set of corridors whose deletion disconnects $v_0$ from every arm; $|\mathrm{cut}(v_0)|$ (funnel width) bounds how many vehicles can leave $v_0$ at once, and the layout factor varies it by adding or removing parallel exits (\texttt{funnel} halves the \texttt{high} value).
A funnel-share $\varphi$ and a far-group cut were tried and not adopted (item~(1) of Section~\ref{sec:future_work}).

Each instance carries a composite lower bound
$\mathrm{LB}=\max(\mathrm{LB}_{\text{chain}},\mathrm{LB}_{\text{load}},
\mathrm{LB}_{\text{cut}})$:
the job-chain relaxation ignores inter-arm moves and queues; the load relaxation ignores precedence and transport; the cut relaxation spreads $X=2n$ crossings of $\mathrm{cut}(v_0)$ (when $\delta_{\mathrm{ret}}=1$) so as to minimize the load on the busiest corridor,
\begin{equation}
\label{eq:lbcut}
C_{\max}\ \ge\ X\Bigl/\sum_{e\in \mathrm{cut}(v_0)}\tau_e^{-1}.
\end{equation}
On these instances $\mathrm{LB}_{\text{cut}}$ is largest on eight of ten and $\mathrm{LB}_{\text{chain}}$ on the other two.
The bound is loose (gap \LBGapLo{} to \LBGapHi{}) and is used only to check the order of magnitude.

\section{Closed-loop bilevel framework}
\label{sec:Chapter_IV}
\subsection{Overview: routing-in-the-loop evaluation and the decision path}
\label{sec:overview}

The framework assigns the four decision groups of Section~\ref{sec:Chapter_III} to two levels (Figure~\ref{fig:framework}).
The upper level searches assignment and sequencing with a memetic algorithm~\cite{zhang2024memetic}; an event-driven decoder turns a candidate into precedence-constrained transport tasks; the lower level plans conflict-free paths by time-window reservation.
The output is an executable timetable and its makespan.
The evaluation path (blue) returns that makespan as fitness, once per candidate, yielding wait included.
The decision path (orange) chooses a vehicle by a two-leg trial query on the reservation table---empty pickup, then loaded delivery; the query does not write the timetable---rather than by an unimpeded-time estimate.
Two further interfaces---a surcharge on occupied corridor--time slots, and reassignment from the yield record---are controls (Sections~\ref{sec:interface} and~\ref{sec:ls}); under an equal wall-clock budget neither yields a stable gain.

%% From fig/fig_framework.py; a schematic, no data.
%% Do not reuse fig/fig_hdp_architecture*.pdf (HDP framework of a previous paper).
\begin{figure*}[t]
\centering
\includegraphics[width=0.8\textwidth]{fig/fig_framework.pdf}
\caption{The closed-loop bilevel framework.
Blue path~1 returns the conflict-free timetable as fitness, once per candidate.
Orange path~2 runs a two-leg trial query for the earliest feasible delivery before a vehicle is chosen; the trial query does not write the final timetable.
The ladder of Section~\ref{sec:Chapter_V} is arranged by whether these two paths are open.}
\Description{Schematic of the closed-loop bilevel framework: an upper memetic
search layer, an event-driven decoder, and a lower time-window reservation
routing layer, with two colour-coded feedback paths entering the upper layer at
different places---the evaluation path returning a realized makespan once per
candidate, and the decision path returning a realizable arrival time once per
transport task.}
\label{fig:framework}
\end{figure*}

\subsection{Lower level: time-window reservation and conflict-free routing}
\label{sec:router}

The only contended resource is the corridor (A6).
The reservation table maps each corridor to a time-ordered list of half-open occupancies $[t,\,t+\tau_e)$, labelled by vehicle and task, and supports four operations: earliest feasible entry no earlier than a given instant; write; revoke a task; and checkpoint/rollback for a trial query.
Nodes are not reserved: traversal takes zero time, waits occur at berths (A6).
The router and an independent checker inspect only corridor occupancy.

A leg is planned by a label-setting search (Algorithm~\ref{alg:route}).
A label $(u,t)$ means standing at the berth of $u$ at time $t$; the cost is $t$.
Querying the table for the earliest entry $t'\ge t$ produces the wait $t'-t$ of \eqref{eq:walk} directly.
Within one decode, written paths are not revoked, so vehicles are planned in decoder order and a later vehicle yields to an earlier one.

\begin{lem}[Earliest arrival is sufficient]
\label{lem:label}
Keeping only the earliest-arrival label at each node is sufficient to obtain a path that reaches the destination earliest under the current reservation table.
\end{lem}

\begin{proof}
  Berths have ample capacity and do not occupy the traversal resource (A6), so a vehicle that has stood at $u$ since time $t$ need only wait further until $t'$
  in order to take any continuation that a vehicle arriving at $u$ only at $t'>t$ could take.
  An earlier-arrival label is therefore never worse, and each node need keep only the earliest arrival.
  Moreover, the later the query time to the reservation table, the fewer the still-feasible entry slots,
  so the time to enter corridor $e$ and finish traversing it,
  $t\mapsto R.\textsc{EarliestEntry}(e,t,\tau_e)+\tau_e$,
  is non-decreasing in $t$.
  The search extracts labels in increasing order of arrival time; the first time the destination is extracted is the earliest arrival.
\end{proof}

Ordinary Dijkstra therefore suffices; there is no need for the per-interval labelling of Safe Interval Path Planning (SIPP)~\cite{kasaura2022sipp}.
A second criterion (a surcharge on busy slots) would force a multi-label Pareto search; Section~\ref{sec:interface} returns to that cost.

\begin{algorithm}[t]
  \caption{\textsc{Route}: conflict-free planning of a single leg}
  \label{alg:route}
  \footnotesize
  \begin{algorithmic}[1]
  \Require origin $s$, destination $g$, earliest departure $t_0$, table $R$, commit flag
  \Ensure occupancy intervals; written to $R$ if commit
  \State push $(s,t_0)$ onto a priority queue keyed by (time, node id)
  \While{the queue is nonempty}
    \State extract the least-key label $(u,t)$
    \If{$u=g$}
      \State backtrack occupancies; if commit, write them to $R$; \Return $t$
    \EndIf
    \ForAll{corridors $e=(u,v)$ leaving $u$}
      \State $t_{\text{in}} \gets R.\textsc{EarliestEntry}(e,t,\tau_e)$
      \State if $t_{\text{in}}+\tau_e$ is strictly earlier than the incumbent at $v$, update $(v,\,t_{\text{in}}+\tau_e)$
    \EndFor
  \EndWhile
  \State \Return failure \Comment{$G$ strongly connected: see text}
  \end{algorithmic}
\end{algorithm}

A path always exists: reservations are finite, time is bounded, and $G$ is strongly connected, so in the worst case the vehicle waits until every occupancy has ended and then takes an unimpeded shortest path.
That is the existence half of Proposition~\ref{prop:decodable}.
A single call costs $O\bigl(|E|\,(W^{2}+\log|V|)\bigr)$: at most $|E|$ corridors are examined; for each, $O(W)$ candidate entries are tested for overlap in $O(W)$ time, plus a heap step of $O(\log|V|)$, where $W$ is the mean number of reserved intervals per corridor.
A logarithmic earliest-entry query would drop this to $O\bigl(|E|(\log W+\log|V|)\bigr)$; this paper uses the linear scan.

The same search on an empty table over all pickup--delivery pairs yields the unimpeded matrix $t^{*}$ of Section~\ref{sec:Chapter_III}, used for open-loop arrival estimates, the reassignment neighbourhood, the two-stage baseline, and the admissible bound of Section~\ref{sec:speedup}.


\subsection{Upper level: encoding and event-driven decoding}
\label{sec:upper}

Let $N$ be the number of operations.
An individual has two segments: a machine-assignment vector of length $N$, the $k$-th entry an arm from $\Omega_{ji}$; and an operation sequence in which job $j$ appears $n_j$ times, the $x$-th appearance standing for $O_{jx}$.
The relative order of operations that share an arm is read from the sequence.
When $\delta_{\mathrm{ret}}=1$, each job appears once more for the dummy of Section~\ref{sec:Chapter_III}, so return transport needs no extra decoder branch.
Vehicle assignment is decided at decode time (Section~\ref{sec:dispatch}): a third segment would enlarge the search space and would no longer guarantee that every chromosome is decodable.

The decoder (Algorithm~\ref{alg:decode}) \emph{is} the evaluation path.
It sweeps the operation sequence from left to right, maintaining the idle time of each robotic arm, the current node and ready time of each job, and the current node and available time of each vehicle.
When the arm of an operation differs from the job's current location, a transport task of \eqref{eq:tasks} is generated, a vehicle is chosen, and two paths are planned against the \emph{live} reservation table: an empty leg to the pickup and a loaded leg to the destination, joined by \eqref{eq:load}.
The operation then starts by \eqref{eq:bind}; the decoder also records which of the two arguments is larger, as a \texttt{bind} field, which later attribution of a postponed start takes as authoritative.

\begin{algorithm}[t]
\caption{\textsc{Decode}: fitness from a complete conflict-free simulation}
\label{alg:decode}
\footnotesize
\begin{algorithmic}[1]
\Require instance, assignment $x$, operation sequence
\Ensure makespan, timetable, per-corridor yield statistics
\State $F_m\gets 0$ for every arm; $\mathrm{pos}_j\gets v_0$, $\mathrm{ready}_j\gets 0$ for every job
\State $\mathrm{loc}_k\gets v_0$, $\mathrm{avail}_k\gets 0$ for every vehicle; clear $R$
\ForAll{operations $O_{ji}$ in sequence order (dummies included)}
  \State $m\gets$ arm of $O_{ji}$ under $x$; $d\gets \mathrm{loc}(m)$
  \If{$\mathrm{pos}_j=d$}
     \State $A_{ji}\gets \mathrm{ready}_j$
  \Else
     \State choose vehicle $k$ (Section~\ref{sec:dispatch})
     \State $a^{\mathrm{E}}\gets\textsc{Route}(\mathrm{loc}_k,\mathrm{pos}_j,\mathrm{avail}_k,R,\text{true})$
     \State $\ell\gets\max(a^{\mathrm{E}},\mathrm{ready}_j)$
     \State $A_{ji}\gets\textsc{Route}(\mathrm{pos}_j,d,\ell,R,\text{true})$; $\mathrm{loc}_k\gets d$; $\mathrm{avail}_k\gets A_{ji}$
     \State accumulate yielding wait by corridor
  \EndIf
  \State $\texttt{bind}\gets\texttt{machine}$ if $F_m>A_{ji}$, else $\texttt{arrive}$
  \State $S_{ji}\gets\max(A_{ji},F_m)$; $C_{ji}\gets S_{ji}+p_{ji}$; $F_m\gets C_{ji}$; $\mathrm{pos}_j\gets d$; $\mathrm{ready}_j\gets C_{ji}$
\EndFor
\State \Return $C_{\max}=\max_{j,i} C_{ji}$, the timetable, and the yield statistics
\end{algorithmic}
\end{algorithm}

Reservations are written in decoder order; ties break by a fixed rule at both sites---the routing queue is keyed by (arrival time, node id) and updates only on a strictly earlier arrival; dispatching scans vehicles by increasing id and keeps the first strictly better one (Section~\ref{sec:speedup}).
A chromosome therefore maps to a unique timetable, so the seed-paired comparisons of Section~\ref{sec:Chapter_V} are deterministic.

\begin{prop}[Every chromosome is decodable]
  \label{prop:decodable}
  For any machine assignment and any operation sequence, Algorithm~\ref{alg:decode} terminates and returns a conflict-free, deadlock-free timetable of finite makespan. The search therefore need not invoke a repair operator.
\end{prop}

\begin{proof}
  By the encoding, the operation sequence is a topological order of each job's precedence constraints: the $x$-th appearance of a job stands for its $x$-th operation, so $\mathrm{ready}_j$ is already known when it is read.
  Ample buffers (A4) mean that an operation is never blocked for lack of storage, so once the job has arrived, \eqref{eq:bind} always supplies a finite start time.
  Each \textsc{Route} call returns in finite time by the argument after Algorithm~\ref{alg:route}, and occupies only a still-free slot on the reservation table, so \eqref{eq:excl} holds by construction.
  Deadlock-freedom is likewise by construction: the corridor is the only contended resource (A6); waits occur only at ample berths and do not occupy corridors; every occupancy written to the table ends at a finite time, so there is no mutually waiting fleet---in the worst case a vehicle waits until every existing occupancy has ended and then proceeds.
  The sweep executes $N+n\delta_{\mathrm{ret}}$ times, and each time schedules a finite number of events at finite times.
\end{proof}

The outer search is a conventional genetic algorithm, so the gains of Section~\ref{sec:Chapter_V} can be attributed to the mechanisms above rather than to operator tuning.
The initial population is random (uniform arm from $\Omega_{ji}$, shuffled sequence), plus two seeds: every operation on its fastest arm, and a load-balance greedy.
Selection is tournament with a small elite; the sequence uses precedence-preserving order crossover, the assignment uniform crossover; mutation swaps two sequence positions or moves an operation to another optional arm.
Fitness is the decoded makespan.
These settings are not listed as contributions; hyperparameters are in Section~\ref{sec:impl}.

\subsection{Reservation-table-aware vehicle dispatching}
\label{sec:dispatch}

A vehicle may be chosen either by an unimpeded-shortest-path estimate or by a trial query on the reservation table.
The open-loop rule scores each vehicle by
\begin{equation}
\label{eq:rule}
\mathrm{score}(k)=\max\bigl(\mathrm{avail}_k+t^{*}(\mathrm{loc}_k,\text{pickup}),\,
\mathrm{ready}_j\bigr)+t^{*}(\text{pickup},\text{destination}),
\end{equation}
which does not consult the reservation table and may therefore select a vehicle that is geometrically near but whose corridor is already occupied.
The formula is the same as the idle-first dispatching of~\cite{li2026framework}, and is a typical single-attribute rule in surveys of multi-AGV dispatching~\cite{lu2022agv}.

The closed-loop default is instead \emph{reservation-table-aware vehicle dispatching}.
For each candidate vehicle: first write and plan the empty leg on the reservation table, then plan the loaded leg without writing the final timetable, record the earliest feasible delivery time obtained by the trial query, and restore the table to its state before the trial query.
The empty leg must be written first; otherwise the loaded leg cannot see that occupancy, the two legs may be packed into the same corridor--time slot, and the trial query would be optimistically early.
The vehicle with the earliest feasible delivery is selected, not the vehicle that \eqref{eq:rule} estimates as best.

Vehicles have identical capability (assumption~A5); they differ only in current location and corridor occupancy, so contention on the reservation table can decide which vehicle to choose.
Reassigning a robotic arm also changes the processing time, and the processing-time gap is often larger than the gap in corridor wait, so the same trial query used to choose an arm yields little.

The trial query raises routing calls per transport from $2$ to at most $2N_A$, about \CostMultBefore{} times an unimpeded-time selection.
Without the speed-ups of Section~\ref{sec:speedup} that overhead eats the makespan gain under an equal wall-clock budget; Section~\ref{sec:Chapter_V} therefore compares the two rules only under the same wall-clock limit.

\subsubsection{Three choices for the inter-layer interface}
\label{sec:interface}

With the two levels fixed, the remaining choice is what the lower level returns.
This paper implements three interfaces and measures each under an equal wall-clock budget; only the first yields a stable makespan gain (Section~\ref{sec:Chapter_V}).

\paragraph{Return realized durations only.}
The lower level returns traversal times after conflict-free routing---the evaluation path, and the quantity that a trial query reads.
Every candidate is ranked by the makespan after yielding wait; schedule-then-route methods see that duration only after the assignment is fixed.

\paragraph{Return a surcharge on corridor--time slots.}
A shadow price $\pi(e,b)$ is attached to each occupied corridor--time slot; path planning then minimizes ``arrival time $+\theta\cdot$cost'' on the Pareto front ($\theta\ge0$ is the only knob).
Two estimates are implemented: perturbing one slot and re-solving, and a low-cost approximation refreshed each generation.
Contention wait already enters arrival time, then makespan through \eqref{eq:bind}; a further cost on the same occupancy counts the delay twice.
Lemma~\ref{lem:label} adds a second cost: price is a second criterion, so single-label Dijkstra must become a multi-label Pareto search whether a surcharge is finally applied or not.
Under an equal wall-clock budget, makespan stably worsens; Section~\ref{sec:Chapter_V} attributes that mainly to the overhead.

\paragraph{Return the yield record.}
A yield triple (corridor, time slot, preceding transport) generates the local-search neighbourhood of Section~\ref{sec:ls} without double-counting the delay.
Under an equal wall-clock budget the neighbourhood is pointed accurately, but the hit rate does not offset the extra evaluations.

\subsection{Low overhead: admissible lower-bound pruning and winner-path reuse}
\label{sec:speedup}

At equal generation count the trial query beats rule-based dispatching by \GainProbeSameGen{}, but one evaluation is \CostMultBefore{} times more expensive, so under equal wall-clock time that gain is eaten and the net is almost zero.
Lowering the evaluation cost without changing any selection is itself the gain.
Two optimizations do that.

\paragraph{Admissible lower-bound pruning.}
Conflict-free routing can only become later because of yielding; it can never be faster than the unimpeded shortest path.
The open-loop score \eqref{eq:rule} is therefore an \emph{admissible lower bound} on the realized delivery time:
\begin{equation}
\label{eq:lb_dispatch}
\mathrm{lb}(k)=\max\bigl(\mathrm{avail}_k+t^{*}(\mathrm{loc}_k,\text{pickup}),\,
\mathrm{ready}_j\bigr)+t^{*}(\text{pickup},\text{destination})\ \le\ \mathrm{est}(k),
\end{equation}
where $\mathrm{est}(k)$ is the earliest feasible delivery time obtained by a complete two-leg trial query for vehicle $k$.
Hence if a vehicle's lower bound is already no earlier than the current best trial-query delivery, routing need not be called for it.

\paragraph{Winner-path reuse.}
After the trial query the reservation table is rolled back as a whole, the winner's two legs included; the decoder then recomputes them, wasting two routing calls.
What $\textsc{Route}(\cdot,\textit{commit}=\text{true})$ does is merely write the legs into the reservation table in order, so the paths already computed for the winner during the trial query can be cached and committed directly, without replanning.

\begin{prop}[The two speed-ups do not change the selected vehicle or the timetable]
  \label{prop:speedup_equiv}
  In the implementation, vehicles are queried in increasing id order, and the selected vehicle is replaced only when some vehicle's feasible delivery is strictly earlier than the current best.
  Under this rule, after the pruning of \eqref{eq:lb_dispatch} and winner-path reuse are added, the selected vehicle, the occupancies written to the reservation table, and the final timetable are the same as when a complete two-leg trial query is run for every vehicle.
\end{prop}

\begin{proof}
  Consider pruning first.
  When vehicle $k$ is reached, let the current best trial-query delivery be $\mathrm{est}^{\ast}$.
  If $\mathrm{lb}(k)\ge \mathrm{est}^{\ast}$, then \eqref{eq:lb_dispatch} gives
  $\mathrm{est}(k)\ge\mathrm{lb}(k)\ge\mathrm{est}^{\ast}$,
  so $k$'s feasible delivery cannot be strictly earlier and would not be selected even if every vehicle received a complete trial query; not calling routing for it leaves the selected vehicle unchanged.
  The reservation table is saved before each trial query and restored afterwards; vehicles for which routing is not called do not rewrite the table and therefore do not affect later queries.
  Consider reuse next: the two legs of the selected vehicle were computed against the same reservation-table state; after restore, the table is the same state the decoder later writes into.
  $\textsc{Route}(\cdot,\textit{commit}=\text{true})$ only writes the given legs into the table, so writing the cached paths and replanning then writing produce identical occupancy intervals.
\end{proof}

Together the two optimizations give a \SpeedupProbe{}-fold speed-up, cut routing calls by \RouteCallCut{}, and reduce the cost multiple of a single evaluation from \CostMultBefore{} to \CostMultAfter{}.
These three numbers are the \emph{joint} effect of the two: the experiment measures them as one on/off switch (Table~\ref{tab:prune}), and this paper does not attribute them separately to pruning or reuse.
Without the two optimizations, the same mechanism has almost no net gain under an equal wall-clock budget.

The effect of pruning varies with fleet size.
The tighter the bound and the earlier a good trial-query delivery is obtained, the more vehicles need not call routing; the larger the fleet, the more candidates, and the total trial-query cost grows approximately linearly with $N_A$.
Section~\ref{sec:Chapter_V} reports the measurement: the pruning effect weakens as the fleet grows, but still cuts \RouteCallCutLo{} of the routing calls on the largest fleet tested.
The two effects together mean that the gain of the mechanism may vanish after the fleet reaches some size; Section~\ref{sec:whichcongestion} explains why the present number of seeds is not sufficient to identify that size.

\subsection{Yield-record-guided local search}
\label{sec:ls}

The third interface of Section~\ref{sec:interface} is written as local-search operators on a typed critical chain (Table~\ref{tab:attrib}).
Backtracking from the event that attains the makespan includes transport legs as well as operations, and labels each link as \texttt{operation}, \texttt{machine}, \texttt{upstream}, \texttt{vehicle}, or \texttt{corridor}.
A yield on a loaded leg is always \texttt{corridor}; on an empty leg, a yielding share above $0.5$ distinguishes \texttt{corridor} from \texttt{vehicle}.
The same labels are used to read the case study of Section~\ref{sec:casestudy}.

\begin{table}[t]
  \caption{Critical-chain labels.
  Only \texttt{corridor} names a concrete corridor, time slot, and occupying transport.}
\label{tab:attrib}
\footnotesize
\begin{tabular}{@{}llp{2.55cm}@{}}
\toprule
Label & Criterion & Remedy \\
\midrule
\texttt{operation} & a real operation is occupying its robotic arm & --- \\
\texttt{machine} & $\texttt{bind}=\texttt{machine}$, blocked by the preceding operation on that arm
  & resequence \\
\texttt{upstream} & the vehicle arrived first, the material is not yet ready & recurse to $O_{j,i-1}$ \\
\texttt{vehicle} & the material is ready, the vehicle is late, and yielding is a small share of the delay & change vehicle \\
\texttt{corridor} & a yield occurs on a specified corridor at a specified time slot & reassignment or staggering \\
\bottomrule
\end{tabular}
\end{table}

Two operators use the yield record.
Reassignment scores each alternative arm $m'\in\Omega_{ji}\setminus\{m\}$ of at most $L=5$ real operations on the chain by
\begin{equation}
\label{eq:reassign}
\mathrm{score}(m')=t^{*}\bigl(\mathrm{pos}_{j,i-1},\mathrm{loc}(m')\bigr)
+t^{P}_{jim'},
\end{equation}
unimpeded travel plus processing time; the lowest-scoring arm produces a neighbour that changes only that assignment, which is the trade-off of this paper (a nearer arm may be slower).
Staggering takes the longest \texttt{corridor} link and either advances the blocked operation or postpones the occupier; each round moves one operation, so the result remains a legal permutation and, by Proposition~\ref{prop:decodable}, remains decodable.
The neighbourhood has at most $L+2=7$ candidates.
Each selected individual is given at most three first-improvement rounds, and the chain is recomputed after every acceptance.
Under an equal wall-clock budget this interface does not yield a stable makespan gain (Section~\ref{sec:Chapter_V}).

\subsection{Complexity and cost}
\label{sec:complexity}

One fitness evaluation processes $N+n\delta_{\mathrm{ret}}$ events.
Under closed-loop dispatching each transport uses at most $2N_A$ routing calls, so each individual costs
$O\bigl((N+n)\,N_A\,|E|\,(W^{2}+\log|V|)\bigr)$; open-loop selection by unimpeded time no longer carries the factor $N_A$.
The lower-bound pruning of Section~\ref{sec:speedup} does not change this worst-case bound, but by Proposition~\ref{prop:speedup_equiv} it reduces the actual number of calls by \RouteCallCut{} without changing the output, i.e.\ it replaces $N_A$ by a smaller effective multiple.
Local search adds at most $3(L+2)$ further evaluations for each selected individual.

Within the framework, the cost of a single evaluation can differ by about two orders of magnitude.
On the ladder batch the measurements are: open-loop \MsOpen{}\,ms, closed-loop rule-based dispatching \MsLoopRule{}\,ms, full closed-loop \MsLoopProbe{}\,ms, about $37$ times the open loop.
Pricing is not in that batch; it comes from an earlier pricing scan; within that batch, turning pricing on raises a single evaluation from \MsPricedRef{}\,ms to \MsPriced{}\,ms, another factor of about $4.8$.
The absolute milliseconds of the two batches must not be divided into each other; the product of the two multiples already shows the span: the most and least expensive configurations differ by about two orders of magnitude in the cost of a single evaluation.
Equal generation counts are therefore not an appropriate comparison, and Section~\ref{sec:Chapter_V} aligns wall-clock time instead.

This framework does not prove convergence to a fixed point.
The lower level plans sequentially, not jointly to optimality; the upper level is a randomized search on a non-convex space; what crosses the interface is a duration or a yield record, not a Lyapunov function.
Proposition~\ref{prop:decodable}, Lemma~\ref{lem:label}, and Proposition~\ref{prop:speedup_equiv} give a narrower guarantee that is enough for a heuristic: every candidate is feasible, its evaluation matches the conflict model of Section~\ref{sec:Chapter_III}, and the two speed-ups do not change any selection; together with the deterministic decode of Section~\ref{sec:upper}, the process is reproducible.
Solution quality is given by the experimental protocol of the next section.

\section{Experiments}
\label{sec:Chapter_V}

This section measures the two paths separately under an equal wall-clock budget, then asks on which instances they hold and where the thicker interfaces fail.

\subsection{Experimental setup}
\label{sec:instances}

\subsubsection{Controlled instance families}
FJSP\_PT benchmarks write travel times as a matrix and supply no corridor network.
Of the \PubNLayout{} layouts used by Homayouni and Fontes~\cite{homayouni2021production},
\PubNLayoutAsym{} matrices are entirely asymmetric and \PubNLayoutBad{} of those also violate the triangle inequality, so they cannot be inverted into the undirected corridors used here without producing a different instance.
This paper therefore generates controlled instances on an explicit road network, so that the features of Section~\ref{sec:features} can be varied one at a time.
Section~\ref{sec:external} removes the contention constraint and compares on public instances; that batch calibrates the upper-level search, not the framework.

All instances share $12$ jobs, $36$ real operations, $8$ robotic arms, $H=0.3$, and $\bar{T}_t/\bar{T}_p$ scaled to $4.0$, so that vehicle contention is the main constraint.
$H$ is standardized per operation so that changing $F$ in Family~C does not carry $H$ with it.
On this core the instances vary along three axes (Table~\ref{tab:instances}).

\emph{Family A: layout.}
A dumbbell joins $v_0$ to the arm area by one or a few parallel lanes---the spine layout of~\cite{suh2022spine}---so adding a lane changes capacity only, not distance; a tandem loop~\cite{rahimikelarijani2020tandem} is not used.
\texttt{funnel}, \texttt{high}, and \texttt{mid} are dumbbells (node/corridor $13/12$ to $15/16$); \texttt{funnel} versus \texttt{high} differs by one station lane and halves the min-cut---the cleanest contrast.
\texttt{low} and \texttt{scatter} are $4\times4$ grids ($16/24$) that differ only in arm placement, and supply the low-contention end a dumbbell cannot reach.
The farthest unimpeded trip is \LegDumbbell{} on every dumbbell and \LegGrid{} on both grids, so only the three dumbbells can be attributed to capacity.
Contention intensity runs from \ContentionAHi{} to \ContentionLo{} and is not monotone (\texttt{low} is above \texttt{mid}); the paper-wide maximum \ContentionHi{} is the $F=1.0$ instance of Family~C.

\emph{Family B: vehicle-to-arm ratio $N_A/N_M$.}
On \texttt{funnel} take $N_A\in\{4,8,12,16\}$, i.e.\ ratios $0.5$, $1.0$, $1.5$, $2.0$, of which $1.5$ is the shared base point (listed as \texttt{A funnel}).
Trial-query cost grows with the fleet and pruning weakens with it (Section~\ref{sec:costlaw}).

\emph{Family C: flexibility $F$.}
On the same layout take $F\in\{0.3,\,0.6,\,1.0\}$, of which $0.6$ is the base point (measured $0.61$).

The three families comprise $\NCases$ instances: \texttt{A funnel} is generated once and occupies one row in Tables~\ref{tab:instances} and~\ref{tab:bytag}, so $5+3+2=\NCases$; Section~\ref{sec:whichcongestion} must count that shared base when it reads Families~B and~C.
Contention intensity is the mean share of yielding wait in travel time on the initial random population ($20$ individuals, seed $42$), read off the instance, not an optimized solution.
Relative to the \PubNInst{} public instances of Section~\ref{sec:external}, the base $F=0.6$ is about one and a half times the public median \PubFMed{}, $H=0.3$ lies above the public maximum \PubHMax{}, and $\bar{T}_t/\bar{T}_p=4.0$ is set above the public maximum \PubTtTpHi{} so that transport is worth modelling.
Section~\ref{sec:whichcongestion} checks that $H$ does not inflate the closed-loop gain, on a pre-speed-up batch of \MatrixCells{} instances, direction only.

\begin{table}[t]
\caption{Controlled instances.
Shared core: $12$ jobs, $36$ operations, $8$ arms, $H=0.3$, $\bar{T}_t/\bar{T}_p\approx4.0$.
Contention intensity is yielding wait as a share of travel time on the initial random population.}
\label{tab:instances}
\small
%% Generated by tab/gen_tables_ladder.py: structure from abc_matrix.build(),
%% bound from simple_lower_bound, contention from baseline_ladder.csv.
%% No column is typed by hand.  This manuscript inputs the _EN fragment.
\input{tab/tab_instances_EN.tex}
\end{table}

\subsubsection{Comparison protocol}
\label{sec:protocol}

Four rules govern every contrast.
Every schedule is executed by the same conflict-free planner and checked by a validator independent of the decoder; the open loop's self-reported makespan is often infeasible (the gap is the contention intensity; on the funnel it can reach \ContentionHi{}), so what is compared is the realized makespan.
A single evaluation can differ by about two orders of magnitude, so equal generation count would give the more expensive method more compute: the main batch shares a $90$-second budget, a generation cap of $2000$ (never fires), and early stopping after $400$ generations without improvement.
Early stopping fires on every open-loop run (a little over four hundred generations, one to three tenths of the budget); both closed-loop configurations run the full $90$ seconds, which favours the baseline---the open loop is not cut off, and it completes as many evaluations as closed-loop rule-based dispatching, or more (Section~\ref{sec:costlaw}).
Companion batches have their own budgets, marked at first citation.
Each instance is run with $\NSeeds$ seeds ($\NCases\times\NSeeds=\NPairs$ pairs) and a two-sided Wilcoxon signed-rank test on $(\text{instance},\text{seed})$; $\NSeeds$ seeds resolve a few-percentage-point effect in the aggregate, but not inside a single instance (Section~\ref{sec:whichcongestion}).
Holm correction is by family: the five aggregate contrasts of Table~\ref{tab:main} form one family; in Table~\ref{tab:bytag} the $\NCases$ instances of one effect form a family each.
Two exceptions are marked where they occur: vehicle-to-arm-ratio $p$-values in Section~\ref{sec:whichcongestion} are shown uncorrected because they do not reach that threshold; counts from the \MatrixCells{}-instance batch are that batch's own uncorrected counts, direction only.
Each aggregate contrast also reports a Hodges--Lehmann pseudomedian and a $95\%$ distribution-free interval (same source as the rank test); the mean pairwise relative gain is convenient for adding across tables but carries no precision.
Error bars in the figures are the SEM of that mean; tables do not report $\pm$.
Runs that fail the validator or fall below the composite lower bound are reported separately and are not folded into any instance mean.

\subsubsection{Implementation and environment}
\label{sec:impl}
The experiments run single-threaded on an Intel Xeon W-1270 @ $3.40$\,GHz, in pure Python ($\ge 3.9.6$), standard library only, no solver.
Hyperparameters are defaults, untuned, and identical across configurations: population $60$, crossover $0.8$, mutation $0.2$, elite $5$, binary tournament; local search each generation on the top $10\%$, at most $L=5$ critical operations per round and three rounds.
They stay conventional and do not carry a mechanism claim.
Absolute runtimes cannot be compared with a compiled implementation: a faster code would let every configuration complete more evaluations inside the same budget, but would not change the exchange among configurations that share this implementation.

\subsection{Four-level baseline ladder}
\label{sec:arms}

The ladder (Table~\ref{tab:arms}) has two factors: whether the search includes conflict-induced delay, and whether a vehicle is chosen by unimpeded time or by a trial query.
Reporting only ablations would not line up with the literature; reporting only a literature comparison would count conflict-free routing itself as a mechanism of this paper.
\textbf{B0} is open-loop planning plus realized execution, corresponding to~\cite{li2026framework} (idle-first, the same formula as \eqref{eq:rule}).
\textbf{B0+} changes only vehicle selection on a frozen open-loop schedule, corresponding to the two-stage structure of~\cite{sun2023integrated}; the trial query is a greedy stand-in for that second stage, and is not necessarily weaker than that paper's genetic algorithm.
\textbf{B1} is closed-loop planning with rule-based dispatching; \textbf{B2} is this paper.
Only a same-row or same-column contrast differs in one factor; $\text{B0+}\to\text{B1}$ crosses both and cannot measure the closed loop.

\begin{table}[t]
\caption{The four-level ladder along two factors.
Only a same-row or same-column contrast differs in one factor; a diagonal contrast cannot be read apart.}
\label{tab:arms}
\footnotesize
\begin{tabular}{@{}lcc@{}}
\toprule
 & Rule-based dispatching & Reservation-table-aware dispatching \\
\midrule
Open-loop planning (search ignores conflicts) & B0~\cite{li2026framework} & B0+~\cite{sun2023integrated} \\
Closed-loop planning (fitness from conflict-free routing) & B1 & \textbf{B2} (this paper) \\
\bottomrule
\end{tabular}
\end{table}

B0 execution has two reproductions: \emph{replay the plan} keeps the vehicles specified by the search; \emph{on-site reassignment} re-selects them at execution time.
Replay is stronger ($63.00$ versus $78.00$ on the funnel instance) and matches~\cite{li2026framework}; the main report uses replay, and on-site reassignment is listed alongside.

\subsection{Aggregate comparison on the ladder}
\label{sec:main}

Table~\ref{tab:main} reports the four-level ladder on $\NCases$ instances $\times$ $\NSeeds$ seeds ($\NPairs$ pairs), equal wall-clock (Section~\ref{sec:protocol}); every run passed the independent check.
Holm over the five contrasts is inert: the largest is $p\PProbeLoop$ before and after; the other four remain below $10^{-4}$.
The text writes the Hodges--Lehmann interval as a reduction (smaller to larger magnitude); the table writes the same interval with a minus sign.

\begin{table}[t]
\caption{Four-level ladder under an equal wall-clock budget, and the four one-factor pairs.
Negative is better.
$n=\NPairs$ pairs on $(\text{instance},\text{seed})$; two-sided Wilcoxon signed-rank; Holm over the five contrasts
($^{*}p<0.05$, $^{**}p<0.01$, $^{***}p<0.001$).
HL is the Hodges--Lehmann pseudomedian and its $95\%$ interval (same source as the test; the mean is for adding across tables, Section~\ref{sec:protocol}).
A bold header marks this paper's configuration, not a per-instance winner (Table~\ref{tab:bytag}).}
\label{tab:main}
\small
%% Generated from baseline_ladder.csv; the same script prints the preamble
%% macros.  p-column is Holm-adjusted; HL pseudomedian and 95% CI are printed.
%% Correction is inert on these five contrasts.
\input{tab/tab_main_EN.tex}
\end{table}

Fixing the dispatching rule, the closed loop is significant under both rules:
$\text{B0}\to\text{B1}$ is $-\GainLoopRule$ ($p\PLoopRule$; HL \HLLoopRule{}, $95\%$ CI \CILoopRule{}),
and $\text{B0+}\to\text{B2}$ is $-\GainLoopProbe$ ($p\PLoopProbe$).
The former interval lies far from zero and its lower end is still a two-digit reduction.
The open loop completes several times as many evaluations inside the same budget (Section~\ref{sec:speedup}) and is still worse: it optimizes a surrogate that excludes yielding wait.

Fixing the loop, reservation-table-aware dispatching inside the closed loop is $-\GainProbeLoop$ ($p\PProbeLoop$; HL \HLProbeLoop{}, $95\%$ CI \CIProbeLoop{}).
The two levels share the same search and budget and differ only in the trial query.
The interval does not cross zero, but its lower end is less than one percentage point from zero; after Holm correction no instance is significant on its own (uncorrected \CellsSigProbe{}), and on ratios $1.0$ and $2.0$ B2 is slower than B1.
Thus $-\GainProbeLoop$ is an aggregate on this family, not a per-instance point estimate (Section~\ref{sec:whichcongestion}).
Under the open loop the same rule is $-\GainProbeOpen$ ($p\PProbeOpen$), significant but far smaller than $\GainLoopRule$, and corresponds to the two-stage segment of~\cite{sun2023integrated} relative to~\cite{li2026framework}.

End-to-end, $\text{B0}\to\text{B2}$ is $-\GainEndToEnd$ ($p\PEndToEnd$; $\WinEndToEnd$ wins, $\LoseEndToEnd$ losses, $1$ tie).
\label{sec:complement}
If the two factors were independent, the compound of $\GainLoopRule$ and $\GainProbeOpen$ would be $\GainIndepProduct$; the measurement is slightly below that---no positive interaction, near-additive.
Open-loop dispatching cannot replace the closed loop: a two-stage structure of the~\cite{sun2023integrated} kind reaches $\GainProbeOpen$; the closed-loop reduction requires yielding wait in fitness.
B2 is best end-to-end because it stacks the two, not because they amplify each other.
The gap of schedule-then-resolve is not the second-stage $\GainProbeOpen$, but that the first stage never sees yielding wait; that drop is a measurement on this family, not a proven bound.

\subsection{Three-factor scans and per-instance resolution}
\label{sec:whichcongestion}

Table~\ref{tab:bytag} and Figure~\ref{fig:prediction3} split the $\NCases$ instances by layout, $N_A/N_M$, and $F$.
The closed loop can be read as a trend by family; dispatching cannot be resolved on a single instance.
Per-instance tests are Holm-corrected by family: the family is the $\NCases$ instances of one effect, not the twenty pooled.
After correction the closed loop remains significant on \CellsSigLoopAdj{} instances (uncorrected \CellsSigLoop{}; largest corrected site $p\PLoopCellMax$); the only exception is the tightest fleet ($\text{B0}\to\text{B1}$ is \GainLoopStarve{}).
Dispatching is significant on \CellsSigProbe{} instances before correction (the two ends of Family~C, $p\PProbeFlexLoRaw$ and $p\PProbeFlexHiRaw$) and on \CellsSigProbeAdj{} after (Holm $p\PProbeFlexLoHolm$, $p\PProbeFlexHiHolm$; Benjamini--Hochberg $p\PProbeFlexLoBH$, $p\PProbeFlexHiBH$, likewise not significant).
A few percentage points is the lower limit that $\NSeeds$ seeds on one instance can resolve; only the $\NPairs$ pairs in the aggregate lift it out (pseudomedian \HLProbeLoop{}, $95\%$ interval \CIProbeLoop{}, lower end less than one point from zero).
Per-instance dispatching readings below are direction only.

\begin{table}[t]
\caption{Per-instance ladder and the two effects, blocked by family ($n=\NSeeds$ seed-pairs).
Stars are Holm-corrected over the $\NCases$ instances of one effect ($^{*}p<0.05$, $^{**}p<0.01$);
$^{\dagger}$ is significant only before correction and is not read as a conclusion.
Negative is better.
Gain columns use the same pairwise-mean estimator as Table~\ref{tab:main}.
\texttt{A funnel} is the shared base point of Families~B and~C and occupies one row.}
\label{tab:bytag}
\small
%% Generated by family A/B/C.  Gain column = mean of pairwise relative gains
%% (same estimator as tab_main).  Stars by Holm p; dagger = raw-only.
\input{tab/tab_bytag_EN.tex}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{fig/fig_prediction3.pdf}
\Description{Three panels of line plots: closed-loop gain and reservation-aware
dispatch gain across layout (vs contention), fleet-to-arm ratio, and flexibility. After
Holm correction over the ten cells the closed-loop curve is filled, hence
significant, at nine of ten cells, while the dispatch curve is hollow at every
cell, and its sign alternates across the four fleet-ratio levels. Error bars
give the standard error of the mean over the ten seeds of each cell; the
dispatch bars cross zero at every cell and the closed-loop bars do not.}
\caption{Closed-loop and dispatching gains on the three families.
Left: layout versus contention intensity; middle: $N_A/N_M$; right: flexibility.
Hollow vertical lines mark the shared base point \texttt{A funnel}.
A filled marker remains significant after Holm correction over the $\NCases$ instances of that effect; a hollow marker does not.
Error bars are the SEM of pairwise relative gains over $\NSeeds$ seeds.
Read whether each curve can be distinguished from zero, not the shape of the dispatching curve.}
\label{fig:prediction3}
%% From fig/fig_prediction3.py and baseline_ladder.csv.
%% x-axis: Family A uses contention intensity; B/C use the factor level in the
%% instance name (written by abc_matrix).  Shared base A funnel has no level in
%% the name; the script fills (1.5, 0.6) from abc_matrix.BASE.
%% Filled/open markers follow Holm p over the ten instances of that effect
%% (not the current panel).  The dispatching curve is all open.
\end{figure}

\emph{Layout.}
The closed loop is significant on all five instances and rises roughly with contention: \GainLoopScatter{} on \texttt{scatter} (\ContentionLo{}), \GainLoopMid{}/\GainLoopLow{} on \texttt{mid}/\texttt{low}, \GainLoopFunnel{}/\GainLoopHigh{} on \texttt{funnel}/\texttt{high}.
Two inversions block a monotone ranking.
\texttt{low} versus \texttt{mid} crosses the grid/dumbbell boundary of Section~\ref{sec:instances} (\LegGrid{} versus \LegDumbbell{}) and is not a pure contention contrast.
\texttt{funnel} versus \texttt{high} differs only by one station lane---the designated cleanest contrast---and is the inversion that is a counter-example.
What remains is the span (\GainLoopFunnel{} versus \GainLoopScatter{}, about two and a half times), not an ordering; contention intensity does not say how much wait can be bypassed by reassignment (limitation~(1) of Section~\ref{sec:limitations}).
Dispatching is not significant on any instance of this family; four of five signs match the closed loop.

\emph{Vehicle-to-arm ratio.}
The intended fleet-size upper bound is not found.
Dispatching at the four levels is $-\GainProbeStarve$ ($0.5$), \GainProbeFleetOne{} ($1.0$), \GainProbeFleetBase{} ($1.5$), \GainProbeFleetTwo{} ($2.0$): the sign alternates, and none is significant even before correction ($p\PProbeFleetHalfRaw$, $p\PProbeFleetOneRaw$, $p\PProbeFleetBaseRaw$, $p\PProbeFleetTwoRaw$).
Dropping the base point manufactures the monotone curve a previous version treated as an upper bound at $N_A/N_M\ge 1$; subset selection of a noisy effect can do that.
The cost-law upper bound of Section~\ref{sec:costlaw} remains an untested prediction.
The closed loop at $1.0$ and $2.0$ is \GainLoopFleetOne{} and \GainLoopFleetTwo{}, both significant: a larger fleet mainly changes how much the closed loop can reduce.

\emph{Flexibility.}
A per-instance conclusion for dispatching was withdrawn after Holm correction.
At $F=0.3$, $0.6$, and $1.0$ the gains are \GainProbeFlexLo{}, \GainProbeFleetBase{}, and \GainProbeFlexHi{}, all the same sign; the two ends are the largest among the $\NCases$ instances and lose significance after correction ($p\PProbeFlexLoHolm$, $p\PProbeFlexHiHolm$).
The family is directional evidence only.
Same sign at $F=0.3$, where reassignment room is smallest, is compatible with contention being handled mainly by vehicle selection and timing, not with a bypass that needs another arm; compatibility is not confirmation.
The closed loop is \GainLoopFlexLo{} and \GainLoopFlexHi{}, still significant per instance after correction.

\paragraph{An independent batch, and heterogeneity is not an amplifier.}
A contended cross-check ($\MatrixCells$ instances $\times$ $10$ seeds, two-stage surrogate baseline, per-instance wall-clock $30.2$--$72.9$\,s) gives an integration gain of \GainIntegMatrix{}, significant on \SigIntegMatrix{} instances (uncorrected counts from that batch; the second exception of Section~\ref{sec:protocol}), from \GainIntegMatrixLo{} to \GainIntegMatrixHi{}.
By $H$ bin the same gain is \GainIntegHZero{} at $H=0$, \GainIntegHLow{} at $0.15$ (measured \MatrixHMeasLow{}, near the public median \PubHMed{}), \GainIntegHMid{} at $0.3$, and \GainIntegHHigh{} at $0.5$; the rank correlation is a weakly negative \RhoIntegH{}.
$H$ above the literature range did not inflate the closed-loop gain; $H=0$ is the highest bin, consistent with Section~\ref{sec:features}.
Dispatching on that batch is \GainProbePre{}, significant on $0$ instances: it ran before the speed-ups of Section~\ref{sec:speedup}.

\subsection{Ablations and controlled negative controls}
\label{sec:pricing}

Of the three interfaces in Section~\ref{sec:interface}, only reservation-table-aware dispatching has a net gain under an equal wall-clock budget.
On the $\MatrixCells$-instance data of Section~\ref{sec:whichcongestion}, turning off yield-record feedback is \GainCertMatrix{} (significant on $1$ instance) and turning off staggering is \GainStaggerMatrix{} (significant on $2$).
Neither is positive: those evaluations are better returned to the population cycle.
This earlier batch may be cited for these two ablations---their cost does not depend on the speed-ups---but not for dispatching.
A $\theta$ scan on two $8\times4\times4$ instances (not Families A/B/C), collected before the speed-ups, confirms the double-counting argument of Section~\ref{sec:interface}: under equal wall-clock every $\theta$ worsens makespan, flat over a ten-fold range; under equal evaluations the same comparison cannot be distinguished from zero.
Priced routing completes about one fifth as many evaluations; $\theta$ is not a useful knob, and the extra cost is the multi-label search of Lemma~\ref{lem:label}.

\label{sec:whycost}
The yield-record neighbourhood is small and directed (Section~\ref{sec:ls}), yet fewer than one in fifty neighbours improve makespan; three repairs of the operators were not significant under an equal number of evaluations.
Removing one corridor delay promotes another constraint, so the realized improvement is only a fraction of the wait removed (Figure~\ref{fig:case})---the same dilution that left dispatching in the noise at \CostMultBefore{} and significant after the multiple fell to \CostMultAfter{}.
The default therefore keeps dispatching and does not use the yield record to generate the neighbourhood.


\subsection{The cost law and the effect of the two speed-ups}
\label{sec:costlaw}

The same dispatching rule is in the noise before the speed-ups and significantly positive after them, so the two themselves are measured.
Figure~\ref{fig:protocol} locks cost per evaluation and evaluations completed onto a shared wall-clock hyperbola.
The cheaper configuration completes more evaluations; the open loop uses less than two tenths of the wall-clock to complete the most.
Before the speed-ups, reservation-table-aware dispatching is \GainProbeSameGen{} ($p\PProbeSameGen$) at equal generation count and \GainProbeSameClock{} ($p\PProbeSameClock$) at equal wall-clock---almost zero.
Equal generation count asks whether the extra routing step improved that generation's ranking; equal wall-clock asks whether makespan fell in the same stretch of time.
Both are well posed; one must not be reported as the other.

\begin{figure}[t]
\centering
\includegraphics[width=0.7\columnwidth]{fig/fig_protocol.pdf}
\Description{A log-log scatter of cost per evaluation against evaluations
completed. A grey hyperbola marks the 90-second budget cap. The two closed-loop
configurations sit on it, while the open-loop configuration sits well below it
because it stalls after about seventeen seconds. Orange markers are the
pruning ablation off and on; a callout marks the on setting.}
\caption{Equal wall-clock time locks cost per evaluation and evaluations completed onto a hyperbola; the grey line is the shared $90$-second cap.
Closed-loop runs sit on the line; the open loop sits below it (early stop after $\sim$17\,s, Section~\ref{sec:protocol}).
B0 and B0$^+$ share a point.
Filled markers are geometric means of three instances (the product that a log axis preserves); pale points are the instances.
Orange markers are pruning and winner-path reuse off and on (callout at the on setting): Proposition~\ref{prop:speedup_equiv} guarantees that the output does not change.}
\label{fig:protocol}
%% From fig/fig_protocol.py; ladder_cost.csv and prune_ablation.csv.
%% If the latter is missing the script omits the orange markers.
%% fig:protocols (two-convention bars) dropped; the pair
%% \GainProbeSameGen / \GainProbeSameClock is reported in the text.
\end{figure}

Reservation-table-aware dispatching raises the routing calls per transport task from $2$ to at most $2N_A$, so cost grows linearly with fleet size.
The pruning bound is tighter when contention is weaker; the larger the fleet and the stronger the contention, the smaller the cut.
The self-check of Section~\ref{sec:speedup} gives this direction: of six instances the highest cut is on the least congested one (\RouteCallCutHi{}) and the lowest on the largest fleet (\RouteCallCutLo{}).
The two trends \emph{predict} that when the fleet is large enough and contention is strong enough, trial-query cost will catch the gain; that remains a prediction.
On the four $N_A/N_M$ levels of Section~\ref{sec:whichcongestion} the sign alternates and none is significant, so the upper bound is not confirmed.
The cost law itself (cost linear in $N_A$, pruning effect falling with $N_A$) is measured; the upper bound deduced from it is not.

The speed-ups and the dispatching rule were introduced one after the other, so a control that moves only the speed-ups is run: same instances, seeds, and budget.
Proposition~\ref{prop:speedup_equiv} guarantees that the two optimizations do not change the output.
On each of six instances six random chromosomes give makespans and dispatch sequences \emph{identical bit for bit} ($36$ groups, no exception); routing calls fall by \RouteCallCut{}.
Turning the optimizations off only makes one evaluation slower, so the equal-wall-clock difference comes only from saved compute.
Table~\ref{tab:prune}: off$\to$on reduces makespan by a further \GainPrune{} (\WinPrune{} wins / \LosePrune{} losses, $p\PPrune$); evaluations inside the same budget are \EvalRatioPrune{} times, and a single evaluation is sped up by \SpeedupProbe{}.
The same rule is \GainProbePre{} on the pre-speed-up matrix batch (significant on $0$ of the $\MatrixCells$ instances) and significantly positive on the post-speed-up ladder; the step in between did not change any selection.

\begin{table}[t]
\caption{Pruning and winner-path reuse off versus on (bold header: the setting used here).
Output is bit-identical, so the difference is saved compute.
Same instances, seeds, and wall-clock; $n=60$ pairs, two-sided Wilcoxon ($^{***}p<0.001$).
Ratio columns take the off setting as denominator, in opposite directions.}
\label{tab:prune}
\small
%% Regenerated from prune_ablation.csv by gen_tables_ladder.py.
\input{tab/tab_prune_EN.tex}
\end{table}

\subsection{Case study: Gantt chart and critical chain on the funnel instance}
\label{sec:casestudy}

Figure~\ref{fig:case} places B0 and B2 on one funnel instance: arms and vehicles share a time axis, and the critical chain is coloured by Table~\ref{tab:attrib}.
In the open-loop solution, yielding wait appears near the station and concentrates on a few vehicles.
The closed-loop solution cuts those waits and assigns several operations to an arm that is not the fastest---assignments of the \eqref{eq:reassign} kind---and, as a consequence rather than a third mechanism, balances vehicle idle and busy time more evenly.
On B0's chain \texttt{corridor} occupies a substantial share; on B2 those links largely disappear, and \texttt{machine} and \texttt{upstream} come to the top.
That is the dilution of Section~\ref{sec:whycost}: remove one corridor delay, and another constraint becomes tight.

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{fig/fig_case.pdf}
\Description{Two Gantt charts of the open-loop and closed-loop solutions on one
funnel instance, with arms and vehicles on a shared time axis, followed by
stacked bars that decompose each critical chain by attribution label.}
\caption{B0 and B2 on one funnel instance (existence reading, not a statistical claim).
Top: arms and vehicles on a shared time axis; a gap inside one transport is yielding wait, a gap between tasks is idle time.
Bottom: critical-chain composition by Table~\ref{tab:attrib}; corridor wait removed versus makespan recovered is the dilution of Section~\ref{sec:whycost}.}
\label{fig:case}
%% From fig/fig_case.py and case_study/*.json (ladder_diag.py --case-study).
%% Merges the former fig_case_gantt / fig_case_chain pair.
\end{figure*}

\subsection{Degenerate comparison on public benchmarks}
\label{sec:external}

The preceding sections compared configurations of the same codebase; they cannot calibrate the absolute level of the implementation.
After \eqref{eq:excl} is removed the model degenerates to standard FJSP\_PT, the \emph{same mathematical problem} as the public benchmarks; $\delta_{\mathrm{ret}}=0$ aligns the makespan definition (Table~\ref{tab:notation}).
Neither mechanism enters; what is tested is the \emph{upper-level search}.

The \PubNInst{} instances compiled by Homayouni and Fontes~\cite{homayouni2021production} (originating in Fattahi et al.~\cite{fattahi2007mathematical}) are taken with two vehicles as in the literature~\cite{lim2023twophase} and no return of finished goods.
Travel times are used as published: \PubNLayoutBad{} of the matrices are not a shortest-path closure, and a synthesis would change the instance.
\PubNOpt{} instances have a MILP-proven optimum~\cite{homayouni2021production}; the remaining $4$ take the published best-known~\cite{lim2023twophase}.

Table~\ref{tab:external} is \PubNInst{} instances $\times$ \NSeeds{} seeds.
All $200$ runs pass the validator and the composite lower bound; on the \PubNOpt{} proven-optimum instances no run falls below that value.
On $\PubNMatch{}/\PubNInst{}$ instances the best solution is identical bit for bit to the reference, including all $10$ small instances; the mean gap is \PubGapMean{} (median \PubGapMed{}).
The four largest instances have mean \PubGapBig{}, worst \PubGapMax{}; none of the $16$ instances with at most $24$ operations exceeds $5.45\%$.

\begin{table}[t]
\caption{Degenerate comparison on public benchmarks (exclusivity removed).
\textsc{opt}: MILP-proven optimum; \textsc{bk}: published best-known; gap from the best of \NSeeds{} seeds.
An asterisk marks a bitwise-identical best solution.}
\label{tab:external}
\centering
\small
\input{tab/tab_external_EN.tex}
\end{table}
%% tab_external() from gap_ideal.csv; the Ops column is counted from
%% hf/*.json (len(proc_time)), not typed.  The same function prints the \Pub*
%% macros.  Edit the CSV, then re-run the script.

Raising the budget on the $10$ \textsc{mfjst} instances (\PubBudgetRatio{} times the wall-clock, generation cap five times) cuts their mean gap from \PubGapLoBudget{} to \PubGapHiBudget{} ($\PubBudgetGainPP$ percentage points); none of the $300$ runs hits the generation cap---a capability boundary, not a short budget (item~(3) of Section~\ref{sec:future_work}).
On the \PubNOpt{} proven-optimum instances the composite lower bound lies a median \PubLBSlack{} below the optimum and \PubLBTotal{} below this paper's best; the search shortfall is a median $\PubLBResidMed$ percentage points and at most $\PubLBResidMax$, so almost all of the gap is slack in the bound.
The self-generated instances cannot inherit this proportion: their bound contains no contention, so \LBGapMed{} \emph{must not} be read as a search gap (limitation~(3) of Section~\ref{sec:limitations}).



\section{Conclusion}

Flexible assignment and conflict-free AGV routing must be decided together: travel time is produced by routing, and once the corridor to a fast arm is congested, a slower, better-connected arm may be the better assignment.
Existing methods schedule on a constant matrix or freeze the assignment first, so yielding wait cannot enter every candidate.
The bilevel framework connects the two levels along an evaluation path and a decision path and measures them separately under the same wall-clock budget.

\subsection{Findings and contributions}
\label{sec:contributions}

The contribution is the solver, not the problem definition~\cite{vanos2026exact}.
\begin{enumerate}[leftmargin=*,topsep=2pt,itemsep=2pt]
\item Routing-in-the-loop evaluation reduces makespan by $\GainLoopRule$ under rule-based dispatching ($p\PLoopRule$) and by $\GainLoopProbe$ under reservation-table-aware dispatching ($p\PLoopProbe$).
Proposition~\ref{prop:decodable} and Lemma~\ref{lem:label} guarantee that every chromosome is decodable and that the evaluation matches the conflict model of Section~\ref{sec:Chapter_III}.
\item Within the closed loop a two-leg trial query reduces makespan by a further $\GainProbeLoop$ ($p\PProbeLoop$).
Admissible lower-bound pruning and winner-path reuse do not change the selected vehicle or the timetable (Proposition~\ref{prop:speedup_equiv}), cut router calls by \RouteCallCut{}, and press the cost multiple from \CostMultBefore{} to \CostMultAfter{}.
\item The two effects are not of the same size and are not substitutes.
The closed loop remains significant after Holm correction on \CellsSigLoopAdj{} of the $\NCases$ instances; dispatching is significant only in the aggregate ($95\%$ interval \CIProbeLoop{}), and the sample is not sufficient to identify on which class of instances that gain is stable.
The end-to-end $\GainEndToEnd$ is close to the independent composite $\GainIndepProduct$ (near-additive, no positive interaction); open-loop dispatching ($\GainProbeOpen$) is far from a substitute for the closed loop.
\item A surcharge on occupied corridor--time slots, and reassignment or staggering from the yield record, do not yield a stable gain under the same budget.
\end{enumerate}

\subsection{Limitations and future work}
\label{sec:limitations}
\label{sec:future_work}

\textbf{(1) Instance families.}
The main experiment fixes size and varies only structure (Section~\ref{sec:features}), so this paper makes no claim about how the mechanisms vary with size.
$H$ is fixed at $0.3$; ``the gain does not rise with $H$'' comes from the smaller independent batch of Section~\ref{sec:whichcongestion}.
The layout family is spliced: the three dumbbells move only capacity; the two grids change topology and distance together (\LegDumbbell{} versus \LegGrid{}, Section~\ref{sec:instances}).
Contention intensity does not say how much wait can be bypassed by reassignment, so only the three dumbbells can be attributed to capacity.
Instances had to be self-generated: published FJSP\_PT is a constant matrix, \PubNLayoutAsym{}/\PubNLayout{} of the matrices are asymmetric and \PubNLayoutBad{} are not a shortest-path closure (Section~\ref{sec:instances}), and work with an explicit road network cannot be compared directly because the reservation unit differs (Section~\ref{sec:fjsppt}).
A follow-up is to remeasure on a larger corridor network and split ``whether congestion can be bypassed by reassignment'' from ``whether the critical chain has only one dominant bottleneck''; $\varphi$ failed to separate those families.

\textbf{(2) Dispatching is positive on average, but not yet resolved per instance.}
$\NSeeds$ seeds suffice for the closed loop ($\CellsSigLoopAdj/\NCases$ after Holm).
Reservation-table-aware dispatching is a few percentage points, of the same order as the seed-to-seed dispersion: $\CellsSigProbe/\NCases$ instances are significant before correction and $\CellsSigProbeAdj/\NCases$ after, so the aggregate of $\NPairs$ pairs is positive ($95\%$ interval \CIProbeLoop{}) but the sample is not sufficient to identify on which class of instances the gain is stable.
An order-of-magnitude more seeds, and denser $N_A/N_M$ levels, would be needed to identify that class and to test the cost-law upper bound of Section~\ref{sec:costlaw}.

\textbf{(3) Distance to the optimum under corridor contention is unknown.}
The configurations share one codebase, so differences among levels are not an artefact of search strength.
Section~\ref{sec:external} is bitwise identical to the literature on $\PubNMatch{}/\PubNInst{}$ instances; all $10$ small instances hit a proven optimum.
The four largest average \PubGapBig{}, and \PubBudgetRatio{} times the compute buys only $\PubBudgetGainPP$ percentage points---a capability boundary, not a short budget.
On the \PubNOpt{} proven-optimum instances the search shortfall is at most $\PubLBResidMax$ percentage points; almost all of the gap is slack in the bound.
On the self-generated instances the bound contains no contention, so the median gap \LBGapMed{} must not be read as a search gap.
A few-percentage-point effect such as \GainProbeLoop{} is of the same order as that unknown distance.
A follow-up is to separate slack from a search shortfall on contended instances; exact solutions by logic-based Benders decomposition~\cite{vanos2026exact} are the comparison at small size.

\textbf{(4) The two thicker interfaces must be remeasured if their own cost is pressed down.}
The surcharge and yield-record-guided local search were judged in the same codebase and under the same wall-clock budget.
A constant-factor speedup of every configuration leaves the exchange rate, and the sign, unchanged; only pressing down the cost of one interface on its own can change the sign, as the dispatching speed-ups of Section~\ref{sec:speedup} already did.
Re-solving only the perturbed suffix of a chromosome would let that local search be remeasured under the same wall-clock.

\textbf{(5) Failures and online arrivals are out of scope.}
Assumption A7 leaves robotic-arm and vehicle failures and dynamic arrivals outside the model.
The reported ratios are for a static horizon; when a failure or an online arrival occurs, the value of a mechanism also includes the speed at which it repairs the schedule~\cite{gui2023dynamic,xiong2024dynamic,dauzereperes2024fjsp}.


%\section*{Acknowledgments}
%This work was supported partly by Key Laboratory of Computer Network and Information Integration (Southeast University) of Ministry of Education of China under Grants NO. 93K-9 and Jiangsu Provincial Key Laboratory of Network and Information Security under Grants No.BM2003201.

%%
%% The next two lines define the bibliography style to be used, and
%% the bibliography file.
\bibliographystyle{ACM-Reference-Format}
\bibliography{reference-base}


%% No appendix.  An empty \appendix leaves a blank TOC/header entry in acmart.

\end{document}
\endinput
%%

